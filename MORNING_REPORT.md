# Alpha App — 晨間報告（2026-08-28）

給使用者起床後看的整晚工作摘要。詳細內容都在`PROGRESS.md`（續19~續29條目）
跟`BACKLOG.md`裡，這份報告是精簡整理版，方便快速掌握狀況。

---

## 1. 版本號（起床第一件事，用這個確認手機吃到新版）

**目前 `APP_VERSION` = `2026-08-28.0329`**（`.git/hooks/pre-commit`每次
commit自動改寫，跟`sw.js`的`CACHE`常數同步）。

打開App，不用點進設定，**首頁最底下**（今日事件卡片下方）會顯示
「Alpha v2026-08-28.0329」；設定頁「關於」卡片也有同一個數字。如果手機
顯示的版本號跟這裡不一樣（比較舊），代表手機還沒吃到最新版——按設定頁
「強制更新（清除快取＋重新整理）」按鈕，或整個關掉瀏覽器分頁重開。

---

## 2. 昨夜完成清單（commit hash + 冒煙測試證據）

| # | 項目 | commit | 冒煙測試 |
|---|---|---|---|
| 1 | 補交冒煙測試完整輸出 | `542111c` | 全部PASS |
| 2 | 動能榜還原權息修正(adj_close) | `aa4a6c5` | 全部PASS |
| 3 | 根治時鐘/重整按鈕P0(第五次回報) + 啟動夜間循環 | `fbb071b` | 全部PASS(10項) |
| 4 | 新增pull-to-refresh手勢 | `11961bf` | 全部PASS(12項) |
| 5 | B23：動能榜創新高/量價配合度因子 + 抓到影響七成股票的日曆缺口真bug | `efd7b6f` | 全部PASS |
| 6 | B24第一步：前瞻選股台帳picks_ledger.json上線 | `1982542` | 全部PASS |
| 7 | UX走查第1輪(修正UX-1) + 更正FinMind封鎖誤判 | `b85f025` | 全部PASS(10項) |
| 8 | B24 PIT回測骨架 | `8f68548` | 不影響App，未跑 |
| 9 | B24 PIT回測第一次結果 | `eb94649` | 不影響App，未跑 |
| 10 | B24 PIT回測重大方法論修正 | `9a0fd2b` | 不影響App，未跑 |
| 11 | 更正：上輪背景任務正常跑完非crash | `99b7733` | 不影響App，未跑 |
| 12 | 修正B23回補計數真bug | `718a5f9` | 不影響App，未跑 |
| 13 | B23回補228檔+動能榜覆蓋率提升 | `96c2557` | **例外：check1/12 FAIL，已獨立驗證是FinMind額度問題非regression，見下方第4節** |

全部commit都已推送到GitHub，GitHub Pages會在push後1-2分鐘自動部署。

---

## 3. 時鐘與重整按鈕的修復驗證方式（30秒內自行確認）

**背景**：這是第五次回報「時鐘不動」，前四次的程式碼修法都是對的（`updateClocks()`
本身沒bug），問題出在手機端Service Worker快取了舊版`index.html`，重新
整理/重新啟動App也不會馬上更新到最新版。

**這輪根治的做法**：
- 每次commit有動到`index.html`/`sw.js`，版本號會自動改寫（見第1節），
  **不用再手動記得改版本號**。
- SW本身其實一直是network-first（優先打網路拿最新版，只有離線才退回
  快取）——這部分沒有新bug，是這輪確認過的。
- 新增了「模擬手機已裝舊版SW快取」的自動化測試（`smoke_test.mjs` check 9），
  驗證「就算快取裡有舊內容，重新整理後還是會顯示最新版」。

**30秒驗證方式**：
1. 打開App，看首頁底部或設定頁「關於」卡片的版本號，是不是
   `2026-08-28.0329`（見第1節）。
2. 是最新版但時鐘還是不動——才是真正的新bug，需要回報並附上「設定頁 → 
   點開紅色錯誤橫幅（如果有的話）」的截圖。
3. 不是最新版——按「強制更新」或整個關掉分頁重開，再檢查一次版本號。

**重整按鈕**：查出真正的根因不是按鈕失效（實測按鈕本來就有正確觸發
資料重新抓取），而是「最後更新」時間戳欄位從來沒有被寫入過（畫面上
永遠卡在`--:--`），使用者才會覺得「按了沒反應」。現在按下重新整理／
下拉重新整理，按鈕文字會變「更新中…」、完成後「最後更新」時間戳會更新
＋跳出一個小提示框確認，30秒內肉眼就能看到差異。

---

## 4. 未解方向調查結論

- **TWSE官方`exchangeReport/STOCK_DAY`（單股歷史日線）反爬蟲**：本輪
  實測整批被擋（53檔測試全部428），這條路目前不可行，改用FinMind即時
  API當替代方案。
- **FinMind額度/IP封鎖是這幾輪最大的瓶頸**：整晚遇到過兩種不同的限制
  機制——(a) 403 `ip banned`（明確倒數秒數的臨時封鎖，比較嚴重）、
  (b) 402 `quota exhausted`（一般額度用盡，通常整點重置）。**這兩種都會
  連帶影響App本身**（`loadStockInfo()`打FinMind的路徑），這是這幾輪冒煙
  測試偶爾FAIL的根因，不是程式碼regression——已在PROGRESS.md逐輪記錄，
  最後一次commit（`96c2557`）附完整的獨立驗證過程。**建議**：白天/尖峰
  時段避免短時間內對FinMind開太多請求（尤其是同時跑多個研究腳本），
  之前已知的「約每小時數百次」估計可能偏樂觀，實測整晚踩到限制的頻率
  比預期高。
- **B24 PIT回測的樣本池太小，需要使用者決定要不要投入更多運算**：
  詳見第6節，這是今晚發現的最重要方法論問題。

---

## 5. UX 走查發現與已修/待修清單

（第4輪走查，2026-08-28 03:25，見`docs/UX_AUDIT.md`完整記錄）

**已修**：
- UX-1：選股頁「主流題材」空狀態訊息把「真的沒有產業符合條件」跟
  「連線失敗」用「或」糊成一句話，使用者看了不知道該不該按重新整理
  ——已改成兩句各自獨立、不含糊的訊息。

**待修**：目前沒有其他已知的P1/P2 UX問題（走查涵蓋六個分頁+個股評分
報告畫面，都功能完整、版面在393×852下沒有溢出/擠壓問題）。**走查方法
侷限性**：這是本機`http.server`直接服務的版本，不是透過GitHub Pages+
Service Worker的真實部署路徑，測不到iOS Safari特有的PWA/觸控行為
——這正是第3節時鐘問題的已知盲點類型，之後如果要更貼近真機體驗，需要
另外設計測試方法。

---

## 6. B24 PIT回測結果（誠實記錄，含重大方法論修正說明）

**第一次結果（`eb94649`）**：用`score_v2.py`的價值成長榜八大因子組合
（=App正式上線的邏輯），月度再平衡、持股20檔、全成本，跑2015-2020
（TRAIN）跟2021-2024（VALIDATION）兩期：

| 期間 | 策略報酬 | 買進持有大盤 | 策略MDD | 大盤MDD | alpha顯著 | 隨機對照百分位(30次) |
|---|---|---|---|---|---|---|
| TRAIN 2015-2020 | -1.03% | +58.86% | -46.61% | -28.72% | 否(p=0.82) | 0.0（30次draws墊底）|
| VALIDATION 2021-2024 | -1.06% | +54.58% | -48.44% | -31.63% | 否(p=0.81) | 0.0（30次draws墊底）|

第一眼看起來是很差的結果（策略小賠、大盤大賺、alpha不顯著、輸給全部
隨機對照組）。

**但這輪做了sanity check，發現重大方法論問題，上面這組數字先保留、
不能當成「App選股能力差」的定論（`9a0fd2b`）**：

用來跑回測的`factor_ic.py`100檔研究樣本，是設計給「因子IC統計檢定」用
的，不是為了支撐「20檔實際持股組合」設計的完整可投資宇宙——扣掉流動性
門檻後，合格股票池平均只有約11.5檔（抽樣20個時間點，範圍0~18檔），
**從未達到20檔持股目標**。回測引擎的資金配置是固定除以20，代表平均
約40-45%的資金整個回測期間都閒置在現金——這對「策略 vs 100%投入的
買進持有大盤」是結構性不公平的比較，**主要在測「現金拖累有多大」，
不是在測「選股能力」**。隨機對照組在合格池不足20檔時等於全拿、跟真實
策略選到同一批股票，「百分位0.0」這個數字也因此不能當統計證據。

（過程中也抓到並修正了兩個真bug：一個是`compute_scores_v2()`在無資料
日期會讓腳本crash，一個是B23回補腳本的「有沒有補到」計數邏輯把「缺口
修好但列數不變」誤判成沒補到——都已修正，不影響資料本身正確性，只影響
統計報告數字。）

**正確的下一步，需要使用者決定**：要得到有意義的PIT回測結果，需要一個
明顯更大的可投資股票池（建議至少300-500檔，確保流動性門檻後仍能穩定
填滿20個持股名額）。這需要對更多股票重新跑因子運算（每檔約13秒，
300-500檔預估一次性載入成本65-110分鐘）＋更多FinMind即時資料請求（可能
再撞到額度/封鎖限制）。**這輪刻意沒有夜間擅自投入這個量級的運算/API
用量，留給使用者醒來後決定要不要做、要做多大規模**。

---

## 7. 今日建議優先序

1. **決定B24 PIT回測要不要擴大樣本池**（見第6節）——這是回答「App到底
   選不選得到會賺錢的股票」最關鍵的下一步，但運算/API成本不小，需要
   使用者拍板。
2. **B23殘留約1,598檔股票的深度回補**——`research/backfill_price_history_gaps.py`
   已可重跑，等FinMind額度自然恢復（通常整點重置）後續跑，每次抓
   幾百檔、不要一次衝完，避免又撞到封鎖。
3. **B23的量價配合度/創新高因子門檻參數敏感度掃描**（±30%）——使用者
   原話「只有單點有效就是曲線擬合」，這個掃描要等B24回測骨架更成熟後
   一起做。
4. **B24第三步：翻倍率/地雷率統計 + 選股成績單UI**——`data/picks_ledger.json`
   的快照機制已經在跑（每天自動累積），回填機制(`update_picks_ledger_returns.py`)
   還是骨架，等有幾天真實快照資料後才有東西可以回填/顯示。
5. 其餘BACKLOG.md裡的❌待處理項目（B18/B19/B20/B21/B22訊號管線相關）
   都還沒開始，依原本的P0後次順位。

---

## 附記：夜間自主循環的運作方式

使用者昨晚指示啟動「每30分鐘一輪」的自主開發循環，整晚透過
`ScheduleWakeup`機制自我排程，持續工作到現在（約6小時，含多輪commit/
push、UX走查、兩個獨立研究腳本的除錯過程）。過程中遇到的意外狀況
（FinMind封鎖、網路瞬斷造成push失敗、背景任務log檔案意外消失等）都在
BACKLOG.md/PROGRESS.md裡逐輪誠實記錄，沒有隱藏或美化任何失敗/卡關的
過程。這份報告產出後，夜間循環會停止（不再需要無人值守繼續跑），
等使用者醒來後給新指示。
