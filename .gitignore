# Phase 2 research pipeline — code (.py) and docs (.md) are always versioned.
# Only large/regenerable/secret files are excluded below. Never add a bare
# "*.py" or "research/" pattern here -- that would silently hide code from
# Cowork's audit, which is exactly the failure this comment exists to prevent.
research/data/
# 2026-09-07（資料一.1）逐筆tick落地目錄。上面的 research/data/ 其實已經蓋住它了，
# 這一條是刻意重複的雙保險兼文件：一天可能好幾百MB的熱資料，絕不能進公開 repo，
# 萬一以後有人放寬上面那條規則，這一條還在。
research/data/ticks/
research/.marathon.lock
research/.hypothesis_queue.lock
# 2026-09-18（順手修，本輪意外發現的真gap）：DevQueue自己的鎖檔一直沒
# 被加進來——跟上面兩個軌道的鎖檔同樣性質（排程行程寫的暫態狀態檔，
# 依CLAUDE.md第十節「排程腳本會改寫的檔案必須進對應清單」的精神，
# scripts/dev_queue_runner.py::MACHINE_WRITTEN已經有regex涵蓋
# `research/.devqueue.lock`（`^research/\.[^/]+$`），但.gitignore本身
# 一直漏了這一行，代表若有人不小心`git add -A`會把這個鎖檔意外commit
# 進去。
research/.devqueue.lock
# 2026-09-23（結案.一方案甲，git_op_lock.py新增）：三支wrapper共用的
# git操作鎖，跟上面幾個軌道鎖檔同樣性質，一併加進來避免同一個gap重演。
research/.git_op.lock
# 2026-09-18（Cowork【重構.B續·先別放棄】三，馬拉松軌能見度）：這兩個
# cycle log被排除在版控之外，是「今天六筆commit全是馬拉松軌做的，但它
# 的運作狀態從repo完全看不見」這個盲點的直接根因——local_task_health的
# mtime新鮮度監控本來就正常運作（本機端看得到「最近有沒有更新」），但
# 判斷「這段安靜期是在想／被節流／死了」需要log內容本身，不是只有一個
# 新鮮度布林值。跟昨天research/dev_queue_cycle.log同一個修法：改成兩軌
# 各自每輪結束自己commit（見run-marathon-cycle.ps1/run-hypothesis-queue-
# cycle.ps1），沿用allowlist只commit這個檔，不要git add -A。
research/ibkr_quotes_cycle.log
research/ibkr_gateway_cycle.log
research/.ibkr_order_token
research/.shioaji_order_token
research/shioaji_quotes_cycle.log
research/data_audit_cycle.log
research/.shioaji_stream.pid
research/shioaji_stream_stdout.log
research/shioaji_stream_stderr.log
research/.alpha_live_token
# 2026-09-03深夜：shioaji_quotes.py常駐行程寫的本機熱檔（秒級快照+當日1分K），只給alpha_live_server.py讀，永遠不commit
research/.live_state_sinopac.json
research/.live_state_sinopac.json.tmp
*.parquet
*.db
fred_key.txt
.env
secrets/
*.pfx
shioaji.log
__pycache__/
*.pyc

# 2026-08-27新增：Node.js + Playwright（給 scripts/smoke_test.mjs 用），
# node_modules 是可重新產生的相依套件，不是原始碼，跟 .py/.md「一律版控」
# 的原則不衝突（那條規則是防止藏程式碼，不是要求commit相依套件）。
node_modules/
package-lock.json
# 2026-09-04：alpha_live_server常駐啟動器的PID/log
research/.alpha_live_server.pid
research/alpha_live_server_cycle.log
research/alpha_live_server_stdout.log
research/alpha_live_server_stderr.log
# 2026-09-05 基礎設施維修：脫離session工作登記簿/log、每輪cycle的stream-json（都在research/data/，已被上面的規則涵蓋，這裡只註明用途）
.security_events.jsonl
research/multibagger_raw/
