# UX.一 App 介面改版提案（提案＋原型，尚未實作）

日期：2026-09-26　帽子：開發　狀態：**等待總司令審閱**（未動 `index.html`）

範圍界線（照交辦）：只借鑑「首頁先講故事、再給資料」的概念，版面與程式碼皆自行撰寫，
不複製任何第三方網站的程式碼／素材；**不含**登入、推播、會員、金流。
收費相關一律暫緩，須先經律師確認《證券投資信託及顧問法》第 4 條、第 107 條適用範圍（本提案不碰）。

原型：`prototype/ux_v2.html`（獨立檔，不影響現行 App）。
本機開啟：在 `alpha-app` 目錄跑 `python -m http.server 8792`，再開 `http://127.0.0.1:8792/prototype/ux_v2.html`。
線上部署後網址為 `https://jlove1314520.github.io/alpha-app/prototype/ux_v2.html`（push 後才有，目前本機連不上 GitHub，尚未 push）。

---

## ① 現況盤點（393×852，2026-09-26 實截，`docs/ux_shots/*.png`）

| 分頁 | 這頁想回答使用者什麼問題 | 答得出來嗎 | 標註 |
|---|---|---|---|
| 今日 | 我的自選股今天怎麼樣？我們的選股有沒有贏 0050？ | 自選股：能。「我們 vs 0050」：**不能下結論**，樣本僅 12 個交易日，畫面自己已承認「不足以判斷優劣」 | 首屏第二張卡就是一張「還不能判斷」的卡，價值主張被稀釋 |
| 市場 | 大盤與類股今天強弱？ | 能（指數、類股漲跌幅、資料時間與來源都有標） | 「前次收盤（09-24）」與「更新 16:52」並存，新手會疑惑哪個才是資料日 |
| 選股 | 哪些股票分數高？為什麼？ | 榜單能；**「分數怎麼算」需點進個股才看得到**，榜單頁本身答不出 | 警語很多（3 條 ⚠）但都是黃字長句，一次塞太多 |
| 交易 | 我可以在這裡下單嗎？ | 能答：**不能**（有明確警語）。但整頁其實是空殼介面規劃 | 對第一次來的人，一個分頁全是「尚未串接」，建議降低導覽權重 |
| 日誌 | 我的交易績效？ | 不能（無交易紀錄） | 空狀態文字清楚，但與「交易」頁的空殼重複 |
| 設定 | 怎麼連券商／調風控／看資料狀態？ | 能 | 資料來源狀態是好內容，但埋在設定頁；使用者很難發現「這些數字可不可信」 |

另有兩個實截看到的具體問題：
1. **顏色語意衝突**：首頁「vs 0050 差距 −45.81pp」用**綠色**顯示。App 慣例是台股「紅漲綠跌」，
   而「輸給大盤」是壞消息卻用綠字，一眼看會讀成好消息。建議「差距」類指標改用中性色＋文字標明「落後／領先」，
   不沿用漲跌色。（此為設計判斷，未逐一核對 `index.html` 內所有同類用法。）
2. 載入 6 個分頁全程 `pageerror` 為 0（本次腳本實測），功能面沒有明顯壞掉；問題在資訊層次，不在錯誤。

## ② 首頁故事化

現況首屏：狀態列 → 自選股 → 「我們 vs 0050」。使用者第一次打開，看到的是「工具」，看不到「這是什麼、為什麼可信」。

提案首屏（原型已實作）：
- **一句話價值主張**：「先看懂一檔股票，再決定要不要相信分數。」＋一行免責（研究／教育用途，非投資建議）。
- **用一檔真實個股示範核心圖表**：原型即時抓 FinMind 台積電 2330 近 60 日收盤，現價、漲跌、走勢線、60 日高低、資料日。
  本次實測顯示 2,475（與現行 App 自選股同值）。
- **抓不到就誠實說**：顯示原因（HTTP 狀態／筆數不足），**不放假數字**。
- 「我們 vs 0050」在樣本 <60 個交易日時**收合到次要位置**，不再佔首屏（前提：總司令同意這個門檻，見「待裁示」）。

## ③ 設計規範（CSS 變數）

現行 `:root` 已有色彩 token（暗色＋香檳金），但**沒有字級／間距／圓角／陰影 token，也沒有淺色模式**（`color-scheme: dark` 寫死）。
原型補齊並示範：

| 類別 | 變數 |
|---|---|
| 色彩 | `--page --surface --surface-2 --hairline --ink --ink-2 --ink-3 --gold --up --down --warn` |
| 字級 | `--fs-xs 12 / sm 13.5 / md 15 / lg 18 / xl 26 / hero 32` |
| 間距 | `--sp-1..6 = 4 / 8 / 12 / 16 / 24 / 32` |
| 圓角 | `--r-sm 8 / md 14 / lg 20` |
| 陰影 | `--shadow` |

深淺色：預設跟隨系統（`prefers-color-scheme`），右上角可手動切換並記住（localStorage）。
淺色的紅綠 `--up/--down` 另外調深，以維持對比；**台股慣例紅漲綠跌在兩種模式都維持**。
（未做完整 WCAG 對比計算，正式實作前應逐色驗證，這是已知缺口。）

## ④ 互動動畫

原型實作：捲動淡入（IntersectionObserver）、數字跳動（現價 0→終值，700ms ease-out）、
載入骨架條、三種空畫面（載入中／無資料／失敗）分開處理、新手導覽（3 步，**可跳過、看過就記住**）。
**`prefers-reduced-motion`**：CSS 一律關閉動畫與過場，數字直接顯示終值。
本次實測：暗色／淺色／減少動態三種模式頁面錯誤皆 0；導覽第一次會出現，按「跳過」後重新整理不再出現。
現行 App 已有 `.rise`／`.flash-up` 等動畫但**沒有統一的減少動態處理**（未逐條核對，建議實作時全面補上）。

## ⑤ 方法論頁

原型已放一版骨架：資料從哪來／內行人會問的（為什麼不是「越高分越該買」）／已知限制。
內容全部取自現行 App 與 `CLAUDE.md` 已有的揭露（排行榜尚未經回測驗證、資料完整度、美股已下市股 7.1% 無價格來源、
報價為盤後／延遲資料），**沒有新增未經驗證的說法**。
正式版建議：每個分數一段「怎麼算／輸入資料／更新頻率／已知偏誤」，並從選股榜每張卡直接連過去（現況要點進個股才看得到）。

## ⑥ 用語審查（推介疑慮清單）

方法：對 `index.html` 搜尋「買進／推薦／必漲／必賺／穩賺／強烈／飆股／抄底／進場點／買點／賣點／目標價／看多／看空／
建議買賣加碼持有／勝率／保證」等字。**限制**：這是關鍵字掃描，不等於逐字通讀 53 萬字元的檔案，
漏網之魚（同義詞、動態組字串）可能存在，實作階段應以「全文逐頁人工走一遍」補強。

| 位置（`index.html` 行） | 現行文字 | 疑慮 | 建議中性替代 |
|---|---|---|---|
| 892 | 「估值區間（非目標價） / **建議進場價** / **分批買入計畫**」 | 「建議…進場價」「買入計畫」讀起來是操作建議，且與同列「非目標價」自相矛盾 | 「估值區間（非目標價）／價位換算（機械規則）／分批資金配置範例」 |
| 5852 等 | 「現價進場」「進場階梯」 | 同上，有下單指令感 | 「以現價為基準」「回檔價位參考」 |
| 1138、5108、5135 | 「買進」按鈕／「送出買進（示範）」「送出買進 Paper 委託」 | 交易頁功能用語，屬功能名稱非推薦；但現況為未串接示範 | 保留動詞（這是下單介面必要用語），確保旁邊恆有「示範／Paper」字樣；未串接時整頁弱化 |
| 5721 | 「追高風險提醒…下方進場階梯照常顯示」 | 風險提醒本身合宜；但「照常顯示」承接前一條的進場語彙 | 隨上兩條一併改用語即可 |
| 2659 | 「持有假設：等權重買進 Top20」 | 屬回測方法說明，非推薦 | 可維持；建議改「假設等權重持有前 20 名」 |
| 選股頁 | 「⚠ 動能/主流…不是『資金正在流入』就代表可以追高」 | 這是**反向警語**，方向正確 | 維持（建議縮短） |

未發現「必漲／必賺／穩賺／飆股／抄底」等強推介詞出現在使用者可見文案（僅程式註解中的討論文字）。
**法遵定性（這些用語是否構成投資建議）不是工程判斷**，須與律師確認同一個問題（證投顧法第 4／107 條），本表僅列風險用語與替代方案。

## ⑦ 產出與下一步

- 本文件＋原型 `prototype/ux_v2.html`＋現況截圖 `docs/ux_shots/`（6 頁現行 App＋原型 3 種模式）。
- **未實作**任何 `index.html` 變更；改版需總司令核准後另開實作項。

### 待總司令裁示（提案先於執行）
1. 首頁是否採用「價值主張＋真實個股示範」取代現行首屏順序？
2. 「我們 vs 0050」樣本不足時收合，門檻用多少交易日（建議 ≥60）？
3. 是否導入淺色模式？（成本：全 App 色彩逐色驗證對比；`index.html` 目前寫死暗色）
4. 用語替代（⑥表前兩列）是否先行套用？（純文字，低風險；但涉及法遵定性，建議律師確認後再改）
5. 「交易」「日誌」兩個目前皆為空殼的分頁，是否降低導覽權重（例如收進「更多」）？

### 誠實揭露
- 截圖與原型驗證皆在本機 Chromium（Playwright）393×852 執行，**未在總司令實際手機上驗證**；原型未部署，故無線上實測。
- 原型的 FinMind 呼叫沿用現行 App 同一端點與免 token 做法，本次只打 1 次，遠低於免費層額度。
- 淺色模式配色未做對比度量化驗證。
