> **⚠️ SUPERSEDED（2026-09-23總司令裁示【拆除機構約束，改集中版】規.一）**：
> 本檔案已作廢，移入`research/archive/`保留稽核軌跡，不得再依此規格
> 開新試驗。作廢理由——總司令裁示目標函數收斂為單一條「贏0050/
> S&P500總報酬」（天條二），不再對標機構化指標（TE≤2.5%、市值權重
> 為底+因子傾斜建構法、持股60~80檔、0050成份股清單與權重重建全部
> 連帶作廢）。取代規格見`research/CONCENTRATED_SPEC.md`（規.二，
> 集中版）。本檔案下方內容原樣保留供稽核追溯「為什麼TE路線被放棄」，
> 不代表現行有效規則。`implied_market_cap_validation.py`已一併歸檔
> 至`research/archive/`（實測`grep`確認沒有任何其他檔案import它，
> 不是方法.二/方法.三控制組市值分位配對的實際來源——那個用的是
> `core_tilt_backtest.py::build_market_cap_lookup()`，PBR×權益法，
> 該工具本身仍在使用中，只是TE約束建構邏輯部分已隨本檔案一併作廢，
> 見`core_tilt_backtest.py`檔頭的SUPERSEDED notice）。

# CORE_TILT_SPEC.md — 基準相對傾斜（core_tilt）策略規格書（v3，2026-09-18修訂）

**狀態：v1核准附三處必改→v2依裁示修訂（四件事）→總司令裁示【市值
重建—先查一件事，再決定要不要做校正工程】，本節（第2.1.2節「驗證
v3」）已完成查證與重跑，**兩法差異未達核准門檻（中位數4.75%達標，
但P90 18.28%超過12%門檻，AND判定不通過）**，`core_tilt_backtest.py`
依總司令原話「我再裁示」明確保留為決策點，不自行推進。** 依總司令
「SPEC寫完先給總司令看，不先回測」裁示（`PENDING_QUEUE.md` 續2章節
【階段二】），這份文件只鎖規則，**不含任何回測結果**。`PENDING_
QUEUE.md` `重構.C` 這一條維持`- [x]`（SPEC文件本身的撰寫/修訂工作
已完成的狀態不變，跟`core_tilt_backtest.py`能不能動筆是兩件事）。

**本輪追加修訂摘要（2026-09-18總司令【裁示】市值重建——先查一件事，
再決定要不要做校正工程；取代下方「v2→v3修訂摘要」第2點的數字，見
第2.1.2節「驗證v3」）**：
1. **用詞更正**：「誤差」全面改成「兩法差異」（這份驗證沒有獨立
   真值，見2.1.2節開頭）。
2. **十分鐘查證找到更好的路徑2資料源**：TWSE`t187ap03_L`有直接股數
   欄位（`已發行普通股數`）+真實面額，取代原本「股本÷10」的假設。
3. **重跑34檔**：中位數4.75%（達標）、**P90 18.28%（不達標，門檻
   12%）**，AND判定**未核准**。金融保險股差異擴大到18~21%（路徑2
   更準確後，路徑1原本的偏差被更完整揭露，不是新問題）。最大10檔
   差異與根因、市值分位表、產業表全部更新，見2.1.2節。
4. **`core_tilt_backtest.py`依然不得動筆**，總司令原話明確保留這個
   決策點（「達不到→回報最大10檔差異，我再裁示」）。

**v2→v3修訂摘要（原文保留，2026-09-18總司令【裁示】市值重建方案有
條件核准，四件事——第2點數字已被上方本輪追加修訂取代，其餘三點不變）**：
1. **第2.1節開頭新增觀念釐清**：績效比較（策略vs0050報酬）只需0050
   真實價格，精確、不受權重重建誤差影響；約束機制（產業中性/主動
   權重上限）才需要重建的0050權重，重建誤差方向保守（只會讓TE控制
   不夠準、偏高，不會讓績效數字變好看）。
2. **驗證重做，樣本擴大到34檔**（原v2只驗台積電1檔）：跨市值三級距
   （大/中/小各10檔）+ 強制納入金控（2882/2881）、控股多子公司
   （2317）、KY股（5871）。**中位誤差5.89%、P90 17.28%、最大
   120.27%**。誤差非隨機：金融保險股系統性偏高9~10%（根因確認：
   FinMind對金融業equity欄位缺值）；少數面額非10元個股誤差可達
   20~120%（根因：路徑2「面額10元」假設不成立，找到免資料源自我
   診斷法）。詳見第2.1.1節完整表格與根因分析。
3. **新增第二條獨立重建路徑**（`market_cap≈收盤價×股本/10`，資料源
   完全獨立於路徑1）用於交叉驗證，也是上面誤差分析的基礎。
4. **第12節補充限制**：安全樣本池delisted佔比9.7%低於全宇宙14.14%，
   下市股在樣本池裡仍被低估約三分之一，已列入既有限制段。

**v1→v2修訂摘要（原文保留，2026-09-18 Cowork【裁示】三處必改+三個
問題回答）**：
1. 第5節單檔權重「絕對上限5%」（管集中度的工具）改成「主動權重上限
   |w_策略,i−w_0050,i|≤2pp」（管追蹤誤差的工具，用錯工具已修正）。
2. 第12節樣本從80檔VAL-only（選股率75~100%，選股在數學上不做任何事）
   改成零新API安全樣本池（universe()合併bug修復後重算為1,588檔，見
   第12節）。
3. 第9節(d)升格為主對照組，第10節主判定改成跑在「因子傾斜−資格池市值
   加權」這一項上，不是「策略−0050」整體（後者混進「持股比0050分散」
   這個不算我們本事的效果）。
4. 三個問題回答：tilt_strength掃描範圍{0.25,0.5,1.0}三格、僅TRAIN期
   決定；產業中性±3pp核准、單檔絕對上限5%不核准（併入第1點）；回測
   期間改TRAIN+VAL全期（10年）取得檢定力，不再只用VAL 3.85年。

這份規格取代（不是刪除）`PORTFOLIO_STRATEGY_SPEC.md`（v2）——v2 對標
TAIEX、20 檔等權挑選、追蹤誤差沒有硬性上限；這裡是總司令 2026-09-18
【研究方向重構】裁示的「階段二」，目標明確改成「贏過 0050」而非證明
CAPM alpha 存在，兩份規格並存，各自的回測程式碼互不覆蓋（v2 的
`portfolio_backtest_v2.py` 保留，`core_tilt` 另立新程式檔）。

---

## 0. 為什麼是這些數字（TE≤2.5% 的來源，不是憑感覺設的）

依 `research/MARATHON_PROTOCOL.md` 「1a-0b 修正」5' 條與總司令【更正】
原話（`PENDING_QUEUE.md` 續5章節）：

- 季頻（t60）贏過 0050 的損益兩平毛 alpha 只要 **2.93%**（`breakeven_
  alpha_table.json`），是目前已知換倉頻率裡經濟上最划算的目標，**不能
  因為好測就改用月頻規避**（1a-0b修正4'條：改用更短持有期一律視為規避，
  不予採納）。
- 用 `power_budget.py::min_detectable_alpha()` 對既有 12 組構造（平均
  年化追蹤誤差 11.5%）反推的線性關係（非精確解析解，僅供 SPEC 設計時
  定量）：
  - TE 5.0% → MDE ≈ 6.7%（仍超過 t60 損益兩平線 3 倍門檻 8.79%，不准跑）
  - TE 2.5% → MDE ≈ 3.4%（接近但未過 8.79% 這條線，**這裡是本 SPEC
    的硬性設計約束**）
  - TE 2.2% → MDE ≈ 2.95%（剛好覆蓋門檻，是更理想但更難達成的目標）
- **因此本 SPEC 每一條交易規則，存在的理由都是把年化追蹤誤差往
  2.2%~2.5% 這個區間壓，不是可有可無的裝飾**——第 1~4 節逐條標註
  「這條規則對 TE 的作用」，依總司令裁示這是強制要求，不能只是照抄
  `PORTFOLIO_STRATEGY_SPEC.md` 的格式卻沒有對應到這個數字目標。

真正的 TE 只有實測回測才知道，這份 SPEC 只能先用規則設計去「瞄準」這個
區間；回測跑完後若實測 TE 仍高於 2.5%，依 1a-0b修正4'條，**唯一被接受
的正確反應是重新設計構造（擴大持股、加強產業中性、換加權方式）把 TE
壓下來，不准改用更短持有期規避**。

## 0之1. 選股 alpha 目標值（天條一.1 實測，2026-09-19，跟第0節的
MDE=3.4% 是不同概念，數字巧合接近，不要混為一談）

`CLAUDE.md`最上方「天條」新增後，**選股alpha的目標值改由「滿足天條一
（MDD≤50%）的最低代價」反推，而不是憑感覺假設**——`research/SURVIVAL_
CONSTRAINT.md`（天條一.1）已實測固定股債比{100/0, 85/15, 70/30, 60/40}
（0050+定存代理）：

| 配置 | 全期MDD | 天條一判定 | 報酬缺口(vs 純0050) |
|---|---|---|---|
| 100/0（純0050） | −55.75% | VIOLATES_SURVIVAL | 0（基準） |
| 85/15 | −49.25%（僅0.75pp margin） | PASS（壓線） | **1.41%/年** |
| 70/30 | −42.06%（7.94pp margin） | PASS（有餘裕） | **2.89%/年** |
| 60/40 | −36.86% | PASS | 3.93%/年 |

**目標值**：選股alpha要打敗的，是「用永久性股債配置換取天條一合規」
所犧牲的報酬——這才是`core_tilt`存在的真正理由（用選股alpha取代降
曝險的報酬犧牲，而不是無條件地跟0050比報酬）。**兩個候選目標值並列**
（85/15數學上代價最小但margin太薄、70/30margin較厚但代價較高，選哪
一個留給總司令或後續設計階段裁量，本節不預設）：
- 保守目標（對齊70/30，margin較厚）：**年化2.89%**
- 積極目標（對齊85/15，margin僅0.75pp，樣本外崩盤風險較高）：
  **年化1.41%**

這個數字**取代**總司令原文暫定的「3.4%（70/30粗估）」估計值——實測
2.89%比粗估的3.4%更精確也更寬鬆（選股alpha需要打敗的門檻更低）。
**跟第0節的MDE≈3.4%是完全不同的兩件事，只是數字剛好接近，容易混淆，
特此並列說明**：第0節的3.4%是「在TE=2.5%假設下，統計上能被目前12組
構造的檢定力偵測出來的最小alpha效果量」（跟能不能達到天條一無關，是
純統計檢定力概念）；這裡的2.89%/1.41%是「投資上選股alpha必須打敗的
機會成本」（不是檢定力極限，是真實的經濟門檻）。兩者剛好同數量級純屬
巧合，**選股alpha的正式目標仍須同時滿足兩條**：統計上要偵測得到
（MDE，第0節）、經濟上要有意義（機會成本，本節）。

**⚠️ 2026-09-19總司令裁示【成本模型更正】成本.三後續影響（第0節TE≤2.5%
需要收緊，本次只註記不逕自改SPEC）**：`power_budget.py`的統計關卡已
從「MDE<3×t60損益兩平線(8.79%)」改成「MDE<`LOCKED_TARGET_ALPHA_PCT`
（本節上方鎖定值2.89%，不隨成本浮動）」——用同一個n_years=4反推，
**第0節現有TE≤2.5%設計約束給出的MDE≈3.50%已經FAIL新門檻**，要讓
MDE<2.89%，TE需要收緊到約**2.0%~2.06%**，比第0節寫的2.2~2.5%都更嚴。
完整推導見`MARATHON_PROTOCOL.md`「1a-0b再修正（成本.三）」小節。
**這裡只記錄這個連帶影響，不逕自把第0節的2.5%改成2.0%**——那涉及
重寫已經影響SPEC其他規則設計的核心數字，需要總司令核准或留給
`core_tilt_backtest.py`真正動筆的那一輪一併處理，此處先誠實掛號。

**⚠️ 2026-09-19後續：`core_tilt_backtest.py`已經動筆（總司令裁示
【#63邊緣案例＋安全邊際倍數重新錨定】三，研究fork執行），完整結果見
`CORE_TILT_TE_FEASIBILITY.md`。headline結論：反推所需TE=2.0631%
（n_years=4）；本次實測的市值加權+主動權重帶構造（9組holdings×band
網格）**全部遠遠不達標**（最佳11.87%，是門檻的5.75倍），且beta系統性
偏高（1.35~1.42）。根因診斷是選股邏輯本身——本次因缺乏0050真實成分股
名單，選股用「先按因子分排序選出N檔，才在其中做市值加權」，這不是
SPEC真正設計的「維持接近0050成分股名單、只做小幅權重傾斜」，所以
**這個FAIL是這個具體實作的FAIL，不是對core_tilt整條設計路線的最終
判決**——真正的答案要等0050成分股精確重建（本節2.1的既有缺口）完成
後才測得出來。第0節的TE≤2.5%數字**依然不逕自改寫**，等那一輪真正的
測試結果出來再一併處理。

## 1. 基準與因子成分

- **對標標的**：0050（元大台灣50 ETF），不是 TAIEX。價格序列用既有
  `adjusted_price_series("0050", "2003-01-01")`（`passive_benchmark_
  tw_v1.py` 已在用同一支函式，直接沿用，不重寫）。
- **因子成分**：沿用 `PORTFOLIO_STRATEGY_SPEC.md` 第1節「版本A」——
  `f_eps_growth` + `f_eps_surprise`（同家族，合併成 `eps_family` 單一
  成分，依 `CLAUDE.md` 七之三節「同家族因子只能算一個獨立發現」）+
  `f_revenue_surprise` + `f_low_vol`，共 **3 個獨立成分**（4 個原始
  訊號合併去重後）。**不含 `f_value_pe`**——那是版本B的「待複驗候選
  CANDIDATE」，不是 PASS，總司令這輪原話只說「沿用已 PASS 的」，不
  納入。
  - **⛔ 2026-09-20但書（財報PIT.二(c)）**：本節「沿用已 PASS 的」
    「已知通過驗證的成分」已過時——`eps_family`（`f_eps_growth`+
    `f_eps_surprise`）與`f_revenue_surprise`因Q4 PIT前視修正全部失去PASS
    （`TRIALS_LEDGER.md`#287/#288/#289，`FACTORS.md`重大更正段）；只有`f_low_vol`
    （純價量、不經`quarterly_pit()`）不受影響。core_tilt已因TE不可行判死
    （#283），**最終判定不變**；但本節與下方任何以「這三個成分IC強」佐證設計的
    論點，一律視為未經驗證的假設，不得引用為證據。
  - **對 TE 的作用**：沿用已知通過驗證的成分，不在這輪同時引入新的
    未驗證因子，避免把「構造設計是否達成 TE 目標」跟「因子本身是否
    有效」兩個問題混在一起測不清楚。

## 2. 加權方式：市值權重為底 + 因子傾斜（不是等權自由挑）

- **基底權重**：資格池內個股依自由流通市值加權（`w_base_i ∝ market_
  cap_i`），不是 `PORTFOLIO_STRATEGY_SPEC.md` 的等權。
- **因子傾斜**：對基底權重做**傾斜調整**，不是重新排名選股——
  `w_final_i = w_base_i × (1 + tilt_strength × zscore_i)`，其中
  `zscore_i` 是個股綜合分（因子同產業 peer z-score 標準化，沿用
  `score.py::_zscore_within_group`）、`tilt_strength` 是唯一新增的
  傾斜強度自由參數（見第7節參數上限）。傾斜後權重需重新正規化使總和
  為1，且單檔權重上限見第3節。
  - **對 TE 的作用**：市值權重為底本身就跟 0050（同樣市值加權）高度
    重疊，只在邊際上因子傾斜，結構上天生比「20檔等權完全重新排名」的
    v2 構造更接近 0050，這是達成 TE≤2.5% 最主要的機制來源，比持股
    檔數或產業中性更關鍵。
  - **不做情境條件式加權**（v2 的 `regime_weighted`）：那個機制是用
    大盤位階切換權重，會製造額外的相對 0050 追蹤誤差來源，跟本 SPEC
    「壓低 TE」的目標方向相反，本輪不用。

### 2.1 市值權重從哪裡來（2026-09-18 新增，動筆前查證，發現比預期更大的缺口）

**⚠️ 觀念先行：誤差進到哪裡，這是觀念問題不是技術問題（2026-09-18
總司令裁示【市值重建方案有條件核准】要求開頭先寫清楚）**：

> 【績效比較】策略 vs 0050 報酬 → 只需 0050 的價格序列，我們有，精確。
> 這是整份研究的判定依據，不受權重重建誤差影響。
>
> 【約束機制】產業中性、主動權重上限 → 需要 0050 成分股權重，只能重建。
> 重建誤差只影響「TE 被控制得多準」，方向保守——
> 控制不準會讓實測 TE 偏高，不會讓績效數字變好看。

**這個區分貫穿本節與整份 SPEC，沒有它，下一輪任何人看到「權重是重建
的」都會誤以為整份結果不可信**：
- 第9節(b)（0050買進持有）、9(c)（CAPM對0050回歸）、第4節（beta約束）
  用的是**0050的真實可交易價格序列**（`adjusted_price_series("0050",
  ...)`），跟本節要解決的資料缺口完全無關，這幾處的數字不因本節的
  重建方法而有任何不確定性。
- 第10節主判定（block bootstrap 相對「資格池市值加權基準」的超額
  報酬）用的是**策略自己的持倉報酬**（哪些股票、多少權重）減去
  「資格池市值加權基準」的報酬——這裡的市值加權基底權重需要
  `implied_market_cap`，重建誤差**可能**讓資格池市值加權基準本身
  偏離真實市值加權該有的樣子，但**這頂多讓「資格池市值加權」這個
  對照組本身的權重不夠精準**，不會讓策略的真實持倉報酬或0050的真實
  報酬跟著錯——**判定用的兩個報酬序列（策略報酬、0050報酬）都是
  真實的，唯一可能失真的是中間拿來拆解貢獻用的「資格池市值加權」
  這個輔助基準的內部組成**。
- 第5節★一（主動權重上限）與第3節（產業中性）是**約束機制**：這兩條
  規則用`implied_market_cap`/其模擬的0050權重去限制換股時的持倉，
  若重建誤差系統性偏向某些股票，會讓這兩條約束「以為」自己控制住了
  TE，但實測TE可能因為約束基準本身不準而偏高——**這個方向是保守的**：
  約束機制設計目的是把TE往下壓，重建誤差最壞的結果是「壓得不夠準、
  實測TE比預期高」，不會讓TE無中生有變好看，也不會讓績效數字（第10
  節主判定的報酬序列）失真，因為績效判定根本不經過這個重建市值。

**⚠️ 2026-09-19總司令裁示【先修SPEC再判死】更正（Cowork自己的設計
錯誤，記原因不是只改結論）**：上面這一段把「0050成分股名單拿不到」
定性為**精度問題**（重建誤差只影響「TE被控制得多準」、方向保守）——
**這個定性是錯的**。`core_tilt_backtest.py`實際建置後（見`CORE_TILT_
TE_FEASIBILITY.md`）暴露出來：問題不是「用重建市值去限制換股時的
持倉，限制得不夠精準」，而是第3節「取綜合分前60~80名」這個選股邏輯
**根本沒有從0050的成分股名單出發**——選股本身用因子分數重新排序，
挑出的是一組跟0050完全不同的個股名單，市值加權只是在這組**已經跟
0050無關**的名單「內部」重新分配權重，救不回選股階段就已經產生的
系統性偏離（beta實測1.35~1.42，遠非市場中性）。**0050成分股名單
拿不到，是這整條「市值加權為底＋小幅傾斜」路線能不能成立的**前提
條件**，不是「約束機制精度」這種可以事後修正、方向保守的次要問題**
——精度問題的錯誤是「差一點」，前提條件的錯誤是「問錯了問題」，
兩者的補救方法完全不同（前者調參數，後者要換整個選股邏輯）。
修正方案見下方「2.1.3 用重建市值近似0050成分股名單」。

**背景**：本節與第5節★一（主動權重上限）、第3節（產業中性 vs 0050）
三處全部需要「個股市值」或「0050 各成分股權重」，但**這兩種資料
FinMind 免費層都拿不到**（`TaiwanStockMarketValue` 是付費資料集，見
`factors.py` 檔頭既有揭露），而且經查證，**0050 實際持股權重本身
也沒有免費結構化歷史來源**——查證過程與結論如下：

**查證1：0050 實際成分股權重（v1 核准條件★一要求的前置查證）**
三個候選來源，比照 `CLAUDE.md` 七之三節「搜尋紀律：三來源查證」：
1. **元大投信官網**（`yuantaetfs.com/product/detail/0050/ratio`）：
   有「持股比重」頁面顯示**當前**權重（例如查證當天2330台積電
   ~56.78%），也有每日 PCF（申購買回清單，`/tradeInfo/pcf/0050`）
   列出成分股與股數。**但兩者都只是HTML頁面配一個手動「匯出excel」
   按鈕，沒有可程式化呼叫的API/CSV端點，也沒有歷史封存**——只能看
   當下這一天，抓不到2003~2024的歷史序列。
2. **台灣指數公司**（`taiwanindex.com.tw`，0050 追蹤的「臺灣50指數」
   編製單位）：只公布指數報酬統計與編製方法文件（自由流通量調整
   規則等），**沒有可下載的成分權重檔案**，方法論文件指向 FTSE
   Russell 是更深層的資料擁有者，屬付費/授權範疇。
3. **TWSE openapi**：重新核對144端點swagger，唯一沾邊的
   `/ETFReport/ETFRank` 是月配息定期定額排名，不是持股組成——跟
   `HYPOTHESIS_QUEUE.md` #39（`index_reconstitution_probe.py`）當初
   查證「歷史成分股名單+調整生效日期」的結論一致：**沒有免費結構化
   API**。
   **結論：0050 真實成分股權重，免費且可回溯多年的結構化來源不存在。**
   當下權重、當日PCF雖然存在，但不是「歷史」資料，無法拿來跑
   2003~2024的回測。

**查證2：個股市值本身（本節基底權重＋查證1若要改用自建模型都需要）**
`TaiwanStockMarketValue` 付費，免費層也沒有股數欄位可以自己乘價格
算市值——這是本次查證意外發現的**第二層缺口**：即使放棄取得0050真實
權重、改成「自己模擬一個市值加權的0050」，一樣缺市值資料，這個缺口
連第2節本身的「基底權重」都會擋住，不是只有★一。

**發現的解法（用兩個已在用的免費欄位互相推導，非新資料源依賴）**：
`TaiwanStockPER` 的 `PBR`（股價淨值比）定義就是
`PBR = 市值 / 歸屬母公司業主權益`，而 `balance_sheet_pit()` 已經有
`EquityAttributableToOwnersOfParent`（免費，`TaiwanStockBalanceSheet`
既有欄位，`factors.py` 的ROE因子已經在用）——兩者相乘就反推出隱含市值，
不需要股數欄位：

    implied_market_cap_i(t) = PBR_i(t) × EquityAttributableToOwnersOfParent_i(t)

**驗證v1（2330台積電，2024-01-02，單檔抽查，總司令指出「台積電是最不
該當唯一樣本的那一檔」，已重做見下方驗證v2）**：PBR=4.59、
`EquityAttributableToOwnersOfParent`≈NT$3.348兆，反推隱含市值
≈NT$15.37兆；同日收盤價568.69元，對照台積電當時實際流通股數
（約259億股）算出的真實市值量級約NT$14.7兆——兩者相差約4.5%，
量級吻合。**單檔4.5%不是重點，誤差的結構才是重點**——台積電是單純
製造業、無特別股、無金融業會計格式差異，是「最好過關」的那一檔，
單驗它等於沒驗到任何結構複雜的情況。

### 2.1.1 驗證v2（2026-09-18，Cowork【裁示】要求擴大樣本+新增獨立路徑）

**⚠️ 本節已被下方「2.1.2 驗證v3」部分取代，保留原文供稽核，不得再依
這個版本的數字執行——v3修正了兩件事：(1) 用詞「誤差」改成「兩法差異」
（這份驗證沒有獨立真值，見v3開頭說明）；(2) 路徑2從「股本÷10」升級成
TWSE `t187ap03_L`的直接股數欄位，v2的面額比值校正法（±15%排除規則）
已被更精確的直接讀值取代，不再需要。v2的root cause分析（候選(a)(b)(c)
與面額異常發現）**方向仍然正確**，v3只是用更好的資料源重新量化，
結論見v3。**v3並未達成總司令設定的核准門檻**，`core_tilt_backtest.py`
依然不得動筆。**這個⚠️只標記段落過時，不代表下面段落沒有價值**——
候選(a)(b)(c)的推理過程、面額比值法本身作為一個免費診斷工具，
在v3裡仍被引用/驗證，不是整段作廢。

**新增第二條獨立重建路徑（總司令原話「這條是新增，不是選配」）**：

    路徑2：market_cap ≈ close_price × (實收資本額 ÷ 10)

台股普通股面額絕大多數為每股新台幣10元，「實收資本額」（股本）在TWSE
公開資訊公司基本資料 `t187ap07_L_ci`（一般業）／`t187ap07_L_fh`（金融
業，一般業端點對金控/銀行/證券/保險一律缺值，需要另外查這個端點）都
是免費欄位——`.github/scripts/update_stock_financials.py::fetch_
balance()` 其實已經在算這個代理股數（`shares_outstanding_approx =
股本/10`），本節沿用同一個推導，資料來源上跟路徑1（FinMind PBR+
BalanceSheet）完全獨立（不同資料提供者：TWSE官方直接開放資料 vs
FinMind整理過的資料）。

**驗證樣本**（`research/implied_market_cap_validation.py`，可重複執行）：
34檔，強制納入金控（2882國泰金/2881富邦金）、控股與多子公司（2317
鴻海，非控制權益占比高）、KY股（5871中租-KY），其餘30檔用路徑2市值
分大/中/小三級距各10檔（避免用路徑1自己的結果去分級、造成循環論證）。
特別股另外測（見下方，PBR結構上不存在，無法比較）。

**誤差分布（全樣本34檔，|路徑1−路徑2|相對誤差）**：

| 統計量 | 數值 |
|---|---|
| 中位數 | 5.89% |
| P90 | 17.28% |
| 最大值 | 120.27%（矽力-KY，6415） |

**誤差 vs 市值級距（30檔非強制樣本，各10檔）**：

| 級距 | 中位數 | 平均值 | 最大值 |
|---|---|---|---|
| 大 | 6.03% | 6.58% | 11.35% |
| 中 | 3.04% | 6.03% | 19.82% |
| 小 | 5.18% | **23.33%** | **120.27%** |

中位數在三個級距間差異不大（3~6%），**但小市值級距的平均值與最大值
被極端值拉得非常高**——誤差不是隨市值平滑遞增/遞減，是集中在少數
結構異常的個股（見下方根因）。

**誤差 vs 產業（34檔全樣本）**：金融保險三檔（2882/2881/2883）誤差
**10.11%/10.15%/9.01%，極度一致**（標準差<0.6個百分點）——這是
**乾淨的系統性偏差**，不是雜訊，根因見下方候選(b)。其餘產業誤差
分散在0~20%，除「創新版股票」（6969，60.11%）與「其他」（3040，
29.11%，其industry分類下另一檔3041的19.82%其實屬半導體業，見下方
逐檔根因）這兩個異常值外，多數落在0~11%區間。

**三個候選根因逐一查證（總司令原話「查不出來就誠實寫查不出來，但不
准跳過」）**：

(a) **PBR分母用的是哪一期淨值**：FinMind沒有公開`TaiwanStockPER`
    的PBR內部計算方法（不是我們的資料庫，查不到原始程式碼），**這點
    誠實承認查不到權威文件**。但用兩條獨立路徑互相印證可以間接檢查：
    對正常公司（2434控制組）與3041，用TWSE自己的「每股參考淨值」
    反推股數對照`股本/10`反推股數，比值分別是1.0001與0.9996——
    **幾乎完全吻合**，代表這兩檔的路徑2「面額10元」假設是對的、
    路徑1計算本身也內部一致，3041仍有的-19.82%誤差**目前查不出
    來源，可能就是候選(a)（PBR的淨值分母跟我們用的PIT淨值取值時點
    不完全一致），但沒有更深入的資料可以再往下查，誠實標記「未解決，
    影響單一個股，不影響整體結論」。
(b) **`EquityAttributableToOwnersOfParent`排除非控制權益，TWSE的PBR
    分母可能用總權益，造成固定比例偏差**——**確認為真，且是金融保險
    誤差一致偏高的根因**：FinMind的`TaiwanStockBalanceSheet`對金控股
    （2882/2881/2883）的`EquityAttributableToOwnersOfParent`欄位
    **一律回傳None**（實測查證，不是猜測），`path1_market_cap()`
    當下的寫法會退而使用`Equity`（總權益，含非控制權益）——這跟其他
    正常公司用`EquityAttributableToOwnersOfParent`不是同一個定義，
    造成金融股系統性、穩定地偏高約9~10%。**這條根因不影響像2317
    （鴻海，非控制權益占比11.1%但`EquityAttributableToOwnersOfParent`
    欄位本身有值）這種公司**——2317實測誤差只有5.21%，落在一般水準，
    代表「有揭露非控制權益」的公司本身沒問題，問題出在「FinMind對
    金融業乾脆不揭露這個欄位，逼我們用不同定義」。
(c) **價格與財報的時點錯位**：**排除，不是根因**——實測全部34檔的
    `as_of`（2024-12-30）、`pbr_date`（2024-12-30）、`bs_pit_date`
    （2024-11-14，Q3財報揭露日）**完全一致**，沒有逐檔浮動，代表
    這三個誤差來源候選裡，時點錯位不會造成個股間的誤差差異。

**新發現的第四個根因（總司令三個候選之外，本輪驗證意外揪出的）——
面額非10元**：用TWSE「每股參考淨值」× 路徑2反推股數 對照
「股本/10」反推股數，比值應該接近1.0（正常公司如2434、3041都是
0.9996~1.0001）。**極端值全部在這個比值上露出馬腳**：
矽力-KY(6415)比值≈3.99、6969比值≈0.62、3040比值≈0.74——**這三檔的
面額或股權結構不是單純的「每股新台幣10元」**，矽力-KY是Cayman
（KY）註冊公司，股權結構本來就可能跟一般台股不同，另外兩檔尚未
逐檔查出具體原因（誠實記錄「查不完全」，不是忽略），但**這個比值
本身就是一個免費、不需要額外資料源的診斷指標**，可以用來自動抓出
「路徑2面額假設不成立」的個股。

**結論與建議（待總司令依這份數字裁示，不是本輪自行核准）**：
1. **候選(b)確認為真**：金融保險股需要另外處理（例如統一用
   `Equity`總權益當金融股的市值近似基準、或直接排除出資格池、或
   接受這9~10%的已知系統性偏差並記錄），三個選項留待總司令選擇，
   本SPEC不自行決定。
2. **新發現的面額異常是可自動偵測、可自動處理的**：建議在
   `implied_market_cap`計算前，先用「TWSE每股參考淨值反推股數 ÷
   股本/10反推股數」算一個比值，**比值偏離1.0超過±15%的個股直接
   排除出資格池**（不強行使用一個已知不成立的假設硬算），路徑2本身
   即可完成這個檢查、不需要額外資料源，這是本輪驗證的直接產出，
   建議納入`core_tilt_backtest.py`實作，但**是否採用仍待總司令核准**。
3. **排除金融股（3檔）與面額異常股（3檔）之後，剩餘28/34檔（82%）
   中位誤差4.83%、P90 10.77%、最大19.82%**，中位數跟v1單檔驗證的
   4.5%量級一致，多數股票的重建方法可信——**但這28檔裡的3041仍有
   -19.82%的個別未解誤差**（見候選(a)討論），嚴格說「乾淨且低誤差」
   的部分是27/34（79%），不是全部28檔都低誤差，如實區分「排除已知
   問題類別」跟「保證每一檔都準」，兩者不是同一件事。

**這只是34檔樣本，不是全樣本驗證**，全樣本（1,588檔安全樣本池）实際
跑`core_tilt_backtest.py`時，建議上述「面額比值檢查」對每一檔都算
一次，不是只在34檔驗證樣本裡查過就假設全樣本沒事。

**因此本 SPEC 採用的解法（待總司令一併核准，不是自行定案）**：
1. 第2節「基底權重」的 `market_cap_i` 一律用
   `implied_market_cap_i = PBR_i × EquityAttributableToOwnersOfParent_i`
   （路徑1）計算，不是真實市值——**這是本SPEC新增的一個已知限制，
   不是隱藏的假設**，繼承`factors.py`既有對PBR PIT狀態的保留（「抽查
   過、非完整驗證」）。**新增：換股日計算時，每一檔先過一次上方
   驗證v2找到的「面額比值檢查」**（TWSE每股參考淨值反推股數 ÷
   股本/10反推股數，偏離1.0超過±15%者排除出資格池），**金融保險股
   另外標記（因`EquityAttributableToOwnersOfParent`缺值被迫使用總
   權益，帶有約9~10%系統性偏高），兩者是否分別排除/如何處理待總司令
   依驗證v2數字裁示，本SPEC不自行決定**。
2. 第5節★一（主動權重上限）與第3節（產業中性）裡的「0050」權重/
   產業分布，**一律改用「資格池內用同一套`implied_market_cap`模擬出
   的市值加權前50大」當作0050的模型替代（proxy）**，不是真實0050
   持股——這一點必須在回測報告裡**每次出現「相對0050」字樣時同步
   揭露**，不得讓讀者誤以為那是跟真實可交易ETF比較。第1、4、9(b)(c)
   節既有直接用0050真實價格序列的部分（beta、CAPM回歸、買進持有
   對照組）**不受影響，繼續用真實0050**——只有「0050內部持股組成/
   權重」這個層級的比較才需要用模型替代，價格層級的比較資料無缺口。
3. 模型版「0050」跟真實0050的已知落差（誠實列出，不假裝模型是真的）：
   真實0050有進出場緩衝規則、單一個股集中度上限的歷史調整、成分股
   審核頻率與生效日的細節規則，本模型單純用「市值前50大、市值加權」
   逼近，**不重現這些規則**，落差方向未知（可能被模型高估或低估某些
   個股權重），回測報告需附此但書。**驗證v2額外確認**：排除金融股
   （3檔）與面額異常股（3檔）後，剩餘28/34檔（82%）中位重建誤差
   4.83%，屬可接受量級；金融股與面額異常股（本次樣本比例約18%，
   全樣本池比例待`core_tilt_backtest.py`實作時逐檔算面額比值後才能
   確認）需要總司令指定的專門處理方式，不能套用同一個未經篩選的公式。

### 2.1.2 驗證v3（2026-09-18，總司令裁示【市值重建—先查一件事，再決定
要不要做校正工程】，取代v2，未達核准門檻）

**用詞更正（貫穿本節與整份SPEC）**：v2用「誤差」描述路徑1與路徑2的
差距，總司令指出這個用詞會誤導——**這份驗證沒有獨立真值**，34檔算的
是兩條重建法互相差多少，不是離真實市值多遠，兩條可以同時往同一方向
偏。全部改用**「兩法差異」（`path1_vs_path2_diff`）**，不再用「誤差」
這個字。「金融股系統性偏高9~10%」這句話同理改成「路徑1相對路徑2高
9~10%」（下方v3實測這個數字本身也已更新，見下文）。

**十分鐘查證（總司令要求先查再決定要不要做校正工程）：TWSE公司基本
資料有沒有直接股數欄位？——有，而且精確**：`t187ap03_L`（上市公司
基本資料）的`已發行普通股數或TDR原股發行股數`欄位直接給股數，不需要
反推。驗證：台積電`實收資本額`259,323,700,670÷10=25,932,370,067，跟
這個欄位的25,932,370,067**完全相符**（面額剛好是10元的公司，兩種算法
本來就該一致）；矽力-KY(6415)的`普通股每股面額`卻是**「新台幣2.5000
元」不是10元**，直接解釋了v2用「股本÷10」算出120%落差的根因（真實
股數是股本÷2.5，是股本÷10算出來的4倍，跟v2用比值法間接算出的≈3.99
倍完全吻合）；成信實業(6969)面額是**「無面額」**，股本÷10這個概念對
它結構上根本不成立。

**涵蓋範圍誠實揭露**：`t187ap03_L`只有查證當下（2026-09-18）的**即時
快照**（swagger規格只有`_L`上市/`_P`公開發行兩個變體，都是「現在」
不是「歷史」），沒有可回溯查詢2015~2024任一天的股數歷史序列。34檔
交叉比對用「今天的股數快照」對比「2024-12-30的價格與財報」，嚴格說
不是同一天——但**面額是公司結構性、極少變動的常數**，拿今天查到的
面額推算歷史股本對應的股數，遠比原本假設「大家都是10元」準確；股數
本身（不是面額）若在這段期間有增資/減資才會有落差，這點誠實標記，
不假裝完全精確。**歷史回測（`core_tilt_backtest.py`）建議做法**：用
「歷史各期股本（資產負債表PIT序列，本來就有）÷ 今天查到的真實面額」
推算歷史股數，不是「今天股數套用全部歷史」——面額變動極罕見可視為
常數，歷史股本變動已被PIT序列捕捉，v2的面額比值校正法（±15%排除
規則）因此不再需要，直接讀值取代間接診斷。

**升級後路徑2重跑34檔結果（`research/implied_market_cap_validation.py`
已更新，可重複執行）**：

| 統計量 | 數值 |
|---|---|
| 中位數 | 4.75% |
| P90 | **18.28%** |
| 最大值 | 22.45%（2528皇普） |

**核准門檻（總司令裁示，全部34檔一起算，不排除問題股）**：
中位數≤5%（**達標**，4.75%）**且**P90≤12%（**未達標**，18.28%）
——**兩個條件是AND，其中一個沒過，整體判定為未核准**。

**最大10檔兩法差異與根因（依裁示格式回報）**：

| 代號 | 名稱 | 差異 | 產業 | 根因 |
|---|---|---|---|---|
| 2528 | 皇普 | 22.45% | 建材營造 | 面額/股數比對乾淨（無v2那類面額問題），差異來源查不出，疑似路徑1側（候選(a)），財務報表類型標記為「2」（非一般類型），可能與此有關，未能完全查證 |
| 2881 | 富邦金 | 20.90% | 金融保險 | 候選(b)確認：FinMind對金控股`EquityAttributableToOwnersOfParent`缺值，被迫用總權益，路徑2升級後這個偏差的真實幅度比v2估的9~10%更大 |
| 2882 | 國泰金 | 19.99% | 金融保險 | 同上 |
| 3041 | 揚智 | -19.82% | 半導體業 | 面額/股數比對乾淨，v2已標記「查不出來源」，v3同樣查不出來，維持未解 |
| 5871 | 中租-KY | 18.28% | 其他 | **新發現**：面額查證正常（10元），但`實收資本額`÷10=1,895,649,584跟`已發行普通股數`=1,745,649,584不一致（差約150,000,000股，疑似庫藏股/待註銷股本相關），路徑2用直接股數已修正這部分，差異來源轉為路徑1側，未完全查證 |
| 2883 | 凱基金 | 17.88% | 金融保險 | 同2881/2882，候選(b) |
| 6969 | 成信實業 | -13.94% | 創新版股票 | 面額「無面額」，v2用比值法間接抓到異常（-60.11%），v3用直接股數大幅收斂到-13.94%，仍有殘餘差異未完全查明 |
| 2069 | 運錩 | 11.36% | 鋼鐵工業 | 面額/股數比對乾淨，未完全查證 |
| 2107 | 厚生（橡膠工業代表） | 11.35% | 橡膠工業 | 面額/股數比對乾淨，未完全查證 |
| 4746 | 台耀 | 10.77% | 化學生技醫療 | 面額/股數比對乾淨，未完全查證 |

**兩法差異 vs 市值級距**（30檔非強制樣本各10檔）：

| 級距 | 中位數 | 平均值 | 最大值 |
|---|---|---|---|
| 大 | 5.34% | 7.96% | 22.45% |
| 中 | 4.73% | 6.43% | 19.82% |
| 小 | 3.98% | 4.16% | 13.94% |

**兩法差異 vs 產業**（34檔全樣本，取最大值排序前幾名）：金融保險
（3檔，中位數**19.99%**，全部落在18~21%區間，是**目前最乾淨、最大
的系統性差異來源**）、建材營造（2檔，中位數16.24%，受2528拉高）、
半導體業（4檔，中位數僅0.94%但受3041拉高到max 19.82%——這個產業
本身沒有系統性問題，3041是個股層級的未解案例）。

**v3與v2的關鍵差異詮釋（重要，不只是數字更新）**：路徑2升級後不再
依賴任何假設（不假設面額10元、不假設capital/10就是股數），是用
TWSE官方直接股數欄位算出來的，**權威性高於路徑1**（路徑1的PBR/Equity
組合本身就已知對金控股有缺欄位問題）。因此金融保險股的差異擴大（v2
測出9~10%，v3測出18~21%）**不代表不確定性變大，代表v2的路徑2本身
也有誤差、恰好部分抵銷了路徑1的偏差，掩蓋了路徑1真正的偏差幅度**——
v3看到的18~21%更接近路徑1對金融股真正偏離的量級。**因此本SPEC建議
（待總司令裁示是否採用）**：核准後的`core_tilt_backtest.py`應該把
路徑2（TWSE直接股數）當市值的主要來源，路徑1（PBR×Equity）降為交叉
驗證用——這正是總司令原話「這條成立的話...路徑1降為交叉驗證」的
條件已經成立（t187ap03_L的股數欄位查得到、精確），但**因為P90仍
未達門檻，本SPEC不自行宣告核准**，等總司令看過這份差異表後裁示是否
降低標準（例如金融股另外處理，不計入34檔的AND門檻判定）、或換其他
路線。

**`core_tilt_backtest.py`依然不得動筆**，這是總司令原話明確保留的
決策點（「達不到→回報最大的10檔差異與各自根因，我再裁示」），不是
「不確定但可還原的技術選擇」可以自行裁量繼續做的情況。

### 2.1.3 用重建市值近似0050成分股名單（2026-09-19總司令裁示【0050成分股：
查證不足，重做，且需求規格放寬】，取代之前「查無」的結論）

**需求規格先放寬（總司令原文）**：`core_tilt`是季頻換股，一年只需4個
時點的「成分股名單」（50個代號），不需要逐日精確權重——有名單就能用
本專案已重建的市值（路徑1 PBR×Equity／路徑2 收盤價×股本÷10）算出
權重，誤差只剩流通量調整那一層。這比先前用「逐日精確0050權重」的
標準去查證，難度低了兩個數量級。

**逐條查證結果（互動視窗CC實測，逐條寫出實際看到什麼，不是「查無」
兩個字）**：

1. **基金年報／半年報（依規定須揭露全部持股明細）**：
   透過TDCC官方「基金資訊觀測站」(`fundclear.com.tw`，機構為臺灣
   集中保管結算所/投信投顧公會/期貨公會共同建置維護，非第三方業者，
   robots.txt無`Disallow`)實際查詢元大台灣卓越50基金（TDCC內部代碼
   98764389，投信事業代號A00005）：**確認「基金財務報告書」這個文件
   類別真實存在**（頁面上有明確的「基金文件」分頁列出「財務報告書」
   /「公開說明書」兩個文件類別可查）。但點開後，實際的文件檢索連結
   指向`mopsov.twse.com.tw/mops/web/t57sb01_q6`——這正是總司令原文
   點名`robots.txt`為`Disallow: /`（已重新驗證：`User-Agent: *` /
   `Disallow: /`，僅對`bingbot`開放`/mops/web`）的那個網域。**結論：
   財務報告書（含全部持股明細的年報/半年報）確認存在，但唯一的
   檢索管道在被禁止爬取的網域上，回報「存在但不可取得」，未繞過。**
   （曾嘗試把網域換成`mops.twse.com.tw`(無robots.txt限制)測試是否為
   同一系統的可爬鏡像，該域名下同樣的路徑`/mops/web/t57sb01_q6`
   重定向到SPA首頁空白頁，不是同一個可用系統，此路徑不通。）

2. **臺灣指數公司(TIP)定期審核/成分股調整公告**：查`taiwanindex.com.tw`
   首頁「重大公告」完整列表（含2025-2026年多筆紀錄）：**實際看到的
   內容是「指數定期審核『日程表』」（例如「2026年10月指數定期審核
   日程表」——這是審核**何時**發生的排程通知，不是審核**結果**的
   成分股增減名單）以及其他指數的終止編製公告，**沒有看到臺灣50
   指數本身的成分股增減結果公告**。另查證TWSE官方資料雲/網路資訊
   商店(`eshop.twse.com.tw`)，確認TWSE本身把「指數成分股檔」（收盤
   價計算市值及權重的結構化檔案）當成正式付費商品線在賣（多檔TIP
   自有指數標價NT$102,000/年/內部使用），臺灣50指數（TWSE與FTSE
   聯名）本身不在這個商品清單裡——**這印證了先前的發現：臺灣50
   指數的深層資料所有權在FTSE Russell，屬於另一層授權範疇，不是
   TIP自己能免費公告的東西**。**結論：定期審核結果的增減名單本身
   沒有找到公開發布的历史彙整來源，此路徑對「遞推重建歷史名單」
   沒有幫助。**

3. **Wayback Machine對元大持股頁的歷史快照**：查`web.archive.org`
   的CDX索引，鎖定元大官網「申購買回清單」頁
   （`yuantaetfs.com/tradeInfo/pcf/0050`，這是每日更新、含全部約50
   檔成分股+**精確股數**的正式頁面，比「持股比重」頁的前十大摘要
   更完整）：**實際抓到18個去重後的相異日期快照，涵蓋2021-10-18至
   2026-05-14**（`product/detail/0050/ratio`前十大摘要頁則有24次
   快照，2022-05-26至2026-08-07），**時間範圍確實涵蓋本專案VAL期
   （2021-2024）**。但實測發現兩個重要限制：(a) Wayback只封存了
   頁面首次載入的前5列，頁面上的「More」按鈕在封存版本點擊無反應
   （該按鈕背後是即時API呼叫載入其餘~45檔，這個API請求本身沒有被
   封存——用CDX查詢同時間窗附近有沒有其他相關API端點被封存，查無）；
   (b) **更關鍵的：這5列本身是按股票代碼數字排序（1101/1216/1301/
   1303/1326），不是按市值或權重排序**——代碼最小的5檔不等於市值
   最大的5檔（例如市值通常最大的2330台積電代碼數字並不小，不會落在
   前5列），**這5檔只能驗證「是不是名單成員」（二元存在性），不能
   驗證「排名/權重對不對」**。**結論：這條路徑「部分成功」——拿到
   18個真實歷史時間點、且是5檔（依代碼排序，非依市值排序）成分股的
   正確身份與精確股數，可以當作「名單成員資格」交叉驗證的錨點，但
   驗證不到權重排序，也拿不到完整50檔名單，不足以單獨拿來重建完整
   的季度名單。**
4. **報酬反推權重（交叉驗證，非主來源）**：規劃留給下方`core_tilt_
   backtest.py`重寫時執行——用重建組合的日報酬對0050真實日報酬
   （已有的真實價格序列，非重建）做相關係數與追蹤誤差比對，驗證
   路徑1/2重建的市值排序組合跟0050實際報酬對不對得上。

**四條路徑逐條走完後的判定**：不是總司令原文假設的兩個乾淨分支
（「任一條成功→用它重建」或「四條全失敗→用近似版」），而是**中間
情況**——路徑1確認資料存在但不可取得、路徑2對重建沒有幫助、路徑3
拿到部分驗證錨點（18個真實日期、5檔依代碼排序的成分股身份+精確
股數，只能驗證名單成員資格不能驗證權重排序）但沒有完整名單、路徑4
待執行。**因此**：主要重建方法維持用路徑1（PBR×Equity）/路徑2
（收盤價×股本÷10）算全市場前50大市值來近似0050成分股名單
（`core_tilt_backtest.py`重寫時採用），**但這次不是憑空近似**——
用路徑3拿到的18個真實日期的5檔真實身份，逐一比對「重建的前50大
名單」在那18個日期是否也把這5檔納入前50大（成員資格檢定），這是
總司令原文「查不到就用重建市值前50大的近似版本，並在SPEC明文標示
這是近似、以及它與真實名單的誤差無法驗證」這句話的部分改善——
**至少在18個真實時間點的這5檔成員資格上，誤差是可以驗證的，不是
完全無法驗證**，其餘~45檔的名單與所有50檔的權重排序依然無法驗證，
這點誠實保留。

**✅ 2026-09-19總司令裁示【0050查證採信；但驗證標準改成「報酬吻合」
不是「名單吻合」】一：四路徑查證結論採信，不再要求重查**——四條路
都走過、每條都寫出實際看到什麼、有一條（路徑3）部分成功、沒有誇大，
查證品質合格。**路徑1「存在但不可取得」的處理被總司令明確記一筆
稱許**：資料（基金財務報告書）確認存在，卻因為唯一檢索管道在
robots.txt明文`Disallow`的網域上而不碰、不繞過——這個克制比「查不到」
更需要紀律，往後任何查證都要維持這個標準。

**⚠️ 驗證判準本身已更正（同一次裁示二）**：路徑3「不足以重建完整
名單」的判斷本身沒錯，但預設了「需要名單正確」這個錯誤前提——
**core_tilt要的是低追蹤誤差，TE的定義本來就是用報酬算的，不是用
名單重疊度算的**，而0050的真實日報酬本來就有（非重建）。路徑4
（報酬反推）因此從配角變成**決定性判準**，路徑3的18個時點×5檔
membership check降級為「第一層健檢」（只驗有沒有系統性漏掉，不是
決定性）。

**✅ 2026-09-19最終結案：decisive判準結果FAIL，`core_tilt`結案**
（`TRIALS_LEDGER.md`#283、`STRATEGY_GRAVEYARD.md`「core_tilt」條目，
完整過程見`CORE_TILT_TE_FEASIBILITY.md`）——修好兩個實作bug（換股日
forward-fill無界凍結價格、日期join型別不一致導致誤報零重疊）後，
乾淨重跑decisive判準：重建組合（純市值前50大，無傾斜）vs 0050真實
日報酬，**相關係數0.0102（幾乎零相關）、年化TE 20.47%（逐年皆遠高於
3%門檻，非單一異常年份）**，落入`>3%`分支，**判死**。舊的策略層
判準（vs TAIEX的CAPM殘差TE）同時期顯示TE=0.762%看似達標，但beta=
0.0009——這是**偽陽性**：beta趨近0代表投組跟大盤根本不同步，TE低
只是因為沒什麼系統性關係可解釋，不是追蹤得準，印證了總司令這次裁示
「改用報酬吻合而非名單吻合／策略層TE」的必要性。**根因是「無法取得
或重建足夠貼近的基準籃子」（樣本宇宙306檔僅146檔有市值代理，重建的
「前50大」只是隨機抽樣子集裡的前50大，可能根本沒抽到2330這類真正的
權值股），不是「因子無效」或選股/傾斜邏輯設計錯誤**——這條區分決定
未來要不要復活這條路線該從哪裡下手（先解決全市場市值排名資料源，
不是調參數）。第0節TE≤2.5%的SPEC設計數字保留不動，這條研究線本身
已經結案。

## 3. 持股範圍與產業中性

- **持股檔數**：60~80 檔（不是 v2 的 20 檔）。資格池依 `score.py::
  eligible_for_ranking()` 既有門檻 + v2 既有流動性門檻（20日均成交
  金額後10%分位數排除）篩選後，取綜合分前 60~80 名（依當期資格池大小
  動態決定，不強制固定數字，但下限60、上限80）。
  - **對 TE 的作用**：持股數越多，個股特異風險（idiosyncratic risk）
    越被分散掉，這是除了市值加權之外第二個壓低 TE 的機制——20檔到
    60~80檔，理論上可分散掉更多非系統性波動。
  - **⚠️ 2026-09-19總司令裁示【先修SPEC再判死】更正（這一行本身就是
    設計錯誤，記原因不是只改規則）**：「取綜合分前60~80名」**本身
    就決定了TE不可能低**——這是用因子分數**重新選股**，不是「貼齊
    基準、只做小幅傾斜」。不管持股數放寬到60檔還是80檔，只要選股
    邏輯是「因子分數排序取前N」，選出來的名單就跟0050的真實成分股
    在系統上是兩回事（`core_tilt_backtest.py`實測驗證：beta持續
    1.35~1.42，遠非市場中性，且band/holdings參數幾乎不影響TE，
    證明問題出在選股這一步，不是加權那一步）。正確設計應該是
    **從基準（重建的市值前50大）名單出發、保留全部成員、只調整
    權重**，因子分數只用來決定「在容許的主動權重帶內怎麼傾斜」，
    不能用來決定「要不要納入」。修正後的選股邏輯見「2.1.3 用重建
    市值近似0050成分股名單」與`core_tilt_backtest.py`v2（若驗證
    通過會取代本節這行）。
- **產業中性**：策略的產業曝險（依產業分類加總權重）**必須貼齊 0050
  的產業權重分布**，容忍帶為 ±3個百分點（單一產業權重相對0050同產業
  權重的絕對偏離超過3個百分點即視為違反，需在下次換股日調整回容忍帶
  內）。0050 的產業權重需在每次換股日當天，依 0050 成分股清單與其
  各自市值重新計算（不得用固定過去某一天的權重表，那會產生未來函數）。
  **⚠️ 2026-09-18查證後更新**：這裡的「0050產業權重」跟第5節★一
  同一個資料缺口——真實0050逐日成分股權重無免費歷史來源，這裡的
  「0050」一律指第2.1節定義的模型替代（資格池內用`implied_market_cap`
  模擬的市值加權前50大），不是真實ETF持股，換股日重新計算時用同一套
  `implied_market_cap_i = PBR_i × EquityAttributableToOwnersOfParent_i`
  推導，不是另外找一套算法，避免兩處對「0050市值」的定義互相打架。
  - **對 TE 的作用**：0050 是市值加權指數化 ETF，其報酬有很大一部分
    由產業配置決定；若策略產業配置跟 0050 差太多，即使個股選得再準，
    產業層級的系統性差異也會直接灌進 TE，這是三個 TE 控制機制裡對
    大盤相關的一環。

## 4. Beta 約束

- **策略對 0050 的 beta**（滾動視窗迴歸，視窗長度待回測時依樣本長度
  決定，需在回測程式碼裡明確記錄視窗天數）必須落在 **0.95~1.05** 之間。
  每次換股後檢查，超出範圍需在換股規則裡加入曝險調整（例如降低傾斜
  強度、拉高現金部位上限），不得放任 beta 漂移出這個帶。
  - **對 TE 的作用**：beta 偏離1會直接放大跟 0050 的系統性報酬差距
    （beta×大盤報酬這一項），是 TE 分解裡最大宗的一塊，鎖住 beta 帶
    是三個機制裡優先順序最高的一個。

## 5. 交易規則

- **換股頻率**：季頻（63個交易日），依第0節理由，不測月頻或週頻——
  這是總司令原話明確指定的頻率，不是待掃描的自由參數。
- **進場/出場**：跌出資格池前60~80名（依當期動態上下限）者於下次換股
  日換出；只換真正進出名單的名字，未變動的既有持倉不強制全部重新
  加權（沿用 v2 精神，降低不必要換手/成本）。
- **停損**：沿用 `backtest/engine.py` 既有15%單檔停損機制（跟 v2、
  `weinstein_stage2` 系列一致，非本輪新設計，不佔額外自由參數）。
- **單檔部位上限（★一，2026-09-18修正，取代v1的絕對上限5%）**：
  v1原本設「單檔權重硬上限5%」，總司令指出這是**用錯工具**——絕對
  上限管的是集中度風險，主動上限管的才是追蹤誤差。0050是市值加權，
  台積電在0050裡權重長期40~50%以上，5%絕對上限＝對0050最大單一
  報酬來源結構性低配35~45個百分點，光這一條就能製造超過10%的TE，
  讓第0節「TE≤2.5%硬性約束」當場失守。**改成主動權重上限**：
      |w_策略,i − w_0050,i| ≤ 2 個百分點
  台積電在策略裡可以照樣持有(接近)0050給它的權重（例如45%），只是
  不能偏離0050太多。**`w_0050,i`一律用第2.1節定義的模型替代**（尚
  待總司令核准第2.1節的解法才能真正實作，見該節）。
  - **對TE的作用**：直接把「跟0050的持股權重差距」設上限，是三個TE
    控制機制之外**第四個、也是直接針對TE本身的機制**，理論上比前三個
    間接機制（市值加權為底/持股數/產業中性）更直接。
  - **另立集中度上限（分開的目的，分開列為自由參數）**：若總司令仍
    想另外控制「非0050成分股」的集中度風險（跟追蹤誤差是不同目的），
    可以加一條「非0050成分股單檔絕對上限3%」——**這條本SPEC先列出
    選項但不預設啟用**，是否納入待總司令下一輪確認；若啟用需在第7節
    參數清單裡新增一個自由參數，不能併入主動權重上限那條算同一個。

## 6. 全成本模型

沿用 `PORTFOLIO_STRATEGY_SPEC.md` 第4節，完全不變：手續費買賣各
0.1425%、證交稅賣出0.3%、`validation/costs.py` 滑價模型測1x/2x/3x
敏感度、`backtest/engine.py` 既有漲跌停無法成交機制、第3節流動性門檻。

## 7. 參數上限閘門（依 `CLAUDE.md` 七之三節第11關）

本構造自由參數（用來fit這個機制、可調整讓結果變好看的，不含事前綁定
有獨立外部理由的常數）逐一列出，共 **4 個**（v1也是4個，v2把第5節★一
的「單檔權重上限5%」換成「主動權重上限2pp」，數量不變，仍低於5個
上限，不需要額外多重比較懲罰計入理由說明，但仍併入
`selection_bias_ledger.py` 的N計數，依既有規則照登記，不因為低於
上限就免登記）：

1. **傾斜強度 `tilt_strength`**（第2節）：直接控制傾斜幅度，是這個
   構造存在的核心自由度，理由是這個機制本身要測的東西，不能不設。
   **掃描範圍（2026-09-18總司令核准）：{0.25, 0.5, 1.0} 三格，不得
   擴大**。**只能用TRAIN期決定，VAL期只驗不調**——選出在TRAIN期
   表現最好（或最達成TE目標）的那一格後，VAL期用那一格算最終判定
   數字，不得回頭在VAL期看到結果又換一格重測。三格全部要透過
   `trial_registry.py::register_trial()`登記進`TRIALS_REGISTRY.jsonl`，
   跑`selection_bias_ledger.py`更新N計數，缺一格不登記都不行。
2. **產業中性容忍帶（±3個百分點）**（第3節）：理由——沿用一般投資
   實務常見的產業中性容忍帶量級（非本專案自創，非從歷史回測結果反
   推調出），過緊會導致每次換股都要大幅調整拉高成本，過鬆則失去
   產業中性的意義。**2026-09-18總司令核准維持這個數字不變**。
3. **beta 約束帶（0.95~1.05）**（第4節）：理由——總司令原話明確
   指定的數字，不是自己調出來的自由參數，此處列出是為了完整揭露
   所有影響結果的可調數值，但不算「可以讓結果變好看而調整」的那種
   參數。
4. **主動權重上限（±2個百分點）**（第5節★一，2026-09-18取代v1的
   單檔絕對上限5%）：理由——直接對應追蹤誤差目標，數字沿用一般
   指數化增強策略「active weight」量級慣例（非回測調出），且v1的
   絕對上限5%已被總司令指出方向性錯誤（管的是集中度不是TE，見第5節
   ★一），v2改成正確工具後沿用同一個「非回測調出」性質，仍列為
   自由參數而非事前綁定常數。

持股檔數（60~80，第3節）**不算自由參數**——總司令原話明確給出「不是
20檔」的區間，且此處用動態上下限而非可調的單一數字。換倉頻率（季頻）
與因子成分（版本A）同理，皆為總司令原話明確指定，不算自由參數。
第5節「非0050成分股單檔絕對上限3%」若下一輪確認啟用，**要另外算
第5個自由參數**，屆時需重新評估是否仍低於5個上限。

## 8. 牛熊制度強制項（依 `MARATHON_PROTOCOL.md` 1a-0c 節，回測前就要內建）

- **分制度表**：回測報告除全期彙總外，必須逐段列出每一個多頭段、
  每一個空頭段各自的：報酬、MDD、beta（相對0050）、相對0050超額報酬。
  只報全期彙總、沒有分制度表的回測結果，一律退回，不得送進
  `TRIALS_LEDGER.md` 判定欄。
- **INSUFFICIENT_REGIME 判定**：回測涵蓋的時間範圍必須至少包含1個
  完整多頭段＋1個完整空頭段（依 `REGIME_CONDITIONS.md` 鎖定表，2000-
  2024共10段空頭、10段多頭）。不足者判 `INSUFFICIENT_REGIME`，不得
  判 `PASS`，也不得直接判 `FAIL`。
- **空頭段beta拆解**：每個空頭段必須單獨報告beta，若空頭段的相對
  優勢可被低beta完全解釋（beta顯著<1，beta調整後超額報酬消失或不
  顯著），必須明文寫成「這是低beta，不是alpha」，不准含混寫成「空頭
  抗跌」。

## 9. 對照組

- **(a) 隨機選股對照組**：同樣持股範圍（60~80檔動態）、同樣換股頻率
  （季頻）、同樣資格池，排名用隨機而非因子傾斜，配對式設計、100次
  重抽算百分位（沿用 v2 精神，範圍改成新的持股數）。
- **(b) 0050 買進持有**：0050 同期總報酬（含股息再投入），零成本、
  不換股。
- **(c) 對 0050 回歸**：策略日報酬對 0050 日報酬做 CAPM 迴歸
  （`scipy.stats.linregress`），報告 alpha（日頻截距年化換算）、beta、
  alpha的t檢定p值——這一項是「照舊報告」，**不是主判定**（見第10節）。
- **(d)（2026-09-18★三升格為主對照組，不再只是輔助拆解用）市值加權
  基準對照**：資格池內所有股票純市值加權（無因子傾斜、無產業中性
  容忍帶），同樣換股頻率、同樣資格池（第2.1節`implied_market_cap`
  一致沿用）。**升格理由**：策略相對0050的超額報酬，數學上可以拆成
  兩項：

      (策略 − 0050) = (資格池市值加權 − 0050) + (因子傾斜 − 資格池市值加權)

  第一項只反映「我們的資格池（已排除流動性最差10%）比0050分散、
  0050只有50檔」這件事，**不是我們的本事**；第二項才是因子傾斜這個
  機制真正的貢獻。第10節主判定必須跑在第二項，不是「策略−0050」
  整體，否則會把「持股比較分散」誤判成alpha。

## 10. 判定方式（雙軌，主判定改變；2026-09-18★三修正：主判定的比較
基準從「0050整體」改成「資格池市值加權基準」）

依總司令原話「判定改成雙軌」，且主判定物件依★三修正：

1. **CAPM alpha 顯著性**（第9節(c)，策略對0050整體）：照舊計算並
   報告，p<0.05且為正視為顯著，**不是主判定依據**，只是照舊揭露。
2. **策略相對「資格池市值加權基準」（第9節(d)）的 block bootstrap
   檢定（主判定，2026-09-18修正）**：對「因子傾斜 − 資格池市值加權」
   這一項的超額報酬序列做block bootstrap（區塊長度需在回測程式碼裡
   記錄，避免打散自相關結構），檢定是否顯著為正。**這是因子傾斜
   本身的檢定，不是策略對0050整體的檢定**——理由見上方第9節(d)的
   拆解：策略對0050整體的超額報酬混進「資格池比0050分散」這個非
   本事的效果，主判定必須排除它才測得出因子傾斜真正的貢獻。
3. **同時報告「資格池市值加權 − 0050」這一項本身的數值與檢定**
   （block bootstrap或CAPM回歸皆可，回測程式碼裡二擇一並說明理由），
   **明文標示這一項不算我們的貢獻**，只是揭露「分散化本身」帶來
   多少效果，避免報告時被誤讀成策略整體的一部分本事。

**兩層拆解＋CAPM對0050整體，三個數字都要報，不准只報好看的那個**
（總司令原話「兩個數字都要報」，本輪因應★三拆解擴大成三個，精神
不變）。

回測時同時報告以下四項，缺一不完整（**已改成同時針對「策略vs資格池
市值加權」與「策略vs0050整體」兩個基準各報一次**，不是只報一次）：

1. 年化追蹤誤差（實測值，對照第0節的2.5%硬性約束——這個約束原本
   就是策略相對0050整體的TE，不因★三拆解而改變分母）
2. 資訊比率（IR = 年化超額報酬 / 年化追蹤誤差）
3. 最小可偵測 alpha（`power_budget.py::min_detectable_alpha()`，輸入
   實測TE與樣本年數）
4. 贏過 0050 的年數 / 總年數（逐年計算相對0050超額報酬是否為正，
   分子分母都要報，不能只報比例）

## 11. Holdout 紀律

**未經使用者明確同意，不准呼叫 `unlock_holdout_once()`。** 沿用
`PORTFOLIO_STRATEGY_SPEC.md` 第7節、`CONSTITUTION.md` 一致，這條規則
沒有例外。

## 12. 樣本與回測期間（2026-09-18 ★二＋Q3修正，取代v1）

**樣本（★二修正）**：v1原本沿用`factor_ic.py`既有100檔種子樣本
（實際可用80檔）——總司令指出這是「把樣本原封不動包起來叫它策略」：
80檔可用樣本 vs 60~80檔持股＝選股率75~100%，選股在數學上不做任何事，
測出來的TE只反映「這80檔跟0050的差異」，跟因子傾斜無關。

改用`portfolio_backtest_v2_bigsample.py::safe_pool_ids()`定義的
「零新API安全樣本池」（yfinance價格快取 ∩ FinMind財報快取 ∩ FinMind
月營收快取 ∩ `universe()`成員）。**因為`universe()`合併bug已修
（delisted佔比6.95%→14.14%，見`PENDING_QUEUE.md`續8章節），這個池子
用修復後的`universe()`重算過**：

    重算後池子大小：1,588 檔（原第325輪算出的舊值1,138檔已過期）
    組成：active 1,434 檔（90.3%）／delisted 154 檔（9.7%）

**⚠️ 已知限制，不得在「我們用了含下市股的宇宙」這句話底下悄悄消失
（2026-09-18總司令裁示要求明文寫出）**：安全樣本池的delisted佔比
9.7%，低於修復`universe()`bug後宇宙本身的14.14%——**下市股在樣本池
裡仍被低估約三分之一**（(14.14-9.7)/14.14≈31%）。原因是安全樣本池
額外要求「yfinance價格快取∩FinMind財報快取∩FinMind月營收快取」三個
交集，下市股本來就比較少有完整快取（下市後不會再有新的排程去抓它們
的資料，快取多是歷史殘留，覆蓋率天生比still-active股票低）。**這不是
本輪新引入的bug，是安全樣本池這個機制既有的限制**，但用這個池子做
研究時，任何跟存活者偏誤有關的結論（例如下市率、下市股占比相關的
判斷）都要把這31%的低估記在心裡，不能只說「宇宙含下市股」就當作
已經完全緩解。

選股率＝60~80/1588≈3.8%~5.0%，遠低於舊版80檔樣本的75~100%，選股這個
機制在數學上才有實質作用空間。**若全量1,588檔在回測時重演第327輪
「process不明原因中途消失」的問題，退而用500檔（隨機種子固定、可
重現），但不得低於400檔**——低於400檔選股率又會拉高到15%以上，
失去這次修正的意義。**實作建議（非規則，供下一輪參考）**：用
`run_detached.py submit`跑（第327輪失敗是直接跑`python -u`沒有
watchdog保護，`run_detached.py`專門解決這類「process中途消失、
無crash可查」的問題），不要重蹈覆轍用裸跑。

**回測期間（Q3修正）**：v1原本只用VAL期（約3.85年）算檢定力所有數字。
總司令算給的數字：TE=2.5%時，VAL only 3.85年→SE=2.5/√3.85=1.27%→
MDE≈3.6%，測不到t60損益兩平線2.93%；TRAIN+VAL全期10年→
SE=2.5/√10=0.79%→MDE≈2.2%，才蓋得住2.93%。**改用TRAIN+VAL全期
取得檢定力，不再只用VAL 3.85年**。

**代價與配套紀律（總司令原話「這個取捨與其限制要寫進SPEC第12節，
不要藏起來」）**：TRAIN+VAL全期當判定期，代表判定期跟構造參數的
選擇期重疊——`tilt_strength`若在同一段資料上「選」又「判定」，會
製造過擬合的偽陽性。配套規則（見第7節第1項）：
1. `tilt_strength`的三格{0.25,0.5,1.0}**只能用TRAIN期資料決定**選
   哪一格。
2. **VAL期只用來驗證選定的那一格，不得回頭因為VAL期表現不好就換
   一格重測**——換格重測等於用VAL期的答案去選參數，會讓「TRAIN+VAL
   全期」這個取捨失去意義，退化成拿全期資料調參數。
3. 最終判定（第10節block bootstrap）用TRAIN+VAL全期資料算，但
   `tilt_strength`已經在步驟1固定，判定期不重新調整任何自由參數。
4. **holdout仍然一次都不准碰**——這個取捨只動到TRAIN/VAL的邊界
   使用方式，不影響第11節holdout紀律，holdout的隔離規則沒有例外。

**既有限制（誠實揭露，v1已列，v2確認仍成立）**：
- 依第0節的反推數字，即使TE成功壓到2.2~2.5%，MDE（3.4%~2.95%）仍
  略高於或剛好貼齊t60損益兩平線的3倍門檻（8.79%）——**這代表就算
  構造設計完全達標，樣本長度本身仍在檢定力的邊緣**，回測結果如果
  是「不顯著」，必須先排除是否為檢定力不足（依`MARATHON_PROTOCOL.md`
  1a-0b規則1~3步驟），不能直接判定「這個構造沒有alpha」。TRAIN+VAL
  全期（10年）已經是這輪能拿到的最大樣本長度改善，MDE降到2.2%後
  跟2.93%損益兩平線的差距明顯縮小，但仍非完全寬裕。
- 台股特有偽影家族⑦（存活者偏誤）、⑧（財報跳空）、⑨（交易時段）
  依`CLAUDE.md`七之三節既有規則全部適用，本SPEC不重複列出細節，
  回測實作時比照`portfolio_backtest_v2.py`既有防範方式處理。
- **本SPEC不含放空腿**，第10關（新增4道關卡之10，借券成本）不適用；
  市值加權+因子傾斜是純多頭構造，這點與v2一致。
- **第2.1.1節`implied_market_cap`已完成34檔跨市值/產業/結構複雜度驗證
  （2026-09-18）**：中位誤差5.89%、P90 17.28%、最大120.27%（矽力-KY）。
  誤差非隨機分布：金融保險股系統性偏高9~10%（根因確認：FinMind對
  金融業`EquityAttributableToOwnersOfParent`缺值被迫用總權益）；
  少數面額非10元個股（矽力-KY等）誤差可達20~120%（根因：路徑2「面額
  10元」假設不成立，已找到免資料源的自我診斷方法，見第2.1.1節）。
  排除這兩類（各3檔）後，剩餘28/34檔中位誤差4.83%。**全樣本池（1,588檔）
  尚未逐檔跑面額比值檢查**，`core_tilt_backtest.py`實作時需要對全池
  跑一次，不能假設34檔驗證樣本沒問題就代表全樣本池沒有更多面額異常股
  或金融股（金融股在全樣本池的實際檔數需要屆時清點）。

---

## 總司令已回答的問題（原「待核准問題清單」，2026-09-18已裁示，供稽核）

1. **`tilt_strength`掃描範圍**：核准{0.25, 0.5, 1.0}三格，不得擴大；
   只能用TRAIN期決定，VAL期只驗不調（見第7節、第12節配套規則）。
2. **產業中性容忍帶±3個百分點**：核准，維持不變。**單檔權重上限5%：
   不核准**，方向錯誤（管集中度不是TE），改成主動權重上限±2個百分點
   （見第5節★一、第7節第4項）。
3. **回測期間**：改用TRAIN+VAL全期取得檢定力，不再只用VAL 3.85年
   （見第12節「回測期間」與配套紀律）。

（以下v1原始問題文字保留供稽核比對，不刪除）

1. ~~傾斜強度 `tilt_strength` 的掃描範圍要不要設上限？目前SPEC沒有
   預設數值，回測時會先用一組保守小範圍網格掃描（例如0.1/0.3/0.5），
   每一格都要登記進`trial_registry.py`（依外部策略/機制掃描一般規則），
   若總司令有具體想測的範圍，回測前先告知可省去無謂的格子。~~
2. ~~產業中性容忍帶±3個百分點、單檔權重上限5%這兩個數字目前是憑一般
   實務量級估的，不是從本專案資料反推——若總司令認為應該更嚴或更鬆，
   回測前先調整，避免測完才發現參數選錯要重測。~~
3. 是否核准這份SPEC進入下一輪回測實作（`core_tilt_backtest.py`，
   尚未動筆）？**v1的回答是「核准但三處必改」（本文件即v2修訂結果），
   v2仍待總司令再次看過確認才進入回測實作**，尚未是最終答案。
