# 原子.一：原子與算子庫

對應`PENDING_QUEUE.md`「2026-09-19【總司令裁示·更正】零件的定義改為
原子層」。依總司令原文明確要求「原子.一做完先回報，我看過再跑原子.二」，
本輪只建庫，不算任何IC、不組任何表達式、不做任何策略判定。程式實作見
`research/ATOM_LIBRARY.py`（含單元測試，`python research/ATOM_LIBRARY.py
--self-test`）。

**✅ 2026-09-19審閱通過（總司令裁示【原子.一審閱通過（補三個算子）＋
#73設上限＋補等待審閱的可見度】一）**：「『0 vs NaN』的區分做得對——
`range=0`回0（事實：今天真的沒波動）、`close_loc`除零回NaN（無定義），
這兩者的差別大部分資料管線是無腦`fillna(0)`一把抓，而那是假訊號的
頭號來源。」同一次裁示要求補三個算子/原子（(a)`op_mul`、(b)
`op_decay_linear`、(c) 10個基準原子），補完後**直接接原子.二，不需
再等審閱**。以下清單已更新為補件後的版本。

## 誠實邊界（請先看這裡）

- **本階段「單一K棒」＝「單一日K棒」**：逐筆tick資料目前只有5檔×9天
  （`research/data/ticks/`），盤中形態/分K層級的原子目前做不了，本庫
  完全建立在日頻OHLCV+成交金額之上。
- **原子計數對不上**：總司令原文說15個，逐字列出的定義（6個原始+8個
  衍生）加總是**14個，不是15個**。本庫如實只做這14個，沒有為了湊數
  自己發明第15個——若總司令原本設想的是別的東西（例如把`delay(c,1)`
  單獨算一個原子），請直接指出，我再補。
- **窗口限制**：所有需要窗口參數的算子只准用`n∈{1,5,20,60}`，不得
  連續掃描，這條紀律由`原子.二`/`原子.三`的呼叫端遵守，本庫的函式
  簽章本身不強制（Python做不到型別層級限制「只能傳這4個值」）。
- **資料源**：`adjust.adjusted_price_series()`（含下市股，納入率約
  79%），只用TRAIN+VAL，HOLDOUT不碰。

## 原子清單（24個：14個個股原子＋10個基準原子，零參數、零前視）

| 原子 | 定義 | 說明 |
|---|---|---|
| `o` | 開盤價（還原後） | 原始欄位 |
| `h` | 最高價（還原後） | 原始欄位 |
| `l` | 最低價（還原後） | 原始欄位 |
| `c` | 收盤價（還原後） | 原始欄位 |
| `v` | 成交量 | 原始欄位，未還原（量本來就不需要除權還原） |
| `amt` | 成交金額 | 原始欄位 |
| `body` | `c − o` | 實體，正＝收紅，負＝收黑 |
| `upper_sh` | `h − max(o,c)` | 上影線，恆≥0 |
| `lower_sh` | `min(o,c) − l` | 下影線，恆≥0 |
| `range` | `h − l` | 當日振幅，停牌鎖死時為0（非NaN） |
| `close_loc` | `(c−l)/(h−l)` | 收盤在區間的相對位置，值域[0,1]；**range=0時回NaN**（鎖死無意義） |
| `gap` | `o − delay(c,1)` | 跳空，第一筆為NaN |
| `true_range` | `max(h,c₋₁) − min(l,c₋₁)` | 真實波幅，含跳空缺口，第一筆為NaN |
| `vwap` | `amt / v` | 量價均值，**停牌無量時回NaN**（不是0） |

### 基準原子（10個，2026-09-19審閱通過後補件(c)）

理由：`ts_corr`簽名收兩個Series，但原本ATOMS裡沒有大盤，導致
`relative_strength`家族在原子層缺席——那既是既有因子之一，也是天條二
（對標0050）的核心。

| 原子 | 定義 | 說明 |
|---|---|---|
| `b50_o/h/l/c/v` | 0050開高低收量 | `adjust.adjusted_price_series("0050")`真實序列，非重建 |
| `btx_o/h/l/c/v` | TAIEX開高低收量 | `TaiwanStockPrice`原始值，TAIEX是指數無除權息可言 |

**合約差異**：這10個原子要求呼叫端的`df`額外帶`"date"`欄位（其餘14個
不需要），用來把大盤序列對齊到個股自己的交易日曆（forward-fill缺值，
理由與`core_tilt_backtest.py`2026-09-19的bug修正同一個道理：個股/大盤
兩邊日期集合不完全一致時，用最近已知值延續，不能讓「沒對齊」偽裝成
「大盤當天憑空變成0」）。個股日期早於大盤資料起點時正確答案是NaN，
不外插。

**⚠️使用規則（2026-09-20總司令裁示【原子.二FAIL採信，轉財報原子家族；
補佇列】二新增，補上原子.一審閱通過時遺漏的一條使用規則）**：**基準
原子不得單獨作為表達式的唯一來源，必須與至少一個個股原子結合才有
意義**（例如`relative_strength`＝個股報酬÷基準報酬，或
`ts_corr(個股原子,基準原子,n)`）。**根因**：基準原子（`b50_*`/`btx_*`）
在任一天對每一檔股票取值完全相同（同一天0050/大盤的價格對誰都一樣），
如果只對基準原子本身套用window/scalar算子（例如`delay(b50_c,1)`、
`ts_std(btx_v,20)`）而不結合任何個股原子，得到的表達式在cross-sectional
橫斷面上仍然是「對每檔股票同一個數字」，Spearman相關係數因零變異數
而數學上無定義——這在原子.二實測中造成3184個測試裡約860個結構性退化
（詳見`research/ATOM_IC_MAP.md`第3節），不是bug，是規格遺漏這條使用
限制的直接後果。**往後任何新的表達式產生器（例如原子.五財報版）在
組合表達式時，必須在產生階段就過濾掉「純基準原子、未結合個股原子」
的組合，讓這類結構性退化的組合根本不進事前登記的搜尋空間N**——跟
原子.二「已經測了才發現、但誠實計入N不事後排除」不同，這是**下一次
設計新表達式空間時要在源頭避免的錯誤**，不是要回頭修原子.二的已登記
結果。

## 算子清單（19個：17個原版＋2個2026-09-19補件）

### 時序算子（11個，n∈{1,5,20,60}）

| 算子 | 定義 | 邊界情況 |
|---|---|---|
| `delay(s,n)` | 往前n期的值 | 前n筆為NaN |
| `delta(s,n)` | `s − delay(s,n)` | 前n筆為NaN |
| `ts_mean(s,n)` | 滾動平均 | 前n-1筆為NaN（`min_periods=n`） |
| `ts_std(s,n)` | 滾動標準差 | n=1恆為NaN（單點無標準差） |
| `ts_max(s,n)` | 滾動最大值 | 同上 |
| `ts_min(s,n)` | 滾動最小值 | 同上 |
| `ts_rank(s,n)` | 窗口內當下值的百分位排名[0,1] | n=1恆為1.0（退化情況） |
| `ts_sum(s,n)` | 滾動加總 | 同ts_mean |
| `ts_corr(x,y,n)` | 滾動相關係數 | n=1恆為NaN（標準差0，相關無定義） |
| `ts_argmax(s,n)` | 窗口內最大值的相對位置 | 同ts_mean |
| `decay_linear(s,n)` | 線性衰減加權平均，`w_i∝(n−i)`(i=0最近) | 窗口內任一期NaN則整體NaN；n=1退化為自身 |

### 橫斷面算子（3個）

| 算子 | 定義 | 邊界情況 |
|---|---|---|
| `rank(cs)` | 同日橫斷面百分位排名[0,1] | 全部值相同時排名仍遞增（資料本身無鑑別力，非本函式責任） |
| `zscore(cs)` | 同日橫斷面z-score | 標準差為0時回NaN（不是0，避免誤讀成「剛好平均」） |
| `ind_neutral(cs,industry)` | 扣除同產業當日平均 | 單檔產業組扣自己恆為0（無法跟同業比較的誠實反映） |

### 純量算子（5個）

| 算子 | 定義 | 邊界情況 |
|---|---|---|
| `sign(s)` | 正負號 | 標準numpy行為 |
| `abs(s)` | 絕對值 | — |
| `log1p(s)` | `log(1+x)` | x≤−1時為−inf/NaN，如實暴露不吃掉 |
| `ratio(x,y)` | `x/y` | y=0時回NaN（不是inf或0） |
| `mul(x,y)` | `x×y` | NaN傳播（含`0×NaN=NaN`不是0） |

## 單元測試涵蓋情境

`ATOM_LIBRARY.py::self_test()`對每個原子/算子至少一項斷言，並專門涵蓋
四類邊界情況（含新補的`mul`/`decay_linear`/基準原子三項，沿用同一套
四情境）：
1. **NaN傳播**：序列開頭（`delay`/`delta`/`gap`/`true_range`第一筆）、
   窗口不足`min_periods`、`mul`/`decay_linear`任一輸入含NaN。
2. **除零**：`close_loc`/`vwap`/`ratio`的分母為0；`mul`的`0×NaN=NaN`
   （不是0，這是「不知道」不是「確定是零」）。
3. **停牌**（量/額為0，價格鎖死一價）：`vwap`回NaN、`range`為0、
   `close_loc`回NaN；`mul`遇量=0正常回0（量本來就是0，不是缺值）；
   `decay_linear`窗口內含NaN整體回NaN不硬湊。
4. **漲跌停鎖死**（h=l=c）：`close_loc`回NaN、`body`為0；`decay_linear`
   對常數序列回該常數本身（非除零壞掉）；基準原子對齊時個股/大盤
   日期不完全重疊用forward-fill延續前值。

執行`python research/ATOM_LIBRARY.py --self-test`目前**全數通過**
（24個原子、19個算子）。

## 下一步：原子.二（審閱通過，已直接接續，不再等待）

`原子.二`：列舉所有depth-1表達式（算子(原子,n)），計算IC（同產業peer
標準化後，對未來20日與60日報酬），輸出`research/ATOM_IC_MAP.md`，
禁止報告「最佳素材」，全部計入`selection_bias_ledger.py`的N。併入
「零件.零續」的範圍（既有因子池只有3訊號/2獨立成分，正確動作是造
新因子，原子層本身就是造因子的機器，不當獨立項目另開）。

---

## 財報原子庫（原子.四，2026-09-20，戴研究帽）

對應`PENDING_QUEUE.md`「2026-09-20補入項目」`原子.四`。程式：`research/FIN_ATOM_LIBRARY.py`
（獨立於價量庫，因為時間軸是季頻）。`python research/FIN_ATOM_LIBRARY.py --self-test`
（合成資料不連網）與`--real-data`（2330本機快取煙霧測試）皆通過。**本節只建庫，
不算IC、不組表達式、不做判定**；IC地圖是`原子.五`。

**11個原子（零參數）**：`revenue`／`eps`／`gross_profit`／`op_income`／`net_income`
（歸屬母公司淨利）、`total_assets`／`equity`（歸屬母公司權益）／`inventory`／
`receivable`（應收帳款淨額）、`ocf`（營業現金流，**由年初至今累計值還原成單季**）、
`shares`（加權平均股數＝淨利/EPS，衍生）。損益表為單季值、資產負債表為時點值。

**7個財報算子＋2個預期函式**：`yoy`／`qoq`／`ttm`／`slope(n)`／`delta_yoy`／
`surprise(實際,預期)`／`ratio`；預期＝`expected_seasonal_naive`（去年同季）或
`expected_seasonal_drift`（SUE慣用：去年同季＋上一季年增量），都零參數。窗口n只准
{1,4,8}季，**超出直接ValueError（程式層強制）**；`slope`只接受4、8（n=1無定義）。

**「往前k季」一律依曆法季度鍵查找，不是位置位移**——缺一季就NaN，不會把不相鄰的
兩季當相鄰。基期≤0的成長率、分母≤0的佔比一律NaN（不回0）。

**PIT設計與一個既有缺陷（請總司令知悉）**：
- 本庫的`pit_date`＝法定申報期限：Q1/Q2/Q3＝5/15、8/14、11/14，**Q4（年報）＝次年3/31**
  （證交法§36，全國法規資料庫）。2012年（含）以前取保守日期（Q4→4/30、Q2→8/31，
  舊制期限未逐條查證，僅為保守估計）。`pit_source`標`assumed_statutory`。
- **既有`research/pit.py::quarterly_pit()`對所有季度用「期末+45日」，Q4被提早到2/14，
  但年報法定期限是3/31（約6週前視偏誤）**。本輪**未動`pit.py`**（不順手改既有已驗證
  程式），也**尚未盤點哪些既有因子/回測引用它**。`[自行裁量]`：把「盤點`quarterly_pit`
  /`balance_sheet_pit`的引用者並評估Q4前視影響」列為`[自走補入]`債務項進佇列，
  由後續輪次處理；在此之前，任何用該函式且橫跨Q4的既有結論，**引用時應附此但書**。
- **財報重編**：FinMind每個(期別,科目)只存最新版本，重編數字會出現在原始pit_date，
  有輕微前視，無版本歷史故無法修正；重複列取最後一列（確定性）。

**單元測試涵蓋**：PIT五種期別＋遞增性＋Q4晚於期末+45日；11個原子各一；缺科目→NaN；
虧損負值照實保留；EPS=0→shares NaN；重編重複列；空輸入；OCF累計還原（含上一季缺）；
每個算子的缺值／負值／基期為零／季度不連續／NaN傳播／窗口限制；`surprise`只用
過去（改動未來值不影響過去值）；`known_at`／`align_to_daily`在申報期限當天前後一日
的可見性。真實資料：2330 2023全年OCF單季還原後加總＝全年累計（相對誤差<1e-6）、
2023Q4營收6,255億與公開數字量級吻合、無晚於VAL_END的期別。

**已知限制（誠實揭露）**：(1)FinMind無真實申報日，用期限＝保守但進場偏晚；(2)`shares`
是由四捨五入到0.01的EPS反推，EPS極小時雜訊大；(3)`ocf`兩個FinMind type
（2012起／2022起）語意視為相同、取前者優先，僅抽樣驗證2330重疊期兩者逐期相等（差0），其他股未驗證；
(4)存貨/應收在金融業無意義（金控/銀行無此科目→整欄NaN，屬預期）。
