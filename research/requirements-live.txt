# live server 與資料抓取用的相依套件（2026-09-06 連線三.4 鎖版本）
#
# 為什麼要鎖：alpha_live_server.py 現在是公開到網際網路的服務，套件版本浮動意味著
# 「哪天自動裝到一個有問題的版本，而我不知道」。鎖住之後升級變成一個有意識的動作，
# 而且要照 CLAUDE.md「七之二、常駐服務發布紀律」四步驗過才算完成。
#
# 版本來自 2026-09-06 這台機器上實際在跑、且已驗過的組合。
# 升級流程：scripts/check_security_updates.py 每週一 08:00 回報 → 總司令裁示 →
#           pip install -U → 重啟 → 四步驗證。**不自動升級。**
fastapi==0.141.1
uvicorn==0.52.4
starlette==1.6.0
requests==2.34.2
certifi==2026.6.17
urllib3==2.7.0
