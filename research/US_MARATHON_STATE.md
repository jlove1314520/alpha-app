

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `US_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

---
**最後更新：2026-09-09T15:00+08:00（馬拉松第490輪）**——取鎖乾淨（cycle`20260909-150036`）。三軌時間戳：FUT 10:30（round484，最舊，依例外條款不選——無新機制候選）／US 12:30（round488，較舊）／TW 13:00（round489，最新）——依輪替選US。開工查`run_detached.py status`：`running=0`，無heavy-job-slot佔用。**核實候選池現況不變**：0a節四條方向＋`#61`~`#65`全數結案，僅`#50`仍卡tick累積（`data/ticks/`仍2/20，較round489無變化）。**本輪工作單位＝回補`data/signal_status.json`遺漏的`#66`**：`grep HYPOTHESIS_QUEUE.md`確認`hypothesis_queue`排程已於本輪之前把`#66`（美股年末稅損收割賣壓／一月效應，第⑮類「稅務動機機械性交易」機制）跑完第1關cheap gate並正式判定FAIL，登記`TRIALS_LEDGER.md`#230（4項判準TRAIN/VAL x 12月/次年1月，僅TRAIN次年1月單項過關），但逐一核對`STRATEGY_GRAVEYARD.md`（`grep gate66`）與`build_signal_status.py`的`DIRECTIONS`常數，發現**兩處都尚未同步**（跟round489發現`#65`遺漏是同一類缺口）。已逐字核對`HYPOTHESIS_QUEUE.md` `#66`最終判定段落與`TRIALS_LEDGER.md`#230列的內容後，先在`STRATEGY_GRAVEYARD.md`新增`us_tax_loss_selling_gate66`條目（比照`f_leader_follower_lag`格式），再比照`#65`格式在`build_signal_status.py`新增對應段落並重跑，`directions`清單`ids=['49','50','51','52','62','63','64','65','66']`共9條。`git diff -b`確認`STRATEGY_GRAVEYARD.md`/`build_signal_status.py`/`data/signal_status.json`三檔案除新增段落外無其他內容被更動（`--stat`：+71/-1，皆為新增區塊）。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（232列，撞號2組皆為歷史存量不回頭改寫）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純核對既有結案文件）。**下一輪任一軌接手**：`#50`tick累積仍是唯一活動候選，查`data/ticks/`是否已到3/20；`hypothesis_queue`排程下一輪會從設計`#67`開始（`HYPOTHESIS_QUEUE.md`已記錄「佇列#1~66全數結案」），非本馬拉松三軌工作範圍，屆時核對其設計是否符合「真正跳脫既有清單」標準即可；候選池已連續多輪（487~490）皆無TW/US/FUT三軌自身可推進的新工作單位，僅剩被動等待`#50`與`hypothesis_queue`自走排程產物的回補維護，若下一輪盤點仍是同樣狀態，應開始認真評估是否接近0a節「無可驗證預測優勢」提報門檻（`#50`本身未結案前嚴格說還不到，但候選池已非常薄）。完整見`STRATEGY_GRAVEYARD.md`（新增`us_tax_loss_selling_gate66`）、`build_signal_status.py`（新增`id=66`段落）、`data/signal_status.json`（本輪新增`66`一條）、`REPORT.md`第490輪心跳。

---
**最後更新：2026-09-09T12:30+08:00（馬拉松第488輪）**——取鎖乾淨（cycle`20260909-123036`）。三軌時間戳：FUT 10:30（round484，最舊，依例外條款不選——無新機制候選）／US 11:30（round486，較舊）／TW 12:00（round487，最新）——依輪替選US。開工查`run_detached.py status`：`running=0`，無heavy-job-slot佔用。**核實候選池現況不變**：0a節四條方向＋`#61`/`#62`/`#63`/`#64`全數結案，僅`#50`仍卡tick累積（`data/ticks/`仍2/20，較round486無變化）。**本輪工作單位＝修正一個真實的基礎設施缺陷：`hypothesis_queue`排程用ad-hoc背景行程（非`run_detached.py`breakaway機制）跑重度計算，session被砍時會陪葬**。round487心跳記錄`hypothesis_queue`排程於10:56設計新假設`#65`（產業龍頭股跨期領先-落後動能，見`HYPOTHESIS_QUEUE.md`第8760行完整設計），12:00自行起了一個背景python行程（PID`141224`，直接導向`/tmp/gate65_run.log`）在算300檔因子快取，round487發現後判定重複、殺掉自己另開的那個而保留原有的。**本輪開工核實**：`Get-CimInstance Win32_Process`查`factor_ic_leader_follower_lag`已無任何行程在跑，`/tmp/gate65_run.log`最後修改時間停在12:00:54（`Loading sample + computing factors...`那一行之後就沒有再更新），代表**那個PID`141224`背景行程在round487的session結束時被一併砍掉、沒有寫出任何結果，也沒有留下錯誤訊息**——這正是`MARATHON_PROTOCOL.md`0b節警告過的「不用`run_detached.py`開的背景工作會陪葬」，`hypothesis_queue`排程目前對長跑腳本沒有套用breakaway機制，這是它自己流程的缺口，非本輪或TW軌造成。**已提出修復做法，但被實況追過（誠實記錄）**：改用`python run_detached.py submit --name leader_follower_lag_gate1_65tw --timeout-min 40 -- python -u factor_ic_leader_follower_lag.py`正式投遞（job`20260909-123209-f2b9`，watchdog breakaway=True），提交路徑寫成相對路徑`factor_ic_leader_follower_lag.py`但`run_detached.py`的工作目錄是`C:\alpha\alpha-app`（非`research\`），**該job因此立刻`failed`（exit=2，`can't open file`，純本輪自己的路徑bug，2秒內結束，未消耗任何算力/API）**。**收工前核實才發現這個job已經是多餘的**：`git status`看到`TRIALS_REGISTRY.jsonl`本輪期間被新增一筆`#229`（`registered_at 2026-09-09T12:31:35`）——`hypothesis_queue`排程自己在12:22的下一個週期已經**乾淨重跑**`factor_ic_leader_follower_lag.py`（命中既有2488個`TaiwanStockPrice`快取，非承接round487陪葬掉的殘留狀態）並正式判定：TRAIN mean_ic=+0.0072/IR=+0.065(n=288)、VAL mean_ic=+0.0094/IR=+0.091/hit_rate=0.51(n=193)，同號但VAL |IC|遠小於0.02門檻、null_percentile=87.5未過90.0，**判FAIL**（`TRIALS_LEDGER.md`/`TRIALS_REGISTRY.jsonl`#229、`STRATEGY_GRAVEYARD.md`已由該排程自行完整寫入，`HYPOTHESIS_QUEUE.md` `#65`章節已附最終判定段落，佇列#1~65全數結案，該排程已把新假設軸留給自己下一輪設計#66）。**結論：round487觀察到的「ad-hoc背景行程會陪葬」現象本身仍是真的（`/tmp/gate65_run.log`那個PID`141224`行程確實在session結束時死掉、無結果無錯誤），但`hypothesis_queue`排程的正常下一輪週期自己重跑就補上了，這次沒有造成延誤**，本輪的補救job變成多餘動作（且因路徑bug本身也沒真的跑成）。`trial_registry.py --check`本輪exit=0（`#229`為`hypothesis_queue`排程自己登記，非本輪呼叫`register_trial()`）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（本輪自己只有一次因路徑錯誤立即失敗的job提交，未觸及任何資料源）。**教訓（留給下一輪，非急迫修復）**：`run_detached.py status`/日誌只涵蓋本框架自己登記的工作，`hypothesis_queue`排程有獨立背景執行機制且有自己的重試週期，兩者在候選池收斂到同一新假設時可能重疊——**接手任何`hypothesis_queue`產物前，先查`TRIALS_LEDGER.md`/`TRIALS_REGISTRY.jsonl`是否已有最新編號的登記，比查行程/log更可靠**（行程可能已經正常結束，log卻因為是舊行程的殘留檔案而顯示「未完成」）。**下一輪任一軌接手**：`#65`已完全結案免動作；候選池持續薄（0a節四條方向+`#61~#65`全數結案，僅`#50`卡tick累積2/20）；查`data/ticks/`是否已到3/20；`hypothesis_queue`排程下一輪會自行設計`#66`（第⑮類機制），非本馬拉松三軌工作範圍，屆時可核對其設計是否符合「真正跳脫既有清單」標準即可。完整見`HYPOTHESIS_QUEUE.md` `#65`最終判定段落、`TRIALS_LEDGER.md`/`TRIALS_REGISTRY.jsonl`#229、`STRATEGY_GRAVEYARD.md`、`REPORT.md`第488輪心跳。

---
**最後更新：2026-09-09T11:30+08:00（馬拉松第486輪）**——取鎖乾淨（cycle`20260909-113036`）。三軌時間戳：US 10:00（round483，最舊）／TW 11:00（round485，最新，FUT round484依例外條款已用畢不重選）——依輪替選US。開工查`run_detached.py status`：`running=0`，無heavy-job-slot佔用。**核實候選池現況不變**：0a節四條方向（#49/#51/#52皆FAIL）＋`#61`/`#62`/`#63`/`#64`全數結案，僅`#50`仍卡tick累積，US軌本地端持續無可在現有資料存取限制下推進的新工作單位（round479/481/483已連續三輪確認）。**本輪工作單位＝跨軌基礎設施維護（兩件，皆屬純bug修復/健康檢查，非新假設）**：(a) 發現`REPORT.md`心跳有多輪（470-479/481/485）被append到檔案**尾端**而非慣例的**頂端插入**（`marathon_brief.py`讀取邏輯是「新的在上面：取前n個條目」），導致round485的心跳完全沒有出現在本輪開工簡報的「REPORT.md最新2輪心跳」區塊裡——本輪開工時一度誤以為round485沒寫心跳，逐一核對`git log`與檔案內容後才確認是位置錯了不是真的漏寫。已把round485搬到正確頂端位置，470-479/481這批更早的錯位條目範圍較大暫不處理（不影響`marathon_brief.py`實際運作，只讀最新2則），留給未來一輪視情況清理。同步補齊`MARATHON_STATE.md`全局計數器（484→486，round485當時漏未同步）。(b) 實測`data/ticks/20260909/`目前9個商品jsonl皆與開工時間（11:32）同步即時更新，確認`#50`唯一前置依賴——tick落地管線本身仍在正常運作、非靜默停擺，排除「等18個交易日結果管線早就壞了沒人發現」的風險。`trial_registry.py --check`本輪無新判定（純維護，非策略判定）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫。**下一輪任一軌接手**：查`data/ticks/`是否已到4/20；候選池持續薄，`#50`未結案前仍未到0a節「無可驗證預測優勢」提報門檻。完整見`REPORT.md`第486輪心跳、`MARATHON_STATE.md`。

（第444/446/448輪已於round454歸檔至`US_STATE_ARCHIVE.md`；第450/452輪已於round458歸檔；第454輪已於round460歸檔；第456輪已於round462歸檔；第458輪已於round464歸檔；第460輪已於round466歸檔；第462輪已於round468歸檔；第464/466輪已於round473歸檔；第468輪已於round476歸檔；第470輪已於round479歸檔；第473輪已於round481歸檔；第476輪已於round483歸檔；第479輪已於round486歸檔；第481輪已於round488歸檔；第483輪已於round490歸檔，僅保留最新3則）
