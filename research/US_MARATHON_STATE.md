

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的 71 則已原文搬到 `US_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-06T09:00+08:00（馬拉松第393輪）**——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：FUT 02:30（第385輪，最舊，明確跳過信號維持有效）／US 08:00（第391輪）／TW 08:30（第392輪，最新）——依輪替選US（較TW舊）。`run_detached.py status`確認：US低波動job`20260906-060311-6a01`已於round392收成完畢；TW的`tw_deep_dive_quality_roe_stability_full_rerun`（`20260906-083408-d6ab`）本輪開工/收工時皆仍`running`（31.3分鐘/150分鐘timeout），heavy-job-slot持續佔用中，故本輪US排定的下一個重度工作`us_deep_dive_valuebm_clean_universe`**無法投遞**（`submit`遇running會被拒絕，exit 3）。**本輪工作單位＝不需heavy-job-slot的輕量診斷：`#151`(round392 EXPERIMENTAL判定)VAL期逐年分解**，接續round392「下一步」對VAL期年化90%+/beta-0.82異常量級的regime-specific疑慮。**關鍵洞察**：`run_one()`裡100 draws隨機控制組才是136分鐘那個重度成本，年度分解只需真實日報酬序列本身重跑一次`run_long_short_us()`（VAL期,1x cost），不需要重跑100次隨機重繪，故可在foreground直接執行（約1分鐘），不佔用run_detached重度插槽。新增`deep_dive_f_us_low_vol_val_year_breakdown.py`，事前綁定判準（leave-2022-out變號或衰減至<30%＝CONFIRMED、同號且保留≥60%＝REFUTED、其餘PARTIAL，逐字比照`margin_debt_level_train_val_heterogeneity.py`#142同一套trichotomy）。**結果：2022單一年份驅動假說REFUTED**（leave-2022-out ann_return=+98.78%，比全期+92.46%還高，同號、保留107%）——但意外發現：VAL期四年（2021+39.79%/2022+80.44%/2023+70.18%/2024+230.64%）**沒有任何一年**落在BAB文獻合理量級，是貫穿全VAL期的系統性放大而非單一事件污染，比原本假設的regime疑慮更嚴重。判定`#151`維持EXPERIMENTAL不變，下一步方向改為leave-top-N-out集中度檢查（比照`US_LEADS.md`#18對value factor的方法論），不再往regime overlay方向查。已寫入`TRIALS_LEDGER.md`#152、`US_LEADS.md`#21更新。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增API呼叫（純讀既有parquet快取）。**下一輪接手**：(1)先`run_detached.py status`確認TW job是否`finished`，若是則插槽空出，投遞`us_deep_dive_valuebm_clean_universe`（`--timeout-min 150`）；(2)若時間允許可先做`#151`的leave-top-N-out集中度檢查（重用`deep_dive_f_us_value_bm_leave_extreme_out.py`#18方法論，同樣是輕量診斷不需heavy slot）。完整見`US_LOG.md`第393輪記錄、`TRIALS_LEDGER.md`#152、`data/deep_dive_f_us_low_vol_val_year_breakdown.csv`（新增）。

---

**上一則保留（第391輪，供對照）**——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：FUT 02:30（第385輪，最舊，但round385自己結論「trend+oi四種組合/變體全FAIL，優先權回TW/US，除非有全新機制假說」，明確跳過信號）／US 06:03（第387輪）／TW 07:30（第390輪，最新）——依輪替本應選FUT但有明確跳過理由，US/TW皆最近有進度，但**US/TW共用同一個run_detached重度工作插槽（一次只能跑一個）**，本輪開工前該插槽被US自己的`20260906-060311-6a01`（round387投遞，150分鐘timeout）佔用，約121分鐘仍`running`（log顯示5/6組合已完成，VAL 3x cost進行中，未逾時不是卡死），這同時也是TW排隊中`tw_deep_dive_quality_roe_stability_full_rerun`卡住的原因——選US處理這個依賴鏈上游的track。**本輪工作單位＝不需重算的地基準備**：新增`deep_dive_f_us_value_bm_clean_universe.py`，供`20260906-060311-6a01`收成、插槽釋放後下一輪立即投遞（round387「下一步」已明列此為低波動測完後的下一個候選）。**單一變因替換手法**：重用`deep_dive_f_us_value_bm.py::run_one_value()`/`PERIODS`/`COST_MULTIPLIERS`/`_load_market_df()`原封不動，資料loader換成round382`us_factor_ic_value_clean_universe.py::load_value_sample()`（乾淨宇宙、SEC EDGAR快取已由round382背景job填滿，預期零新增API呼叫），跟`deep_dive_f_us_low_vol_clean_universe.py`（round386）同一套改法。**本輪刻意不執行main()**（只做`ast.parse`語法檢查+`import`匯入檢查確認無誤），避免在插槽已被佔用時再搶跑第二個重度工作。`is_holdout_consumed()`開工/收工前皆確認`False`。零新增API呼叫。**下一輪接手**：`run_detached.py status`確認`20260906-060311-6a01`是否`finished`；若是，先讀SUMMARY寫入`TRIALS_LEDGER.md`（優先檢查TRAIN期percentile與beta方向，注意本次log顯示VAL期beta已達-0.821、遠超TRAIN的-0.316，市場中性程度隨樣本期推移惡化，是否合理需要在深挖判定時一併檢視，不要只看alpha/ann_return漂亮就放行），接著插槽空出後投遞`python research/run_detached.py submit --name us_deep_dive_valuebm_clean_universe --timeout-min 150 -- python -u research/deep_dive_f_us_value_bm_clean_universe.py`（**--timeout-min用150起跳，不要用20分鐘預設值，這是round386→387已經證實過一次的教訓**）。完整見`US_LOG.md`第391輪記錄。**另外提醒**：`REPORT.md`心跳跟`MARATHON_STATE.md`全局計數器在round386~390期間被跳過5次未寫，本輪已於這兩份檔案補記，往後每輪務必不要漏這個硬性步驟（`MARATHON_PROTOCOL.md`第6節第4點）。

---

**上一則保留（第387輪，供對照）**——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：TW 02:00（第384輪，最舊）／FUT 02:30（第385輪）／US 05:37（第386輪，最新）——依輪替本應選TW，但round386投遞的背景深挖工作`20260906-053411-6d75`在本輪開工時已是`timeout`狀態（20分鐘timeout到期被看門狗砍掉，非正常完成），需要先處理這個懸而未決的job才能讓US軌繼續往下走，故選US收成並處理（比照round383/382「已完成/已逾時的背景工作優先收成」的例外邏輯）。**收成結果：job `20260906-053411-6d75`（`deep_dive_f_us_low_vol_clean_universe.py`）在跑完`load_clean_sample_with_factors()`（248/248可用，SPY基準8038筆）並印出第一個組合「TRAIN 2015-01-01..2020-12-31, cost 1x」的標頭後，20分鐘內未產出任何後續輸出即被砍**（`run_detached.py log --tail 60`確認）。**根因判讀（非猜測，有history佐證）**：查`US_LOG.md`round336/339記錄，同一份`run_one()`/`run_long_short_us()`邏輯在225檔規模（跟本次248檔同量級）的舊`cached_universe`版本上，歷史上也曾跨越兩輪session budget才跑完單一次完整重跑——`run_long_short_us()`每個period/cost組合要跑1（真實）+100（隨機控制組）＝101次完整逐日回測，每次回測內層用`idx[sid].loc[day]`逐檔逐日查表（非向量化），101次×約1500個交易日×約50檔/腿的查表量級，單一組合本身就可能需要10幾分鐘，而本輪任務共有2期×3成本＝6個組合，總計需求遠超20分鐘。**這不是本輪新腳本的bug**（`load_clean_sample_with_factors()`本身跑得很快，248/248零錯誤），是`deep_dive_f_us_low_vol.py::run_one()`這支被多輪沿用、刻意保持不變以維持方法學可比較性的共用回測函式，在完整248檔×101次重跑規模下單純運算量大，20分鐘timeout訂得太保守。**處理方式**：不改動`run_one()`/`run_long_short_us()`計算邏輯本身（維持跟#15/#41/#104/#115同一套方法學，不引入新的比較基準差異），只重新以更寬裕的timeout投遞同一支腳本：`run_detached.py submit --name us_deep_dive_lowvol_clean_universe_retry --timeout-min 150 -- python -u research/deep_dive_f_us_low_vol_clean_universe.py`（job_id`20260906-060311-6a01`），session內等待2分鐘確認仍`running`（非立即崩潰），依協定第0b節本輪不繼續等待，job以`breakaway=True`繼續在背景執行，150分鐘上限應足以跨越接下來數輪session完整跑完6個組合。`is_holdout_consumed()`開工/收工前皆確認`False`。本輪session內零新增API呼叫（純讀log+resubmit，248檔全部命中既有parquet快取）。**下一輪接手**：先`run_detached.py status`確認`20260906-060311-6a01`是否`finished`；若仍`running`且未逾150分鐘，不必重複投遞，直接跳過等下一輪再檢查（避免`submit`因已有running工作而被拒絕，exit 3）；若`finished`則`run_detached.py log 20260906-060311-6a01 --tail 60`讀SUMMARY，優先檢查TRAIN期percentile與beta方向（吸取#15/#41/#115教訓），寫入`TRIALS_LEDGER.md`新列。若這次仍`timeout`（150分鐘仍不夠），下一輪應該考慮的是否要對`run_one()`做效能優化（例如把`idx[sid].loc[day]`逐檔查表換成預先對齊好的numpy陣列向量化計算）——但這屬於「新的架構/效能變更」，按`CLAUDE.md`「提案先於執行」鐵律，效能重構前應先寫提案給總司令，不能自行決定改寫這支被多處沿用的共用回測函式。跑完低波動後，同樣手法對`f_us_value_bm`（#20）做1b深挖（`deep_dive_f_us_value_bm_clean_universe.py`）。完整見`US_LOG.md`第387輪記錄。

