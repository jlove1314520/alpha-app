

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的 71 則已原文搬到 `US_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-06T06:03+08:00（馬拉松第387輪）**——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：TW 02:00（第384輪，最舊）／FUT 02:30（第385輪）／US 05:37（第386輪，最新）——依輪替本應選TW，但round386投遞的背景深挖工作`20260906-053411-6d75`在本輪開工時已是`timeout`狀態（20分鐘timeout到期被看門狗砍掉，非正常完成），需要先處理這個懸而未決的job才能讓US軌繼續往下走，故選US收成並處理（比照round383/382「已完成/已逾時的背景工作優先收成」的例外邏輯）。**收成結果：job `20260906-053411-6d75`（`deep_dive_f_us_low_vol_clean_universe.py`）在跑完`load_clean_sample_with_factors()`（248/248可用，SPY基準8038筆）並印出第一個組合「TRAIN 2015-01-01..2020-12-31, cost 1x」的標頭後，20分鐘內未產出任何後續輸出即被砍**（`run_detached.py log --tail 60`確認）。**根因判讀（非猜測，有history佐證）**：查`US_LOG.md`round336/339記錄，同一份`run_one()`/`run_long_short_us()`邏輯在225檔規模（跟本次248檔同量級）的舊`cached_universe`版本上，歷史上也曾跨越兩輪session budget才跑完單一次完整重跑——`run_long_short_us()`每個period/cost組合要跑1（真實）+100（隨機控制組）＝101次完整逐日回測，每次回測內層用`idx[sid].loc[day]`逐檔逐日查表（非向量化），101次×約1500個交易日×約50檔/腿的查表量級，單一組合本身就可能需要10幾分鐘，而本輪任務共有2期×3成本＝6個組合，總計需求遠超20分鐘。**這不是本輪新腳本的bug**（`load_clean_sample_with_factors()`本身跑得很快，248/248零錯誤），是`deep_dive_f_us_low_vol.py::run_one()`這支被多輪沿用、刻意保持不變以維持方法學可比較性的共用回測函式，在完整248檔×101次重跑規模下單純運算量大，20分鐘timeout訂得太保守。**處理方式**：不改動`run_one()`/`run_long_short_us()`計算邏輯本身（維持跟#15/#41/#104/#115同一套方法學，不引入新的比較基準差異），只重新以更寬裕的timeout投遞同一支腳本：`run_detached.py submit --name us_deep_dive_lowvol_clean_universe_retry --timeout-min 150 -- python -u research/deep_dive_f_us_low_vol_clean_universe.py`（job_id`20260906-060311-6a01`），session內等待2分鐘確認仍`running`（非立即崩潰），依協定第0b節本輪不繼續等待，job以`breakaway=True`繼續在背景執行，150分鐘上限應足以跨越接下來數輪session完整跑完6個組合。`is_holdout_consumed()`開工/收工前皆確認`False`。本輪session內零新增API呼叫（純讀log+resubmit，248檔全部命中既有parquet快取）。**下一輪接手**：先`run_detached.py status`確認`20260906-060311-6a01`是否`finished`；若仍`running`且未逾150分鐘，不必重複投遞，直接跳過等下一輪再檢查（避免`submit`因已有running工作而被拒絕，exit 3）；若`finished`則`run_detached.py log 20260906-060311-6a01 --tail 60`讀SUMMARY，優先檢查TRAIN期percentile與beta方向（吸取#15/#41/#115教訓），寫入`TRIALS_LEDGER.md`新列。若這次仍`timeout`（150分鐘仍不夠），下一輪應該考慮的是否要對`run_one()`做效能優化（例如把`idx[sid].loc[day]`逐檔查表換成預先對齊好的numpy陣列向量化計算）——但這屬於「新的架構/效能變更」，按`CLAUDE.md`「提案先於執行」鐵律，效能重構前應先寫提案給總司令，不能自行決定改寫這支被多處沿用的共用回測函式。跑完低波動後，同樣手法對`f_us_value_bm`（#20）做1b深挖（`deep_dive_f_us_value_bm_clean_universe.py`）。完整見`US_LOG.md`第387輪記錄。

---

**上一則保留（第386輪，供對照）**——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：US 01:30（第383輪，最舊）／TW 02:00（第384輪）／FUT 02:30（第385輪，最新）——依輪替選US。`run_detached.py status`確認無running中工作需優先收成。**本輪工作單位＝接續round383「下一輪接手」：對`f_us_low_vol`（#21）做1b深挖**，新增`deep_dive_f_us_low_vol_clean_universe.py`（重用`deep_dive_f_us_low_vol.py`的`run_one()`/`PERIODS`/`COST_MULTIPLIERS`/`REBALANCE_DAYS`/`_load_market_df()`原封不動，唯一差異是ticker清單換成`data/us_stratified_universe_sample.csv`的usable==True清單，控制變因單一，比照round383`us_factor_ic_lowvol_clean_universe.py`同一套改法）。**踩到一個小bug**：初版`UNIVERSE_CSV`用裸字串`"data/us_stratified_universe_sample.csv"`（相對路徑），第一次提交（job`20260906-053320-99d4`）因`run_detached.py`的cwd不保證等於`research/`而`FileNotFoundError`失敗（exit=1，0.1分鐘）——**這不是「cwd永遠是research/」的既有假設有問題，是這支新腳本沒有像`us_factor_ic_lowvol_clean_universe.py`一樣用`Path(__file__).parent / "data" / ...`那樣位置無關的寫法**，已修正並重新驗證`.exists()`為`True`才重新投遞。修正後用`run_detached.py submit`重新投遞（job_id`20260906-053411-6d75`，timeout 20分鐘），session內等待約3分鐘仍`running`（`watchdog_alive=True`，248檔×2期×3成本倍數×100 draws隨機控制組，比round383單純cheap gate的IC計算重，跑超過3分鐘不意外），**依協定第0b節本輪不繼續等待，job以`breakaway=True`繼續在背景執行**，20分鐘timeout內應可跑完。`is_holdout_consumed()`開工/收工前皆確認`False`。本輪session內零新增API呼叫（248檔全部已是`us_price_series()`parquet快取命中，round383已驗證）。**下一輪接手**：先跑`run_detached.py status`確認`20260906-053411-6d75`是否`finished`，若是則`run_detached.py log 20260906-053411-6d75 --tail 40`讀SUMMARY區塊（TRAIN/VAL兩期×3成本倍數的ann_return/beta/alpha/Sortino/random_control_percentile），**優先檢查TRAIN期percentile與beta方向**（吸取#15/#41/#115舊池子皆敗在TRAIN期輸給隨機控制組或beta非市場中性大幅負值的教訓），寫入`TRIALS_LEDGER.md`新列；若失敗（`failed`/`timeout`）則讀log查根因再決定重跑或改路線。跑完低波動後，同樣手法對`f_us_value_bm`（#20）做1b深挖（`deep_dive_f_us_value_bm_clean_universe.py`，可比照本輪腳本改寫，注意路徑寫法用`Path(__file__).parent`）。完整見`US_LOG.md`第386輪記錄、`deep_dive_f_us_low_vol_clean_universe.py`（新增，可重複執行）。

---

**上一則保留（第383輪，供對照）**——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：TW 22:00（第380輪，最舊）／US 01:30（第383輪，最新）——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：TW 22:00（第380輪，最舊）／FUT 18:30（第372輪，但近10輪已佔20%配額上限，優先權回TW/US，跳過）／US 01:00（第382輪，最新）——依例外規則本應選TW（最舊），但round382投遞的背景SEC EDGAR fetch工作`20260906-010314-7940`已在本輪開工前正常完成（收成優先，避免斷點閒置），故仍選US收成並接續。**收成結果1：`f_us_value_bm`乾淨宇宙cheap gate重測完成**——159/248可用（84檔無財報資料/4檔全NaN/1檔無法解析CIK），train mean_ic=+0.0432 IR=+0.273（n=76）、val mean_ic=+0.0853 IR=+0.508 hit_rate=0.69（n=49），train/val同號，**null percentile=100.0（門檻90.0），CHEAP_PASS**——翻轉#128舊池子（`cached_ticker_ids()`）的FAIL判定，證實原判定FAIL的根因是universe選樣偏誤，非value factor本身無edge。**本輪接續工作單位：同樣手法重跑`f_us_low_vol`（#115 FAIL）**（round376「下一步」明列項目）。新增`us_factor_ic_lowvol_clean_universe.py`（重用`factor_ic.py::build_snapshots()`/`evaluate_factor()`與`us_factors.py::prepare_us_factors()`/`us_price_series()`原封不動，唯一差異是ticker清單換成同一份`data/us_stratified_universe_sample.csv`）：**低波動因子純價格資料，248檔全部已在`cached_ticker_ids()`快取內，零新增API呼叫，本輪原地執行（未投遞背景）在數分鐘內即跑完**。**結果：248/248可用，train mean_ic=+0.0683 IR=+0.397（n=76）、val mean_ic=+0.1380 IR=+0.689 hit_rate=0.73（n=49），train/val同號，null percentile=100.0（門檻90.0），CHEAP_PASS**——同樣翻轉#15/#115舊池子FAIL判定。**本輪最有資訊量的發現**：兩個完全不相關機制的因子（value/book-to-market vs low-vol）在同一份乾淨宇宙上都清楚CHEAP_PASS，且未重現舊池子那種「兩個不相關因子VAL期報酬量級近乎相同」的可疑一致性，雙重印證#17/#18/#19/#139的universe假影判定成立、且已被本輪證實可以透過換乾淨宇宙修復。寫入`TRIALS_LEDGER.md`#144/#145、`US_LEADS.md`#20/#21。**誠實保留**：本輪兩者都只是cheap gate（IC）過關，尚未做1b策略回測深挖——#128（value）跟#14/#15/#115（low-vol）舊池子的死因都發生在1b深挖階段（value死於VAL期報酬量級不合理、low-vol死於TRAIN期輸給隨機控制組或beta非市場中性大幅負值），換乾淨宇宙的IC過關**不能假設1b深挖會通過**，下一輪必須重新走一次train/val長短倉+配對式隨機控制組N=100+成本1x/2x/3x+beta對照，且要優先檢查TRAIN期percentile與beta方向（吸取舊池子兩者皆敗在這裡的教訓）。`is_holdout_consumed()`開工/收工前皆確認`False`。本輪session內零新增API呼叫（新增SEC EDGAR成本全在round382背景job，本輪只讀log；low-vol重測純讀既有FinMind快取）。**下一輪接手**：對#20（`f_us_value_bm`）或#21（`f_us_low_vol`）擇一做1b深挖，可直接改寫`deep_dive_f_us_value_bm.py`/`deep_dive_f_us_low_vol_cached_universe.py`换成乾淨宇宙ticker清單（`data/us_stratified_universe_sample.csv`），預期跑幾分鐘內可完成（135~248檔規模，非新API呼叫），若跑超過5分鐘依協定投遞背景。完整見`US_LOG.md`第383輪記錄、`TRIALS_LEDGER.md`#144/#145、`US_LEADS.md`#20/#21、`us_factor_ic_value_clean_universe.py`（round382新增）、`us_factor_ic_lowvol_clean_universe.py`（本輪新增，可重複執行）。

# US_MARATHON_STATE.md — 美股軌斷點狀態（覆寫式）

**這份檔案只描述美股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `US_LOG.md`；候選判定看 `US_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。
