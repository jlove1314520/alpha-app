

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `US_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

---
**最後更新：2026-09-09T02:30+08:00（馬拉松第468輪）**——取鎖乾淨（cycle`20260909-023037`）。三軌時間戳：US 01:30（round466，最舊）／TW 02:00（round467，最新）——依輪替選US（FUT依例外條款不選）。`run_detached.py status`確認job`20260909-013149-9992`（`sp500_index_changes_backfill_51us`全範圍回補）已`finished`（exit=0，35.7分鐘，累積76個列表頁、645篇符合關鍵字文章），heavy-job-slot空出。**本輪工作單位＝依round466交辦，進入`#51-US`第1關cheap gate**。

**事前綁定**（寫在`sp500_addition_runup_gate51us.py`模組docstring，跑數字前定案）：(1)只測Addition不測Deletion——Deletion常見成因是被收購/破產下市，會踩到`CLAUDE.md`「下市股資料取得不等於正確」的yfinance低覆蓋污染雷，Addition公司必然仍活躍可交易不受此雷影響，此為唯一篩選理由；(2)事件窗口＝公告日（`article_date_raw`）→生效日（`effective_date_raw`）CAR，事前方向假設為正（被動基金搶跑買進）；(3)**價格源改用`yf_price_client.fetch_yf_index()`（yfinance原生美股）而非`us_factors.us_price_series()`（FinMind USStockPrice）**——後者違反`CLAUDE.md`2026-09-08「禁止用台灣資料商作為美股宇宙或價格的主來源」裁示，本輪發現既有`us_8k_pead_gate52.py`系列/`us_factor_ic_by_size.py`系列US軌試驗仍在用FinMind USStockPrice後，主動在新試驗改正，既有試驗是否需重工留給後續驗證帽/總司令判斷；(4)控制組`own_ticker_window`/`cross_ticker_window`兩獨立變體各N=200（2026-09-07升級標準：訊號需嚴格大於全部400次抽樣最大值）；(5)TRAIN/VAL依`validation.holdout`，判定看VAL。

**smoke test**：`--max-events 12 --n-draws 5`與`--max-events 150 --n-draws 25`兩組驗證管線端到端正常。**正式全量job**`20260909-023739-d4cd`（`--timeout-min 25`），session內`wait --max-min 4`內即`finished`（遠快於上限，只需下載約320檔ticker的yfinance歷史，非受節流的MOPS/SEC EDGAR來源）。

**結果**：390筆Addition事件中222筆可用（47無價格歷史/116超出價格涵蓋範圍/5生效日不晚於公告日）。TRAIN（2012-2020,n=25）mean_CAR=+11.59%嚴格大於控制組最大值5.36%（percentile100.0，CHEAP_PASS，但n=25過小不足採信）；**VAL（2021-2024,n=197）mean_CAR=+1.56%，控制組百分位97.8非常接近但未嚴格大於控制組最大值2.38%，判定FAIL**。方向與index-effect文獻（Harris & Gurel 1986等）完全一致，是模組docstring事前寫明「近二十年因套利搶跑而明顯減弱」已知風險下的誠實邊緣FAIL，非管線問題。

已登記`TRIALS_LEDGER.md`#222、`US_LEADS.md`#32、`STRATEGY_GRAVEYARD.md`新增段落、`data/signal_status.json`（`#51`整體結案，新增`51-us`子事件節點）。`trial_registry.py --check`確認PASS（exit=0，224列）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零FinMind/SEC EDGAR歷史API呼叫（僅yfinance每檔一次OHLCV下載，約320次請求）。

**#51整體結案為FAIL**（台股三子事件2026-09-07已結案+美股Addition子測試本輪結案，美股Deletion子測試因下市股價格污染雷事前排除未測）。**下一輪US軌接手**：`MARATHON_PROTOCOL.md`0a節四條方向中，#49已結案FAIL、#50卡在前置tick依賴、#51本輪結案FAIL、#52台股IN_PROGRESS（gate2延續檢定）美股已結案FAIL——US軌四條新方向自身能做的部分已無剩餘工作單位，下一輪建議：(a)查核#52-TW gate2 job進度（若heavy-job-slot空出可協助收成，雖屬TW軌工作但避免浪費輪次）、或(b)依`MARATHON_PROTOCOL.md`0a節誠實判斷點，盤點四條方向是否已全部結案到可以下「無可驗證預測優勢」正式結論的程度（目前#50仍NOT_STARTED卡在前置依賴、#52-TW仍IN_PROGRESS，尚未到達可以下整體結論的時點，不得提早下結論）。完整見`HYPOTHESIS_QUEUE.md` #51-US條目第468輪新增段落、`REPORT.md`第468輪心跳、`sp500_addition_runup_gate51us.py`（新增，可重複執行）。

---
**最後更新：2026-09-09T01:30+08:00（馬拉松第466輪）**——取鎖乾淨（cycle`20260909-013037`）。三軌時間戳：US 00:30（round464，最舊）／TW 01:00（round465，最新）——依輪替選US（FUT依例外條款不選）。`run_detached.py status`確認TW軌job`1783`（`material_news_car_gate1_52tw`）已`finished`（exit=0，10.1分鐘），heavy-job-slot已空出（TW軌CAR gate N=200結果讀取與`register_trial()`登記屬下一個TW輪次工作範圍，本輪US軌不動）。**本輪工作單位＝依round464交辦，投遞`#51-US`全範圍回補（74頁）背景job**。腳本`backfill_sp500_index_changes.py`無需修改，直接以`--batch-size 74`投遞：`python research/run_detached.py submit --name sp500_index_changes_backfill_51us --timeout-min 60 -- python -u research/backfill_sp500_index_changes.py --batch-size 74`（省略`--expect`：產出為動態offset命名的多檔parquet，非單一固定路徑，改以檔案數量佐證進度，比照`run_detached.py`該參數本為選填），job`20260909-013149-9992`。session內`wait --max-min 4`逾時仍`running`（60分鐘timeout內屬正常），查`data/raw_sp_index_changes/`檔案數確認非卡死：4分鐘內列表頁4→12、文章48→121（新增8頁、73篇），持續正常前進。`git status`確認除`.live_watchlist.json`/`dev_queue_cycle.log`/`external_connectivity.jsonl`三個其他常駐服務殘留外無其他變更。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）確認PASS（exit=0，222列，本輪無新判定，純資料管線投遞非試驗）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零FinMind/SEC EDGAR歷史API呼叫，對`press.spglobal.com`發出約80+次請求（節流1.5秒/次，官方公開新聞稿頁面，無登入無驗證碼）。**下一輪US軌接手**：先查`run_detached.py status`確認job`20260909-013149-9992`是否已`finished`（預估60分鐘內，約02:30前後完成）——若已完成，讀取累積列表頁/文章數確認涵蓋約74頁全部年份範圍，即可進入`#51-US`第1關cheap gate（生效日±N日報酬 vs 隨機交易日null，比照`fut_settlement_event_gate60.py`/`cbc_decision_event_gate61.py`框架），不得跳關搶跑；若仍`running`未逾時則比照本輪模式單純查核進度（看檔案數成長）；若已`timeout`，腳本本身可斷點續傳（已快取頁/文章不重複抓），檢討是否60分鐘對74頁+潛在逾千篇文章仍不足，可加大`--timeout-min`重投而非從頭跑。完整見`REPORT.md`第466輪心跳、`HYPOTHESIS_QUEUE.md` #51-US條目第466輪新增段落。

---
**最後更新：2026-09-09T00:30+08:00（馬拉松第464輪）**——取鎖乾淨（cycle`20260909-003037`）。三軌時間戳：US 23:30（round462，最舊）／TW 00:00（round463，最新）——依輪替選US（FUT依例外條款不選）。`run_detached.py status`確認heavy-job-slot仍被TW軌job`95c8`（`material_news_backfill_52tw`）佔用中（running，已執行約37~41分鐘），本輪繼續不投遞新重度背景工作。`git status`乾淨（僅例行探針log：`.live_watchlist.json`/`dev_queue_cycle.log`/`external_connectivity.jsonl`）。**本輪工作單位＝依round462交辦，建立`backfill_sp500_index_changes.py`並端到端smoke test驗證**。新增回補腳本：逐頁掃描`offset=0,50,100,...`直到連續2頁真正回傳空為止（非寫死74頁這個數字，避免之後新聞稿增加就漏掃，另加`MAX_OFFSET=20000`安全網），已快取的列表頁/文章不佔批次額度也不觸發HTTP（比照`backfill_material_news.py`可續跑慣例）。**smoke test**（前景執行`--batch-size 3`，非`run_detached`背景job，因為只是驗證管線、預期數分鐘內結束，且非MOPS端點不與TW軌job搶算力）：3個新列表頁（offset 0/50/100，offset 0此前round462已快取直接沿用），符合關鍵字48篇（含round462已測試的3篇），新抓文章45篇，全數`fetch_and_parse_article()`成功落盤（無解析例外）。累積快取：列表頁4、文章48。**驗證通過，管線可行**，`data/raw_sp_index_changes/`（已gitignore不進repo）目前累積進度約4/74頁（offset停在150）。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）確認PASS（exit=0，222列，本輪無新判定，純資料管線建置非試驗）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零FinMind/SEC EDGAR歷史API呼叫，對`press.spglobal.com`發出約50+次請求（3個列表頁+45篇文章，皆為公開新聞稿頁面，無登入無驗證碼，同round460/462判定無公開速率上限文件，本輪節流1.5秒/次）。**下一輪US軌接手**：先查heavy-job-slot是否已空出（TW軌job`95c8`預估00:50前後完成）——若已空，用`run_detached.py submit --name sp500_index_changes_backfill_51us --timeout-min 60 --expect <產出摘要或用檔案數判斷> -- python -u research/backfill_sp500_index_changes.py --batch-size 74`投遞全範圍背景回補（74頁全跑，比照`backfill_material_news.py`同款用法，注意`run_detached.py`工作目錄固定在`alpha-app`非`research`，指令需帶`research/`前綴，這是round459踩過的路徑陷阱）；若仍被佔用則比照本輪模式繼續小批次前景累積或查核TW軌job進度。backfill完成後才進入`#51-US`第1關cheap gate（生效日±N日報酬 vs 隨機交易日null，比照`fut_settlement_event_gate60.py`/`cbc_decision_event_gate61.py`框架），不得跳關搶跑。完整見`HYPOTHESIS_QUEUE.md` #51-US條目第464輪新增段落、`REPORT.md`第464輪心跳、`backfill_sp500_index_changes.py`（新增，可重複執行）。

（第444/446/448輪已於round454歸檔至`US_STATE_ARCHIVE.md`；第450/452輪已於round458歸檔；第454輪已於round460歸檔；第456輪已於round462歸檔；第458輪已於round464歸檔；第460輪已於round466歸檔；第462輪已於round468歸檔，僅保留最新3則）
