

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的 68 則已原文搬到 `US_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-06T00:32+08:00（馬拉松第381輪）**——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：TW 22:00（第380輪，最新）／US 20:35（第376輪，次舊）／FUT 18:30（第372輪，最舊，但round372自建議近10輪佔配額20%上限、優先權回TW/US，跳過）——依例外規則選US。開工前確認`data/rate_limit_state.json`**已不存在**（round376記錄的鎖定到期時間22:38+08:00已過去1.8小時以上），FinMind額度應已恢復。**本輪工作單位＝接續round376「下一步」：`us_stratified_universe_sample.py`分層抽樣宇宙續抓**（冪等重跑，已抓到的72檔large tier命中本機parquet快取不重複耗額度）。用`run_detached.py submit`投遞（job_id`20260906-003203-4f80`，timeout 40分鐘），session內監看約3分鐘：**log顯示第80/300檔（round376卡住的第79檔之後）正常抓到，證實FinMind額度確實已恢復**，持續監看至第146/300檔（mid tier中段）皆無新的402錯誤。**本輪未等待job跑完便收工**（符合第0b節「重度工作不在session內等待超過5分鐘」規則），job以`breakaway=True`繼續在背景執行，預估依3秒/新請求速率、剩餘約154檔，約8分鐘可跑完（40分鐘timeout內足夠）。**下一輪接手**：`run_detached.py status`查`20260906-003203-4f80`是否`finished`，若是則讀`data/us_stratified_universe_sample.csv`統計最終可用檔數，用這份乾淨宇宙重跑`f_us_value_bm`（#128 FAIL）/`f_us_low_vol`（#115 FAIL）cheap gate＋1b深挖，驗證round374 leave-top10-out已REFUTED的集中度假說之外、換乾淨宇宙後原implausible VAL期報酬量級是否消失（這是round374/376已定案的下一步路徑，非新方向）。`is_holdout_consumed()`開工/收工前皆確認`False`。本輪session內零新增API呼叫（額度消耗全在背景job，非session直接呼叫）。完整見`US_LOG.md`第381輪記錄。

---

**上一則保留（第376輪，供對照）**——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：FUT 18:30（第372輪，最舊，但FUT近期已佔用超過20%配額且round372自建議優先權回TW/US，跳過）／US 19:30（第374輪，次舊）／TW 20:00（第375輪，最新）——依例外規則選US（次舊，跳過FUT）。**本輪工作單位＝round374「下一步(a)」：設計並啟動市值分層隨機抽樣的乾淨美股宇宙，取代`cached_ticker_ids()`熱門股偏誤池**（#128/#139記錄的universe層級假影，唯一被判定可信的修法）。新增`us_stratified_universe_sample.py`：用`us_universe.active_stock_ids()`既有`market_cap`欄位（單一`USStockInfo`快照呼叫即可覆蓋全部6,618檔活躍股，零額外API成本建立抽樣框架）依`pd.qcut(q=3)`切大/中/小三個等count分位（跟`us_factor_ic_by_size.py`既有tertile定義完全一致，5,566檔market_cap>0可分層），**各分位預先綁定不同seed（20260905_1/_2/_3）隨機抽100檔（3×100=300檔，比照TW`factor_ic.SAMPLE_SIZE=300`的統計力基準）**，逐檔呼叫`us_factors.us_price_series()`抓歷史價格建立快取（重用`us_factor_ic.py`既有402/429優雅停止邏輯）。**互動抽樣邏輯本輪已驗證正確**（300檔=100/tier×3，tertile切割5,566檔→1856/1855/1855）；**300檔的實際價格抓取已用`run_detached.py submit`投遞至背景**（job_id`20260905-203513-d82d`，timeout 45分鐘）。**收工前job已提前結束（3.5分鐘，非逾時）**：抓到第79/300檔（large tier的PYPL）時FinMind回傳HTTP 402（額度用盡），腳本依既有`QUOTA_ERROR_MARKERS`優雅停止（非崩潰），**寫出`data/us_stratified_universe_sample.csv`部分結果：large tier 72/100可用、mid/small tier各0/100（`drop_reason=not_attempted_quota_hit`，尚未嘗試，非因子不好）**。查`data/rate_limit_state.json`：FinMind被鎖到**2026-09-05T22:38+08:00**（約還剩2小時，`block_reason`=HTTP 402），**這是整個專案共用的FinMind額度，接下來約4輪（每輪30分鐘）TW/US任何需要FinMind新資料的工作都會立即被擋**，不是這個腳本專屬的限制——下一輪若選TW/US軌，開工前應先查`data/rate_limit_state.json`的`blocked_until`是否已過期，未過期就该改做不需要新FinMind資料的工作（純讀已快取parquet的分析、程式碼撰寫、文件整理）。**下一輪接手（US軌選中且額度已恢復時）**：直接重跑`python research/us_stratified_universe_sample.py`（冪等，large tier已抓到的72檔會命中本機parquet快取不重複耗額度，只會續抓剩餘28檔large+全部mid/small），跑完再統計可用檔數，取代`cached_ticker_ids()`重跑`f_us_value_bm`/`f_us_low_vol`cheap gate+1b深挖。**明確保留（誠實揭露，不誇大這一步的完成度）**：目前只有large tier部分完成（72/300），mid/small完全還沒抓；這份新宇宙只修正「熱門股優先被快取」的選樣偏誤，`us_universe.py`既有、已記錄的存活者偏差缺口（僅5檔手動查證下市股）完全沒有被這份工作觸及，兩者是分開的、都已記錄的限制，不能混為一談。`is_holdout_consumed()`開工/收工前皆確認`False`。完整見`US_LOG.md`第376輪記錄、`us_stratified_universe_sample.py`（新增，可重複執行）。

**上一則保留（第374輪，供對照）**：2026-09-05T19:30+08:00（馬拉松第374輪）——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：US 10:34（第366輪，最舊）／FUT 18:30（第372輪）／TW 19:00（第373輪，最新）——依輪替選US。**本輪工作單位＝收成round366投遞的脫離session背景工作**：`deep_dive_f_us_value_bm_leave_extreme_out.py`（PID 1910）已在本輪開工前正常跑完並產出`data/deep_dive_f_us_value_bm_leave_extreme_out.csv`。**結果：135檔全樣本VAL期重現ann_return+121.38%/beta+0.003（與round360記錄完全吻合，複現無誤）；排除own-return前10名極端贏家後（125檔）：ann_return反而升至+148.17%、beta-0.027、percentile仍100.0**。事前綁定判準（腳本docstring）：<+30%=集中度假說CONFIRMED、>+60%=REFUTED、中間=PARTIAL——實測+148.17%遠超+60%門檻，**判定REFUTED（集中度假說被否證，非CONFIRMED）**。**這比round360原先猜測的「少數熱門股主導」更嚴重**：拿掉top10後報酬不降反升，代表`cached_ticker_ids()`池子的異常量級分佈廣泛在中段贏家，不是集中在少數幾檔名字——未來要挽救這個池子上的因子測試，**目標式排除幾檔股票不是解方，必須換一個非「熱門股優先被快取」的隨機/分層抽樣宇宙重建**才能重新信任這個池子上的VAL期報酬量級。`f_us_value_bm`本身的FAIL判定（#128/#17）不受本查核影響，不重啟。同一結論回頭適用於`f_us_low_vol`（#15，同一池子、近乎相同VAL期量級）——若之後要重測`f_us_low_vol`，不必再做一次leave-one-out（本查核已示範這條路對value factor不管用，沒理由對low_vol會不同），應直接考慮換乾淨宇宙。已寫入`TRIALS_LEDGER.md`#139、`US_LEADS.md`#18。`is_holdout_consumed()`開工/收工前皆確認`False`。零本輪新增API呼叫（純讀取round366已產出的CSV）。**（後續：round376已依此設計乾淨分層抽樣宇宙，抓取途中撞FinMind額度上限（約2小時），large tier部分完成72/100、mid/small尚未開始，見上方最新條目。）**

# US_MARATHON_STATE.md — 美股軌斷點狀態（覆寫式）

**這份檔案只描述美股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `US_LOG.md`；候選判定看 `US_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。
