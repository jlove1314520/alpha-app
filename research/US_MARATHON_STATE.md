

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的 67 則已原文搬到 `US_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-05T10:34+08:00（馬拉松第366輪）**——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：US 08:31（第363輪，最舊）／FUT 09:02（第364輪）／TW 10:05（第365輪，最新）——依輪替選US。**本輪工作單位＝依round363下一步(a)+(c)：重跑`deep_dive_f_us_value_bm_leave_extreme_out.py`，這次改用`python -u`（unbuffered）＋開工先確認無其他重度背景process佔用（`ps -W`僅見無關系統python process，環境乾淨）**。**關鍵結果：載入226檔SEC company-facts純本地JSON解析這次僅耗時約2分鐘內完成**（vs round363同一腳本buffered執行20分鐘內連loading都跑不完）——**強烈支持round357/360對『疑似掛起其實是Python預設block-buffered stdout、非真的效能退化』的假說**，而非round363自己猜測的「226檔載入本身逼近30分鐘預算」。own-return排名成功印出（top10極端贏家：ADMA/VST/LNTH/SFM/EP/TLN/SKT/APO/GVA/AGX，與round360named suspects重疊`{TLN, VST}`2/7），process接著進入`run_one_value()`的VAL期100次隨機控制組回測階段（此階段無逐次進度輸出，同round357對`deep_dive_f_us_value_bm.py`本體的既有記錄，非bug）。**因本session預算提前吃緊，本輪未能等到(a)/(b)兩組回測數字跑完便需收工**——比照round350先例，**process留在背景繼續跑，未殺掉**（PID 1910，log `round366_deep_dive_f_us_value_bm_leave_extreme_out.log`）。下一輪接手：先查`data/deep_dive_f_us_value_bm_leave_extreme_out.csv`是否已產出，有就依腳本docstring預先綁定的判準（(b)排除top10後ann_return<+30%＝集中度假說CONFIRMED／>+60%＝REFUTED／中間＝PARTIAL）做最終判定並補`TRIALS_LEDGER.md`#128備註＋`US_LEADS.md`#17；若process已消失但無CSV，腳本零新增API呼叫可安全重跑，且已知載入階段本身不再是瓶頸（本輪已證實~2分鐘可完成）。`is_holdout_consumed()`開工/收工前皆確認`False`。零新增API呼叫（純本地SEC快取JSON解析＋既有價格快取，backtest不打外部API）。完整見`US_LOG.md`第366輪記錄。

**上一則保留（第363輪，供對照）**：2026-09-05T08:31+08:00（馬拉松第363輪）——取鎖時偵測到`LOCK_STALE`（round360的pid 44236持有30.2分鐘後被回收，上一輪疑似失敗，`git log`已確認round360工作本身成功commit+push`6406cc5`，疑似只是收工序release lock前process卡住，同round361 FUT軌pid58488先例）。**本輪工作單位＝延續round360「下一步(c)」建議，對`f_us_value_bm`135檔樣本做leave-one-out集中度檢查（輕量版），驗證universe選樣假影假說**：新增`deep_dive_f_us_value_bm_leave_extreme_out.py`——預先綁定（hash-lock）判準：排除VAL期(2020-2024)自身買進持有報酬前10名「極端贏家」（固定筆數、執行前決定，不依觀察到的報酬分布回頭挑門檻）後重跑同套decile long-short回測，<+30%=確認集中度假說成立、>+60%=否證（偏誤更廣泛分布非少數個股）、中間=部分成立，判準寫死於程式碼docstring。**執行結果：未完成，非失敗**——開工執行後發現`load_value_sample()`226檔（135可用+91drop）SEC company-facts純本地JSON解析（零新增API呼叫，先前所有輪次的紀錄都確認這是快取命中）耗時遠超預期，背景輸出檔案20分鐘內只顯示到loading loop本身（[226/226] ZSQR: OK，loop剛好跑完），後續的own-return排名/回測結果完全沒印出就因為逼近鎖檔25分鐘陳舊門檻被迫`taskkill`中止該process（pid 60876），未產出`data/deep_dive_f_us_value_bm_leave_extreme_out.csv`。**這是本輪最主要發現**：`cached_ticker_ids()`+`load_value_sample()`這條路徑即使純讀本地快取，226檔的實際載入時間本身就可能逼近甚至超過單輪30分鐘預算的一大部分，比round357/360觀察到的「載入約4~6分鐘」慢很多（可能跟本輪同時有其他背景負載、或JSON檔案大小差異有關，未查證）——**下一輪待辦**：(a)直接重跑`python deep_dive_f_us_value_bm_leave_extreme_out.py`（程式碼邏輯本身完整、不用改，只是這輪沒能跑完）；(b)或改用round350`f_us_value_bm`原深挖的背景process跨輪次模式（啟動後不等待、下一輪才回來讀CSV），避免單輪時間預算被載入階段吃光；(c)先查證是否本機同時有其他負載拖慢這次執行，避免下次重蹈覆轍。`is_holdout_consumed()`開工/收工前皆確認`False`。零新增API呼叫（純本地快取讀取，即使慢也未觸發任何外部請求）。完整見`US_LOG.md`第363輪記錄。

**上一則保留（第360輪，供對照）**：2026-09-05T06:33+08:00（馬拉松第360輪）——取鎖時偵測到`LOCK_STALE`（TW round359的pid 50060持有30.0分鐘後被回收，上一輪疑似失敗，根因未查明）。三軌時間戳：TW 06:03（第359輪，最新）／FUT 05:33（第358輪）／US 05:13（第357輪，最舊）——依輪替選US。**本輪工作單位＝判讀round357留下的`f_us_value_bm`完整1b深挖背景process結果**：開工確認`data/deep_dive_f_us_value_bm.csv`已於06:20完成產出（pid 1905已正常結束，非異常消失）。TRAIN(2015-2020) ann_return+16.05%~16.43%、beta+0.506~0.507、alpha+21.77%~22.17%、percentile 100.0；VAL(2020-2024) ann_return**+120.63%~121.38%**（總報酬+2287%）、beta**+0.003~0.004**（近乎market-neutral）、alpha+130.24%~131.83%、percentile 100.0。表面上比`f_us_low_vol`家族「更乾淨地通過」（TRAIN/VAL皆100.0、同號、beta合理），**但VAL期報酬量級遠超value溢酬文獻量級（HML長期年化約3~5%），且跟`US_LEADS.md`#15（`f_us_low_vol`不分層版，同一批`cached_ticker_ids()`超集、同一VAL窗口，VAL ann_return+111~113%）幾乎同一量級**——兩個完全不相關因子在高度重疊的樣本池上得出近似極端報酬，判定為`cached_ticker_ids()`池子本身的選樣偏誤（log可見PLTR/SOFI/NTLA/VST/STEM/SANA/TLN等多檔2021-2024巨幅漲勢股，疑似熱門股優先被快取，非隨機/存活者偏差已處理的宇宙），少數極端贏家主導任一十分位切割報酬，判定**FAIL**。**這是US軌迄今第一次發現universe層級的系統性假影，會同時影響多個不相關因子的深挖判定**——**⚠️ 給未來所有在`cached_ticker_ids()`池子上做1b深挖的輪次：VAL期（2020-12-31~2024-12-31）任何因子若出現年化報酬>50%等級的「表面PASS」，先比對是否跟`f_us_low_vol`(#15)/`f_us_value_bm`(#17)同一量級，不要直接當成該因子的真實edge，優先做leave-one-out集中度檢查或換非熱門股偏誤宇宙重測。** 已寫入`TRIALS_LEDGER.md`#128、`US_LEADS.md`#17。`is_holdout_consumed()`開工/收工前皆確認`False`。零本輪新增API呼叫（純讀取既有CSV）。下一輪建議：(a)對135檔做leave-one-out集中度檢查驗證假影假說（比照`STRATEGY_GRAVEYARD.md`銅金比overlay#126）；(b)或換市值分層隨機抽樣宇宙重跑；(c)或轉向PIT財報其他基本面因子家族（品質/成長）。完整見`US_LOG.md`第360輪記錄。2026-09-05T05:13+08:00（馬拉松第357輪）——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：TW 04:32（第356輪，最新）／FUT 04:04（第355輪）／US 03:42（第354輪，最舊）——依輪替選US。**本輪工作單位＝驗證round354留下的「跨process資源競爭」假說**：開工先`ps -ef | grep python`確認環境乾淨（無其他軌背景process在跑，TW round356留下的`deep_dive_loo_no_low_vol_independent_sample.py`process此時已不在process list裡，本輪未去動它，只是確認不佔用CPU）。乾淨環境下重跑`deep_dive_f_us_value_bm.py`（第三次執行）：先用預設（buffered）stdout背景執行，等待約6分鐘後log仍停在資料載入尾端（WHLR附近）沒有任何新增輸出——**懷疑是Python對檔案輸出預設block-buffered、而非真的卡住**，kill掉改用`python -u`（強制unbuffered）重跑第四次驗證：確認資料載入226檔（135可用）約4分鐘內完整跑完並即時印出（含`===TRAIN 2015-01-01..2020-12-31, cost 1x===`），之後進入`run_one_value()`的100次隨機控制組迴圈——這個階段腳本設計上本來就沒有逐次進度輸出（不是bug），等待期間process持續存活（`ps -ef`確認pid 1905在跑）。**結論：round354的假設(b)成立——不是掛起，是工作量真的大**（135檔×6組period/cost×100次疊代=600次完整回測，光資料載入就要4分鐘，運算階段更久）；同時「每次執行卡在不同位置」這個現象有了更完整的解釋——不只是round354猜測的跨process資源競爭，buffered stdout恰好在不同進度點被flush也會造成「卡在哪」看起來每次不同的錯覺，兩個因素可能同時存在、不互斥。process已留在背景繼續跑（pid 1905，`deep_dive_f_us_value_bm_run4_unbuffered.log`），未殺掉。**下一輪工作單位建議**：開工先查`data/deep_dive_f_us_value_bm.csv`是否已產出，若有直接讀取六組period/cost結果做1b判定寫進`US_LEADS.md`/`TRIALS_LEDGER.md`；若process仍在跑，可用該log（已是unbuffered）即時追蹤真實進度，不必再猜buffering問題；若process已消失但無CSV，才需要真的懷疑crash，屆時建議把硬超時拉長（例如1800秒）並保留`-u`旗標重跑。`is_holdout_consumed()`確認`False`（開工/收工前皆確認）。零新增API呼叫（全部命中既有快取，跟round347/350/351/354一致，backtest本身不打外部API）。完整見`US_LOG.md`第357輪記錄、`deep_dive_f_us_value_bm_run3.log`／`deep_dive_f_us_value_bm_run4_unbuffered.log`（新增）。

# US_MARATHON_STATE.md — 美股軌斷點狀態（覆寫式）

**這份檔案只描述美股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `US_LOG.md`；候選判定看 `US_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。
