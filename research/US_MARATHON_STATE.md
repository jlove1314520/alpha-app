

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `US_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-08T04:30+08:00（馬拉松第429輪）**——取鎖乾淨（cycle`20260908-043037`）。三軌時間戳：FUT 09-06 12:00（round399，最舊，依例外條款不選，FUT_MARATHON_STATE.md明列「除非有全新機制假說優先權重排回TW/US」）／US 09-07 05:30（round427）／TW 09-08 04:00（round428，最新）——依輪替本應選US（次舊），FUT依例外條款跳過。`run_detached.py status`確認heavy-job-slot空（0個running）。**本輪工作單位＝round427「下一輪US軌接手」明列待辦：收成job`20260907-054341-a450`（`deep_dive_f_us_gross_profitability`）並判讀是否重蹈`#20`/`#21`的`adj_close`資料完整性陷阱**。收成結果：VAL 1x ann_return=+33.77%、beta=-0.084、alpha=+44.65%、random_control_percentile=100.0——量級跟`#20`/`#21`當初的異常同一種形狀（大幅正alpha＋接近零/負beta＋100.0百分位），但深挖腳本本身只檢查了污染ticker是否在樣本內（5/84），沒有做leg分解，不足以下判斷。新增`deep_dive_f_us_gross_profitability_contamination_check.py`（逐字沿用`deep_dive_f_us_low_vol_leave_extreme_out.py`的leave-out+事前綁定判準方法論：CONFIRMED＝排除後retained_fraction<30%或變號，REFUTED＝同號且retained>=60%，PARTIAL＝介於兩者之間；本次以5檔已知污染ticker`AMTX`/`CIIT`/`DVLT`/`MNTS`/`WULF`為排除對象，而非#21原版的top-N-by-return排除法），零新增API呼叫（複用`load_quality_sample()`既有快取，原地執行約1分鐘，未投遞`run_detached.py`）。**結果：VAL期51次換倉走查，5檔污染ticker合計79次腿位分配中76次（96%）落在空頭腿、僅3次落在多頭腿；排除這5檔後VAL 1x ann_return由+33.77%驟降至+1.53%（retained_fraction=0.05，同號）**，觸發CONFIRMED門檻。**判定：`f_us_gross_profitability`的CHEAP_PASS（#191）降級為FAIL**——跟`#20`/`#21`是同一個資料完整性陷阱（死亡螺旋型反向分割微型股的`adj_close`回溯膨脹歷史名目價格），不是真正的獲利能力異常，排除出`portfolio_multifactor_v2`（US版）成分候選池。已登記`TRIALS_LEDGER.md`#193（`register_trial()`）、`US_LEADS.md`#23更新（新增round427/round429兩段更新）。`trial_registry.py --check`確認PASS（195列，下一可用編號#194）。`is_holdout_consumed()`開工/收工前皆確認`False`，全程零新增API呼叫。**US軌現況盤點**：至今US軌測過的三個PIT基本面/量價因子（`#20`value_bm、`#21`low_vol、本輪gross_profitability）全數FAIL，且成因高度收斂到同一個根因——這批乾淨分層抽樣宇宙（`us_stratified_universe_sample.csv`）裡有一小撮死亡螺旋型反向分割微型股，任何把它們排進空頭腿的排序規則都會產生虛假的巨額「空頭獲利」。**下一輪US軌接手**：(1) 優先評估是否該從宇宙層級根治——直接把這批已知污染ticker（`WATT`/`AMTX`/`MNTS`/`DVLT`/`WULF`/`CIIT`/`PALI`/`LEE`，`#20`round410以來累積列名）永久排除出`us_stratified_universe_sample.csv`（或加一個「reverse-split-adjusted history」黑名單過濾器），重新驗證`#20`/`#21`/本輪因子排除污染後是否還有殘餘訊號，這比每個新因子各自撞一次同一個陷阱更有效率；(2) 若不採(1)，需要尋找一個完全不依賴`adj_close`十分位排序的新因子構造（例如僅做多、不做空頭腿的Top-N構造，`#22`已測過FAIL但只測了value_bm+low_vol combo，尚未測gross_profitability單獨的Top-N版本）。完整見`US_LEADS.md`#23本輪更新、`TRIALS_LEDGER.md`#193、`deep_dive_f_us_gross_profitability_contamination_check.py`（新增，可重複執行）、`data/deep_dive_f_us_gross_profitability_contamination_check.csv`（新增）。

---

**上一則保留（第427輪，供對照）**——取鎖乾淨（cycle`20260907-053036`）。三軌時間戳：US 04:30（round425，最舊）／TW 05:00（round426）／FUT 09-06 12:00（round399，依例外條款不選，除非有全新機制假說）——依輪替選US。`run_detached.py status`確認heavy-job-slot空（0個running），無背景工作衝突；`git status`確認`.github/workflows/audit.yml`/`research/.devqueue.lock`/`research/.live_watchlist.json`/`research/DEV_QUEUE_PROMPT.txt`/`research/cb_conversion_price_reset_gate1.py`/`research/data_cache/`/`research/dev_queue_cycle.log`為另一條獨立自動化排程（hypothesis_queue，處理`#51`子事件3 CB轉換價重設）正在編輯的檔案，本輪未觸碰，保留原狀。**本輪工作單位＝round425「下一輪US軌接手」明列的既定待辦：尋找全新的因子/組合機制候選**——`#20`/`#21`（value_bm/low_vol）已窮盡9輪診斷確認FAIL成因是`adj_close`資料完整性問題（非策略/機制問題），round425明確指出US軌需要一個「不重蹈同一條路」的新假說。**本輪新增`f_us_gross_profitability`（Novy-Marx 2013 GrossProfit/Assets）**：跟`#20`(價值)/`#21`(低波動)是完全不同的獲利能力機制，非`MARATHON_PROTOCOL.md`0a節定義下的換皮（不同經濟敘事，非同一因子換宇宙/窗口）。新增`us_factors_quality.py`（`gross_profitability_pit()`/`add_quality_factor()`，刻意只用10-K年度數字避免損益表流量項目季/年混用假影）+`us_factor_ic_quality_clean_universe.py`（沿用`#20`/`#21`同一份248檔乾淨分層抽樣宇宙），**因為`GrossProfit`/`Assets`都是`#20`已下載的SEC companyfacts JSON裡的既有欄位，零新增API呼叫**。**結果：84/248可用（154檔無標籤/9檔全NaN/1檔無CIK），TRAIN mean_ic=+0.0503 IR=+0.350（n=76）、VAL mean_ic=+0.0499 IR=+0.291 hit_rate=0.63（n=49），train/val不只同號、量級幾乎相同（跟#20/#21train顯著弱於val的形狀不同），null percentile=100.0（門檻90.0）**。**判定：CHEAP_PASS**（**round429更新：深挖後CONFIRMED為#20/#21同款污染陷阱，降級為FAIL，見上方最新條目**）。已登記`TRIALS_LEDGER.md`#191（`register_trial()`，過程中發現一個純顯示層問題：Bash終端機的Windows主控台codepage無法正確顯示中文，一度誤判成檔案內容被寫壞，改用Read工具核對後確認`TRIALS_LEDGER.md`/`TRIALS_REGISTRY.jsonl`實際寫入內容都是正確UTF-8，只是bash終端顯示是亂碼，非資料損毀，未執行任何救援動作只是虛驚一場的診斷）、`US_LEADS.md`#23新增。**誠實揭露已知污染風險**：`#20`round410列名的死亡螺旋型反向分割微型股`MNTS`/`DVLT`/`AMTX`/`WULF`/`CIIT`共5檔也落在這84檔可用樣本中，深挖階段可能重蹈`#20`/`#21`的資料完整性陷阱，尚未證明這是一個乾淨的候選。已投遞`deep_dive_f_us_gross_profitability.py`（`run_detached.py` job`20260907-054341-a450`，timeout 150分鐘，`run_detached.py wait --max-min 3`確認正常執行中，本輪未等待收成）。`is_holdout_consumed()`開工/收工前皆確認`False`，全程零新增SEC EDGAR API呼叫、零FinMind呼叫。`trial_registry.py --check`確認PASS（193列，最大編號#191，下一可用#192，撞號2組為既有歷史存量非本輪新增）。完整見`US_LEADS.md`#23（新增）、`TRIALS_LEDGER.md`#191、`us_factors_quality.py`/`us_factor_ic_quality_clean_universe.py`/`deep_dive_f_us_gross_profitability.py`（三支新增，皆可重複執行）。

---

**上一則保留（第425輪，供對照）**——取鎖乾淨（非陳舊鎖檔，cycle`20260907-043037`）。三軌時間戳：US 03:36（round423，最舊）／TW 04:00（round424）／FUT 12:00（09-06，round399，依例外條款不選，除非有全新機制假說）——依輪替選US。`run_detached.py status`確認heavy-job-slot空（0個running），無背景工作衝突；`git status`確認`.github/workflows/audit.yml`/`research/.live_watchlist.json`/`research/DEV_QUEUE_PROMPT.txt`/`research/data_cache/`/`research/dev_queue_cycle.log`為其他既存未commit異動，本輪未觸碰，保留原狀。**本輪工作單位＝round423「下一輪US軌接手」明列選項(2)：`#20`/`#21`誠實記錄「異常成因收斂到主流免費資料源共通限制」並降級處理**——Alpha Vantage註冊（選項(1)）需要使用者同意才能用其email申請key，無人值守馬拉松輪次不得代為決定，故不採(1)；判斷繼續尋找US軌新因子候選（選項(3)）前，應先把這條已窮盡9輪、6個候選解釋全數REFUTED的線正式結案，避免掛著EXPERIMENTAL卻遲遲不下判斷。**本輪純文件/記錄整理，零新增API呼叫、零新程式碼、零回測**：更新`US_LEADS.md`#20/#21判定欄位（`CHEAP_PASS →EXPERIMENTAL→**FAIL（降級關閉）**`），並在兩列notes末尾各補一段round425關閉說明，交叉引用彼此避免重複贅述；新增`STRATEGY_GRAVEYARD.md`「`f_us_value_bm`／`f_us_low_vol`乾淨宇宙版本」條目，把round382-423共9輪、6類候選解釋（單一年份/持股集中度/宇宙離散度/long-short腿拆解/成本模型/換手率/threshold list/免key替代來源）依時序整併成一份完整死因記錄，並明列`#22`（Top-N長多組合繞開短腿）也在組合層級FAIL，避免下一位無記憶的執行個體重新踏入同一條路徑。**判定：`#20`/`#21`正式從EXPERIMENTAL降級關閉為FAIL（資料完整性問題——短腿報酬無法驗證為真實可執行，非策略或經濟機制問題），排除出`portfolio_multifactor_v2`（US版）成分候選池**。若使用者未來主動核准Alpha Vantage註冊或其他合法raw報價來源，可重新開啟；在那之前不再消耗馬拉松輪次深挖這條線。`trial_registry.py --check`確認本輪未新增判定（此為既有判定的降級收斂，非新試驗），維持190筆不變。`is_holdout_consumed()`開工/收工前皆確認`False`，全程零新增API呼叫。完整見`US_LEADS.md`#20/#21本輪更新、`STRATEGY_GRAVEYARD.md`新增條目。
