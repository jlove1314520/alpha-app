

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `US_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-07T04:30+08:00（馬拉松第425輪）**——取鎖乾淨（非陳舊鎖檔，cycle`20260907-043037`）。三軌時間戳：US 03:36（round423，最舊）／TW 04:00（round424）／FUT 12:00（09-06，round399，依例外條款不選，除非有全新機制假說）——依輪替選US。`run_detached.py status`確認heavy-job-slot空（0個running），無背景工作衝突；`git status`確認`.github/workflows/audit.yml`/`research/.live_watchlist.json`/`research/DEV_QUEUE_PROMPT.txt`/`research/data_cache/`/`research/dev_queue_cycle.log`為其他既存未commit異動，本輪未觸碰，保留原狀。**本輪工作單位＝round423「下一輪US軌接手」明列選項(2)：`#20`/`#21`誠實記錄「異常成因收斂到主流免費資料源共通限制」並降級處理**——Alpha Vantage註冊（選項(1)）需要使用者同意才能用其email申請key，無人值守馬拉松輪次不得代為決定，故不採(1)；判斷繼續尋找US軌新因子候選（選項(3)）前，應先把這條已窮盡9輪、6個候選解釋全數REFUTED的線正式結案，避免掛著EXPERIMENTAL卻遲遲不下判斷。**本輪純文件/記錄整理，零新增API呼叫、零新程式碼、零回測**：更新`US_LEADS.md`#20/#21判定欄位（`CHEAP_PASS →EXPERIMENTAL→**FAIL（降級關閉）**`），並在兩列notes末尾各補一段round425關閉說明，交叉引用彼此避免重複贅述；新增`STRATEGY_GRAVEYARD.md`「`f_us_value_bm`／`f_us_low_vol`乾淨宇宙版本」條目，把round382-423共9輪、6類候選解釋（單一年份/持股集中度/宇宙離散度/long-short腿拆解/成本模型/換手率/threshold list/免key替代來源）依時序整併成一份完整死因記錄，並明列`#22`（Top-N長多組合繞開短腿）也在組合層級FAIL，避免下一位無記憶的執行個體重新踏入同一條路徑。**判定：`#20`/`#21`正式從EXPERIMENTAL降級關閉為FAIL（資料完整性問題——短腿報酬無法驗證為真實可執行，非策略或經濟機制問題），排除出`portfolio_multifactor_v2`（US版）成分候選池**。若使用者未來主動核准Alpha Vantage註冊或其他合法raw報價來源，可重新開啟；在那之前不再消耗馬拉松輪次深挖這條線。`trial_registry.py --check`確認本輪未新增判定（此為既有判定的降級收斂，非新試驗），維持190筆不變。`is_holdout_consumed()`開工/收工前皆確認`False`，全程零新增API呼叫。**下一輪US軌接手**：US軌至今累計32+筆試驗、0筆通過完整GATE_SEQUENCE的候選（`#20`/`#21`已關閉、`#22`FAIL），需要尋找全新的因子/組合機制候選；可參考`MARATHON_PROTOCOL.md`0a節精神——US軌目前不在四條「結構性優勢」新方向名單內（那四條是TW專屬的容量/強制交易者/事件反應/隔夜拆解），US軌若要延續「多因子組合策略」主軸，建議先盤點是否有其他免費美股資料源可提供的PIT財報欄位尚未試過的因子構造，或考慮US軌是否也適用類似TW的結構性優勢盤點（例如美股逐筆tick取得成本遠高於台股，容量受限小型股方向對US軌可行性存疑，需先評估資料可得性再提案）。完整見`US_LEADS.md`#20/#21本輪更新、`STRATEGY_GRAVEYARD.md`新增條目。

---

**上一則保留（第423輪，供對照）**——取鎖乾淨（非陳舊鎖檔，cycle`20260907-033036`）。三軌時間戳：US 02:30（round421，最舊）／TW 03:00（round422）／FUT 12:00（round399，依例外條款不選，除非有全新機制假說）——依輪替選US。`run_detached.py status`確認heavy-job-slot空（0個running），無背景工作衝突；`git status`確認`data/rate_limit_state.json`/`.github/workflows/audit.yml`/`research/.devqueue.lock`/`research/.live_watchlist.json`/`research/DEV_QUEUE_PROMPT.txt`/`research/REGISTRATION_COVERAGE.md`/`research/TRIALS_REGISTRY.jsonl`/`research/cash_increase_dilution_gate1.py`/`research/data_cache/`/`research/dev_queue_cycle.log`/`research/registration_coverage_audit.py`/`research/trial_registry.py`為另一條獨立自動化排程（dev_queue／hypothesis_queue，處理`#51`子事件2）正在編輯的檔案，本輪未觸碰，保留原狀。**本輪工作單位＝round421「下一輪US軌接手」明列的既定待辦：查是否有除FinMind外的免費來源能取得`f_us_value_bm`(#20)/`f_us_low_vol`(#21)常駐短腿ticker（`WATT`/`AMTX`/`MNTS`/`DVLT`/`WULF`/`CIIT`/`PALI`）的真實未還原歷史收盤價**，非新假說，是round412/round421四個「執行不可行」診斷鏈收斂後的下一步。新增`us_alt_source_raw_price_probe.py`：查證三個候選來源——(1) yfinance（Yahoo Finance官方公開chart API，免token免登入）：用`DVLT`實測`auto_adjust=False, back_adjust=False`，2020-12-31 Close=53100.0與FinMind adj_close完全逐位元相同，`splits()`確認2020/2023/2024三次反向分割，證實Yahoo的Close本身就是split back-adjusted（含未來反向分割回溯），並非raw報價——WebSearch查證官方`help.yahoo.com/kb/SLN28256.html`與Bogleheads論壇討論佐證這是Yahoo資料本身的既定行為，非FinMind獨有限制；(2) stooq.com：curl直接請求觸發JS proof-of-work反機器人驗證頁面（SHA-256挑戰+`/__verify`回呼），非單純公開CSV，依`CLAUDE.md`取得方式鐵律不得繞過，判定不可用（未嘗試繞過）；(3) Alpha Vantage `TIME_SERIES_DAILY`：WebSearch查證官方文件字面寫「raw (as-traded)」報價，與`TIME_SERIES_DAILY_ADJUSTED`分開提供，理論上是候選，但需要免費API key，本專案目前查無已存在的key，本輪未實測。**結果：「免key免登入的替代來源可取得raw報價」這個最寬版本假說REFUTED**（yfinance證實同樣back-adjusted、stooq不可用），Alpha Vantage列為**未驗證候選**（需API key才能實測，非本輪REFUTED範圍）。已登記`TRIALS_LEDGER.md`#188（`trial_registry.register_trial()`，verdict=REFUTED，範圍已明確限定在「免key免登入」這個子集）、`US_LEADS.md`#20/#21更新。`#20`/`#21`判定維持EXPERIMENTAL不變，且至此確認`adj_close`回溯膨脹歷史名目價格是Yahoo Finance/FinMind兩個獨立資料源**共通**的限制，不是單一廠商的資料品質問題。`is_holdout_consumed()`開工/收工前皆確認`False`，全程零新增付費/需登入API呼叫（yfinance 1次輕量請求+2次WebSearch，無實際下載資料）。**下一輪US軌接手**：(1) 若使用者同意，可註冊Alpha Vantage免費API key實測`TIME_SERIES_DAILY`是否真為raw報價，直接驗證異常量級是否消失（需先問使用者是否要用其email註冊，本輪未自行註冊）；(2) 若不採(1)，`#20`/`#21`應誠實記錄「異常成因收斂到主流免費資料源的共通限制，短期無法用免費管道進一步驗證」並考慮降級處理或排除出`portfolio_multifactor_v2`成分候選；(3) 或改尋找US軌新的因子候選，不再繼續深挖`#20`/`#21`這條已窮盡9輪診斷的線。完整見`TRIALS_LEDGER.md`#188、`US_LEADS.md`#20/#21、`us_alt_source_raw_price_probe.py`（新增，可重複執行）。

---

**上一則保留（第421輪，供對照）**——取鎖乾淨（非陳舊鎖檔，cycle`20260907-023036`）。三軌時間戳：US 00:30（round418，最舊）／TW 02:04（round420）／FUT 12:00（round399，依例外條款不選，除非有全新機制假說）——依輪替選US。`run_detached.py status`確認heavy-job-slot空（0個running），無背景工作衝突；`git status`確認無其他行程正在編輯同一批檔案。**本輪工作單位＝round418「下一步」明列的檢定：查證`f_us_value_bm`(#20)/`f_us_low_vol`(#21)常駐短腿ticker在FINRA/Nasdaq Reg SHO threshold securities list的歷史記錄**（round410列名的死亡螺旋型微型股常客`WATT`/`AMTX`/`MNTS`/`DVLT`/`WULF`/`CIIT`/`PALI`；`LEE`為NYSE掛牌，本探針資料源不涵蓋，另記錄未查證），非新假說，是round418建議的既定快殺檢定。新增`finra_threshold_probe.py`：官方免費匿名FTP（`ftp://ftp.nasdaqtrader.com/SymbolDirectory/regsho/nasdaqth{YYYYMMDD}.txt`，2026-09-07手動curl測試確認可用且可回溯歷史日期，無需登入無需token）月頻抽樣VAL期(2020-12~2024-12，49個樣本點)。**結果：7檔ticker在49個樣本點全數0命中（0/49，0.0%）**，即月頻抽樣完全沒有偵測到任何一檔曾出現在該清單上。**判定：round418「長期掛在threshold list代表借不到券、應判FAIL」這個快殺假說REFUTED**——但誠實揭露限制：月頻抽樣（49點/約1000個交易日）只能捕捉「長期持續」的threshold狀態，抓不到短暫（數天到數週）的FTD事件，0/49不構成「這些股票容易借到券」的正面證據，只排除了「長期持續掛在監理FTD清單」這個最強版本的結構性不可能理由。至此round410(a)(b)兩候選機制＋round412 price floor篩選＋round421 threshold list共四個「執行不可行/成本被低估」假說全部被REFUTED或未獲支持，**唯一有直接證據支持、尚未被推翻的解釋收斂到round412已發現的`adj_close`回溯膨脹歷史名目價格本身這個資料完整性問題**。判定`#20`/`#21`**維持EXPERIMENTAL不變**（本輪是排除法，不足以單獨翻案為FAIL或PASS）。已寫入`TRIALS_LEDGER.md`#185、`US_LEADS.md`#20/#21更新。`is_holdout_consumed()`開工/收工前皆確認`False`，全程零新增付費/需登入API呼叫（純官方免費匿名FTP，49次輕量請求）。**下一輪US軌接手**：不建議再測「這個異常是否可被執行限制解釋」這條線（四個假說已窮盡皆REFUTED/未獲支持）；改查是否有除FinMind外的免費資料源能提供這批常駐短腿ticker（僅約10檔規模，非全宇宙）的真實未還原歷史收盤價（例如stooq.com等），若能取得可直接用真實原始價格重跑VAL回測，直接驗證異常量級是否消失，這會是比繼續排除執行限制假說更直接的檢定；若查無替代來源，`#20`/`#21`可能需要誠實記錄「異常成因收斂到資料源本身限制，短期無法進一步驗證」並考慮降級處理。完整見`TRIALS_LEDGER.md`#185、`US_LEADS.md`#20/#21、`finra_threshold_probe.py`（新增，可重複執行）、`data/raw_finra_threshold/`（新增快取）。
