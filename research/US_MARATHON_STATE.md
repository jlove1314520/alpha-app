

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `US_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

---
**最後更新：2026-09-08T10:00+08:00（馬拉松第438輪）**——取鎖乾淨（cycle`20260908-100037`）。三軌時間戳：TW 09:30（round437，最新）／US 09:00（round436）——依輪替選US（FUT依例外條款不選）。`run_detached.py status`確認heavy-job-slot佔用中（`20260908-095451-4ffe`『tw_backfill_day_trading_detail』running，由`hypothesis_queue`排程自走投遞、非本track所為，該job承接round437發現的`--cwd`修正，本輪觀察到它進度偏慢：10:03時僅50/250、10:07才100/250，比過去幾批『50/250約6分鐘』的正常步調略慢但方向相同，尚未到異常判準）。**本輪工作單位＝嘗試投遞round436排定的`us_portfolio_gross_profitability_v1.py`正式N=100版本**：先確認腳本讀取宇宙的方式（`load_quality_sample()`→`us_factor_ic_quality_clean_universe.py`→`load_clean_universe_tickers()`→模組層級`from us_contamination_blacklist import KNOWN_CONTAMINATED_TICKERS`），確認這是**執行期讀取模組全域變數**、非快取的舊值，round436把黑名單25→48檔的更新會在下次執行時自動生效，**腳本本身不需要任何修改即可反映最新黑名單**。實際`run_detached.py submit`確認被拒絕（exit=3，`REFUSED: 已有1個工作在跑`），符合預期（heavy-job-slot忙）。依`MARATHON_PROTOCOL.md`0b節第3條「這時這輪改做不需要重算的工作，不要`--allow-concurrent`硬跑」，`run_detached.py wait 20260908-095451-4ffe --max-min 4`等滿4分鐘仍`STILL_RUNNING`（exit=4），本輪未再繼續等待。`trial_registry.py --check`確認PASS（exit=0，202列，本輪無新判定）。`is_holdout_consumed()`開工/收工前皆確認`False`。**本輪未新增任何API呼叫、未產生任何回測結果**——純粹是資源競爭下的等待與腳本正確性確認，誠實記錄「本輪無實質研究產出」而非勉強找事做。**下一輪US軌接手**：先`run_detached.py status`確認TW backfill job是否已結束（`finished`/`timeout`皆可，只要heavy-job-slot空出），空出後立刻投遞：`python research/run_detached.py submit --name us_portfolio_gross_profitability_v1 --timeout-min 40 --expect data/us_portfolio_gross_profitability_v1.csv -- python -u research/us_portfolio_gross_profitability_v1.py`；收成後若TRAIN/VAL任一期percentile≥90.0且alpha_p<0.05，才進一步討論是否列入`portfolio_multifactor_v2`成分候選，否則登記`TRIALS_LEDGER.md`為FAIL。完整見`REPORT.md`第438輪心跳。

---
**最後更新：2026-09-08T09:00+08:00（馬拉松第436輪）**——上一輪（cycle`20260908-083037`）以`reason=ERROR`結束（互動/前一個headless執行個體，非本輪馬拉松session），取鎖乾淨（非陳舊鎖，cycle`20260908-090037`）。開工前`git status`發現`research/us_reverse_split_corporate_action_verify.py`有未commit差異：內容比對後確認是那個ERROR session已經寫完程式碼、實際跑完SEC EDGAR查證（`data/us_reverse_split_corporate_action_verify.csv`已是完整59列輸出，檔案時間08:25早於08:30:37 ERROR session開始時間，代表查證本身在更早的一次互動session完成，ERROR session可能只是在做收尾但中途出錯），只是沒有commit——非中途寫一半的殘骸，程式邏輯讀完確認自洽（`FCNCA`正確判為未確認，驗證了item-5.03條件能排除round433原始粗篩會誤殺的樣板文字偽陽性）。三軌時間戳：TW 06:00（round432，最舊）／US 07:30（round435，最新）——依輪替應選TW，但`run_detached.py status`確認heavy-job-slot目前空（0個running，`hypothesis_queue`排程的`#57`回填job剛結束一批），TW軌下一步（`#57`第1關sanity）仍需回填跑到100%（目前47.9%，由`hypothesis_queue`自走排程續跑，非本track），本輪判斷TW仍無新工作可做，改選US軌完成這份已經做完但未收尾的工作（FUT依例外條款不選）。**本輪工作單位＝收尾`#24`留下的59檔未確認候選之個股層級公司行動查證**（round433/434「下一輪US軌接手(2)」）：確認`us_reverse_split_corporate_action_verify.py`程式碼與`data/us_reverse_split_corporate_action_verify.csv`輸出一致（59列，23確認/36未確認），更新`us_contamination_blacklist.py`黑名單25→**48檔**（新增23檔SEC 8-K item-5.03確認名單，含`#24`原本點名為「疑似正常業務衰退」的`AMN`/`IBCP`——兩者其實都查到window內帶5.03的申報），更新`US_LEADS.md`#26。**本輪未重新呼叫SEC API**（複用上一輪已完成的查證結果），`trial_registry.py --check`確認PASS（本輪屬地基工作，同`#19`/`#24`先例，不進`TRIALS_LEDGER.md`）。`is_holdout_consumed()`開工/收工前皆確認`False`。**US軌現況**：`#20`/`#21`/`#23`反向分割污染診斷鏈（根因發現→系統性掃描→個股逐一查證）至此全部結案，48檔黑名單透過`load_clean_universe_tickers()`對後續新因子自動生效。**下一輪US軌接手**：`run_detached.py submit`跑`us_portfolio_gross_profitability_v1.py`正式N=100版本（round435已寫好指令，heavy-job-slot現在空的，可以投遞了）。TW軌待辦不變：`run_detached.py status`確認`#57`回填進度，仍未到100%就繼續等`hypothesis_queue`自走，到100%才進第1關sanity。完整見`US_LEADS.md`#26（新增）、`us_contamination_blacklist.py`（本輪擴充至48檔）、`us_reverse_split_corporate_action_verify.py`（承接上一輪完成，本輪補commit）。

---
**最後更新：2026-09-08T07:30+08:00（馬拉松第435輪）**——取鎖乾淨（cycle`20260908-073037`）。三軌時間戳：TW 06:00（round432，最舊）／US 07:00（round434，最新）——依輪替本應選TW（次舊）；`run_detached.py status`確認heavy-job-slot仍佔用中（`20260908-072211-66ad`『tw_backfill_day_trading_detail』running，由`hypothesis_queue`排程自走投遞、非本track所為），TW軌唯一待辦仍是等這個job跑完，本輪無新工作可做，改選US軌（FUT依例外條款不選）。**本輪工作單位＝round429/431/433/434反覆列出但從未執行的選項(2)：`f_us_gross_profitability`單獨Top-N長多構造**（`#22`（value_bm+low_vol等權combo）已FAIL，本輪是它的單因子對照組，非新假說——目的是分辨#22的FAIL是combo稀釋造成，還是Top-N長多構造本身對這族因子就是無效）。新增`us_portfolio_gross_profitability_v1.py`：逐字沿用`us_portfolio_multifactor_v1.py`的引擎/設定（`USPortfolioConfig`Top-N=15、季頻換倉、15%停損、N=100 matched-universe隨機控制、`alpha_significance`），只把`combo_signal_fn`換成`f_us_gross_profitability`單一因子z-score（`_zscore_cross_section`不必改，只是換`col`參數），universe改用`load_quality_sample()`（`deep_dive_f_us_gross_profitability.py`既有、`date`/`adj_close`/`f_us_gross_profitability`三欄，零新增API呼叫）。**本輪只跑了小draws（n=3）的接線冒煙測試**（heavy-job-slot被TW backfill佔用，`run_detached.py submit`會被拒絕，故未投遞正式N=100版本）：確認可執行、universe=74可用檔（因黑名單擴充至25檔後223個候選中74檔有效財報，非round412原本84檔——這是黑名單修復帶來的預期內宇宙縮小，非錯誤），TRAIN/VAL皆能算出alpha/beta/percentile，holdout檢查前後皆`False`。n=3的percentile數字（TRAIN 66.7/VAL 0.0）樣本太小不具統計意義，**不構成任何判定，僅供驗證接線正確**，冒煙測試輸出檔已刪除不留存誤導性小樣本結果。`trial_registry.py --check`確認PASS（exit=0，202列，本輪無新判定，未登記trial）。`is_holdout_consumed()`開工/收工前皆確認`False`，全程零新增API呼叫（純本地既有快取）。**下一輪US軌接手**：`run_detached.py submit`跑正式N=100版本：`python research/run_detached.py submit --name us_portfolio_gross_profitability_v1 --timeout-min 40 --expect data/us_portfolio_gross_profitability_v1.csv -- python -u research/us_portfolio_gross_profitability_v1.py`（若TW backfill job屆時仍running，先等它或TW軌自己收成後再投遞，勿`--allow-concurrent`硬跑）；收成後若TRAIN/VAL任一期percentile≥90.0且alpha_p<0.05，才進一步討論是否列入`portfolio_multifactor_v2`成分候選，否則登記`TRIALS_LEDGER.md`為FAIL。TW軌待辦不變：`run_detached.py status`/`log 20260908-072211-66ad`收成`#57`回填下一批。完整見`us_portfolio_gross_profitability_v1.py`（新增，可重複執行，含`--n-draws`/`--out`參數供冒煙測試與正式跑分開）。

