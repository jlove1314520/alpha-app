

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `US_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

---
**最後更新：2026-09-09T20:00+08:00（馬拉松第500輪）**——取鎖乾淨（cycle`20260909-200036`）。三軌時間戳：FUT 10:30（round484，最舊，依例外條款不選——無新機制候選）／US 19:00（round498，較舊）／TW 19:30（round499，最新）——依輪替選US。開工查`run_detached.py status`：`running=0`；`git log`確認round499之後僅自動排程commit（IBKR報價20:01/新聞事件），無馬拉松三軌或`hypothesis_queue`新進度——`git status`額外發現`hypothesis_queue`目前有一批未提交的工作中變更（`factors.py`/`twse_odd_lot_client.py`/`HYPOTHESIS_QUEUE.md`/`MARATHON_LOG.md`/`dev_queue_cycle.log`/`external_connectivity.jsonl`修改中，新檔`factor_ic_odd_lot_imbalance.py`），研判是另一條自走排程正在進行`#67`零股委託簿失衡度的cheap gate，屬另一個track的中間狀態，本輪未觸碰這些檔案、也未commit它們。**核實候選池現況不變**：0a節四條方向＋`#61`~`#66`全數結案，`data/ticks/`仍2/20（`20260909.parquet.tmp`未finalize，正常），`#50`gate50提案（TW round495新增）**核對`PENDING_QUEUE.md`確認仍無總司令回應**，`PENDING_QUEUE.md`頂端仍是題材新聞管線任務，非本馬拉松三軌範圍。**本輪工作單位＝核實US軌是否有round498之後的新工作單位**：額外核對`CALIBRATION_PROBE.md`對US軌`#47`（大型股tier `f_us_low_vol`）/`#52`（中型股tier）的重跑指令狀態——`US_LEADS.md`#27（round440/442）已用N=90重跑`f_us_low_vol`/`f_us_momentum_12m`/`f_us_reversal_1m`三因子，percentile分別86.1/73.5/68.8皆未過96.7門檻，`#47`正式結案FAIL；`#52`（中型股tier）性質不同，是`TRIALS_LEDGER.md`#68既有深挖失敗（TRAIN期輸給隨機控制組），不受calibration probe影響，維持既有FAIL——確認這條指令鏈早於本輪已100%執行完畢，非本輪遺漏。**結論：候選池連續14輪（487~500）維持同一狀態，US軌本地端無新可推進工作單位**，僅剩`#50`（tick累積2/20，被動等待總司令對gate50提案的回應）與`hypothesis_queue`自走排程（現在`#67`）的被動維護。本輪未產生新的統計判定（純核實性質），`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（232列，撞號2組皆為歷史存量不回頭改寫，不需要新的`register_trial()`呼叫）。`validation/holdout.py::is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純讀既有`.md`/`.py`檔案與`git log`/`git status`）。**下一輪任一軌接手**：`#50`gate50提案仍待總司令回應，核准前`#50`本身仍視為未結案；查`data/ticks/`是否已到3/20；候選池已連續14輪（487~500）無TW/US/FUT三軌自身可推進的新統計工作單位——若下一次盤點仍是同樣狀態，建議下一輪不必再重複整份四角度/地基覆核（已反覆驗證多次、資訊量已飽和），改為精簡確認（鎖/`run_detached`/`git log`/ticks進度/gate50回應）並直接記錄「狀態不變」，把節省下來的預算留給`#50`一旦解鎖後的cheap gate。完整見`REPORT.md`第500輪心跳、`US_LEADS.md`#27（本輪僅核實未新增）。

---

**上一則保留（第498輪，供對照）**——取鎖乾淨（cycle`20260909-190036`）。三軌時間戳：FUT 10:30（round484，最舊，依例外條款不選——無新機制候選）／US 18:00（round496，較舊）／TW 18:30（round497，最新）——依輪替選US。開工查`run_detached.py status`：`running=0`；`git log`確認round497之後僅`hypothesis_queue`排程自走commit（`#67`零股回補701→951/1092，87.1%），非本track範圍。**核實候選池現況不變**：0a節四條方向＋`#61`~`#66`全數結案，`data/ticks/`仍2/20（`20260909.parquet.tmp`未finalize，正常），`#50`gate50提案（TW round495新增）仍待核准，`PENDING_QUEUE.md`頂端2026-09-09裁示為「題材」新聞管線任務，非本馬拉松三軌範圍。**本輪工作單位＝收斂round468（`US_LEADS.md` #32）留下、此後約30輪都沒人接手處理的一個真實開放問題**：round468查證發現`#29`/`#30`/`#31`（3支SEC EDGAR 8-K事件研究）與`us_factor_ic_by_size.py`規模分層因子系列，價格源皆走`us_factors.us_price_series()`（FinMind`USStockPrice`），牴觸`CLAUDE.md`2026-09-08裁示「禁止用台灣資料商作為美股宇宙或價格的主來源」，當時寫「是否值得用yfinance重跑覆核，留給後續判斷」但此後無人接手，屬候選池薄時仍能找到的真實維護缺口。本輪逐一核對後正式判定**不需重跑**：(1)這4支腳本判定全部FAIL，資料源偏誤風險是「誤殺真訊號」而非「誤放行假訊號」，優先度遠低於後者；(2)同源的`#21`（`f_us_low_vol`乾淨宇宙版）已在round423查證確認yfinance對這批股票同樣是back-adjusted、round434確認異常真正成因是`adj_close`回溯膨脹（非FinMind特有限制，換來源不解決）；(3)沒有任何依賴`us_price_series()`的候選現處於CHEAP_PASS/PASS/EXPERIMENTAL待決狀態。已將判斷依據與2026-09-09起新規範（新試驗一律走`yf_price_client.py::fetch_yf_adjusted()`/`fetch_yf_index()`，不得再新增使用`us_price_series()`當主來源）寫入`DATA.md`新增章節「美股價格源：FinMind→yfinance過渡現況」，並在`US_LEADS.md` #32備註補上round498收斂結論，避免下一輪重複發現同一問題。本輪未產生新的統計判定（純方法論/資料源治理決策，非新試驗），`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（232列，撞號2組皆為歷史存量不回頭改寫，不需要新的`register_trial()`呼叫）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純讀既有`.py`/`.md`檔案）。**下一輪任一軌接手**：`#50`gate50提案仍待核准；查`data/ticks/`是否已到3/20；候選池已連續12輪（487~498）皆無TW/US/FUT三軌自身可推進的新統計工作單位，僅剩被動等待`#50`與`hypothesis_queue`自走排程（現在`#67`）的被動維護，若下一次盤點仍是同樣狀態，應開始認真評估是否接近0a節「無可驗證預測優勢」提報門檻（`#50`本身未結案前嚴格說還不到）。完整見`DATA.md`（本輪新增章節）、`US_LEADS.md` #32（round498更新）、`REPORT.md`第498輪心跳。

---

**上一則保留（第496輪，供對照）**——取鎖乾淨（cycle`20260909-180036`）。三軌時間戳：FUT 10:30（round484，最舊，依例外條款不選——無新機制候選）／US 17:00（round494，較舊）／TW 17:30（round495，最新）——依輪替選US。開工查`run_detached.py status`：`running=0`，`git log`確認round495之後僅`hypothesis_queue`排程自走commit（`#67`盤中零股交易回補進度202→452/1092），非本track範圍。**核實候選池現況不變**：0a節四條方向＋`#61`~`#66`全數結案，`data/ticks/`仍僅2個完整交易日parquet（`20260909.parquet.tmp`未finalize，較round494無變化），TW軌round495發現的`#50`gate50提案（`PROPOSAL_2026-09-09_gate50_tick_universe_mismatch.md`）仍待核准、未直接執行（本輪未碰該提案或`shioaji_quotes.py`）。**本輪工作單位＝`candidate_report.py --audit`稽核（0違規）後比照round490/491先例核對`build_signal_status.py`的`DIRECTIONS`清單是否有遺漏方向**：發現`#61`（央行理監事會議決策事件，round457已在`STRATEGY_GRAVEYARD.md`#61條目＋`TRIALS_LEDGER.md`#219判定FAIL）從未被寫進`build_signal_status.py`公開清單——0a節與各軌心跳文字多次提及「#61~#66全數結案」，但`ids`實際只有`49/50/51/52/62/63/64/65/66`共9條，漏了61（跟round489/490發現`#65`/`#66`遺漏是同一類缺口，這次是更早的一條被漏得更久）。逐字核對`HYPOTHESIS_QUEUE.md` `#61`條目與`STRATEGY_GRAVEYARD.md`第2378行`#61`條目內容後，比照`#62`格式在`build_signal_status.py`新增對應段落並重跑，`directions`清單新增`id=61`後共10條（`49/50/51/52/61/62/63/64/65/66`）。`git diff -b`確認`build_signal_status.py`/`data/signal_status.json`兩檔案除新增段落外無其他內容被更動（`--stat`：+41/-1，皆為新增區塊）。`trial_registry.py --check`（PYTHONIOENCODING=utf-8）exit=0 PASS（232列，撞號2組皆為歷史存量不回頭改寫；本輪未產生新判定，只是補齊既有FAIL判定的公開曝光，不需要新的`register_trial()`呼叫）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純核對既有結案文件）。**下一輪任一軌接手**：`#50`gate50提案（TW round495新增）仍待總司令核准，核准前`#50`本身仍視為未結案；查`data/ticks/`是否已到3/20；建議下一輪比照本輪做法再核對一次`build_signal_status.py`是否還有其他遺漏id（例如是否所有`STRATEGY_GRAVEYARD.md`有結案標記的假說都已同步），這類「既有判定未公開曝光」的缺口目前是候選池薄時仍能找到的少數真實維護工作之一。完整見`REPORT.md`第496輪心跳、`build_signal_status.py`（新增`id=61`段落）、`data/signal_status.json`（本輪新增`61`一條）。

---



（第444/446/448輪已於round454歸檔至`US_STATE_ARCHIVE.md`；第450/452輪已於round458歸檔；第454輪已於round460歸檔；第456輪已於round462歸檔；第458輪已於round464歸檔；第460輪已於round466歸檔；第462輪已於round468歸檔；第464/466輪已於round473歸檔；第468輪已於round476歸檔；第470輪已於round479歸檔；第473輪已於round481歸檔；第476輪已於round483歸檔；第479輪已於round486歸檔；第481輪已於round488歸檔；第483輪已於round490歸檔；第486輪已於round492歸檔；第488輪已於round494歸檔；第490輪已於round496歸檔；第492輪已於round498歸檔；第494輪已於round500歸檔，僅保留最新3則）
