

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `US_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

---
**最後更新：2026-09-08T13:00+08:00（馬拉松第444輪）**——取鎖乾淨（cycle`20260908-130037`）。三軌時間戳：TW 12:30（round443，最新）／US 12:00（round442）——依輪替選US（FUT依例外條款不選）。`run_detached.py status`確認heavy-job-slot**已空**（`run=0`）。**本輪工作單位＝收成round442投遞的`us_factor_ic_by_size_large_n90`（job`20260908-120138-803e`，已finished，exit=0）**：`log`顯示78/90可用（12檔因FinMind價格抓取失敗或天數不足<260被剔除），三因子（`f_us_low_vol`/`f_us_momentum_12m`/`f_us_reversal_1m`）結果——`f_us_low_vol`：TRAIN mean_ic=-0.0124/VAL mean_ic=+0.0255（train/val**反號**），percentile=86.1（門檻96.7）；`f_us_momentum_12m`：TRAIN=-0.0012/VAL=-0.0186（同號），percentile=73.5；`f_us_reversal_1m`：TRAIN=+0.0356/VAL=+0.0166（同號），percentile=68.8。**三者皆FAIL**，其中`f_us_low_vol`（`#47`）是`CALIBRATION_PROBE.md`裁示的最終複驗對象——依探針指示「不論PASS/FAIL都要登記」，本輪用`register_trial()`登記三筆（`TRIALS_LEDGER.md`#203/#204/#205），並在`US_LEADS.md`新增#27條目。**`#47`（大型股tier `f_us_low_vol`）自此正式結案為FAIL，不再標「未定」**：percentile從原N=29版83.4小幅升至86.1，但同號判準轉差（原本train幾乎零、本輪train轉負），跟`#15`（201檔已快取全樣本不分tier重跑，percentile 100.0清楚過關）對照，佐證訊號需要跨市值混合樣本才穩定浮現，單一large tier分層內樣本仍不足。`#52`（中型股tier）性質不同（是`1b`深挖策略層失敗，非cheap-gate樣本太小問題），不受本輪影響、維持既有FAIL。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）確認PASS（exit=0，207列，下一可用編號#206）。`is_holdout_consumed()`（`validation.holdout`）開工/收工前皆確認`False`。**本輪零新增API呼叫**（純判讀已完成job的log並登記）。**下一輪US軌接手**：`CALIBRATION_PROBE.md`裁示清單至此僅剩TW`#91`（若尚未複驗，需grep確認）與FUT`#34`；US軌自身待複驗清單已清空。可考慮：(1)檢查TW`#91`是否已由TW軌複驗過（grep `TRIALS_LEDGER.md`確認）；(2)`us_portfolio_gross_profitability_v1.py`正式N=100版本仍未投遞（round435/436/438/442多輪備妥指令但heavy-job-slot持續被TW backfill佔用，現在slot已空，可優先投遞）；(3)依`MARATHON_PROTOCOL.md`0a節四條結構性優勢方向評估US軌是否有對應可做的地基工作。完整見`REPORT.md`第444輪心跳、`TRIALS_LEDGER.md`#203/#204/#205、`US_LEADS.md`#27（新增）。

---
**最後更新：2026-09-08T12:00+08:00（馬拉松第442輪）**——取鎖乾淨（cycle`20260908-120037`）。三軌時間戳：TW 11:30（round441，最新）／US 11:00（round440）——依輪替選US（FUT依例外條款不選，時間戳雖最舊但其state明寫優先權排回TW/US）。`run_detached.py status`確認heavy-job-slot**已空**（`run=0`，TW`#57`回填最後一批`fdef`已於round441收成完成）。**本輪工作單位＝round440排定的`us_factor_ic_by_size.py`（large tier, N=90）正式投遞**：round440發現這支腳本比預期慢（16秒/檔，foreground撞240秒逾時），本輪確認`--cwd`參數用法後投遞——**第一次投遞路徑重複犯了round437同款`--cwd`錯誤**（`--cwd "...research"`搭配`research/us_factor_ic_by_size.py`造成路徑重複`research\research\...`，exit=2立即失敗，job`20260908-120124-79c4`），**立即發現並修正**：改用相對於`--cwd`的路徑`us_factor_ic_by_size.py`（不帶`research/`前綴）重新投遞，job`20260908-120138-803e`正常啟動。`wait --max-min 4`後仍`STILL_RUNNING`，但觀察log進度30/90（約9秒/檔，比round440觀察的16秒/檔快，推測是磁碟快取或宇宙資料已預熱），推算90檔全部跑完約13~14分鐘，在30分鐘job timeout內可望自然跑完，不需要下一輪再重投。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）確認PASS（exit=0，203列，本輪無新判定不涉及登記）。`is_holdout_consumed()`開工/收工前皆確認`False`。**本輪未新增任何API呼叫**（腳本讀取本機yfinance快取的量價資料，非即時抓取）。**下一輪US軌接手**：`run_detached.py status`/`log 20260908-120138-803e`收成，若`finished`就讀輸出判定`#47`（large tier）percentile/IC是否≥90.0門檻（依`CALIBRATION_PROBE.md`「未定，待N=90重跑」的裁示，這次結果才是最終判定，不論PASS/FAIL都要登記`TRIALS_LEDGER.md`並更新`US_LEADS.md`）；判定完成後把`TIER`改成`"mid"`（`SAMPLE_SIZE`維持90）重跑`#52`。若job仍`running`就再等或改做`us_portfolio_gross_profitability_v1.py`（round438已備妥指令）。完整見`REPORT.md`第442輪心跳、`data/jobs/20260908-120124-79c4.log`（路徑錯誤紀錄）、`data/jobs/20260908-120138-803e.log`（正常執行中）。

---
**最後更新：2026-09-08T11:00+08:00（馬拉松第440輪）**——取鎖乾淨（cycle`20260908-110037`）。三軌時間戳：TW 10:30（round439，最新）／US 10:00（round438）——依輪替選US（FUT依例外條款不選）。`run_detached.py status`確認heavy-job-slot仍佔用中（`20260908-105304-92e0`『tw_backfill_day_trading_detail』running，本輪觀察15.6分鐘，同round438起連續第三輪佔用）。**本輪工作單位＝`CALIBRATION_PROBE.md`裁示清單剩餘項「US軌#47/#52（`f_us_low_vol`規模分層重測）同理各自重跑」**（TW軌`#79`已於round?/`TRIALS_LEDGER.md`#153複驗確定FAIL；US這兩項自2026-08-26原始100檔年代（`TRIALS_LEDGER.md`#176/#183）從未依探針指示複驗）。`us_factor_ic_by_size.py`原設計`SAMPLE_SIZE=30`寫死、與`factor_ic.py::SAMPLE_SIZE`（TW，100→300）是獨立常數不會自動繼承——先確認宇宙規模：`us_universe.universe()`現況6,618檔active／5,566檔market_cap可用／三分位各1,855~1,856檔（與08-26原始數字一致，宇宙本身未擴大）。依TW`3x`加碼精神將`TIER="large"`（round95原#47）、`SAMPLE_SIZE`30→90本地試跑：**發現此腳本遠比預期慢**——90檔跑到12/90即撞240秒逾時（約16秒/檔，TW同類腳本300檔僅約5分鐘），研判是逐檔重算量價因子（非單純讀parquet）疊加磁碟I/O，**foreground無法在5分鐘內跑完**，且此刻heavy-job-slot被佔用中`run_detached.py submit`會被拒絕（`REFUSED`），故本輪**未取得任何新結果、未產生任何輸出檔**（腳本本身不寫檔，逾時中止未留半成品）。已將`TIER`/`SAMPLE_SIZE`程式碼異動保留（`us_factor_ic_by_size.py`：`TIER="large"`、`SAMPLE_SIZE=90`，未執行完成不代表判定，純粹是備妥下一輪`run_detached.py submit`的參數），並在檔案內加註明round440由來。`trial_registry.py --check`確認PASS（exit=0，202列，本輪無新判定不涉及登記）。`is_holdout_consumed()`開工/收工前皆確認`False`。**下一輪US軌接手**：`run_detached.py status`確認TW backfill job（`92e0`）是否已結束，heavy-job-slot空出後投遞：`python research/run_detached.py submit --name us_factor_ic_by_size_large_n90 --timeout-min 30 --expect none -- python -u research/us_factor_ic_by_size.py`（`--expect`留意腳本不寫檔，只能靠`log`判讀完成；跑完後把`TIER`改成`"mid"`重跑#52，同樣N=90）；若TW backfill仍忙且US這條也卡住，考慮改回`SAMPLE_SIZE=30`（原始值，維持向後相容供未來對照）或改走`us_portfolio_gross_profitability_v1.py`（round438已确认queue排在前面、同樣等heavy-job-slot）視何者先空出優先投遞。完整見`REPORT.md`第440輪心跳、`us_factor_ic_by_size.py`（本輪異動，未執行完成）。

（第436/438輪已於round444歸檔至`US_STATE_ARCHIVE.md`，僅保留最新3則）
