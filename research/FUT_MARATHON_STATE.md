# FUT_MARATHON_STATE.md — 期貨軌斷點狀態（覆寫式）

**這份檔案只描述期貨軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `FUT_LOG.md`；候選判定看 `FUT_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。

**最後更新：2026-08-23T02:00:00+08:00**（馬拉松設立時的初始快照，尚未有任何馬拉松輪次執行過）

**地基狀態：❌ 完全未搭建。** 沒有期貨版的資料抓取/連續合約/回測邏輯。**第一批馬拉松輪次的工作是搭地基，不是測策略**——見 `MARATHON_PROTOCOL.md` 第 5 節。

**已知資訊（避免重複調查）：**
- TAIFEX 期貨資料是 CSV 格式，**開頭有 BOM，編碼要注意**（`CLAUDE.md` 已記錄的地雷，直接沿用）。
- `CLAUDE.md` 提過既有資料源「期貨（TAIFEX CSV）」跟「三大法人期貨部位（TAIFEX 資料源）」，這是 `alpha-data/fetch.py`（凍結區）已經在用的端點分類，**具體 URL/端點格式要自己重新查證，不能假設 `research/` 這邊能直接沿用凍結區的程式碼**（那是凍結區，只能參考、不能動用或複製）。
- 完全沒有台指期歷史資料的抓取程式碼在 `research/` 底下。
- 完全沒有連續合約（近月轉倉）建構邏輯——這是期貨特有的技術問題，`adjust.py`（股票還原股價）的邏輯不能直接套用，需要重新設計。

**第一輪建議工作單位（只做其中一項）：**
1. 調查 TAIFEX 官網或 FinMind 的期貨歷史價量資料端點，實測可用性跟歷史深度（比照台股 `DATA.md` 里程碑1的驗證方式，直接打 API/CSV 下載測試，不要用猜的）。
2. 調查三大法人期貨部位、未平倉量資料源的具體端點跟欄位結構。
3. 設計連續合約銜接方法（比價法 vs 價差調整法），**先寫清楚選了哪一種、為什麼，再動手寫程式碼**——這個決定會影響所有後續策略的正確性，值得先想清楚。

**Holdout 狀態：✅ 未被使用**（跟主線共用同一套機制）。

---

## 下一步

見上方「第一輪建議工作單位」，一次只做一項。
