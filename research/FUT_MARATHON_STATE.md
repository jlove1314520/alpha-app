# FUT_MARATHON_STATE.md — 期貨軌斷點狀態（覆寫式）

**這份檔案只描述期貨軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `FUT_LOG.md`；候選判定看 `FUT_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。

**最後更新：2026-08-23T13:02:00+08:00**（馬拉松第二輪期貨軌執行後）

**地基狀態：❌ 仍未搭建（連續兩輪都取不到任何真實期貨資料，兩輪都撞到 FinMind IP 封鎖）。** 連續合約銜接方法的**設計決策**（第一輪完成，不需要 API 資料的部分）維持不變，見 `FUT_CONTINUOUS_CONTRACT_DESIGN.md`。**轉倉時點規則（結算日轉倉 vs 成交量交叉轉倉）仍未驗證，需要真實近月/次近月成交量資料才能判斷**——本輪再次因封鎖未能取得。

**⚠️ FinMind API 目前仍被封鎖（本輪第二次撞到，下一輪要先看這個）：**
- 第一輪（2026-08-23T11:47:30+08:00 左右）：`403 ip banned, retry_after=1782`（約 30 分鐘）。
- 本輪（2026-08-23T13:01:40+08:00）：**距離第一輪封鎖已超過 30 分鐘，理論上應該解封，但實際呼叫 `TaiwanFuturesDaily`（data_id=TX, 2024-06-03~06-07）仍回傳 `403 ip banned, retry_after=828`**（約 13.8 分鐘，預估解封時間約 2026-08-23T13:15:28+08:00）。只呼叫了一次就照協定停手，沒有重試。
- **這代表封鎖不是單純「上一輪自己的 403 讀秒還沒到」，而是這個網路環境的 IP 被 FinMind 判定為需要更長冷卻，或者封鎖視窗每次違規會重新計時（新的 403 又給了一個新的 retry_after，而不是延續原本的倒數）。下一輪就算距離「上一次記錄的 403 時間」已經超過該次的 retry_after，也不能假設一定解封，第一次呼叫前心理準備仍可能吃到 403，遇到就照樣只呼叫一次、記錄新的 retry_after、優雅收工，不要連續嘗試。**
- 注意：這個封鎖影響全站（跨資料集），可能也影響 TW/US 軌——如果那兩軌同一時段也在跑，可能是共用同一個 IP 額度池被撞到。
- `TaiwanFuturesDaily` 這個資料集名稱**連續兩輪都還沒有成功呼叫過一次**，依然完全未確認是否正確，也還沒看過它的欄位結構長什麼樣。`TaiwanFuturesInstitutionalInvestors` 同樣未驗證。

**已知資訊（避免重複調查）：**
- TAIFEX 期貨資料是 CSV 格式，**開頭有 BOM，編碼要注意**（`CLAUDE.md` 已記錄的地雷，直接沿用）——這條是官方 TAIFEX 網站直接下載 CSV 時的地雷，如果改走 FinMind API 則不適用（FinMind 回傳的是 JSON，不是 CSV）。
- `CLAUDE.md` 提過既有資料源「期貨（TAIFEX CSV）」跟「三大法人期貨部位（TAIFEX 資料源）」，這是 `alpha-data/fetch.py`（凍結區）已經在用的端點分類，**具體 URL/端點格式要自己重新查證，不能假設 `research/` 這邊能直接沿用凍結區的程式碼**（那是凍結區，只能參考、不能動用或複製）。
- `FUT_LEADS.md` 先前記錄過 FinMind 三大法人期貨部位資料集名稱可能是 `TaiwanFuturesInstitutionalInvestors`——**這個名稱本輪也還沒實測驗證過，只是先前留下的線索，不是已確認的事實**。
- **連續合約銜接方法：已做設計決策**（比價法為主+保留原始價格），完整推理見 `FUT_CONTINUOUS_CONTRACT_DESIGN.md`。轉倉時點規則還沒定案，列了兩個對照假說（結算日轉倉 H1 vs 成交量交叉轉倉 H2），需要真實資料才能驗證。
- 完全沒有連續合約建構的**程式碼**——設計決策已做完，程式碼還沒寫（`MARATHON_PROTOCOL.md` 明確要求先設計再寫程式碼，這輪只做到前半）。

**下一輪建議工作單位（只做其中一項）：**
1. **優先**：`fut_probe_milestone1.py` 已經寫好（這一輪產出，不用重寫），內容是打一小段日期範圍（2024-06-03~06-07）的 `TaiwanFuturesDaily`（data_id=TX）跟 `TaiwanFuturesInstitutionalInvestors`，外加一次全歷史深度檢查。下一輪只要確認距離上次 403 已經過了 `retry_after` 秒（本輪記錄的是 828 秒、約 2026-08-23T13:15:28+08:00 之後），直接 `python fut_probe_milestone1.py` 執行即可，不用重寫探測邏輯。如果又吃到 403，比照本輪做法：只呼叫一次、記錄新的 retry_after、更新這份檔案、優雅收工，不要重試。成功的話把結果（欄位結構、資料集名稱是否正確、歷史深度）記錄回 `DATA.md`。
2. 如果 1 做完且時間還夠：用抓到的近月+次近月成交量資料，驗證轉倉時點規則 H1（結算日轉倉）vs H2（成交量交叉轉倉）哪個更符合台指期實際流動性轉移型態，把結論寫回 `FUT_CONTINUOUS_CONTRACT_DESIGN.md` 的「尚待驗證」章節（驗證完就不再是待驗證，要更新狀態）。
3. 待 1、2 都完成、轉倉規則確定後，才動手寫連續合約建構程式碼（不是這一輪，也可能不是下一輪，看進度）。

**Holdout 狀態：✅ 未被使用**（跟主線 `MARATHON_STATE.md` 共用同一套 `validation/holdout.py`，同一個 `HOLDOUT_LOCK.json`）。

---

## 下一步

見上方「下一輪建議工作單位」，一次只做一項。下一個馬拉松輪次接手時，先讀 `FUT_LOG.md` 最新一條看上一輪實際做到哪裡，再讀 `FUT_CONTINUOUS_CONTRACT_DESIGN.md` 了解連續合約的設計決策跟尚待驗證項目。
