# TW_MARATHON_STATE.md — 台股軌斷點狀態（覆寫式）

**這份檔案只描述台股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `TW_LOG.md`；候選判定看 `TW_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `TW_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-10T22:30+08:00（馬拉松第525輪）**——取鎖乾淨（cycle`20260910-223036`）。開工先照CLAUDE.md「交辦優先於自走」鐵律讀`PENDING_QUEUE.md`：兩條阻塞項（S4U排程註冊／claude CLI非互動驗證）維持阻塞、題材七維持未拆解（規模過大非有界工作單位）、【題材三】已全數完成（123/123），**交辦佇列無可執行未開始項，回落自走**。依round524建議依輪替選TW（US round524 21:00較舊／TW round523 20:30更舊——round524建議下一輪選TW）。查`run_detached.py status`：`running=0`（60筆歷史紀錄，無新增）；`git log`確認round524後僅IBKR報價自動排程commit（最新`1e8b518d`22:31），未動`alpha.db`/`fetch.py`/`parsers.py`/`config.py`凍結區；`git status`僅既有`dev_queue`/`hypothesis_queue`軌未追蹤檔案（`backfill_tpex_3insti.py`／`tpex_3insti_client.py`等）與探針log在動，非本馬拉松三軌產物，本輪未觸碰、未越權處理。**核實現狀（精簡確認，無新統計工作單位可做）**：`data/ticks/`累積進度仍**4/20**（`20260907`~`20260910`四個`.parquet`皆已finalize，與round523/524一致，距20日仍差16日）；`PENDING_QUEUE.md`第298~313行`gate50`查證段落仍原封不動，總司令尚未回應三條件具體定義，`#50`維持未解鎖；`STRATEGY_GRAVEYARD.md`掃描`## #6x`/`## #7x`標題，最新結案仍為`#68`／`#70`（`#70`屬`hypothesis_queue`軌非本馬拉松範圍），本馬拉松三軌自身最新結案仍為`#68`（round510），無新結案。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（結構化登記`TRIALS_REGISTRY.jsonl`55筆起於#187；LEADS帶判定列101、存量未引用帳本22列僅報不擋；撞號2組皆歷史存量不回頭改寫；本輪未產生新試驗判定）。`validation/holdout.py::is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純讀既有`.md`/`.json`檔案與`git log`/`git status`/`run_detached.py status`）。**結論：候選池連續38輪（487~525）US/TW/FUT三軌自身皆無新可推進工作單位，僅剩`#50`被動等待總司令對gate50三條件的回應與tick累積（仍需16日）**。**下一輪任一軌接手**：`#50`gate50原文仍待總司令回應；FUT仍維持skip（除非出現全新機制假說），依輪替下一輪建議選US軌。完整見`REPORT.md`第525輪心跳、`MARATHON_STATE.md`（輪次計數器525）。

---

**上一則保留（第523輪，供對照）**——取鎖乾淨（cycle`20260910-203037`）。開工先照CLAUDE.md「交辦優先於自走」鐵律讀`PENDING_QUEUE.md`：兩條阻塞項（S4U排程註冊／claude CLI非互動驗證）維持阻塞、題材七維持未拆解（規模過大非有界工作單位），【題材三】已於稍早全數完成（123/123），交辦佇列無可執行未開始項，回落自走。**選軌判斷**：round522建議「下一輪選FUT重新評估例外條款」，但`FUT_MARATHON_STATE.md`round520自身結論已明寫「FUT例外條款已複核確認不成立，**往後除非出現真正跳脫清單的全新機制假說，不需要每輪重新複核FUT**，建議選TW軌」——round522的建議與round520這條自身的skip指示矛盾，本輪依`MARATHON_PROTOCOL.md`「除非某一軌state明確寫著『這裡卡住了、下一輪換別軌』就照著跳過」的規則，採信較明確的round520 FUT自身skip指示，跳過FUT，依輪替改選次舊的TW軌（TW round521 17:30／US round522 20:00，TW較舊）。查`run_detached.py status`：`running=0`（60筆歷史紀錄，無新增）；`git log`確認round522後僅IBKR報價自動排程commit，未動`alpha.db`/`fetch.py`/`parsers.py`/`config.py`凍結區；`git status`出現其他track（`dev_queue`/`hypothesis_queue`）留下的未追蹤檔案（`backfill_tpex_3insti.py`／`tpex_3insti_client.py`等），非本馬拉松三軌產物，本輪未觸碰、未越權處理。**核實現狀（精簡確認，無新統計工作單位可做）**：`data/ticks/`累積進度仍**4/20**（`20260907`~`20260910`四個`.parquet`皆已finalize，與round521/522一致，距20日仍差16日）；`PENDING_QUEUE.md`第298~313行`gate50`查證段落仍原封不動，總司令尚未回應三條件具體定義，`#50`維持未解鎖；`STRATEGY_GRAVEYARD.md`掃描`## #6x`/`## #7x`標題，最新結案仍為`#68`／`#70`（`#70`屬`hypothesis_queue`軌非本馬拉松範圍），本馬拉松三軌自身最新結案仍為`#68`（round510），無新結案；深讀二.3已於round521之後由他處核實完成（三個曾贏買進持有名目報酬的候選皆因gate 8以外的更嚴格檢定判死），本輪未見新的類似遺留工作成果可收尾。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（243列，撞號2組皆為歷史存量不回頭改寫，本輪未產生新試驗判定）。`validation/holdout.py::is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純讀既有`.md`/`.json`檔案與`git log`/`git status`/`run_detached.py status`）。**結論：候選池連續36輪（487~523）US/TW/FUT三軌自身皆無新可推進工作單位，僅剩`#50`被動等待總司令對gate50三條件的回應**。**下一輪任一軌接手**：`#50`gate50原文仍待總司令回應；FUT仍維持skip（除非出現全新機制假說），依輪替下一輪建議選US軌（US round522 20:00 vs 本輪TW round523 20:30，US較舊）。完整見`REPORT.md`第523輪心跳、`MARATHON_STATE.md`（輪次計數器523）。

---

**上一則保留（第521輪，供對照）**——取鎖乾淨（cycle`20260910-173037`）。依round520建議依輪替選TW（FUT本輪複核例外條款不成立，回落TW）。開工查`run_detached.py status`：`running=0`（60筆歷史紀錄，無新增）；`git log`確認round520後除round520自身commit外，僅IBKR報價自動排程commit，未動`alpha.db`/`fetch.py`/`parsers.py`/`config.py`凍結區。**本輪工作單位**：發現`git status`裡有一份未追蹤、從未commit過的檔案`research/validation/passive_benchmark_gate.py`（182行，非本馬拉松三軌state登記過的產物，推測為某次互動session針對`PENDING_QUEUE.md`「深讀二.2」留下但未收尾的成果）——查`PENDING_QUEUE.md`確認「深讀二.1」（被動基準引擎`passive_benchmark_tw_v1.py`）已於本日稍早完成並登記`TRIALS_LEDGER.md`#240，但「深讀二.2」（把引擎接進候選判定流程、成為六關第2關升級版）仍標`[ ]`未完成，這份未追蹤檔案的docstring自述正是深讀二.2的實作（`evaluate_gate8()`＋`match_by_risk()`：讀`passive_benchmark_tw_v1_result.json`真實結果檔、找不到就拋錯不用假基準、在12個w網格點裡找MDD最近鄰匹配、回傳完整比較明細不折疊成單一bool）。**驗證**：`python passive_benchmark_gate.py --self-test`共7項全PASS（含對真實結果檔跑極端高/低報酬各一次確認非自我循環）。**判定為可採用的既有工作成果**：純新增檔案、不涉及回測結論、不碰凍結區、不需要新試驗登記（工具函式本身不產生報酬數字，非alpha候選），予以commit並在`PENDING_QUEUE.md`「深讀二.2」補上完成註記（誠實揭露：只做了gate函式本身，尚未回頭批次重評既有候選，該工作量登記為深讀二.3，留給下一輪）。`data/ticks/`累積進度**4/20**（與round520一致，`20260910`當日盤中tick尚未到隔日finalize時點）；`PENDING_QUEUE.md`第99~113行`gate50`查證段落仍原封不動，總司令尚未回應三條件具體定義，`#50`維持未解鎖；`STRATEGY_GRAVEYARD.md`本馬拉松三軌自身最新結案仍為`#68`（round510），無新結案。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（243列，本輪未產生新試驗判定，深讀二.2屬工具函式非候選）。`validation/holdout.py::is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫。**結論：本輪找到並收尾一份先前遺留的可用工作成果（深讀二.2 gate8模組），打破連續34輪「無新可推進工作單位」的僵局；`#50`本身仍未解鎖，TW/US/FUT三軌自身仍無新統計工作單位**。**下一輪任一軌接手**：深讀二.3（批次重評既有已通過舊版第2關的候選）是明確可推進的新工作單位，優先於繼續等待`#50`；依輪替下一輪建議選US軌。完整見`REPORT.md`第521輪心跳、`PENDING_QUEUE.md`深讀二.2完成註記、`research/validation/passive_benchmark_gate.py`（新增）。

---

（第430/432/437/439/441/443/445/447/449/451/453/455/457/459/461/463/465/467/469/470/471/472/474/475/477/478/480/482/485/487/489/491/493/495/497/499/501/503/505/507/509/512/514/516/518輪已歸檔至`TW_STATE_ARCHIVE.md`，僅保留最新3則）
