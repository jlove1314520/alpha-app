# TW_MARATHON_STATE.md — 台股軌斷點狀態（覆寫式）

**這份檔案只描述台股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `TW_LOG.md`；候選判定看 `TW_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `TW_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-07T02:04+08:00（馬拉松第420輪）**——取鎖乾淨（非陳舊鎖檔，cycle`20260907-020036`）。三軌時間戳：TW 23:30（round416，最舊，另有獨立`hypothesis_queue`排程於00:00~01:29多次即時推進#49並已結案FAIL，commit`582ef0d`）／US 00:30（round418）／FUT 12:00（round399，依例外條款不選，除非有全新機制假說）——依輪替選TW。`run_detached.py status`確認heavy-job-slot空（0個running），無背景工作衝突；`git status`確認無其他行程正在編輯同一批檔案。**本輪工作單位＝`HYPOTHESIS_QUEUE.md`#51（強制交易者事件）子事件1「反推重建」備用路徑查證，接續round419前一批hypothesis_queue排程留下的「(b)查證子事件1反推重建備用路徑的官方交易日數規則」待辦**：純WebSearch查證+本機既有快取欄位盤點，零新增API呼叫。查得(1)官方法規資料庫`twse-regulation.twse.com.tw`《證券商辦理有價證券買賣融資融券業務操作辦法》第76條：停止融券賣出始於停止過戶前6個營業日、還券了結截止於停止過戶第6個營業日（含）；(2)證券投資人及期貨交易人保護中心網站同一條規則的教育版本，數字一致；(3)多個來源（含《公司法》第165條基準日前5日內停止過戶的法定框架）確認台灣T+2交割下「除權除息交易日＝停止過戶日前2個營業日」。盤點本機已有`TaiwanStockDividend`快取（2010-2024，2170檔，`CashExDividendTradingDate`/`StockExDividendTradingDate`欄位，`adjust.py`既有管線在用）確認**足以反推**：停止過戶日=ExDividendTradingDate+2營業日，強制回補截止日=停止過戶日-6營業日=ExDividendTradingDate-4營業日。**判定：子事件1從round419留下的「未死透待查」改判「可行（待實作驗證）」**——不需要抓預告表端點即可重建強制回補時間表，零新增API呼叫。因來源屬WebSearch（官方法規資料庫+投資人保護中心，2類），未達`CLAUDE.md`「四類來源涵蓋三類」滿門檻，**下一輪實作前先用少量真實個股手動核對推算結果跟BFI84U即時預告表是否吻合**，吻合即視為驗證通過。已寫入`HYPOTHESIS_QUEUE.md`#51子事件1新增段落。`is_holdout_consumed()`開工/收工前皆確認`False`。**下一輪TW軌接手**：(i)實作`forced_trader_events_probe.py`的反推公式並用近期1~2檔真實除息案例核對BFI84U快照驗證；(ii)完成子事件3（可轉債轉換價重設）的MOPS查證補齊三來源門檻；(iii)子事件1/3皆確認後，設計#51具體事件研究第1關cheap gate（先以子事件2現金增資，或子事件1強制回補，兩者已確認可行）。完整見`TW_LOG.md`第420輪記錄、`HYPOTHESIS_QUEUE.md`#51。

---

**上一則保留（第416輪，供對照）**——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：TW 22:00（round413，最舊）／US 23:00（round415）／FUT 12:00（round399，依例外條款不選，除非有全新機制假說）——依輪替選TW。`run_detached.py status`確認heavy-job-slot空（0個running），無背景工作衝突。**本輪工作單位＝`HYPOTHESIS_QUEUE.md`#49（日內/隔夜報酬結構分解）必要前置工程**：round415的hypothesis_queue排程已指出`adjust.py::adjusted_price_series()`的FinMind回退路徑只還原`close`（產生`adj_close`），`open`/`high`/`low`維持原始未還原，直接拿現有`open`算隔夜報酬會被除權息事件污染。本輪擴充該函式：(1) yfinance路徑新增`adj_open`/`adj_high`/`adj_low`三欄（別名自已調整的`open`/`high`/`low`，因`auto_adjust=True`本身就同步調整OHLC）；(2) 用真實資料（`2330.TW` 2024年241個交易日，`auto_adjust=True` vs `False`兩次抓取比對）**實測驗證**yfinance確實對open/close套用同一乘數因子（逐日比對誤差<1e-6），不是只憑docstring假設；(3) FinMind回退路徑重構為先算單一`factor_cum`累積因子序列再分別套用到close/open/high/low（原本只對close算），用合成資料證明重構後`adj_close`數值與原邏輯逐位元相同（無回歸）；(4) **過程中抓到一個新bug並修正**：FinMind原始`TaiwanStockPrice`欄位其實是`max`/`min`（不是`high`/`low`），若照docstring字面寫`raw["high"]`會直接`KeyError`——用真實2330資料(2023-01-01起，8筆除權息事件)跑過FinMind回退路徑全流程確認`adj_open`/`adj_high`(`max`)/`adj_low`(`min`)三欄的還原因子都跟`adj_close`一致，無crash。全程零新增API呼叫（yfinance走既有快取，FinMind走`load_dev`既有快取），純本地計算+驗證，未跑任何回測或cheap gate。`is_holdout_consumed()`開工/收工前皆確認`False`。**下一輪TW軌接手**：`adjust.py`前置工程已完成，直接進`HYPOTHESIS_QUEUE.md`#49第1關cheap gate——對TAIEX（`fetch_yf_index`）或0050逐日拆解`overnight_return_t=open_t/close_{t-1}-1`與`intraday_return_t=close_t/open_t-1`（用新的`adj_open`），TRAIN/VAL兩期各自做單樣本t檢定+複利貢獻占比拆解，事前綁定門檻見`HYPOTHESIS_QUEUE.md`#49條目，不跳關。完整見`TW_LOG.md`第416輪記錄、`HYPOTHESIS_QUEUE.md`#49、`adjust.py`（修改，`adjusted_price_series()`新增`adj_open`/`adj_high`/`adj_low`三欄）。

---

**上一則保留（第413輪，供對照）**——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：TW 21:00（round411，最舊）／US 21:34（round412）／FUT 12:00（round399，依例外條款不選，除非有全新機制假說）——依輪替選TW。`run_detached.py status`確認heavy-job-slot空（0個running），無背景工作衝突。**本輪工作單位＝`HYPOTHESIS_QUEUE.md`#48（董監事/經理人/大股東股權質押比例）地基建置，接續#48資料可行性查證(a)(b)(c)全過後「下一輪直接進第1關cheap gate」的地基回補**：新增`backfill_irb130_pledge.py`，把`irb130_pledge_probe.py`的少量測試請求擴大為完整TRAIN(2015-2020)+VAL(2021-2024)月頻回補（2015-01~2024-12共120個月x{sii,otc}兩市場=240次請求，逐月寫入`data/raw_irb130_pledge/`獨立CSV快取，可安全中斷重跑）。先手動測試單一月份（`sii`2024-06，回傳1018列）確認fetch/parse函式正確運作，才投遞正式工作。**因240次請求x1.5秒節流理論耗時約6分鐘、實際含網路延遲可能落在數十分鐘量級，依`MARATHON_PROTOCOL.md`第0b節規則用`run_detached.py`脫離session投遞**（首次提交因未指定`--cwd`在錯誤工作目錄下找不到腳本立即failed，重投時加`--cwd`修正，job`20260906-220343-82ae`，timeout 60分鐘，預期產出`data/irb130_pledge_combined.csv`，提交後確認`status`為`running`），本輪僅投遞、未收成，未在session內等待。已寫入`HYPOTHESIS_QUEUE.md`#48狀態更新段落。`is_holdout_consumed()`開工/收工前皆確認`False`。**下一輪TW軌接手**：先跑`python run_detached.py status`確認job`20260906-220343-82ae`完成狀態（`finished`則收成，`failed`/`timeout`則看log查原因重投），完成後直接進第1關cheap gate：讀`irb130_pledge_combined.csv`，用`board_pledge_pct`水位與其MoM變化兩種訊號口徑，比照`#41`/`#42`/`#43`同一套train/val分期+時序洗牌null對照框架測forward報酬Spearman相關性，不跳關。完整見`TW_LOG.md`第413輪記錄、`HYPOTHESIS_QUEUE.md`#48、`backfill_irb130_pledge.py`（新增，可重複執行）、`data/jobs/20260906-220343-82ae.log`。**（後續：#48已由獨立hypothesis_queue排程於round415前結案FAIL，見`HYPOTHESIS_QUEUE.md`#48最終判定；round416已接手#49前置工程，見上方最新條目。）**

---

（第411輪已於round420歸檔至`TW_STATE_ARCHIVE.md`，僅保留最新3則）
