# TW_MARATHON_STATE.md — 台股軌斷點狀態（覆寫式）

**這份檔案只描述台股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `TW_LOG.md`；候選判定看 `TW_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `TW_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-09T01:00+08:00（馬拉松第465輪）**——取鎖乾淨（cycle`20260909-010037`）。三軌時間戳：TW 00:00（round463，最舊）／US 00:30（round464，最新）——依輪替選TW（FUT依例外條款不選）。`run_detached.py status`確認TW軌job`20260908-235353-95c8`（`material_news_backfill_52tw`）已`finished`（exit=0，55.2分鐘），heavy-job-slot空出。**本輪工作單位＝依round463交辦，正式進入`#52`第1關cheap gate**：先確認`data/raw_mops_material_news/`落地2607/2609個parquet（99.9%），接受此完成度（協定精神：不為了0.1%完整度額外耗MOPS請求）。**已知限制(2)解決**：事件表`stock_id`涉及2,900個不重複代號明顯超過台股上市櫃總數，`material_news_aggregate.py`新增`universe.py`官方存活者偏差處理宇宙清單過濾（649,417→623,496筆，保留96.0%，2,900→2,400檔代號，被排除的500檔多為陸股/興櫃/債券等MOPS特殊編碼），重跑彙總，`data/mops_material_news_events_classified.parquet`已是過濾後版本。**新增`material_news_car_gate.py`**：實作#52規格(g)/(l)段落CAR事件研究——PIT依`announce_time`（HH:MM:SS）判斷盤後公告（>13:30:00）順延reaction_day一交易日，CAR窗口=[reaction_day,+1交易日]、基準=reaction_day前一交易日收盤、與TAIEX同期比較取絕對值；控制組依`control_group_standard.py`2026-09-07升級標準（matched_stock/unmatched_universe兩變體、各N=200、通過門檻=嚴格大於全部400次抽樣最大值，非贏平均/90百分位）；零新增API呼叫（只讀本機已快取`TaiwanStockPrice__{id}__2010-01-01__2024-12-31.parquet`，命中1,796/2,400檔）。**smoke test**：4組小規模測試（60檔/2類/N=5、150檔/3類/N=25兩次，驗證效能優化前後數值一致）皆正常無崩潰。**中規模前景驗證**（全部1,796檔快取股票、8類、N=50，耗時3分鐘）：8類中6類PASS（併購/增減資/財務/人事/處分資產+停復牌），但效果量級偏大（部分VAL_mean|CAR|>10%），依協定「效果量級越誇張越應懷疑」暫不採信。**已投遞正式N=200全量job**`20260909-011334-1783`（`--timeout-min 40`），session內等待3分鐘觀察到隨N增加部分類別（停復牌、訴訟）從PASS轉為FAIL，符合「小N易虛胖PASS，控制組max隨抽樣數增加而更極端」的統計預期，非bug。`trial_registry.py --check`確認PASS（exit=0，222列，本輪無新判定，尚無正式N=200結果可登記）。`is_holdout_consumed()`開工/收工前皆確認`False`，全程零新增外部API呼叫。**下一輪TW軌接手**：先查`run_detached.py status`確認job`20260909-011334-1783`是否已`finished`（`--timeout-min 40`，預估40分鐘內完成），讀`data/material_news_car_gate_result.json`取得8類正式N=200判定——**PASS的類別必須先呼叫`register_trial()`登記才能寫進`TW_LEADS.md`**，且要留意本輪N=50→N=200觀察到的「小N虛胖」現象，過關類別要交叉檢查control_percentile是否仍然極端（>99）而非勉強過關；若job仍`running`未逾時則單純查核記錄進度；若`timeout`則檢討是否N=200對~1800檔仍太慢，考慮縮減至N=100或進一步優化。完整見`HYPOTHESIS_QUEUE.md` #52條目(u)段落（下方新增）、`REPORT.md`第465輪心跳、`material_news_car_gate.py`（新增，可重複執行）。

---

**上一則保留（第463輪，供對照）**——取鎖乾淨（cycle`20260909-000037`）。三軌時間戳：TW 23:00（round461，最舊）／US 23:30（round462，最新）——依輪替選TW（FUT依例外條款不選）。`run_detached.py status`確認heavy-job-slot被job`20260908-235353-95c8`（`material_news_backfill_52tw`）佔用中（running，已執行約9.2分鐘）——**此job非本馬拉松輪次投遞，是`hypothesis_queue`排程（獨立自動化排程，非本輪）在23:53:53接手前一個逾時job`b142`後重投的**（見`HYPOTHESIS_QUEUE.md` #52條目(q)段落），本輪僅查核不重投，避免與`hypothesis_queue`排程同時對MOPS端點發出並行請求。**本輪工作單位＝查核job`95c8`進度＋覆核#50/#52是否已如(q)段落條件式所述雙雙解鎖**。`data/raw_mops_material_news/`實際落地快取檔數**1,812個parquet**（累積進度69.4%，job開工時已快取1,658，本輪查核時已快取1,812，9.2分鐘處理154天，約16.8天/分鐘，與先前估算一致）；剩餘797天，同速率估算約47分鐘可跑完，**在100分鐘timeout內可望完整跑完**，不同於round461/(o)段落預期的「會撞timeout」——因為hypothesis_queue排程已經重投一次、進度已推進到接近尾聲。**覆核(q)段落「若下一輪#50/#52仍雙雙未解鎖才設計#62」的條件**：`#52`job仍`running`（非`timeout`非`finished`，且預估很快會跑完），判定為**仍在正常進行中，非「卡住不動」**，不符合(q)所指「未解鎖」的原意（(q)的原意是job逾時/卡死才轉向#62，非「job還在跑」就轉向）；`#50`（`data/ticks/`）仍為2個parquet，對照20交易日門檻仍遠（2/20）。**判斷：不設計#62**，因為#52正在健康推進、預估本輪之後1~2輪內即可完整跑完並進入cheap gate，過早跳去設計新假設軸反而浪費#52即將到手的成果。`git status`確認僅`.live_watchlist.json`/`dev_queue_cycle.log`/`external_connectivity.jsonl`三個其他常駐服務殘留變更，未觸碰。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）確認PASS（exit=0，222列，本輪無新判定）。`is_holdout_consumed()`開工/收工前皆確認`False`，本輪零新增API呼叫（僅查詢job狀態、檔案數與#50 ticks檔案數）。**下一輪TW軌接手**：先查`20260908-235353-95c8`是否已`finished`（預估約00:50前後完成）——若已完成，2,600天全數落地，**直接進入`#52`第1關cheap gate**（8類`type`分類CAR檢定，規格已寫在`HYPOTHESIS_QUEUE.md` #52條目(l)段落，不得跳關搶跑）；若仍`running`未逾時則比照本輪模式繼續單純查核記錄進度；若已`timeout`則直接重投同一指令（自動跳過已快取天數，不得從頭重跑）。完整見`HYPOTHESIS_QUEUE.md` #52條目、`REPORT.md`第463輪心跳。

---

**上一則保留（第461輪，供對照）**——取鎖乾淨（cycle`20260908-230036`）。三軌時間戳：TW 22:00（round459，最舊）／US 22:30（round460，最新）——依輪替選TW（FUT依例外條款不選）。`run_detached.py status`確認heavy-job-slot被TW軌本身既有job`b142`（`material_news_backfill_52tw`）佔用中（running），本輪不投遞新重度工作。開工前核對round459記載的「查證#51資料可行性」提示已過時——`#51`三個子事件已於2026-09-07正式結案FAIL（見`HYPOTHESIS_QUEUE.md` #51條目「(i)子事件3結案」段落），不得誤判#51仍待查證。**本輪工作單位＝查核job`b142`進度並記錄速率推算**。`run_detached.py status --last 5`確認`b142`仍為`running`（已執行61.6分鐘），實際落地快取檔數996個parquet（累積進度38.3%，比round459投遞時的3個大幅前進）。**速率推算**：61.6分鐘處理996天，平均約16.2天/分鐘；剩餘1,604天以同速率估算還需約99分鐘，總計約161分鐘——**明顯會在100分鐘timeout（約23:42）被watchdog中止**，屬預期內、可續跑設計（per-day parquet快取，已存在就跳過），非錯誤。`git status`確認僅`dev_queue_cycle.log`/`external_connectivity.jsonl`兩個其他排程殘留變更，未觸碰。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）確認PASS（exit=0，222列，本輪無新判定）。`is_holdout_consumed()`開工/收工前皆確認`False`，本輪零新增API呼叫（僅查詢job狀態與檔案數）。**下一輪TW軌接手**：先查`b142`是否已標記`timeout`（預期約23:42後發生）——若已逾時，直接用同一指令（`backfill_material_news.py --batch-size 2600`，`--cwd research`）重投，會自動跳過已快取的996+天從斷點接續，**不得從頭重跑**；若屆時仍`running`未逾時則比照本輪模式繼續單純查核記錄進度，不重投不等待；完整跑完2600天後才進入`#52`第1關cheap gate（8類`type`分類CAR檢定，規格已寫在`HYPOTHESIS_QUEUE.md` #52條目），不得跳關搶跑。完整見`HYPOTHESIS_QUEUE.md` #52條目(o)段落、`REPORT.md`第461輪心跳。

---

（第430/432/437/439/441/443/445/447/449/451/453/455/457/459輪已於round439/441/443/445/447/449/451/453/455/457/459/461/463/465歸檔至`TW_STATE_ARCHIVE.md`，僅保留最新3則）
