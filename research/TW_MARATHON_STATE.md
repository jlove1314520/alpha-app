# TW_MARATHON_STATE.md — 台股軌斷點狀態（覆寫式）

**這份檔案只描述台股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `TW_LOG.md`；候選判定看 `TW_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `TW_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-08T12:30+08:00（馬拉松第443輪）**——取鎖乾淨（cycle`20260908-123037`）。三軌時間戳：TW 11:30（round441，最舊）／US 12:00（round442，最新）——依輪替選TW（FUT依例外條款不選）。開工簡報顯示round441下一步是`#57`第2關隨機控制組，但`git log`發現**在本輪開始前，`hypothesis_queue`自走排程已搶先完成**（commit`6dce8fd`）：`#57`第2關FAIL（四項判定皆未過），`#53～#57`家族正式結案0勝5敗，並已新增假設軸`#58`（反向波動度加權投資組合）且完成第1關sanity（PASS，commit`95bb6a3`）。本輪一開始先用同一套`cross_sectional_dispersion_gate53_control.py`框架重寫並跑了`day_trading_ratio_dispersion_gate57_control.py`，跑完才發現與已提交版本完全重複（覆寫到已追蹤檔案），確認數字一致後`git checkout`還原，未浪費額外commit，但確認了自走排程領先的事實。**本輪真正工作單位＝改做`#58`第2關隨機控制組**：判斷`#29`（`equal_weight_rebalance_control_v1.py`）的bootstrap子集控制組模板不適用於`#58`（`#29`測「換一批股票效果還在不在」，`#58`要測的是「權重高低是否真的對應個股波動度高低」，換股票測不出這個），改設計專屬控制組：固定`REBAL_FREQ=21`/`VOL_WINDOW=60`，每次再平衡把算出的權重值集合原封不動、打散股票-權重對應（保留邊際分布），2變體`per_rebal_permutation`（每次事件獨立重排）／`fixed_permutation`（全程固定一組重排）各N=100，統計量=TRAIN/VAL年化Sharpe（invvol_ret本身，不扣buyhold）。新增`inverse_vol_weighted_portfolio_gate58_control.py`，用`run_detached.py submit --cwd`投遞（過程中修正一個`_period_mask()`的`.to_numpy()`型別錯誤，DatetimeIndex比較已回傳ndarray，二次投遞後正常）。**結果：TRAIN PASS**（真實訊號年化Sharpe+1.1747嚴格贏過200次控制組抽樣最大值+1.0512）**，VAL FAIL**（真實訊號+1.5163沒有贏過控制組最大值+1.5389，百分位99.0但2026-09-07標準升級後不算通過）。GATE_SEQUENCE要求TRAIN+VAL皆PASS，**`#58`結案：FAIL**（邊緣結果，VAL僅差一點點，不泛化為風險平價機制本身無效，完整不泛化聲明見`STRATEGY_GRAVEYARD.md`）。已登記`TRIALS_LEDGER.md`#202，更新`HYPOTHESIS_QUEUE.md` #58條目與`STRATEGY_GRAVEYARD.md`新增條目。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）確認PASS（exit=0，204列，下一可用編號#203）。`is_holdout_consumed()`開工/收工前皆確認`False`。**本輪零新增API呼叫**（複用#29/#58已快取個股parquet）。**下一輪TW軌接手**：**佇列#1~58全數結案**，需要判斷新的假設方向——優先檢視`MARATHON_PROTOCOL.md` 0a節四條結構性優勢方向（#49隔夜vs日內、#50容量受限小型股、#51強制交易者事件、#52事件反應速度）進度，或依「回顧已死假設共同模式」原則在③portfolio construction大類（目前只測過#29等權重、#58反向波動度加權，皆FAIL但死法不同）設計新的經濟機制假說，避免重複已測兩次的①timing overlay大類（#53~#57皆屬此類，0勝5敗）。完整見`HYPOTHESIS_QUEUE.md` #58條目結案段落、`STRATEGY_GRAVEYARD.md` #58條目、`REPORT.md`第443輪心跳、`inverse_vol_weighted_portfolio_gate58_control.py`（新增）。

---

**上一則保留（第441輪，供對照）**——取鎖乾淨（cycle`20260908-113037`）。三軌時間戳：TW 10:30（round439，最舊）／US 11:00（round440，最新）——依輪替選TW（FUT依例外條款不選，其state明寫「除非有全新機制假說否則優先權排回TW/US」）。`run_detached.py status`確認heavy-job-slot最後一批`#57`回填job（`20260908-112207-fdef`，已跑8.7分鐘）仍在跑，`wait --max-min 4`後收成：`exit_code=0`，本批148天全數處理完（其中8天無資料），**累積快取確認2609/2609＝100.0%，回填正式完成**。**本輪工作單位＝進`#57`第1關sanity**（比照`#53`/`#54`/`#55`同一套框架）：新增`day_trading_ratio_dispersion_gate57.py`——分子`dt_value=day_trade_sell_value+margin_offset_value`（沿用`#37`docstring既有的「當沖賣出+資券互抵」合併慣例，非本輪新創），分母沿用`turnover_concentration_gate54.py::_load_money_long()`同一批`Trading_money`快取（與#54同一分母，相關係數比較才有意義），4碼普通股過濾比照#55。抽樣核對2024-01-02 2330：dt_value=383,185,000／money=16,549,619,798／dt_ratio=2.32%，量級合理。因單日讀取2,609個逐日detail檔+約2,400檔個股money快取預估較慢，用`run_detached.py submit`（**務必`--cwd "C:\alpha\alpha-app\research"`**，否則重蹈48ca覆轍）投遞，4分鐘內收成完成（實跑約1.7分鐘）。**結果：三項sanity皆PASS**——sanity1非退化（disp_std=0.0113、vel_std=0.197、有效vel天數2,411）；sanity2危機窗口3/3中2/3（2020Q1、2022全年）vel_pctile高於無條件基準；sanity3條件式前瞻20日TAIEX報酬，level_pctile與vel_pctile兩版方向皆正確（高tertile報酬<低tertile報酬）。與#53/#54/#55相關係數檢查：|corr_level|/|corr_vel|全數<0.7（最高0.226 vs gate53_vel），**確認非同家族，是獨立發現**。`day_trading_ratio_dispersion_gate57.csv`已產生（2,431個有效交易日）。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）確認PASS（exit=0，202列，sanity非最終判定，比照#53/#54/#55先例暫不登記，待第2關控制組出結果才登記）。`is_holdout_consumed()`開工/收工前皆確認`False`。**本輪零新增TWSE API呼叫**（純讀取已100%落地的本機parquet快取）。**下一輪TW軌接手**（**注：此下一步已被hypothesis_queue自走排程搶先完成，見round443最新條目**）：進`#57`第2關隨機控制組——比照`cross_sectional_dispersion_gate53_control.py`同一套框架（`control_group_standard.py::evaluate_vs_control()`，控制組3變體circular_shift/block_shuffle_5/block_shuffle_20各N=100，曝險函數`exposure_t=clip(1-pctile_{t-1},0,1)`固定映射不掃斜率，TRAIN/VAL分開判定），新增`day_trading_ratio_dispersion_gate57_control.py`，沿用本輪`build_dt_dispersion_series()`與`day_trading_ratio_dispersion_gate57.csv`，若嫌foreground跑太久（#53控制組N=100×3變體×2規格歷史上約數分鐘等級）一樣用`run_detached.py submit`+`--cwd`。完整見`HYPOTHESIS_QUEUE.md` #57條目（本輪更新）、`REPORT.md`第441輪心跳、`day_trading_ratio_dispersion_gate57.py`（新增）、`data/day_trading_ratio_dispersion_gate57.csv`（新增）。

---

**上一則保留（第439輪，供對照）**——取鎖乾淨（cycle`20260908-103037`）。三軌時間戳：TW 09:30（round437）／US 10:00（round438，最新）——依輪替本應選TW（次舊，round437早於round438）。`run_detached.py status`確認heavy-job-slot佔用中（`20260908-102224-c3f5`『tw_backfill_day_trading_detail』running，由`hypothesis_queue`自走排程投遞，非本track所為；同時發現前兩批`20260908-093146-c524`/`20260908-095451-4ffe`皆以`reason=timeout`收場，`log`顯示皆在印出「200/250」後被20/25分鐘逾時砍掉，但快取量仍持續成長1500→1743→1961，判斷是**每批處理進度變慢但增量仍在**，非卡死；已把觀察寫進本輪記錄供下一輪對照，未採取行動——`SLEEP_BETWEEN_CALLS=2.0秒`節流設計不變，逾時後由自走排程自動重投是既有機制的正常運作，非本輪待修的bug）。依`MARATHON_PROTOCOL.md`0b節第3條「heavy-job-slot忙時改做不需重算的工作，不要`--allow-concurrent`硬跑」，US軌下一步（`us_portfolio_gross_profitability_v1.py`正式版）同樣會被拒絕（round438已驗證過一次），本輪不重複嘗試。**本輪工作單位＝完成`PENDING_QUEUE.md`長期未結的`資料一.4`驗收**（隔日回報tick落地檔數/筆數/檔案大小＋2330前5筆與最後5筆，這是`資料一.1~.3`已完成、`#50`容量受限小型股前置依賴、且完全不需要heavy-job-slot的純本地檔案檢視工作）：讀取`data/ticks/20260907.parquet`（第一個完整交易日，13:45收盤壓縮流程已自動產出）確認1個檔案／146,846筆／20個標的／1,120,633 bytes；抽查`2330`（6,192筆）前5筆與最後5筆時間戳/價格/五檔，並額外驗證`simtrade`欄位在全天的分佈（`False`真實成交5,799筆集中在09:00:09~13:30:00連續交易時段、`True`試撮393筆全部落在開盤前/收盤前集合競價窗口）——確認資料管線的試撮/真實成交旗標語意正確，不是常數或誤植。已勾選`PENDING_QUEUE.md`『資料一.4』並附完整數字，同步更新`HYPOTHESIS_QUEUE.md`「資料源可用性對照」表（資料一從「待做」改為「進行中，1/20個交易日」，`#50`前置依賴尚未達標，仍待門檻累積）。**本輪未新增任何API呼叫**（純讀取已落地的本機parquet檔案），`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）確認PASS（exit=0，202列，本輪屬地基驗收工作，比照`#19`/`#24`先例不進`TRIALS_LEDGER.md`）。`is_holdout_consumed()`開工/收工前皆確認`False`。**下一輪TW軌接手**：`run_detached.py status`確認`#57`回填job（`20260908-102224-c3f5`或其後續批次）是否已到100%（2,609/2,609），未到就繼續等自走排程續跑（heavy-job-slot騰出前不要`submit`新工作）；到100%後才進`#57`第1關sanity（比照`#53`/`#54`/`#55`同一套`cross_sectional_*_gate5X.py`框架）。若heavy-job-slot仍忙，可再檢查`資料一`累積天數是否已達`#50`的20天門檻。完整見`PENDING_QUEUE.md`『資料一.4』（新增驗收內容）、`HYPOTHESIS_QUEUE.md`「資料源可用性對照」表、`REPORT.md`第439輪心跳。

---

（第430/432/437輪已於round439/441/443歸檔至`TW_STATE_ARCHIVE.md`，僅保留最新3則）
