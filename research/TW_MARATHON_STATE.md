# TW_MARATHON_STATE.md — 台股軌斷點狀態（覆寫式）

**這份檔案只描述台股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `TW_LOG.md`；候選判定看 `TW_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `TW_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-08T09:30+08:00（馬拉松第437輪）**——取鎖乾淨（cycle`20260908-093037`）。三軌時間戳：TW 06:00（round432，最舊）／US 09:00（round436，最新）——依輪替選TW（FUT依例外條款不選）。`run_detached.py status`確認heavy-job-slot空（0個running），但發現最近一次由`hypothesis_queue`自走排程投遞的`#57`回填job（`20260908-092309-48ca`，09:23）**立即失敗（exit=2）**：`log`顯示`python: can't open file 'C:\\alpha\\alpha-app\\backfill_day_trading_detail.py'`——那次呼叫`run_detached.py submit`時沒帶正確`--cwd`（`backfill_day_trading_detail.py`實際位於`research/`底下，不是`alpha-app/`根目錄），導致工作目錄錯誤、腳本找不到檔案。**本輪工作單位＝診斷此次失敗並修正重投**：確認`data/raw_twse_day_trading_detail/TWTASU_detail_*.parquet`實際快取量為1500/2609（57.5%，與上一輪commit訊息一致，未因這次失敗而倒退——失敗發生在啟動前，未寫入任何半成品），用`--cwd "C:\alpha\alpha-app\research"`明確指定工作目錄重新投遞（job`20260908-093146-c524`），`wait --max-min 2`確認這次正常啟動並印出「已快取1500，待處理1109」，非再次立即失敗，處理中未等待收成。**本輪未修改`backfill_day_trading_detail.py`程式邏輯本身**——問題是呼叫端（自走排程）的`--cwd`參數缺失，不是腳本的錯，故不歸咎腳本設計；若之後`hypothesis_queue`排程再次以同樣方式投遞失敗，下一輪要往排程呼叫端（而非這支腳本）找根因。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）確認PASS（exit=0，202列，下一可用編號#201，本輪無新判定不涉及登記）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增TWSE端點呼叫（本輪僅檢視既有快取檔案數量與job log），回填階段的API呼叫由背景job執行、遵守既有`SLEEP_BETWEEN_CALLS=2.0秒`節流。**下一輪TW軌接手**：先用`run_detached.py status`/`log 20260908-093146-c524`收成本輪投遞的job（預期處理250天，跑完後累積約1750/2609≈67.1%，仍未到100%）；未跑完就再投遞下一批`--batch-size 250`（**務必帶`--cwd "C:\alpha\alpha-app\research"`**，腳本會自動跳過已快取日期）；跑完100%後才進`#57`第1關sanity（比照`#53`/`#54`/`#55`同一套`cross_sectional_*_gate5X.py`框架）。完整見`REPORT.md`第437輪心跳、`data/jobs/20260908-092309-48ca.log`（失敗紀錄）、`data/jobs/20260908-093146-c524.log`（修正後正常執行紀錄）。

---

**上一則保留（第432輪，供對照）**——取鎖乾淨（cycle`20260908-060037`）。開工前`git status`發現4個未commit異動：`research/HYPOTHESIS_QUEUE.md`（hypothesis_queue排程`#55`結案文字，`TRIALS_LEDGER.md`/`STRATEGY_GRAVEYARD.md`/`TRIALS_REGISTRY.jsonl`#198已在前一個commit`4b66554`入帳但這份文件本身遺留未commit）、`data/sector_flow.json`/`scripts/build_sector_flow.py`/`research/dev_queue_cycle.log`（`dev_queue_cycle.log`本身註明「互動視窗正在做事，本輪跳過」，判斷是另一條獨立的金流一.2互動開發工作，本輪未觸碰，保留原狀）。嘗試補commit `HYPOTHESIS_QUEUE.md`時發現hypothesis_queue排程自己已搶先在同一分鐘完成該commit（`388db8f`），核對內容確認一致、非衝突。三軌時間戳：TW 05:00（round430，最舊）／US 05:31（round431，最新）——依輪替選TW（FUT依例外條款不選）。`run_detached.py status`確認heavy-job-slot空（0個running）。**本輪工作單位＝`HYPOTHESIS_QUEUE.md`「五條執行順序」排隊第一的`#57`（逐檔當沖比重截面離散度）前置查證與回填**（`#53`/`#54`/`#55`已於round430/431前全數結案FAIL，`#57`卡在「前置未備」）。三來源查證（`docs/DATA_SOURCE_MAP.md`新增條目）發現**原本判斷錯誤**：`#57`不需要新端點——`#37`已在用的`TWTASU`端點（`https://www.twse.com.tw/rwd/zh/afterTrading/TWTASU`）回應本來就是逐檔列表（本輪實測`date=20260904`回傳1332列，個股列如`2330 台積電`，最後一列才是「合計」），只是`fetch_day_trading_ratio_day()`（`#37`用）刻意只取合計列丟棄逐檔列。原本懷疑的另一端點`TWTB4U`經openapi swagger（`Date`/`Code`/`Name`/`Suspension`四欄）+GitHub社群實作（`twjackysu/TWSEMCPServer`）交叉核對，確認是「當沖資格清單」不含量值，是名稱相近但不同的資料集。新增`twse_day_trading_client.py::fetch_day_trading_detail_day()`（逐檔版本，跟`#37`共用同一端點與反爬蟲偵測但快取到獨立目錄`data/raw_twse_day_trading_detail/`，不影響`#37`既有快取）＋`backfill_day_trading_detail.py`（同一套`SLEEP_BETWEEN_CALLS=2.0秒/次`節流）。**代價**：`#37`既有回補的原始逐檔JSON從未落地存檔，2015-01-01~VAL_END共2,609個交易日須重新呼叫一次TWTASU，非重工。回填已用`run_detached.py`投遞（job`20260908-061051-6640`，`--batch-size 250`，本輪`wait --max-min 2`確認正常執行中，處理中未等待收成）。已更新`HYPOTHESIS_QUEUE.md`#57條目（狀態改為「資料源已查證可得，回填進行中」）。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）確認PASS（exit=0，200列，下一可用編號#199，本輪無新判定不涉及登記）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程僅本輪查證階段呼叫TWSE端點約5次（含2014/2013年歷史涵蓋度確認），回填階段的API呼叫由背景job執行、遵守既有節流。**下一輪TW軌接手**：先用`run_detached.py status`/`log 20260908-061051-6640`收成本輪投遞的job（預期還沒跑完2,609天），未跑完就再投遞下一批`--batch-size 250`（腳本會自動跳過已快取日期），跑完後才進`#57`第1關sanity（比照`#53`/`#54`/`#55`同一套`cross_sectional_*_gate5X.py`框架）。完整見`REPORT.md`第432輪心跳、`docs/DATA_SOURCE_MAP.md`「全市場現股當沖逐檔成交量值」條目、`twse_day_trading_client.py`/`backfill_day_trading_detail.py`（新增，可重複執行）。

---

**上一則保留（第430輪，供對照）**——取鎖乾淨（cycle`20260908-050037`）。開工前`git status`發現`research/TRIALS_LEDGER.md`有1行未commit差異：hypothesis_queue排程的commit`e10e2c8`（#53第2關結案FAIL）登記了`TRIALS_REGISTRY.jsonl`#194但漏了對應的`TRIALS_LEDGER.md`markdown列，逐字核對兩邊內容一致後直接commit補齊，非重工。三軌時間戳：TW 04:00（round428，最舊）／US 04:30（round429）——依輪替選TW（FUT依例外條款不選）。`run_detached.py status`確認heavy-job-slot空（0個running）。**本輪工作單位＝`HYPOTHESIS_QUEUE.md`「五條執行順序」排隊第一的`#54`成交值集中度速度（Turnover Concentration Velocity）第1關sanity**（規格已於round428前寫死，本輪首次開跑）。新增`turnover_concentration_gate54.py`：沿用`cross_sectional_dispersion_gate53.py::_discover_stock_files()`同一套2,111檔候選池，逐檔`Trading_money`算日內占比HHI（`conc_t=Σw²`）與20日速度版，另用`data/raw_twse_market_volume/FMTQIK_*.parquet`（TWSE官方FMTQIK月檔）做涵蓋率交叉檢查（<80%標不可用，2015-01前FMTQIK無資料的33.7%天數比照排除），零新增API呼叫，執行約27秒。**結果：sanity1(非退化)PASS；sanity2(危機窗口)僅1/3通過(門檻>=2/3)，2018Q4/2020Q1皆方向不符；sanity3(前瞻報酬方向)level與vel兩版皆方向反轉(高集中度組前瞻報酬反而更高)——兩項獨立檢定皆與事前綁定方向相反，非訊號太弱**。依`MARATHON_PROTOCOL.md`1a節「沒過便宜關卡不調參數硬救，立刻FAIL換下一個」，本輪不進入GATE_SEQUENCE第2關即判**FAIL**，登記`TRIALS_LEDGER.md`#195。**與#53相關係數同步量出**：level_pctile/vel_pctile相關係數分別為+0.0535/+0.0570，遠低於0.7門檻，確認#53/#54為獨立發現非同家族。已更新`HYPOTHESIS_QUEUE.md`#54條目與「五條的執行順序」（移出#54，換#55排隊第一）、`STRATEGY_GRAVEYARD.md`新增條目。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）確認PASS（exit=0，197列，下一可用編號#196）。`is_holdout_consumed()`開工/收工前皆確認`False`，全程零新增API呼叫（複用#53已快取parquet與既有FMTQIK月檔）。**下一輪TW軌接手**：`#55`三大法人買賣超截面離散度速度——開跑前須先做T86資料缺口二選一裁定（`HYPOTHESIS_QUEUE.md`#55條目「資料缺口」段：回填2024-06-25~12-31的126個交易日，或事前把VAL截到2024-06-24，二選一寫死不得先跑再挑），且T86只涵蓋上市不含上櫃，全市場一律寫成「全上市市場」。完整見`REPORT.md`第430輪心跳、`TRIALS_LEDGER.md`#195、`turnover_concentration_gate54.py`（新增，可重複執行）、`data/turnover_concentration_gate54.csv`（新增）。

---

（第428輪已於round437歸檔至`TW_STATE_ARCHIVE.md`，僅保留最新3則）
