# TW_MARATHON_STATE.md — 台股軌斷點狀態（覆寫式）

**這份檔案只描述台股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `TW_LOG.md`；候選判定看 `TW_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `TW_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-09T06:00+08:00（馬拉松第475輪）**——取鎖乾淨（cycle`20260909-060037`）。開工查`run_detached.py status`：round474開工時running的800天回補job（`20260909-052241-9b09`）背景持續推進，session內以`run_detached.py wait --max-min 3`（背景）等到`finished`（41.3分鐘，exit=0），`data/raw_twse_block_trade/`累積到1600個交易日快取（2015-01-01～2021-02-17，**首次跨過`TRAIN_END=2020-12-31`進入VAL期**）。**本輪工作單位＝#62正式N=200判定**：先用`--n-permutations 5`快篩發現VAL期`n_val_usable=177`（N5/10/20皆同）已遠超20門檻（比`hypothesis_queue`排程同時段用`--max-events 500`抽樣smoke test誤判`n_val=0`更準確——差異純粹是抽樣限制把VAL期事件抽掉了，非資料本身不足，已在`HYPOTHESIS_QUEUE.md` #62(h)段落說明避免下一輪誤判為矛盾）；接著跑`python -u block_trade_gate62.py --n-permutations 200`（無`--max-events`限制，全部19027筆聚合配對交易事件，實測僅47秒完成），最終VAL期n_val=237（三個N值皆同）。**結果：N5百分位0.8、N10百分位7.8、N20百分位0.8，三個N值方向一致朝反方向（VAL期mean_post_ret皆為正而非事前假設的負），全部FAIL**，非邊緣case。已`register_trial()`登記`TRIALS_LEDGER.md`#224、`STRATEGY_GRAVEYARD.md`新增段落、`TW_LEADS.md`#16、`data/signal_status.json`新增`62`節點。`trial_registry.py --check`用`PYTHONIOENCODING=utf-8`確認`exit=0`PASS（226列）。**`#62`gate1整體結案為FAIL**，第⑫類「知情大額交易跟隨/資訊不對稱」機制大類0勝1敗結案。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增FinMind/SEC EDGAR呼叫（純讀既有回補快取），僅本機`run_detached.py`查詢。**下一輪任一軌接手**：`MARATHON_PROTOCOL.md`0a節四條方向（#49/#51/#52已FAIL、#50卡tick 2/20未解鎖）與本輪新增第⑫類（#62）皆已結案或卡外部依賴，尚未到可下「無可驗證預測優勢」結論的時點（僅`#50`待tick累積）。`hypothesis_queue`排程另投遞的job`20260909-060428-cae6`（800天批次，為#62回補的延續，已結案不再需要但無害）若仍在跑可略過不必等待。建議下一輪查核US/FUT軌是否有新方向可查證，若皆無，協助盤點`#50`tick累積現況（`research/data/ticks/`）。完整見`HYPOTHESIS_QUEUE.md` `#62`條目(h)段落、`REPORT.md`第475輪心跳、`block_trade_gate62.py`（可重複執行）。

---

**上一則保留（第474輪，供對照）**——取鎖乾淨（cycle`20260909-053037`）。三軌時間戳：TW 04:30（round472，最舊）／US 05:00（round473，最新）——依輪替選TW；FUT依例外條款不選。開工先跑`run_detached.py status`：round472投遞的`block_trade_backfill_62tw`（job`6457`，700天batch）已`finished`（38.7分鐘，exit=0），但獨立自動化排程`hypothesis_queue`已在本輪開工前搶先投遞下一批800天job（`20260909-052241-9b09`，commit`d4149f1`），本輪開工時已`running`8.4分鐘、log顯示`150/800`正常推進中，非卡死。heavy-job-slot已被佔用，依`MARATHON_PROTOCOL.md`0b節第3點不加`--allow-concurrent`硬跑第二個重度工作。查`data/raw_twse_block_trade/`目前972個交易日快取（2015-01-01～2018-09-21，全在TRAIN期）；`data/ticks/`仍僅2個parquet（`#50`門檻2/20未解鎖，非本輪可推進，被動累積）；`trial_registry.py --check`用`PYTHONIOENCODING=utf-8`重跑確認`exit=0`（PASS，225列，本輪無新判定；順帶記錄：此腳本在本機預設cp950主控台編碼下印出`⚠`字元會`UnicodeEncodeError`直接崩潰噴exit=1，是純console編碼顯示問題非登記邏輯錯誤，非本輪工作範圍不修，供下一輪知悉避免誤判為登記閘門真的擋下）；`is_holdout_consumed()`確認`False`。**本輪工作單位＝誠實查核輪，不勉強生新工作**（比照round473 US軌同款判斷）：TW軌目前唯一在製品`#62`回補正由`hypothesis_queue`背景job推進、且尚未到VAL期（2021-2024）覆蓋門檻，這一輪若要有進度只能等回補累積或協助收成，兩者現在都做不了——job未完成無結果可收，heavy-job-slot被佔用不能疊加新回補。**下一輪任一軌接手**：先查`run_detached.py status`確認job`20260909-052241-9b09`是否已`finished`（60分鐘timeout內，800天*約2秒≈27分鐘+開銷，預估約06:00前後完成）；若完成，查`data/raw_twse_block_trade/`累積天數（完成後理論上約1600/2609天，粗估可能已越過TRAIN_END進入VAL期前段，但需實測確認，別假設）並視VAL期覆蓋度決定是否重跑`block_trade_gate62.py`（若VAL期事件數仍<20則繼續等下一批，不勉強硬跑）；若VAL期仍全空，`hypothesis_queue`排程應會自動接續投遞下一批，本輪不需要手動介入；若job失敗/timeout，查log後視情況調整batch-size或timeout-min。完整見`HYPOTHESIS_QUEUE.md` `#62`條目、`REPORT.md`第474輪心跳。

---

**上一則保留（第472輪，供對照）**——取鎖乾淨（cycle`20260909-043037`）。三軌時間戳：TW 04:00（round471，最新）／US 03:30（round470，最舊）——依輪替本應選US，但US軌round470心跳已明確表態「US軌四條新方向自身能做的部分已無剩餘工作單位」（#49/#51/#52-US皆FAIL、#50卡外部依賴非US軌能解），依協定第0節第3步「若某軌state明確寫著卡住了、下一輪換別軌」跳過US；FUT依例外條款不選；故選TW。開工先跑`run_detached.py status`確認`running=0`，heavy-job-slot空；查`research/data/ticks/`仍僅2個parquet（`#50`門檻2/20未解鎖，非本輪可推進）；`is_holdout_consumed()`確認`False`。**本輪工作單位＝依round471「下一輪TW軌接手」交辦，設計#62（Block Trade跟隨訊號）第1關cheap gate**。開工發現工作目錄已有兩個未提交的既有檔案`twse_block_trade_client.py`（BFIAUU端點client，含反爬蟲封鎖偵測、原子寫入快取）與`backfill_block_trade.py`（可重複呼叫的批次回補腳本），研判是上一輪（第471輪）資料可行性查證過程中順手寫的基礎設施，但當輪心跳只提到「純資料可行性查證未跑訊號數字」未提及寫檔案且未commit——本輪判定這兩個檔案品質良好（同`twse_t86_client.py`/`backfill_day_trading_ratio.py`同款框架，含編碼備註/反爬蟲/斷點續傳），與#62(a)(b)段落查證結果完全一致，予以承接沿用，本輪一併commit（不算「順手重構」，是接續前一輪未完成的委託）。查`data/raw_twse_block_trade/`已有100個交易日快取（2015年初，全部落在TRAIN期）。**內容**：(1)投遞背景回補job延續累積：`run_detached.py submit --name block_trade_backfill_62tw --timeout-min 40 -- python -u research/backfill_block_trade.py --batch-size 700`（第一次submit因誤用相對路徑`backfill_block_trade.py`失敗`exit=2`找不到檔案，run_detached.py的cmd是相對`alpha-app`根目錄執行非`research/`，改用`research/backfill_block_trade.py`路徑後正常送出，job`20260909-043537-6457`，送出後15秒確認`running`狀態）；(2)新增`block_trade_gate62.py`：依`HYPOTHESIS_QUEUE.md`#62(a)(b)段落事前綁定——只取`trade_type==配對交易`、按(stock_id,date)聚合避免同日多筆偽複製、方向代理=vwap相對當日收盤價（vwap<close→sell-initiated，僅測sell方向未來N日市場調整報酬，事前假設為負，buy-initiated僅記錄對照不判定）、N測5/10/20三個值各自獨立判定、控制組matched_stock/unmatched_universe兩變體N=200（比照`material_news_car_gate.py`框架，`_signed_ret()`直接reuse自`material_news_car_gate2_continuation.py`零程式碼歧異）、依`control_group_standard.py`規則因指標「越小越好」對signal與control同步取負號後比較。**smoke test**：`--max-events 500 --n-permutations 25`，管線正確執行無崩潰——500筆聚合事件、90/117檔可用價格、386筆可判斷方向（sell=202/buy=184），三個N值VAL期事件數皆為0（現有回補資料只到2015年初，全部在TRAIN期，VAL期2021-2024完全沒有覆蓋），跳過邏輯正確觸發（`n_val<20`），非bug、是資料未就緒的誠實結果。**未跑正式N=200判定**：VAL期0事件無法判定，統計檢定力為零，跑了也沒有意義，誠實記錄「等回補job推進到VAL期範圍才能判定」，不勉強硬跑。`trial_registry.py --check`本輪無新判定（無PASS/FAIL可登記）。全程零新增FinMind/SEC EDGAR呼叫，對`www.twse.com.tw`因背景回補job新增約700次請求上限（2秒/次節流，官方公開JSON端點，同`backfill_day_trading_ratio.py`已驗證安全的請求間隔）。**下一輪TW軌接手**：先查`run_detached.py status`確認job`20260909-043537-6457`是否已`finished`（40分鐘timeout內，700天*2秒≈23分鐘+開銷）；若完成，查`data/raw_twse_block_trade/`累積天數是否已涵蓋部分VAL期（2021年後），若有則重跑`block_trade_gate62.py`（不限max-events，正式N=200）看初步方向；若VAL期仍完全空白，判斷是否需要再投遞一批（累計~2500個交易日，700一批需約4批才能到VAL_END）；若job失敗或timeout，查log後視情況調整batch-size或timeout-min。完整見`HYPOTHESIS_QUEUE.md` `#62`條目、`REPORT.md`第472輪心跳、`block_trade_gate62.py`／`twse_block_trade_client.py`／`backfill_block_trade.py`（皆可重複執行）。

（第430/432/437/439/441/443/445/447/449/451/453/455/457/459/461/463/465/467/469/470/471輪已歸檔至`TW_STATE_ARCHIVE.md`，僅保留最新3則）
