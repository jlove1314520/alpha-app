# TW_MARATHON_STATE.md — 台股軌斷點狀態（覆寫式）

**這份檔案只描述台股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `TW_LOG.md`；候選判定看 `TW_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `TW_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-09T13:00+08:00（馬拉松第489輪）**——取鎖乾淨（cycle`20260909-130036`）。三軌時間戳：FUT 10:30（round484，最舊，但無新機制、依例外條款不選）／TW 12:00（round487）／US 12:30（round488，最新）——FUT跳過後依輪替選TW（TW較US舊）。開工查`run_detached.py status`：`running=0`，無heavy-job-slot佔用。**核實候選池現況不變**：0a節四條方向＋`#61`~`#65`全數結案，僅`#50`仍卡tick累積——實測`data/ticks/`本輪仍僅2個完整交易日parquet（`20260907`/`20260908`），較round487無變化，距20日門檻仍差18個交易日。**本輪工作單位＝`data/signal_status.json`完整性維護（純資料/文件補齊，比照round485先例）**：round488心跳確認`hypothesis_queue`排程已於12:22正式將`#65`（產業龍頭股跨期領先-落後動能）判定FAIL並登記`TRIALS_LEDGER.md`/`TRIALS_REGISTRY.jsonl`#229、寫入`STRATEGY_GRAVEYARD.md`（`f_leader_follower_lag`條目），但`build_signal_status.py`的`DIRECTIONS`常數尚未補上這一條，會讓App將來對使用者揭露的「測過、沒用」清單漏掉`#65`。已逐字核對`STRATEGY_GRAVEYARD.md`的`f_leader_follower_lag`條目內容後，比照`#62`/`#63`/`#64`格式新增對應段落（含`note`欄位註明「非0a節四條方向之一」），重跑`build_signal_status.py`驗證：`directions`清單`ids=['49','50','51','52','62','63','64','65']`共8條，`git diff -b`確認除新增段落外無其他內容被更動。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（231列，撞號2組皆為歷史存量不回頭改寫）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純核對既有結案文件）。**下一輪任一軌接手**：`#50`tick累積仍是唯一活動候選，純被動等待（查`data/ticks/`是否已到3/20）；`hypothesis_queue`排程下一輪會自行設計`#66`（美股年末稅損收割/一月效應，見`HYPOTHESIS_QUEUE.md`尾端，尚未跑第1關），非本馬拉松三軌工作範圍，屆時可核對其設計是否符合「真正跳脫既有清單」標準即可；若TW/US/FUT三軌本輪皆無新工作單位可做，且找不到真正跳脫既有清單的新機制，下一次盤點時應誠實評估是否已接近0a節「無可驗證預測優勢」正式結論的提報時機（但`#50`未結案前嚴格說還不到門檻）。完整見`data/signal_status.json`（本輪新增`65`一條）、`build_signal_status.py`（本輪新增對應`DIRECTIONS`段落）、`REPORT.md`第489輪心跳。

---

**上一則保留（第487輪，供對照）**——取鎖乾淨（cycle`20260909-120036`）。三軌時間戳：FUT 10:30（round484，最舊，但無新機制、依例外條款不選）／TW 11:00（round485）／US 11:30（round486，最新）——FUT跳過後依輪替選TW。開工查`run_detached.py status`：`running=0`，無heavy-job-slot佔用（`run_detached.py`自身登記簿沒有工作在跑）。**核實候選池現況不變**：0a節四條方向＋`#61`/`#62`/`#63`/`#64`全數結案，僅`#50`仍卡tick累積——實測`data/ticks/`本輪仍僅2個完整交易日parquet（`20260907`/`20260908`），較round485/486無變化，距20日門檻仍差18個交易日。**本輪發現並處理一次真實的跨排程重疊事故（非新假設，是流程異常處理）**：開工時`git status`看到未追蹤檔案`research/factor_ic_leader_follower_lag.py`——`HYPOTHESIS_QUEUE.md`核對得知這是`hypothesis_queue`排程（獨立於本馬拉松的另一支自走腳本）在2026-09-09T10:56設計的全新假設`#65`（產業龍頭股跨期領先-落後動能，Hou 2007資訊擴散機制，經濟機制與已FAIL的`#11`同期橫斷面相對強度明確不同，屬真正跳脫既有清單的新機制，非換皮），佇列排隊第一、尚未跑過第1關。**本輪原本直接執行該腳本以推進第1關，執行後用`Get-CimInstance Win32_Process`查行程才發現`hypothesis_queue`排程本身已經在背景跑同一支腳本**（PID`141224`，`python -u factor_ic_leader_follower_lag.py > /tmp/gate65_run.log`，是另一支自走排程自己開的行程，非本輪`run_detached.py`登記簿管理範圍，故`run_detached.py status`本輪一開始沒有顯示它）——**立即判定重複計算，找出並終止本輪自己另外啟動的那個重複行程（PID`141012`），保留`hypothesis_queue`原有的行程繼續跑，避免CPU互搶與結果混淆**。查`/tmp/gate65_run.log`，該背景行程仍在「載入300檔樣本＋計算因子快取」階段（`CALIBRATION_PROBE.md`記錄首次載入約13分鐘），尚未跑完，本輪未等待（依協定不在session內等超過5分鐘），也未動`factor_ic_leader_follower_lag.py`檔案內容（該腳本邏輯完整可重複執行，本輪僅新增至版控，不修改）。**教訓（寫給下一輪）**：`run_detached.py status`只涵蓋本框架自己登記的工作，`hypothesis_queue`排程有自己獨立的背景執行機制，兩者可能在候選池收斂到同一個新假設時發生時間重疊；下一輪接手`#65`前，**務必先`Get-CimInstance Win32_Process | Where-Object CommandLine -like '*<腳本名>*'`或看`/tmp/gate65_run.log`是否已有結果**，不要直接重跑。`trial_registry.py --check`本輪無新判定（純流程處理，未觸發`register_trial()`）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（重複行程被殺前尚未完成任何API request，且原本設計就是零新增API呼叫、純讀既有300檔快取）。**下一輪任一軌接手**：查`/tmp/gate65_run.log`是否已印出`train:`/`val:`結果行，若已完成則核對輸出、`register_trial()`登記`#65`第1關結果並寫入`HYPOTHESIS_QUEUE.md`/`TW_LEADS.md`（或`FUT_LEADS.md`視哪一軌接手，因為這是`hypothesis_queue`排程產物，非任一軌獨佔）；同時查`data/ticks/`是否已到3/20。完整見`research/factor_ic_leader_follower_lag.py`（本輪新增至版控，未修改內容）、`/tmp/gate65_run.log`（背景執行中，非版控檔案）、`REPORT.md`第487輪心跳。

---

**上一則保留（第485輪，供對照）**——取鎖乾淨（cycle`20260909-110038`）。三軌時間戳：TW 09:30（round482，最舊）／US 10:00（round483）／FUT 10:30（round484，最新，依例外條款觸發，`#64`已FAIL結案）——依輪替選TW。開工查`run_detached.py status`：`running=0`，無heavy-job-slot佔用。**核實候選池現況**：`MARATHON_PROTOCOL.md`0a節四條方向（#49/#51/#52皆FAIL）＋`#61`/`#62`/`#63`/`#64`全數結案，僅`#50`（容量受限小型股）仍卡tick累積——實測`data/ticks/`目前2個完整交易日parquet（`20260907.parquet`/`20260908.parquet`，`20260909`為當日進行中），距20日門檻仍差18個交易日，純被動等待，本輪無演算法可推進之處，尚未到0a節「無可驗證預測優勢」提報門檻（`#50`本身未結案）。**本輪工作單位＝data/signal_status.json完整性維護（純資料/文件補齊，非新變更，比照round481先例）**：發現`build_signal_status.py`的`DIRECTIONS`常數只補了`#62`（round470/475先例），但同樣已正式結案FAIL的`#63`（借券費率異常飆升，round482結案）與`#64`（台指期貨基差regime，round484結案）尚未補入，會讓App將來對使用者揭露的「測過、沒用」清單漏掉這兩條。已比照`#62`條目格式，逐字核對`STRATEGY_GRAVEYARD.md`的`f_lending_fee_spike`與`fut_basis_regime_gate64`兩則條目內容後新增對應段落（含`note`欄位註明「非0a節四條方向之一」），重跑`build_signal_status.py`驗證：`directions`清單`ids=['49','50','51','52','62','63','64']`共7條，`git diff -b`確認除新增段落外無其他內容被更動。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`避開round484記錄的cp950 emoji編碼問題）exit=0 PASS（230列，撞號2組皆為歷史存量不回頭改寫）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純核對既有結案文件）。**下一輪任一軌接手**：`#50`tick累積仍是唯一活動候選，純被動等待（每日開盤結束後`data/ticks/`會新增一個parquet，下一輪查是否已到3/20）；若TW/US/FUT三軌本輪皆無新工作單位可做，且找不到真正跳脫既有清單的新機制，下一次盤點時應誠實評估是否已接近0a節「無可驗證預測優勢」正式結論的提報時機（但`#50`未結案前嚴格說還不到門檻）。完整見`data/signal_status.json`（本輪新增`63`/`64`兩條）、`build_signal_status.py`（本輪新增對應`DIRECTIONS`段落）、`REPORT.md`第485輪心跳。

（第430/432/437/439/441/443/445/447/449/451/453/455/457/459/461/463/465/467/469/470/471/472/474/475/477/478/480/482輪已歸檔至`TW_STATE_ARCHIVE.md`，僅保留最新3則）
