# TW_MARATHON_STATE.md — 台股軌斷點狀態（覆寫式）

**這份檔案只描述台股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `TW_LOG.md`；候選判定看 `TW_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `TW_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-07T03:00+08:00（馬拉松第422輪）**——取鎖乾淨（非陳舊鎖檔，cycle`20260907-030037`）。三軌時間戳：TW 02:04（round420，最舊）／US 02:30（round421）／FUT 12:00（round399，依例外條款不選，除非有全新機制假說）——依輪替選TW。`run_detached.py status`確認heavy-job-slot空（0個running），無背景工作衝突；`git status`確認無其他行程正在編輯同一批檔案（`data/rate_limit_state.json`/`.github/workflows/audit.yml`/`research/.live_watchlist.json`/`research/DEV_QUEUE_PROMPT.txt`/`research/data_cache/`/`research/dev_queue_cycle.log`為其他既存未commit異動，本輪未觸碰，保留原狀）。**本輪工作單位＝round420「下一步(iii)」：設計`HYPOTHESIS_QUEUE.md`#51子事件1（融券強制回補）具體事件研究第1關cheap gate**（子事件1反推公式已於round419-421用2330/1808真實個股核對通過，本輪首次寫成可重複執行測試）。新增`forced_short_covering_gate1.py`：`factor_ic.py`同一個300檔樣本(SAMPLE_SEED=20260822，248/300已有本機融券快取、52檔本輪新抓)，套用驗證過的反推公式（`停止過戶日=CashExDividendTradingDate後第2交易日`、`強制回補視窗=[停止過戶日-6交易日,停止過戶日-3交易日]`），測「視窗開始前既有融券部位大小（`ShortSaleTodayBalance/ShortSaleLimit`）是否預測視窗內超額報酬」（事前綁定方向為正——部位越大、被迫買盤越大），Spearman IC+VAL期洗牌null N=200，跟已測過的`#30`/`#36`（無條件橫斷面融資/融券使用率因子）是完全不同的事件條件式交互作用假說，非重測。**結果：現金股利除息事件1984筆，窗口+融券資料皆可用1707筆（`share_zero`=82.2%）。TRAIN IC=+0.0192(p=0.54)、VAL IC=-0.0373(p=0.34)，正負號不一致，null percentile=19.0（門檻>=90.0），四個事前綁定判準全數未過**。**判定：FAIL（僅這個連續比例規格）**——不影響子事件1反推公式本身的驗證（那是PIT可得性問題，本輪測的是機制對報酬有沒有預測力，是獨立的問題）。誠實揭露：82.2%事件short_ratio為零，樣本高度右偏，未測「有無融券部位」二元對照這個可能更合適的規格。已寫入`TRIALS_LEDGER.md`#186、`STRATEGY_GRAVEYARD.md`、`HYPOTHESIS_QUEUE.md`#51(e)段落。`is_holdout_consumed()`開工/收工前皆確認`False`，全程零新增付費/需登入API呼叫。**另誠實記錄一項欠款**：`MARATHON_PROTOCOL.md`0a節要求#50/#51/#52/#49成績（含FAIL）都要進`data/signal_status.json`供App公開，本輪盤點確認該檔案至今仍不存在，未回補（非本輪範圍，schema設計需要獨立想清楚），下一輪或研究帽應優先處理。**下一輪TW軌接手**：(i) 若判斷值得追加，子事件1可測二元規格（`short_ratio>0`vs`==0`兩組比較mean_CAR）；(ii) 續攻子事件3 MOPS查證（`HYPOTHESIS_QUEUE.md`#51(d)段落列的(1)/(2)/(3)三候選方向）；(iii) 改用子事件2（現金增資，已確認可行）設計對等的第1關cheap gate；(iv) 設計`data/signal_status.json`schema並回補#49/#51已有的FAIL結果。完整見`TW_LOG.md`第422輪記錄、`TRIALS_LEDGER.md`#186、`HYPOTHESIS_QUEUE.md`#51、`forced_short_covering_gate1.py`（新增，可重複執行）。

---

**上一則保留（第420輪，供對照）**——取鎖乾淨（非陳舊鎖檔，cycle`20260907-020036`）。三軌時間戳：TW 23:30（round416，最舊，另有獨立`hypothesis_queue`排程於00:00~01:29多次即時推進#49並已結案FAIL，commit`582ef0d`）／US 00:30（round418）／FUT 12:00（round399，依例外條款不選，除非有全新機制假說）——依輪替選TW。`run_detached.py status`確認heavy-job-slot空（0個running），無背景工作衝突；`git status`確認無其他行程正在編輯同一批檔案。**本輪工作單位＝`HYPOTHESIS_QUEUE.md`#51（強制交易者事件）子事件1「反推重建」備用路徑查證，接續round419前一批hypothesis_queue排程留下的「(b)查證子事件1反推重建備用路徑的官方交易日數規則」待辦**：純WebSearch查證+本機既有快取欄位盤點，零新增API呼叫。查得(1)官方法規資料庫`twse-regulation.twse.com.tw`《證券商辦理有價證券買賣融資融券業務操作辦法》第76條：停止融券賣出始於停止過戶前6個營業日、還券了結截止於停止過戶第6個營業日（含）；(2)證券投資人及期貨交易人保護中心網站同一條規則的教育版本，數字一致；(3)多個來源（含《公司法》第165條基準日前5日內停止過戶的法定框架）確認台灣T+2交割下「除權除息交易日＝停止過戶日前2個營業日」。盤點本機已有`TaiwanStockDividend`快取（2010-2024，2170檔，`CashExDividendTradingDate`/`StockExDividendTradingDate`欄位，`adjust.py`既有管線在用）確認**足以反推**：停止過戶日=ExDividendTradingDate+2營業日，強制回補截止日=停止過戶日-6營業日=ExDividendTradingDate-4營業日。**判定：子事件1從round419留下的「未死透待查」改判「可行（待實作驗證）」**——不需要抓預告表端點即可重建強制回補時間表，零新增API呼叫。因來源屬WebSearch（官方法規資料庫+投資人保護中心，2類），未達`CLAUDE.md`「四類來源涵蓋三類」滿門檻，**下一輪實作前先用少量真實個股手動核對推算結果跟BFI84U即時預告表是否吻合**，吻合即視為驗證通過。已寫入`HYPOTHESIS_QUEUE.md`#51子事件1新增段落。`is_holdout_consumed()`開工/收工前皆確認`False`。完整見`TW_LOG.md`第420輪記錄、`HYPOTHESIS_QUEUE.md`#51。

---

**上一則保留（第416輪，供對照）**——取鎖乾淨（非陳舊鎖檔）。三軌時間戳：TW 22:00（round413，最舊）／US 23:00（round415）／FUT 12:00（round399，依例外條款不選，除非有全新機制假說）——依輪替選TW。`run_detached.py status`確認heavy-job-slot空（0個running），無背景工作衝突。**本輪工作單位＝`HYPOTHESIS_QUEUE.md`#49（日內/隔夜報酬結構分解）必要前置工程**：round415的hypothesis_queue排程已指出`adjust.py::adjusted_price_series()`的FinMind回退路徑只還原`close`（產生`adj_close`），`open`/`high`/`low`維持原始未還原，直接拿現有`open`算隔夜報酬會被除權息事件污染。本輪擴充該函式：(1) yfinance路徑新增`adj_open`/`adj_high`/`adj_low`三欄（別名自已調整的`open`/`high`/`low`，因`auto_adjust=True`本身就同步調整OHLC）；(2) 用真實資料（`2330.TW` 2024年241個交易日，`auto_adjust=True` vs `False`兩次抓取比對）**實測驗證**yfinance確實對open/close套用同一乘數因子（逐日比對誤差<1e-6），不是只憑docstring假設；(3) FinMind回退路徑重構為先算單一`factor_cum`累積因子序列再分別套用到close/open/high/low（原本只對close算），用合成資料證明重構後`adj_close`數值與原邏輯逐位元相同（無回歸）；(4) **過程中抓到一個新bug並修正**：FinMind原始`TaiwanStockPrice`欄位其實是`max`/`min`（不是`high`/`low`），若照docstring字面寫`raw["high"]`會直接`KeyError`——用真實2330資料(2023-01-01起，8筆除權息事件)跑過FinMind回退路徑全流程確認`adj_open`/`adj_high`(`max`)/`adj_low`(`min`)三欄的還原因子都跟`adj_close`一致，無crash。全程零新增API呼叫（yfinance走既有快取，FinMind走`load_dev`既有快取），純本地計算+驗證，未跑任何回測或cheap gate。`is_holdout_consumed()`開工/收工前皆確認`False`。完整見`TW_LOG.md`第416輪記錄、`HYPOTHESIS_QUEUE.md`#49、`adjust.py`（修改，`adjusted_price_series()`新增`adj_open`/`adj_high`/`adj_low`三欄）。

---

（第413輪已於round422歸檔至`TW_STATE_ARCHIVE.md`，僅保留最新3則）
