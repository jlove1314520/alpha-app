# TW_MARATHON_STATE.md — 台股軌斷點狀態（覆寫式）

**這份檔案只描述台股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `TW_LOG.md`；候選判定看 `TW_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `TW_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-15T07:00+08:00（馬拉松第531輪）**——取鎖乾淨（cycle`20260915-070037`）。開工先照CLAUDE.md「交辦優先於自走」鐵律讀`PENDING_QUEUE.md`：兩條阻塞項（S4U排程註冊／claude CLI非互動驗證，等待總司令有管理員權限）維持阻塞、gate50三條件仍待總司令釐清具體定義原文、題材七維持部分完成待續（由`hypothesis_queue`軌持續推進，非本馬拉松三軌範圍），**交辦佇列無可執行未開始項，回落自走**。依round530建議依輪替選TW。查`run_detached.py status`：`running=0`（60筆歷史紀錄，無新增）；`git log`確認round530後除round530自身commit外，僅IBKR報價自動排程commit與`hypothesis_queue`軌題材七待辦4 commit（`7e95b46f`）、`轉向.四`commit（`8fb5eb35`），均非本馬拉松三軌範圍，未動`alpha.db`/`fetch.py`/`parsers.py`/`config.py`凍結區；`git status`僅既有`dev_queue`軌未追蹤檔案（`.devqueue.lock`）在動，本輪未觸碰、未越權處理。**本輪額外複查`CALIBRATION_PROBE.md`操作指令清單（`#77`/`#79`/`#91`/`#47`/`#52`/`#34`）是否有殘留未結案項**：grep `TRIALS_LEDGER.md`/`TW_LOG.md`確認全部已於round327~373完成複驗並改判定案（`#79`/`#77`確定FAIL、`#91`整體FAIL但高關注度子組留待未來複驗、`portfolio_multifactor_v2`全家族含leave-one-factor-out/train-only權重/獨立樣本外複驗共12種以上具體構造已於round373正式整併結案，見`STRATEGY_GRAVEYARD.md`1713行條目，明文記載「優先權應轉向尋找新的成分因子候選，而非在同一組已知無顯著alpha的因子上疊加regime overlay/下檔保護」），無殘留可做工作。**核實現狀**：`data/ticks/`累積進度仍**6/20**（`20260907`/`08`/`09`/`10`/`11`/`14`六個交易日已finalize，與round529/530一致，距20日仍差14日，今日09/15盤中tick尚未到隔日finalize時點）；`PENDING_QUEUE.md`gate50查證段落仍原封不動，總司令尚未回應三條件具體定義，`#50`維持未解鎖；`STRATEGY_GRAVEYARD.md`本馬拉松三軌自身最新結案仍為`#68`（round510），無新結案。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（244列，本輪未產生新試驗判定）。`validation/holdout.py::is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純讀既有`.md`/`.json`檔案與`git log`/`git status`/`run_detached.py status`）。**結論：候選池狀態與round527~530時實質相同，本輪額外確認`CALIBRATION_PROBE.md`舊清單無殘留、`portfolio_multifactor_v2`家族已窮盡不需重查，TW/US/FUT三軌自身仍無新可推進工作單位，僅剩`#50`被動等待總司令對gate50三條件的回應與tick累積（仍需14日）**。**下一輪任一軌接手**：`#50`gate50原文仍待總司令回應；FUT仍維持skip；依輪替下一輪建議選US軌。完整見`REPORT.md`第531輪心跳、`MARATHON_STATE.md`（輪次計數器531）。

---

**上一則保留（第529輪，供對照）**——取鎖乾淨（cycle`20260915-043037`）。開工先照CLAUDE.md「交辦優先於自走」鐵律讀`PENDING_QUEUE.md`：兩條阻塞項（S4U排程註冊／claude CLI非互動驗證）維持阻塞、gate50三條件仍待總司令釐清、題材七維持未拆解，**交辦佇列無可執行未開始項（0條未開始），回落自走**。依round528建議依輪替選TW。查`run_detached.py status`：`running=0`（60筆歷史紀錄，無新增）；`git log`確認round528後僅IBKR報價自動排程commit，未動`alpha.db`/`fetch.py`/`parsers.py`/`config.py`凍結區；`git status`僅既有`dev_queue`/`hypothesis_queue`軌未追蹤檔案在動，本輪未觸碰、未越權處理。**核實現狀**：`data/ticks/`累積進度仍**6/20**（`20260907`/`08`/`09`/`10`/`11`/`14`六個交易日已finalize，與round528一致，距20日仍差14日，今日09/15盤中tick尚未到隔日finalize時點）；`PENDING_QUEUE.md`gate50查證段落仍原封不動，總司令尚未回應三條件具體定義，`#50`維持未解鎖；`STRATEGY_GRAVEYARD.md`本馬拉松三軌自身最新結案仍為`#68`（round510），無新結案。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（243列，與round528一致，本輪未產生新試驗判定）。`validation/holdout.py::is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純讀既有`.md`/`.json`檔案與`git log`/`git status`/`run_detached.py status`）。**結論**：round528已確認4天週限額停擺為根因、非候選池問題；本輪核實候選池狀態與round527/528時實質相同，TW/US/FUT三軌自身仍無新可推進工作單位，僅剩`#50`被動等待總司令對gate50三條件的回應與tick累積（仍需14日）。**下一輪任一軌接手**：`#50`gate50原文仍待總司令回應；FUT仍維持skip；依輪替下一輪建議選US軌。完整見`REPORT.md`第529輪心跳、`MARATHON_STATE.md`（輪次計數器529）。

---

**上一則保留（第527輪，供對照）**——取鎖乾淨（cycle`20260910-233037`）。開工先照CLAUDE.md「交辦優先於自走」鐵律讀`PENDING_QUEUE.md`——本輪額外做了一件前幾輪沒做的事：不只看檔案最上方幾則摘要，逐段檢查了更早的區塊（【新聞一】【題材六】【題材八】【停擺一】【停擺二】），確認這些條目在佇列文字裡從未被劃記完成，但用程式碼實查（`data/news_urls.json`：6068/6087筆已有`published`、`extract_diag`含HTTP狀態碼與例外分類診斷、`low_priority_deferred`機制存在；`scripts/build_themes.py`／`.github/scripts/news_body_extract.py`：題材八公司名稱片段反向排除、題材六版面雜訊黑名單皆已實作）確認全部已落地，只是先前輪次沒有回填✅標記——**交辦佇列確認無可執行未開始項**（2條阻塞：S4U排程註冊／claude CLI非互動驗證，需總司令有管理員權限操作；1條阻塞：gate50三條件待總司令釐清；1條大型未拆解：題材七），回落自走。依round526建議依輪替選TW（TW round525 22:30較舊／US round526 23:00較新）。查`run_detached.py status`：`running=0`（60筆歷史紀錄，無新增）；`git log`確認round526後僅IBKR報價自動排程commit，未動`alpha.db`/`fetch.py`/`parsers.py`/`config.py`凍結區；`git status`僅既有`dev_queue`/`hypothesis_queue`軌未追蹤檔案（`backfill_tpex_3insti.py`／`capital_reduction_gap_screen.py`等）與探針log在動，非本馬拉松三軌產物，本輪未觸碰、未越權處理。**核實現狀（精簡確認，無新統計工作單位可做）**：`data/ticks/`累積進度仍**4/20**（`20260907`~`20260910`四個`.parquet`皆已finalize，與round525/526一致，距20日仍差16日）；`PENDING_QUEUE.md`第298~313行`gate50`查證段落仍原封不動，總司令尚未回應三條件具體定義，`#50`維持未解鎖；`STRATEGY_GRAVEYARD.md`掃描`## #6x`/`## #7x`標題，最新結案仍為`#68`／`#70`（`#70`屬`hypothesis_queue`軌非本馬拉松範圍），本馬拉松三軌自身最新結案仍為`#68`（round510），無新結案。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（帳本243列，結構化登記`TRIALS_REGISTRY.jsonl`55筆起於#187；LEADS帶判定列101、存量未引用帳本22列僅報不擋；撞號2組皆歷史存量不回頭改寫；本輪未產生新試驗判定）。`validation/holdout.py::is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純讀既有`.md`/`.json`檔案與`git log`/`git status`/`run_detached.py status`，PENDING_QUEUE核實用的是既有程式碼與已產出資料檔，非新抓取）。**結論：候選池連續40輪（487~527）US/TW/FUT三軌自身皆無新可推進工作單位，僅剩`#50`被動等待總司令對gate50三條件的回應與tick累積（仍需16日）**。**下一輪任一軌接手**：`#50`gate50原文仍待總司令回應；FUT仍維持skip（除非出現全新機制假說），依輪替下一輪建議選US軌。完整見`REPORT.md`第527輪心跳、`MARATHON_STATE.md`（輪次計數器527）。


（第430/432/437/439/441/443/445/447/449/451/453/455/457/459/461/463/465/467/469/470/471/472/474/475/477/478/480/482/485/487/489/491/493/495/497/499/501/503/505/507/509/512/514/516/518/521/523/525輪已歸檔至`TW_STATE_ARCHIVE.md`，僅保留最新3則）
