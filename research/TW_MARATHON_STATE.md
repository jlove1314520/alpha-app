# TW_MARATHON_STATE.md — 台股軌斷點狀態（覆寫式）

**這份檔案只描述台股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `TW_LOG.md`；候選判定看 `TW_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `TW_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-08T04:00+08:00（馬拉松第428輪）**——取鎖乾淨（cycle`20260908-040037`）。開工前`git status`發現hypothesis_queue排程於round427後繼續跑了兩個週期（`MARATHON_LOG.md`2026-09-07T05:33/05:56兩則心跳可對照）完成的工作未commit（確認無殘留python行程、無`.devqueue.lock`，純粹收工前漏做commit這一步，非中途卡死）。**本輪工作單位＝核對並收尾這批遺留工作**：`MARATHON_PROTOCOL.md`新增3c節「相位敏感度」（`phase_sensitivity.py`，`BacktestConfig.rebalance_phase`）、`backtest/engine.py`新增`rebalance_phase`欄位（純加法擴充，預設0行為不變）、**`#51`（強制交易者事件）三個子事件全數FAIL正式結案**（子事件3 CB轉換價重設：`TRIALS_LEDGER.md`#190，TRAIN IC=+0.0117(p=0.60)/VAL IC=+0.0430(p=0.16)方向與事前綁定相反、null percentile=8.0遠低於90.0門檻，FAIL；同步更新`data/signal_status.json`與`build_signal_status.py`）、**`#53`（全市場報酬離散度速度）第1關sanity完成並登記`TRIALS_LEDGER.md`#192**（未結案，非最終判定——非退化PASS、危機窗口2/3通過、前瞻報酬方向level版正確但vel版方向錯誤，誠實記錄「水位→速度是待驗假設非先驗」，新增`cross_sectional_dispersion_gate53.py`）。`trial_registry.py --check`（需設`PYTHONIOENCODING=utf-8`避開Windows主控台cp950編碼限制，否則印警告字元會UnicodeEncodeError誤判成失敗）確認PASS（exit=0，194列，下一可用編號#193）。push時與遠端（另有Shioaji報價自動更新commit）rebase，`data/rate_limit_state.json`一個`last_request_at`時間戳欄位衝突，取較新值解決，非研究內容衝突。**未觸碰**：`.github/workflows/audit.yml`（檔案本身註明待總司令用有workflow權限的PAT處理）、`DEV_QUEUE_PROMPT.txt`/`.live_watchlist.json`/`dev_queue_cycle.log`/`data_cache/`（另一條獨立的「開發佇列自走」track工作檔案，非本track範圍）、`data/deep_dive_f_us_gross_profitability.csv`（US軌背景job`20260907-054341-a450`產出，留給US軌收成判讀）。**另發現一個顯示層問題**：第424～427輪的心跳被插入`REPORT.md`中段（約1635行）而非規則要求的檔案最上方，且`MARATHON_STATE.md`全局輪次計數器停留在423未更新（連續4輪缺口），本輪已在`MARATHON_STATE.md`補齊計數器到428並記錄缺口說明，`REPORT.md`舊錯位條目本身不回頭搬動。`is_holdout_consumed()`開工/收工前皆確認`False`，全程零新增API呼叫。**下一輪TW軌接手**：`#53`走第2關隨機控制組（`control_group_standard.py`統一標準，需含circular shift與shuffle兩種控制組各自至少兩個參數變體）＋相位敏感度掃描（`phase_sensitivity.py`）；若判斷#53與#54（成交值集中度速度，尚未開始）相關係數>0.7則合併計為一個發現，不得分開算兩個獨立候選。完整見`REPORT.md`第428輪心跳、`TRIALS_LEDGER.md`#190/#192、`MARATHON_PROTOCOL.md`3c節、`phase_sensitivity.py`/`cross_sectional_dispersion_gate53.py`（新增，可重複執行）。

---

**上一則保留（第426輪，供對照）**——取鎖乾淨（非陳舊鎖檔，cycle`20260907-050036`）。三軌時間戳：TW 04:00（round424，最舊）／US 04:30（round425）／FUT 09-06 12:00（round399，依例外條款不選，除非有全新機制假說）——依輪替選TW。`run_detached.py status`確認heavy-job-slot空（0個running），無背景工作衝突；`git status`確認`.github/workflows/audit.yml`/`research/.live_watchlist.json`/`research/DEV_QUEUE_PROMPT.txt`/`research/data_cache/`/`research/dev_queue_cycle.log`為其他既存未commit異動，另有hypothesis_queue排程正在編輯`research/HYPOTHESIS_QUEUE.md`/`research/MARATHON_LOG.md`並新增`research/mops_cb_conversion_price_client.py`（子事件3 CB轉換價重設），本輪選擇不衝突的子事件1工作、在`HYPOTHESIS_QUEUE.md`末尾追加(h)段落而非修改(f)(g)。**本輪工作單位＝round424「下一輪TW軌接手(i)」：子事件1二元規格對照（`short_ratio>0`vs`==0`比較mean_CAR）**，接續`#186`連續比例規格FAIL時留下的待辦（誠實揭露82.2%事件`short_ratio`為零，懷疑連續規格可能被大量零值稀釋訊號）。新增`forced_short_covering_gate1_binary.py`：直接import`forced_short_covering_gate1.py`既有事件建構函式，重建同一批1707筆事件（零新增API呼叫，全部命中`#186`留下的本機快取），改成`short_ratio>0`（有融券部位）vs`==0`（無部位）兩組VAL期mean_CAR差檢定，事前綁定方向為正，null分布=組別標籤洗牌N=200次。**結果：TRAIN diff=+0.00160、VAL diff=-0.00174，train/val正負號不一致，null percentile=37.5（門檻>=90.0），四項判準全數未過**。**判定：FAIL，與連續規格結論一致，排除「零值稀釋訊號」解讀**——子事件1兩種合理規格皆FAIL。已登記`TRIALS_LEDGER.md`#189（`trial_registry.register_trial()`，`--check`確認PASS，下一可用編號#190）、`STRATEGY_GRAVEYARD.md`新增條目、`HYPOTHESIS_QUEUE.md`#51(h)、`data/signal_status.json`（51-1摘要更新，同步`build_signal_status.py`的`DIRECTIONS`常數並重跑腳本，確認合法UTF-8 JSON）。`is_holdout_consumed()`開工/收工前皆確認`False`，全程零新增API呼叫。**下一輪TW軌接手**：子事件1已窮盡連續/二元兩種規格皆FAIL，優先權轉向(a)子事件3新資料cheap gate（`HYPOTHESIS_QUEUE.md`#51(g)待辦，比照子事件2`cash_increase_dilution_gate1.py`同一套事件研究框架）——**先查`git status`/`run_detached.py status`確認hypothesis_queue排程是否已做完這步，避免重工**；若已完成則改做(b)子事件3小規模回填歷史涵蓋度驗證，或(c)若子事件3也FAIL則#51正式結案並設計新方向。完整見`TW_LOG.md`第426輪記錄、`forced_short_covering_gate1_binary.py`（新增，可重複執行）。

---

**上一則保留（第424輪，供對照）**——取鎖乾淨（cycle`20260907-040036`）。三軌時間戳：TW 03:00（round422，最舊）／US 03:36（round423）／FUT 12:00（round399，依例外條款不選，除非有全新機制假說）——依輪替選TW。`run_detached.py status`確認heavy-job-slot空（0個running），無背景工作衝突；`git status`確認`.github/workflows/audit.yml`/`research/.live_watchlist.json`/`research/DEV_QUEUE_PROMPT.txt`/`research/data_cache/`/`research/dev_queue_cycle.log`/`research/survivor_sharpe_check.py`為其他既存未commit異動，本輪未觸碰，保留原狀。**本輪工作單位＝round422「下一輪TW軌接手」(iv)：設計`data/signal_status.json`schema並回補#49/#51已有結果**——round422/round423皆誠實記錄這是欠下的地基工作，本輪回補而非再挖新假說；(i)子事件1二元規格對照與(ii)子事件3 MOPS查證留給下一輪或hypothesis_queue排程（`git status`顯示hypothesis_queue排程正在編輯`#51`相關檔案，本輪選擇不衝突的地基工作）。新增`research/build_signal_status.py`：schema定義四條方向（`#49`~`#52`）的公開狀態，欄位含`status`(NOT_STARTED/IN_PROGRESS/FAIL/EXPERIMENTAL/PASS)、`summary`、`refs.trials_ledger`、`refs.docs`；`#51`用`sub_events`陣列拆三個子事件。人工核對`TRIALS_LEDGER.md`#180/#184（#49最終FAIL，第6關逐年一致性VAL 3/4年同號未過83.3%門檻）、#186（#51子事件1FAIL）、#187（#51子事件2FAIL）三筆既有結果後填入，非自動剖析markdown（避免正則斷章取義）。跑腳本產生`data/signal_status.json`（4條方向：#49 FAIL、#50 NOT_STARTED、#51 IN_PROGRESS含3個sub_events、#52 NOT_STARTED），確認UTF-8合法JSON。`trial_registry.py --check`維持PASS（本輪未新增判定，純資料整理）。`is_holdout_consumed()`開工/收工前皆確認`False`，零新增API呼叫。**下一輪TW軌接手**：(i) 子事件1二元規格對照（`short_ratio>0`vs`==0`比較mean_CAR）；(ii) 子事件3 MOPS查證三候選方向；(iii) `#51`子事件3有進度後記得回頭更新`data/signal_status.json`（`build_signal_status.py`的`DIRECTIONS`常數，非自動同步，每次有新結果要手動改常數再重跑腳本）；(iv) 待App端（開發帽）決定`signal_status.json`要不要接進`index.html`顯示，本輪只做資料面、未動`index.html`。完整見`TW_LOG.md`第424輪記錄、`build_signal_status.py`（新增，可重複執行）、`data/signal_status.json`（新增）。

---

（第422輪已於round428歸檔至`TW_STATE_ARCHIVE.md`，僅保留最新3則）
