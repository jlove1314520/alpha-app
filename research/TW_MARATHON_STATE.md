# TW_MARATHON_STATE.md — 台股軌斷點狀態（覆寫式）

**這份檔案只描述台股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `TW_LOG.md`；候選判定看 `TW_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `TW_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-10T17:30+08:00（馬拉松第521輪）**——取鎖乾淨（cycle`20260910-173037`）。依round520建議依輪替選TW（FUT本輪複核例外條款不成立，回落TW）。開工查`run_detached.py status`：`running=0`（60筆歷史紀錄，無新增）；`git log`確認round520後除round520自身commit外，僅IBKR報價自動排程commit，未動`alpha.db`/`fetch.py`/`parsers.py`/`config.py`凍結區。**本輪工作單位**：發現`git status`裡有一份未追蹤、從未commit過的檔案`research/validation/passive_benchmark_gate.py`（182行，非本馬拉松三軌state登記過的產物，推測為某次互動session針對`PENDING_QUEUE.md`「深讀二.2」留下但未收尾的成果）——查`PENDING_QUEUE.md`確認「深讀二.1」（被動基準引擎`passive_benchmark_tw_v1.py`）已於本日稍早完成並登記`TRIALS_LEDGER.md`#240，但「深讀二.2」（把引擎接進候選判定流程、成為六關第2關升級版）仍標`[ ]`未完成，這份未追蹤檔案的docstring自述正是深讀二.2的實作（`evaluate_gate8()`＋`match_by_risk()`：讀`passive_benchmark_tw_v1_result.json`真實結果檔、找不到就拋錯不用假基準、在12個w網格點裡找MDD最近鄰匹配、回傳完整比較明細不折疊成單一bool）。**驗證**：`python passive_benchmark_gate.py --self-test`共7項全PASS（含對真實結果檔跑極端高/低報酬各一次確認非自我循環）。**判定為可採用的既有工作成果**：純新增檔案、不涉及回測結論、不碰凍結區、不需要新試驗登記（工具函式本身不產生報酬數字，非alpha候選），予以commit並在`PENDING_QUEUE.md`「深讀二.2」補上完成註記（誠實揭露：只做了gate函式本身，尚未回頭批次重評既有候選，該工作量登記為深讀二.3，留給下一輪）。`data/ticks/`累積進度**4/20**（與round520一致，`20260910`當日盤中tick尚未到隔日finalize時點）；`PENDING_QUEUE.md`第99~113行`gate50`查證段落仍原封不動，總司令尚未回應三條件具體定義，`#50`維持未解鎖；`STRATEGY_GRAVEYARD.md`本馬拉松三軌自身最新結案仍為`#68`（round510），無新結案。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（243列，本輪未產生新試驗判定，深讀二.2屬工具函式非候選）。`validation/holdout.py::is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫。**結論：本輪找到並收尾一份先前遺留的可用工作成果（深讀二.2 gate8模組），打破連續34輪「無新可推進工作單位」的僵局；`#50`本身仍未解鎖，TW/US/FUT三軌自身仍無新統計工作單位**。**下一輪任一軌接手**：深讀二.3（批次重評既有已通過舊版第2關的候選）是明確可推進的新工作單位，優先於繼續等待`#50`；依輪替下一輪建議選US軌。完整見`REPORT.md`第521輪心跳、`PENDING_QUEUE.md`深讀二.2完成註記、`research/validation/passive_benchmark_gate.py`（新增）。

---

**上一則保留（第518輪，供對照）**——取鎖乾淨（cycle`20260910-140037`）。三軌時間戳：FUT 10:30（round484，最舊）／US 13:30（round517，較舊）／TW 13:00（round516，最新）——round517建議先重新評估FUT例外條款再選TW。**FUT例外條款複核**：查`HYPOTHESIS_QUEUE.md`確認`#70`（選擇權波動度偏斜/尾部避險需求訊號）目前地基階段（`bs_iv_solver.py`本輪`git status`顯示為`hypothesis_queue`軌新增未commit產物，IV solver壓力測試中）**是由`hypothesis_queue`自走排程獨立推進，非本馬拉松FUT track的工作單位**——`FUT_MARATHON_STATE.md`round484記錄的「例外條款觸發」指的是`#64`（已結案FAIL），`#70`是另一條獨立於馬拉松FUT track之外的假設軸，FUT track本身在round484之後仍無新機制候選，**例外條款維持不選**，依輪替回落選TW。開工查`run_detached.py status`：`running=0`（60筆歷史紀錄，無新增）；`git log`確認round517後新增`hypothesis_queue`軌自走commit（`26c5665c`外部一改.4三軌register修bug、`c07fcd68`外部三.1寫入CLAUDE.md外部策略匯入紀律、`75b04cc4`外部三.2 signal_status標註來源與衰減風險、`66403140`外部二改SPEC完成美股3+台股2期貨3條）與排程commit（Shioaji/IBKR報價），皆非本馬拉松範圍，未動`alpha.db`/`fetch.py`/`parsers.py`/`config.py`凍結區。`git status`僅`DEV_QUEUE_PROMPT.txt`／`MARATHON_LOG.md`／`HYPOTHESIS_QUEUE.md`／`bs_iv_solver.py`（新增）等`hypothesis_queue`軌自身產物與報價/探針log在動，本輪未觸碰。**tick累積進度推進為4/20**（`20260907`/`20260908`/`20260909`/`20260910`四個`.parquet`皆已finalize，距`#50`要求的20日仍差16日）；`PENDING_QUEUE.md`第99~113行`gate50`查證註記（round517新增）仍無總司令進一步回應；`STRATEGY_GRAVEYARD.md`掃描`## #6x`/`## #7x`標題，確認最新結案仍為`#68`（round510結案），無新結案。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（241列，撞號2組皆為歷史存量不回頭改寫，本輪未產生新試驗判定）。`validation/holdout.py::is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純讀既有`.md`檔案與`git log`/`git status`/`ls`）。**結論：候選池連續29輪（487~518）維持同一狀態，TW/FUT軌本地端皆無新可推進工作單位**，僅剩`#50`（tick累積4/20，被動等待總司令對gate50三條件原文或確認摘要已足夠的回應）。**下一輪任一軌接手**：`#50`gate50仍未解鎖；依輪替下一輪建議選US軌。完整見`REPORT.md`第518輪心跳。

---

**上一則保留（第516輪，供對照）**——取鎖乾淨（cycle`20260910-130037`）。三軌時間戳：FUT 10:30（round484，最舊，依例外條款不選——無新機制候選）／TW 03:00（round514，較舊）／US 12:30（round515，最新）——依round515建議依輪替選TW。開工查`run_detached.py status`：`running=0`（60筆歷史紀錄，最新一筆`20260910-010718-3549`已於round510前finished，無新增）；`git log`確認round515之後僅一筆互動session維運commit`d9b67653`（修排程器三個靜默故障：`AlphaTwsePublishProbe`一次性觸發器過期、`run_daily.py`因cp950編碼連續20天崩潰、`AlphaDepCheck`未走PATH，皆屬App維運track非本馬拉松範圍，未動`alpha.db`/`fetch.py`/`parsers.py`/`config.py`凍結區）；`git status`僅`DEV_QUEUE_PROMPT.txt`／`dev_queue_cycle.log`／`external_connectivity.jsonl`／`connectivity_check.log`／`twse_probe.log`／`twse_publish_probe.jsonl`等自動化log在動，屬`hypothesis_queue`與探針排程自身產物，本輪未觸碰。**依round500~515建議延續採精簡確認**：`data/ticks/`維持**3/20**（`20260907`/`20260908`/`20260909`已finalize，`20260910/`原始目錄新出現正在累積中，距`#50`要求的20日仍差17日）；`PENDING_QUEUE.md`全文搜尋`gate50`仍無總司令回應；`STRATEGY_GRAVEYARD.md`掃描`## #6x`/`## #7x`標題，確認最新結案仍為`#68`（2026-09-10，round510結案），無新結案。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（241列，撞號2組皆為歷史存量不回頭改寫，本輪未產生新試驗判定）。`validation/holdout.py::is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純讀既有`.md`檔案與`git log`/`git status`/`ls`）。**結論：候選池連續28輪（487~516）維持同一狀態，TW軌本地端無新可推進工作單位**，僅剩`#50`（tick累積3/20，被動等待總司令對gate50提案的回應）。**下一輪任一軌接手**：`#50`gate50提案仍待總司令回應，核准前`#50`本身仍視為未結案；依輪替下一輪建議選US軌。完整見`REPORT.md`第516輪心跳。

---

（第430/432/437/439/441/443/445/447/449/451/453/455/457/459/461/463/465/467/469/470/471/472/474/475/477/478/480/482/485/487/489/491/493/495/497/499/501/503/505/507/509/512/514輪已歸檔至`TW_STATE_ARCHIVE.md`，僅保留最新3則）
