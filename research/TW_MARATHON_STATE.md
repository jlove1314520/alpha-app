# TW_MARATHON_STATE.md — 台股軌斷點狀態（覆寫式）

**這份檔案只描述台股軌「現在」的狀態，會被覆寫，不是 append-only。** 細節動作記錄看 `TW_LOG.md`；候選判定看 `TW_LEADS.md`；累積試驗數看 `TRIALS_LEDGER.md`；操作規則看 `MARATHON_PROTOCOL.md`。

> 2026-09-05 起本檔只保留最新 3 則（每輪開工簡報會印這 3 則）；更早的已原文搬到 `TW_STATE_ARCHIVE.md`（append-only），需要時 grep 那裡。

**最後更新：2026-09-09T21:30+08:00（馬拉松第503輪）**——取鎖乾淨（cycle`20260909-213036`）。三軌時間戳：FUT 10:30（round484，最舊，但無新機制、依例外條款不選）／TW 20:30（round501，較舊）／US 21:00（round502，最新）——FUT跳過後依輪替選TW。開工查`run_detached.py status`：`running=0`；`git log`確認round502後僅自動排程commit（IBKR報價）；`git status`確認`hypothesis_queue`仍有一批未提交的工作中變更（`odd_lot_imbalance_portfolio_v1.py`等），屬另一track中間狀態，本輪未觸碰、未commit這些檔案。**依round500/502建議延續採精簡確認（鎖/`run_detached`/`git log`/ticks進度/gate50回應/`STRATEGY_GRAVEYARD.md`新結案掃描）**：`data/ticks/`仍2/20（`20260907.parquet`/`20260908.parquet`已finalize，`20260909.parquet.tmp`未finalize，正常，較round501無變化）；`PENDING_QUEUE.md`核對確認`PROPOSAL_2026-09-09_gate50_tick_universe_mismatch.md`本身仍無總司令回應，`PENDING_QUEUE.md`頂端仍是2026-09-09題材新聞管線裁示，非本馬拉松三軌範圍；`STRATEGY_GRAVEYARD.md`掃描`## #6x`/`## #7x`標題，確認`#67`仍是唯一收錄到gate1的條目、無`#68`~`#79`新結案，與round502盤點一致。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（233列，撞號2組皆為歷史存量不回頭改寫，無新判定）。`validation/holdout.py::is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純讀既有`.md`/`.json`檔案與`git log`/`git status`）。**結論：候選池連續17輪（487~503）維持同一狀態，TW軌本地端無新可推進工作單位**，僅剩`#50`（tick累積2/20，被動等待總司令對gate50提案的回應）與`hypothesis_queue`自走排程（現在`#67`gate2/`#68`起）的被動維護。**下一輪任一軌接手**：`#50`gate50提案仍待總司令回應，核准前`#50`本身仍視為未結案；查`data/ticks/`是否已到3/20；候選池已連續17輪無TW/US/FUT三軌自身可推進的新統計工作單位，建議下一輪繼續採精簡確認節省預算。完整見`REPORT.md`第503輪心跳。

---

**上一則保留（第501輪，供對照）**——取鎖乾淨（cycle`20260909-203037`）。三軌時間戳：FUT 10:30（round484，最舊，但無新機制、依例外條款不選）／TW 19:30（round499，較舊）／US 20:00（round500，最新）——FUT跳過後依輪替選TW。開工查`run_detached.py status`：`running=0`；`git log`確認round500後僅自動排程commit（IBKR報價），`git status`額外發現`hypothesis_queue`仍有一批未提交的工作中變更，屬另一track中間狀態，本輪未觸碰。`PENDING_QUEUE.md`核對`#50`gate50提案仍無總司令回應；`data/ticks/`仍2/20。**本輪工作單位＝執行`PROPOSAL_2026-09-09_gate50_tick_universe_mismatch.md`第2步（明文寫「不需要新增外部API呼叫」的準備工作，非提案本身要核准的訂閱變更部分）：用既有本機快取篩出#50候選代號清單**。新增`gate50_candidate_universe_screen.py`：讀`universe.py::active_stock_ids()`（吃`data/raw/TaiwanStockInfo__ALL__2000-01-01__latest.parquet`快取，零API呼叫）取上市中股票，排除ETF（`00`開頭/`industry_category`含ETF）與權證，再對每檔讀本機已快取的`data/raw/TaiwanStockPrice__{id}__2010-01-01__2024-12-31.parquet`（2488檔已快取，零API呼叫），計算近60個交易日`Trading_money`（FinMind既有欄位，日成交值NTD，非自行用close×volume估算）均值，篩500萬~5,000萬區間。**結果：排除ETF/權證後母體2593檔，561檔落在目標區間**（missing_price_cache=794檔無快取跳過、insufficient_history=226檔近60日有停牌/成交量掛零跳過），輸出`data/gate50_candidate_universe.json`（含`stock_id`/`stock_name`/`industry_category`/`avg_daily_turnover_ntd`/`as_of_last_date`，561檔依成交值排序）。**誠實揭露限制（已寫進腳本docstring）**：(1)成交值資料是本機快取上限到`VAL_END=2024-12-31`（`validation/holdout.py`），不是即時成交值，提案本文第4點已指出成交值會隨時間漂移，這份清單是「起點候選」不是「核准當下即可直接訂閱的最終名單」，執行前應重新用當下最新資料確認一次；(2)本腳本**未動`shioaji_quotes.py`、未動`.live_watchlist.json`、未執行任何訂閱變更**——提案要核准的部分（把候選代號寫進常駐服務訂閱清單、需要`CLAUDE.md`七之三常駐服務發布紀律的重啟+驗證流程）依然完全沒動，仍待總司令對提案本身的回應，這裡做的只是「核准後可以直接拿去用」的前置準備，不構成執行提案。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（無新判定，本輪是基礎設施準備非統計判定，不需要`register_trial()`呼叫）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純讀本機既有parquet快取）。**下一輪任一軌接手**：`#50`gate50提案本身仍待總司令回應；候選清單已備妥（`data/gate50_candidate_universe.json`），核准後可直接用於訂閱清單擴充；查`data/ticks/`是否已到3/20。完整見`gate50_candidate_universe_screen.py`（新增，可重複執行）、`data/gate50_candidate_universe.json`（新增）、`REPORT.md`第501輪心跳。

---

**上一則保留（第499輪，供對照）**——取鎖乾淨（cycle`20260909-193037`）。三軌時間戳：FUT 10:30（round484，最舊，但無新機制、依例外條款不選）／TW 18:30（round497，較舊）／US 19:00（round498，最新）——FUT跳過後依輪替選TW。開工查`run_detached.py status`：`running=0`；`git log`確認round498之後僅`hypothesis_queue`排程自走commit（`#67`零股委託簿失衡度資料回補全數完成1092/1092，地基建置完工），非本track範圍。**核實候選池現況不變**：0a節四條方向＋`#61`~`#66`全數結案，`data/ticks/`仍僅2個完整交易日parquet（`20260909`當日檔仍`.tmp`未finalize，較round497無變化）；`#50`提案（`PROPOSAL_2026-09-09_gate50_tick_universe_mismatch.md`）仍待核准。**本輪工作單位＝對TW軌候選池做一次橫向收斂盤點（非新試驗，核對既有結案記錄有無被漏掉的可推進項）**：逐一核對四個角度——

1. `MARATHON_PROTOCOL.md`第3節因子家族清單：`TW_LEADS.md`第389輪已明確結案「因子家族至此已全數至少測過第一批，TW軌沒有殘留的全新家族可掃」。本輪額外逐項核對應計項目(`f_accruals`#63FAIL)/資產成長(`f_asset_growth`#62FAIL方向不一致)/毛利率穩定度(`f_gross_margin_stability`#67FAIL)/52週高點接近度(`HYPOTHESIS_QUEUE.md`#17FAIL)/殘差動能(`f_residual_momentum`FAIL)/短期反轉(`f_short_reversal_1m`#46FAIL)/季節性(月效應/財報季效應，round373因資料源結構性限制結案)，確認皆已FAIL在案，非本輪疏漏。
2. `portfolio_multifactor_v2`家族：`STRATEGY_GRAVEYARD.md`確認2026-09-06已整併結案——12種以上具體構造（等權/IC加權/情境加權×月/季頻×含或不含value_pe×三種leave-one-out子版本）全部卡在alpha顯著性這一關，且樣本越大訊號越弱（300檔重跑後p值從邊緣的0.053惡化到0.2159~0.6451），判定「真的沒有可累加alpha」而非檢定力不足。round373曾建議的「regime overlay/下檔保護證明」路線，已被同一份結案記錄明確排除——原文「在alpha都不顯著的前提下，繼續疊regime overlay/下檔保護證明在這個家族上屬於錦上添花而非解決根本問題」——round373那則「下一輪建議」是2026-09-05寫的，比2026-09-06的整併結案早一天，已過時，不是漏做。
3. `CALIBRATION_PROBE.md`給馬拉松的操作指令鏈：round493已確認100%執行完畢（本輪重新核對`TRIALS_LEDGER.md`#100/#101/#104/#106/#99數字一致，未變動）。
4. `#63`借券費率異常飆升（hypothesis_queue軌，非本track但一併核實）：已於round482 gate4成本敏感度FAIL正式結案（`TRIALS_LEDGER.md`#227，N5/N10於1x成本即轉負，N20僅撐到1x）。

**結論：四個角度皆確認無新工作單位，非本輪疏漏，是連續13輪（487~499）候選池維持同一狀態**。順帶修正一個文件紀律小落差：round498的commit（`66da4790`）依協定第6節步驟4應更新`MARATHON_STATE.md`輪次計數器，但只寫了`REPORT.md`心跳、計數器留在497，本輪一併補上跳到499（純文件紀律修正，未動任何統計判定，依`CLAUDE.md`純bug修復例外不需先提案）。`trial_registry.py --check`（`PYTHONIOENCODING=utf-8`）exit=0 PASS（無新判定，本輪不需要`register_trial()`呼叫）。`is_holdout_consumed()`開工/收工前皆確認`False`。全程零新增外部API呼叫（純讀既有`.md`檔案）。**下一輪任一軌接手**：`#50`gate50提案仍待核准；查`data/ticks/`是否已到3/20；候選池已連續13輪維持同一狀態，若下一輪仍是同樣結論，建議認真評估是否已接近0a節「無可驗證預測優勢」提報門檻（`#50`本身未結案前嚴格說還不到，但候選池已非常薄，橫向盤點也確認沒有遺漏）。完整見`REPORT.md`第499輪心跳、`STRATEGY_GRAVEYARD.md`（`portfolio_multifactor_v2`/`f_accruals`等既有結案條目，本輪未新增內容，只是核對）。

---

（第430/432/437/439/441/443/445/447/449/451/453/455/457/459/461/463/465/467/469/470/471/472/474/475/477/478/480/482/485/487/489/491/493/495/497輪已歸檔至`TW_STATE_ARCHIVE.md`，僅保留最新3則）
