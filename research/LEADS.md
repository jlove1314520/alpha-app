# LEADS.md — 策略候選登記簿

每個被正式評估過的策略候選一列，含最終判定。這份檔案的目的是讓任何人（包含換了一輪的 session）一眼看出「已經試過什麼、結果是什麼」，不用重新挖一次已經挖過的坑，也不會把失敗的嘗試悄悄遺忘掉重新試一次。

**規則：**
- **失敗的候選也要記，而且要記清楚為什麼失敗。** 只記成功的等於在說謊——見 `CONSTITUTION.md`「誠實記錄正負結果」。
- 判定欄只能是下面幾種之一，不能自創模糊字眼：
  - `PENDING` — 還在跑驗證框架的關卡，還沒有結論
  - `PASS` — 打贏隨機控制組、通過鎖定的事前標準、holdout 驗證過（一個候選最多只能有一次 `PASS`+holdout 紀錄，holdout 燒過就是燒過了）
  - `FAIL` — 沒打贏隨機控制組，或沒通過鎖定標準的某一項
  - `EXPERIMENTAL` — 只在 train/val 跑過、還沒碰 holdout，或碰過會沾到 `pit_source='assumed'` 的資料（見 `DATA.md`），結果僅供參考不可採信
  - `ABANDONED` — 中途放棄，通常是因為地基/資料問題而非策略本身的問題
- **每一列都要附最大回撤（MDD）跟 Sortino ratio**（2026-08-22 起，Cowork 要求）——只看報酬率會漏掉「贏是贏在承擔多少下檔風險」這件事。

| 候選名稱 | 假說 | 提出日期 | 判定 | Val 報酬/MDD/Sortino | Train 報酬/MDD/Sortino | 隨機控制組百分位 | 備註 |
|---|---|---|---|---|---|---|---|
| `weinstein_stage2_pilot_v1` | 手選 30 檔大型股試點宇宙掃 Weinstein 第二階段（價>上揚150日均線）+ 加權指數200日均線總體閘門 + 60日動量前10名，週頻換股，三層風控 | 2026-08-22 | **FAIL**（隨機控制組沒打贏） | +135.8% / −34.83% / 0.913 | +168.4% / −13.66% / 1.209 | **24.5**（靜態對照組，方法論已知有缺陷，見備註） | 對照組用「靜態買進持有」而非動態週頻重抽，且試點宇宙是手選知名大型股（後見之明式贏家集中池）——這兩個方法論缺陷後來被 Cowork 抓到，見下一列的修正版。**這不是引擎 bug**：`audit_ledgers.py` 對實際交易紀錄（train 324／val 160 筆）全部 PASS。**絕對沒碰 holdout**。 |
| `weinstein_stage2_unbiased` | 同上訊號邏輯，改用 `universe.py` 全市場（post-2003+含下市股）隨機抽樣 100 檔 + 修正後的「配對式」隨機控制組（同進出場日期只換股） | 2026-08-22 | **EXPERIMENTAL**（train/val 全過，但依規則未碰 holdout 不能標 PASS） | +145.4% / −22.41% / 0.702 | +99.5% / −29.11% / 0.444 | **99.5**（配對式對照組：同 133 組進出場日期，200 次只換股不換時間，全部達標 50/90/95/99 百分位門檻） | 82/100 抽樣個股有效資料。買進持有：val 通過（+54.6%）、train 通過（+58.9%）。成本 1x/2x/3x 敏感度穩健（val +145.4%→+136.4%→+127.4%）。交易數 val 276／train 514，都 ≥100，可信。跑這輪時抓到並修好 3 個真 bug（`adjust.py` 兩處在檢查空值前就呼叫 `.sort_values()`、引擎在 `fill_price<=0` 時會除以零），詳見 `REPORT.md`。**這是目前唯一四項關卡（買進持有/成本敏感度/交易數/隨機控制組）全部通過的候選，但依 `LEADS.md` 規則，沒有 holdout 驗證前一律只能標 `EXPERIMENTAL`，不能標 `PASS`。是否進入一次性 holdout 測試，等使用者決定並明確授權。** **絕對沒碰 holdout**（`HOLDOUT_LOCK.json` 依然不存在）。 |
