# LEADS.md — 策略候選登記簿

每個被正式評估過的策略候選一列，含最終判定。這份檔案的目的是讓任何人（包含換了一輪的 session）一眼看出「已經試過什麼、結果是什麼」，不用重新挖一次已經挖過的坑，也不會把失敗的嘗試悄悄遺忘掉重新試一次。

**規則：**
- **失敗的候選也要記，而且要記清楚為什麼失敗。** 只記成功的等於在說謊——見 `CONSTITUTION.md`「誠實記錄正負結果」。
- 判定欄只能是下面幾種之一，不能自創模糊字眼：
  - `PENDING` — 還在跑驗證框架的關卡，還沒有結論
  - `PASS` — 打贏隨機控制組、通過鎖定的事前標準、holdout 驗證過（一個候選最多只能有一次 `PASS`+holdout 紀錄，holdout 燒過就是燒過了）
  - `FAIL` — 沒打贏隨機控制組，或沒通過鎖定標準的某一項
  - `EXPERIMENTAL` — 只在 train/val 跑過、還沒碰 holdout，或碰過會沾到 `pit_source='assumed'` 的資料（見 `DATA.md`），結果僅供參考不可採信
  - `ABANDONED` — 中途放棄，通常是因為地基/資料問題而非策略本身的問題
  - `樣本不足、暫不採信`（2026-08-24 新增，Cowork 要求）— train/val 甚至隨機控制組都可能已經跑出結果，但樣本規模（例如宇宙覆蓋率）不足以讓人信任這個結果不是巧合，在樣本規模明顯改善之前，這個判定取代原本可能會標的 `PASS`/`EXPERIMENTAL`，直到重新驗證前都只能當探索性/診斷性參考，不能當候選依據
- **每一列都要附最大回撤（MDD）跟 Sortino ratio**（2026-08-22 起，Cowork 要求）——只看報酬率會漏掉「贏是贏在承擔多少下檔風險」這件事。

| 候選名稱 | 假說 | 提出日期 | 判定 | Val 報酬/MDD/Sortino | Train 報酬/MDD/Sortino | 隨機控制組百分位 | 備註 |
|---|---|---|---|---|---|---|---|
| `weinstein_stage2_pilot_v1` | 手選 30 檔大型股試點宇宙掃 Weinstein 第二階段（價>上揚150日均線）+ 加權指數200日均線總體閘門 + 60日動量前10名，週頻換股，三層風控 | 2026-08-22 | **FAIL**（隨機控制組沒打贏） | +135.8% / −34.83% / 0.913 | +168.4% / −13.66% / 1.209 | **24.5**（靜態對照組，方法論已知有缺陷，見備註） | 對照組用「靜態買進持有」而非動態週頻重抽，且試點宇宙是手選知名大型股（後見之明式贏家集中池）——這兩個方法論缺陷後來被 Cowork 抓到，見下一列的修正版。**這不是引擎 bug**：`audit_ledgers.py` 對實際交易紀錄（train 324／val 160 筆）全部 PASS。**絕對沒碰 holdout**。 |
| `weinstein_stage2_unbiased` | 同上訊號邏輯，改用 `universe.py` 全市場（post-2003+含下市股）隨機抽樣 100 檔 + 修正後的「配對式」隨機控制組（同進出場日期只換股） | 2026-08-22 | **【2026-08-26馬拉松第100輪更新】否決：純 beta，非真 alpha，不解鎖 holdout**（見下方新備註，原`EXPERIMENTAL`判定作廢） | +145.4% / −22.41% / 0.702 | +99.5% / −29.11% / 0.444 | **99.5**（配對式對照組：同 133 組進出場日期，200 次只換股不換時間，全部達標 50/90/95/99 百分位門檻——**這是舊指標，見下方新備註為何這個數字本身沒有錯但不足以支持解鎖**） | 82/100 抽樣個股有效資料。買進持有：val 通過（+54.6%）、train 通過（+58.9%）。成本 1x/2x/3x 敏感度穩健（val +145.4%→+136.4%→+127.4%）。交易數 val 276／train 514，都 ≥100，可信。跑這輪時抓到並修好 3 個真 bug（`adjust.py` 兩處在檢查空值前就呼叫 `.sort_values()`、引擎在 `fill_price<=0` 時會除以零），詳見 `REPORT.md`。原記錄：「這是目前唯一四項關卡（買進持有/成本敏感度/交易數/隨機控制組）全部通過的候選，但依`LEADS.md`規則，沒有holdout驗證前一律只能標`EXPERIMENTAL`」——**這個結論被下面的alpha/beta關卡推翻**。**絕對沒碰 holdout**（`HOLDOUT_LOCK.json` 依然不存在）。**【2026-08-26馬拉松第100輪、`WEINSTEIN_ALPHA_GATE_TASK.md`結果】**：這裡贏隨機控制組的99.5百分位測的是「原始總報酬」，含大盤beta暴露——`weinstein_alpha_gate.py`（新增）把總報酬拆成alpha/beta兩塊（`decompose_alpha_beta()`），並對配對式隨機控制組的**每一次抽樣**也做同樣拆解（N=200，`_matched_draw_equity_curve()`平行於`run_matched_control_group()`的`one_draw()`新寫，回傳完整每日淨值序列），拿真實策略的「純alpha累積報酬」對這個新分布算百分位。結果：**實測beta明顯不是市場中性（VAL +0.5865、TRAIN +0.3156），大盤貢獻占VAL總報酬47.43%裡的34.87pp、TRAIN總報酬42.44%裡的21.08pp**——大部分「贏」是搭了大盤順風車，不是選股能力。**VAL期（alpha對隨機控制組）percentile=28.5**，不只沒過90.0單測門檻，還低於50——真實策略扣掉beta後的純alpha累積報酬（+12.56%）反而輸給隨機對照組中位數（+25.20%），代表這個策略的選股動作本身（相對於同樣beta暴露、同樣換股時點的隨機選股）沒有貢獻正面alpha。**TRAIN期percentile=96.5**（過90.0門檻），但成本敏感度顯示這個train期alpha完全不穩健：1x成本alpha+21.36%，**2x成本alpha轉負−10.84%，3x成本−23.28%**——只要成本假設稍微保守一點，train期的「alpha」就消失甚至倒賠，不是可信的訊號。**判定：VAL期alpha關卡清楚未過（且方向上輸給隨機控制組），TRAIN期即使單測百分位過關也因成本極不穩健而不可信，兩期都不構成「明確為正且穩健的alpha」——依`WEINSTEIN_ALPHA_GATE_TASK.md`規則直接否決，不進一步計算BH-FDR多重比較（VAL期百分位遠低於任何FDR門檻，不需要），不解鎖holdout，也不回報使用者要求解鎖**（否決本身不是懸而未決，這裡不會再等使用者）。完整輸出：`weinstein_alpha_gate.py`（新增，可重複執行，全程零新API呼叫、同一批既有快取樣本）、`data/weinstein_alpha_gate_summary.csv`／`data/weinstein_alpha_gate_{val,train}_cost_sensitivity.csv`（皆gitignored）、`TRIALS_LEDGER.md`新增列、`STRATEGY_LOG.md`本輪記錄。 |
| `score_topn_v1` | AI 選股引擎 Phase A `score.py` 綜合分（去重後 3 個獨立成分：EPS成長/意外家族、營收意外、低波動，同產業 peer z-score，等權平均）前 10 名，週頻檢查換股（`backtest/engine.py` 原生機制，只換真正進出榜的名字，不強制全部重新等權），扣真實成本 | 2026-08-23 | **EXPERIMENTAL**（train/val 全過，未碰 holdout） | +97.58% / −22.83% / 0.629 | +131.65% / −19.67% / 0.544 | **100.0（兩期皆是）**（配對式對照組：60 次重抽，同樣的換股時點/檔數/成本，只換挑哪些股票——val 期中位數隨機對照組期末權益僅 1,017,076（幾乎打平出發的 1,000,000），真實綜合分策略達 1,975,796，完勝全部 60 次重抽；train 期更懸殊，隨機對照組中位數 704,681（**倒賠約 30%**），真實策略 2,316,487） | 80/100 抽樣個股有效資料（`f_eps_growth`/`f_eps_surprise`/`f_revenue_surprise`/`f_low_vol` 的既有樣本）。成本 1x/2x/3x 全部維持正報酬、單調遞減、沒有翻負（val +97.58%→+75.63%→+53.77%）。**誠實的反直覺結果**：絕對報酬輸給「全樣本80檔等權買進持有、不換股、零成本」（val +269.53%、train +278.91%）——但這不是策略沒有訊號，是週頻換股機制本身摩擦成本很高（同機制的隨機對照組期中位數幾乎打平甚至倒賠），**真正的訊號強度要看配對式隨機對照組百分位（100.0，完勝），不是看跟零成本被動基準的絕對報酬差**，這是本專案一貫的方法論（呼應 `weinstein_stage2` 系列同樣的教訓）。隨機對照組抽樣數（60）低於 Weinstein 前例的 200——因為每次重抽都要重跑一次完整多年回測，200 次在合理時間內跑不完，已誠實揭露為較小的統計預算，不是刻意壓低。因子相關性去重見 `FACTORS.md`。**絕對沒碰 holdout**（`HOLDOUT_LOCK.json` 依然不存在）。 **2026-08-24 更新：Cowork 稽核指出「贏配對隨機對照組」證明因子有真訊號沒錯，但「這個用法本身」（週頻多單前10名、輸給零成本買進持有）沒有實際價值，且買進持有基準本身帶著80檔抽樣的選股偏差——建議改用多空市場中性評估法。回應見下方 `score_longshort_v1` 兩列，這條紀錄保留不刪，是這個結論的歷史起點。** |
| `score_longshort_v1`（週頻） | 同上綜合分，改用多空市場中性設計：每期買前decile、空後decile，等權兩腳，看價差報酬對大盤的**實測** beta（不是假設）跟扣兩腳真實成本後的淨值 | 2026-08-24 | **PENDING（放空可行性＋券源確認後再議，2026-08-24 三次覆核）** | Val：年化+13.27% / beta−0.095 / Sortino 0.900 | Train：年化+4.21% / beta−0.105 / Sortino 0.342 | 100.0（四期全部，但宇宙覆蓋率不足下的結果） | 見下方多空專節完整解讀；宇宙 170 檔，受 FinMind 流量限制未達全市場 3,196 檔目標。**2026-08-24 三次覆核：Cowork 明確指出「純多前decile是更穩健且可實際執行的方向，放空腿在台股不穩又常常借不到券，先擱置為研究備選」**——判定從「樣本不足、暫不採信」改為 `PENDING`，明確理由是放空可行性＋券源未確認（見 `long_only_vs_market.py` 的 `check_shortability()`，上次因限流沒有可信結果），不是繼續糾結宇宙覆蓋率問題（那是 `score_longonly_v1` 的主要限制）。全市場回補仍在馬拉松持續進行（`MARATHON_PROTOCOL.md` 5b 節）。 |
| `score_longshort_v1`（月頻） | 同上，改月頻換股 | 2026-08-24 | **PENDING（放空可行性＋券源確認後再議，同上）** | Val：年化+14.15% / beta−0.048 / Sortino 0.976 | Train：年化−1.73% / beta−0.080 / Sortino −0.028 | 100.0（四期全部，但宇宙覆蓋率不足下的結果） | Train 期月頻絕對報酬是負的（誠實記錄）。跨期不一致根源已查（`diagnose_monthly_inconsistency.py`）：複利負報酬被少數極端壞月份主導，換股名單相鄰重疊率僅約50-56%，判定跨期一致關卡目前無法通過。**2026-08-24 三次覆核：多空版本整體擱置為研究備選，優先發展 `score_longonly_v1`（純多、不需要放空）**，見下方多空專節。 |
| `f_rel_strength_regime_switch`（大盤位階開關：多頭做多相對強度十分位多空/空頭平倉觀望） | `f_rel_strength`（相對強度，原始6因子之一，`FACTORS.md`判定FAIL：train/val方向相反）分群IC顯示四組事前可觀測條件（大盤位階/波動度/市值規模/流動性）**全部一致**：多頭+小型股+低波動+低量能環境下IC為正，空頭+大型股+高波動+高量能環境下IC為負，沒有一組模稜兩可，經濟解釋對應文獻中的「動量崩潰」（momentum crash）跟「動量效應在小型/低流動性股更強」現象——這輪（馬拉松第83輪）把這個因子層級發現真的建成規則型策略跑完整關卡 | 2026-08-26 | **FAIL（策略層完整驗證未通過，TW_LEADS.md#4/TRIALS_LEDGER.md#40）** | +0.50% / MDD未計算（腳本未輸出此欄位） / Sortino 0.141（1x成本；2x/3x轉負，見備註） | −6.82% / MDD未計算（腳本未輸出此欄位） / Sortino −0.144（1x成本，三個成本情境皆負） | TRAIN 84.0~89.0（1x/2x/3x）、VAL 93.0~94.0（1x/2x/3x）——雖仍算「贏隨機控制組中位數」，但明顯低於本專案其他候選慣見的99~100百分位 | **這是本輪（馬拉松第83輪，2026-08-26 04:17）執行互動session已寫好但未跑過的`regime_switch_f_rel_strength.py`的結果，把上面PENDING狀態正式結案。** 十分位多空（k=8/腳、20日換倉），大盤位階用`weinstein_stage2.py`既有`gate`欄位（TAIEX vs MA200，PIT-safe）當開關，空頭時平倉觀望（非反向）。TRAIN(2015-2020)×1x/2x/3x成本：ann_return −6.82%/−8.03%/−9.22%，alpha −4.66%/−5.89%/−7.11%（**全負**），Sortino −0.144/−0.189/−0.233（**全負**），beta+0.073。VAL(2021-2024)×1x/2x/3x：ann_return +0.50%/−0.62%/−1.74%，alpha +1.77%/+0.62%/−0.51%（**1x略正，2x/3x轉負，對成本極敏感**），Sortino 0.141/0.103/0.066，beta+0.191（比TRAIN期明顯偏離market-neutral）。**這是誠實的策略層負面結果，不是因子層IC分析被推翻**——兩者測的是不同指標（IC排序能力 vs 十分位多空扣成本後的絕對/相對報酬）：分群IC顯示的「動量崩潰+套利限制」經濟解釋在因子排序能力層級是對的，但沒有轉化成扣成本後能打贏隨機選股的可交易邊際優勢，跟`f_quality_roe_stability`（同`REGIME_CONDITIONS.md`）出現過同款「IC穩健但絕對報酬另有故事」的模式相同，是這個通用觀察的第二個實例。**未測項（刻意的範圍限制，非疏漏）**：空頭「反向做空」變體（腳本docstring明確說明只測「平倉」這個較保守版本，未驗證反向版本能否扣成本後穩定獲利）。樣本：80檔驗證樣本（`factor_ic.py`既有100檔抽樣，有效80檔），未經全市場複驗。完整數字：`TW_LEADS.md`#4、`TRIALS_LEDGER.md`#40、`data/regime_switch_f_rel_strength.csv`（gitignored，本機保留）。 |
| `portfolio_multifactor_v1`（等權/IC加權/情境條件式加權三版本） | 4個已通過因子(`f_eps_growth`/`f_eps_surprise`/`f_revenue_surprise`/`f_low_vol`)+`f_value_pe`（待複驗候選）組成複合分數，前10名週頻換股，測equal/ic_weighted/regime_weighted（依波動度環境動態調整權重）三版本 | 2026-08-26 | **FAIL（三版本在驗證期alpha都沒有通過顯著性檢定，不符合使用者裁定的評判順序第3關）** | equal: 報酬+58.68%/MDD−11.52%/Sortino1.024/alpha+8.45%(p=0.133不顯著)；ic_weighted: 報酬+50.25%/MDD−16.64%/Sortino0.597/alpha+8.12%(p=0.442不顯著)；regime_weighted: 報酬+35.74%/MDD−14.99%/Sortino0.582/alpha+4.55%(p=0.537不顯著) | equal: 報酬+80.64%/MDD−14.46%/Sortino0.677/alpha+9.21%(p=0.190不顯著)；ic_weighted: 報酬+100.08%/MDD−11.21%/Sortino1.088/alpha+10.85%(**p=0.022顯著**)；regime_weighted: 報酬+90.63%/MDD−12.77%/Sortino0.840/alpha+10.12%(p=0.092不顯著) | 未跑隨機控制組（三版本評判標準已在alpha顯著性這關落敗，暫不需要再花算力跑控制組確認） | **使用者裁定的評判順序（淨利/MDD→Sortino→alpha顯著性→Sharpe）逐項检查**：淨利/MDD三版本都過關（MDD −11%~−17%之間，沒有失控）；Sortino沒有單一版本兩期都贏（`ic_weighted`train期最強1.088但val期最弱0.597，`equal`反過來val期最強1.024但train期最弱0.677——train/val排名不一致本身就是一個誠實的負面訊號）；**關鍵：alpha顯著性只有`ic_weighted`在TRAIN期通過（p=0.022），但同一版本在VALIDATION期完全不顯著（p=0.442），不符合本專案一貫要求的跨期一致性（同`factor_ic.py`的same_sign規則精神）——三個版本沒有一個在VALIDATION期達到alpha顯著為正，這是這輪最重要的誠實負面結果**。`regime_weighted`版本額外在VAL期3x成本敏感度下轉負（−3.58%），是三版本中對成本假設最脆弱的。**判定：三個版本都不夠格成為holdout候選，不會、也不需要問使用者是否解鎖holdout（本來就沒有先過關）。** 情境條件式加權目前只用了波動度單一維度（見`REGIME_CONDITIONS.md`／`portfolio_backtest.py`docstring說明的範圍縮減），沒有勝過只是靜態的IC加權或等權，值得注意但樣本(80檔驗證樣本，同`factor_ic.py`既有抽樣)有限，這個負面結果本身也待全市場複驗(`TW_MARATHON_STATE.md`覆蓋率已達81.3%，但這次沒有換更大樣本重跑)。回測程式碼：`portfolio_backtest.py`（新增），完整交易規則/成本模型/CAPM回歸顯著性檢定見該檔案docstring，數字見`data/portfolio_backtest_results.csv`。 |
| `portfolio_multifactor_v2`（等權/IC加權/情境加權(大盤位階)×月/季頻×2因子版本，共12組合） | 依`PORTFOLIO_STRATEGY_SPEC.md`完整規格：20檔持股、15%停損、單檔部位上限1/20、流動性門檻(20日均成交金額後10%分位數排除)、全成本；因子版本A(3成分)/B(+`f_value_pe`,4成分) | 2026-08-26（晚） | **FAIL（依alpha顯著性這關，全部12組合p值皆>0.05；但兩個最佳組合p=0.053，明確標記「接近顯著」而非否決封存）** | 見下方「VAL期12組合完整結果」 | 見`data/portfolio_backtest_v2_quick_scan.csv`（TRAIN快速版） | 15次重抽配對式隨機選股，最佳組合percentile=100.0（多組同樣100.0） | **VAL期(2021-2024)最佳兩組合**：A/IC加權/季頻：報酬+68.33%/MDD−8.41%/Sortino1.029/Sharpe1.218/alpha+10.40%(p=0.053)；B/IC加權/季頻：報酬+68.42%/MDD−8.65%/Sortino1.036/Sharpe1.219/alpha+10.26%(p=0.053)——**兩者絕對報酬都贏買進持有大盤(+54.58%)、MDD遠優**，不需要動用「報酬輸但MDD更低也算有價值」這條使用者裁定的判讀原則就已經站得住腳。次佳：A/情境加權(大盤位階)/月頻：報酬+64.55%/MDD−12.69%/Sortino1.053/alpha+9.82%(p=0.066)。全部12組合明細（含等權版本、月頻其餘組合）見`REPORT.md`2026-08-26（晚）條目的完整表格。**季頻穩定優於月頻**（成本拖累少）；**等權版本全面弱於IC/情境加權**（Sortino/alpha都較低）；**納入`f_value_pe`(版本B)影響很小**（跟版本A同組合差距在雜訊範圍內）。**誠實限制**：(1) alpha未達5%顯著性門檻，樣本外說服力有限；(2) IC加權的權重是用同一批train+val資料算出的靜態常數，不是完全乾淨的樣本外設計；(3) 樣本仍是100檔(80可用)驗證樣本，不是已達81.3%的全市場覆蓋率——下一步最高優先是換更大樣本重跑「IC加權+季頻」看p值會不會改善。**判定不通過evaluator第3關，不構成holdout候選，未觸碰、也不需要問使用者是否解鎖holdout。**取代（不刪除）`portfolio_multifactor_v1`（週頻/10檔/單一波動度情境維度）的舊記錄，v1本身的誠實負面結果依然是有效的既有記錄。**【2026-08-27馬拉松第137輪補充】**：`REPORT.md`「建議下一步(c)」——大盤本身同期MDD/Sortino基準——已補算（`benchmark_taiex_stats.py`，新增，零新API，同`backtest/engine.py`公式）：**TAIEX買進持有VAL期(2021-2024)：報酬+54.58%/MDD−31.63%/Sortino0.677/Sharpe0.721；TRAIN期(2015-2020)：報酬+58.86%/MDD−28.72%/Sortino0.561/Sharpe0.613**（return_pct跟原已知的+54.58%/+58.86%完全吻合，確認日期窗口/價格序列跟原回測一致）。用這組數字量化對照：「A/IC加權/季頻」VAL期MDD−8.41%對比大盤−31.63%，**回撤只有大盤的約1/4**；Sortino1.029對比大盤0.677，風險調整後報酬明顯較優。**這只是把「MDD明顯更低」的質化判讀換成量化基準，不是新的判定，`portfolio_multifactor_v2`本身的FAIL判定不變**。「換更大樣本重跑」(a)、「train-only嚴格樣本外」(b)兩個較高成本的選項本輪仍未動，繼續留給使用者決定優先序，理由同第110輪。完整見`REPORT.md`第137輪條目、`data/benchmark_taiex_stats.csv`（gitignored）。 |
| `score_longonly_v1` | 拿掉放空腳，只做多前decile，對照真正的大盤指數（TAIEX，不是帶偏差的同批買進持有）——回應 Cowork「不靠放空是否還有alpha」的問題 | 2026-08-24 | **樣本不足、暫不採信（但四期結果一致強勁，值得續查）** | Val(週)：年化+23.77% / beta+0.601 / Sortino 1.283；Val(月)：年化+33.67% / beta+0.611 / Sortino 1.694 | Train(週)：年化+24.12% / beta+0.692 / Sortino 1.425；Train(月)：年化+23.82% / beta+0.714 / Sortino 1.390 | **100.0（四期全部）** | **這輪最扎實的正面發現**：四期（train/val×週/月頻）全部一致強勁，年化+23.77%~+33.67%，遠優於同期TAIEX大盤（+54.58%~+58.86%總報酬）、對大盤超額+72.83pp~+193.31pp，beta落在+0.6~+0.7（做多本該有的市場暴露，沒有偽裝市場中性）、alpha仍顯著為正（+15.90%~+25.12%年化）、贏過配對隨機對照組（同機制隨機挑股票）100百分位。**沒有多空版本的train/val方向不一致問題**，強烈暗示多空版本的不穩定根源在放空腳，不是選股邏輯本身。不需要放空，沒有融券資格/借券成本/強制回補疑慮，是目前唯一「馬上可以真的執行」的版本雛形。**仍受宇宙覆蓋率不足限制（223/3,196≈7.0%），一樣標記「樣本不足、暫不採信」**，等宇宙擴大後才能提升信心，不是最終判定。**2026-08-24 三次覆核，新增alpha/beta拆解**（`decompose_alpha_beta()`）：確認贏隨機真的是選股alpha不只是beta——四期純alpha年化+15.22%~+24.31%，Sortino多數>1.0，MDD約−11%~−13%；成本敏感度顯示alpha在3x成本下四期全部維持正值，但Validation(週頻)在3x下已相當薄弱（+6.17%，Sortino僅0.180），月頻的alpha對成本比週頻更有韌性。詳見 `REPORT.md` 2026-08-24（三次覆核）條目。 |
