# REGIME_OVERLAY_PROTOCOL.md — regime擇時／下檔保護overlay 協定＋規格書

（2026-09-04 總司令裁示「研究賽道轉向」原話全文，2026-09-15 依 `PENDING_QUEUE.md`
「一」項寫成正式協定＋規格書；門檻在看任何結果之前先寫死存證並 git commit，
之後不得回頭調整——精神同 `CRITERIA_V2_LOCK.md`。）

## 0. 背景與轉向理由（總司令原話，2026-09-04）

> 研究賽道轉向：從「橫截面選股」轉為「regime 擇時／下檔保護 overlay」。
> #1–#27 橫截面因子與複合全 FAIL，且 300 檔校準證明管線正常（B31）。在免費
> 資料宇宙裡「買哪幾檔」無 edge，停止再丟裸因子。改打「什麼時候該降曝險」，
> 這直接服務「永遠不要賠錢」第一原則。

跟 `HYPOTHESIS_QUEUE.md` #10 的關係：#10 在 2026-09-02 已建置
`regime_overlay.py` 方法論框架並做過 sanity（見該檔案 docstring），但當時把
套用對象定位成「等未來有選股候選通過 1~8 關後才正式套用」，屬於**基礎設施
就緒但非正式判定**。本協定書把 2026-09-04 原始指示的第 1 點重新讀清楚：

> 1. 基底固定為最簡單的被動部位：TAIEX（或 0050）買進持有；美股軌用 SPY。
>    **overlay 若連被動指數都保護不了，就保護不了任何東西。**

這句話講的不是「拿被動部位當 sanity 對象」，是「overlay 套用在被動部位本身
就是要正式判定的候選」——因為橫截面選股全滅，目前佇列裡本來就沒有選股
候選可套用，被動部位是唯一現成、可以立刻正式測的基底。本協定把這件事
正式訂正：**overlay 套用在 TAIEX／0050 買進持有本身，就是這一輪要正式拿
去對照下方鎖定門檻的候選，不再是"等未來候選"的 sanity 佔位**。

## 1. 資料範圍與 TRAIN/VAL/HOLDOUT 邊界（沿用既有鎖定，不新開）

- 沿用 `validation/holdout.py` 既有鎖定：`TRAIN_END = 2020-12-31`、
  `VAL_END = 2024-12-31`、HOLDOUT = VAL_END 之後（成長中，需
  `unlock_holdout()` 才能碰）。
- TAIEX 資料起點探測（`CLAUDE.md` 七之三第 10 關「資料源歷史起點探測」）：
  `finmind_client.load_dev("TaiwanStockPrice","TAIEX", factor_ic.START_DATE)`
  本機開發快取（train+val，免占用當日 FinMind 額度）實測範圍
  **2010-01-04 ～ 2024-12-31**，`START_DATE="2010-01-01"`。
- **本協定只用 TRAIN 期（≤2020-12-31）訂門檻與做快殺判斷**，VAL/HOLDOUT
  留給日後真正走到「候選要進 forward-paper」那一關再看，現在不看。

## 2. 危機視窗清單與誠實的資料覆蓋度落差（第 10 關要求的探測結果）

原始指示列了 6 個歷史危機視窗：2008／2011／2015／2018／2020／2022。逐一
探測結果：

| 危機視窗 | 落在哪個資料/切分區間 | TRAIN 期可測？ |
|---|---|---|
| 2008（金融海嘯） | 早於資料起點 2010-01-04，**完全沒有資料**，連 HOLDOUT 都測不了，不是只是 TRAIN 測不了 | ❌ 不可測（資料缺口，非本輪判斷選擇） |
| 2011（歐債危機／美債降級） | 2011-07-01～2011-12-31，落在 TRAIN 期 | ✅ |
| 2015（中國股災，黑色星期一） | 2015-06-01～2015-09-30，落在 TRAIN 期 | ✅ |
| 2018（Q4 貿易戰急跌） | 2018-10-01～2018-12-31，落在 TRAIN 期 | ✅ |
| 2020（Q1 新冠崩盤） | 2020-02-01～2020-04-30，落在 TRAIN 期 | ✅ |
| 2022（全年空頭） | 2022-01-01～2022-12-31，落在 **VAL 期**（>TRAIN_END），依「門檻 TRAIN 先訂死、不准看結果回調」原則不可用於 TRAIN 判斷 | ❌ 不可測（切分邊界，非資料缺口） |

**誠實結論**：原指示「6 個視窗中 ≥5 個縮小回撤」與快殺規則「TRAIN 期 6 個
視窗若 <4 個縮小回撤即判死」，兩條寫死的數字都是以 6 個視窗為分母，但
TRAIN 期實際能測的只有 4 個（2011／2015／2018Q4／2020Q1）。**本協定不
自行把 5/6、4/6 換算成新的比例門檻**——換算涉及「要不要修改總司令原始
寫死的數字」，屬於需要總司令裁示的參數調整範疇（`CLAUDE.md`「提案先於
執行」判準：這不是「已交辦」的具體做法，是資料限制逼出的新選擇）。
本協定做法：**如實回報 4 個 TRAIN 可測視窗各自的結果（幾個縮小回撤／
幾個沒有），不换算、不宣稱通過或不通過原始的「5/6」「4/6」門檻**，PASS/
FAIL 判定留待總司令看到 4/4 或更低的實際數字後裁示是否等價於原規則的
精神。2008／2022 兩個視窗的資料缺口在這裡登記存證，不因為只有 4 個能測
就悄悄略過不提。

**2026-09-15 總司令裁示補充（第一個訊號#243結案後追加，適用範圍擴大）**：
這條「4個TRAIN可測視窗、非原設計假設的6個」限制**不只適用於#243這一個
候選**——本協定第4節列的候選訊號清單所有訊號共用同一個TRAIN/VAL資料
邊界，往後任何一個訊號跑到這個危機視窗門檻時，都要**附上這條但書**
（4個可測、非6個），不得每測一個新候選就重新裝作這個資料缺口不存在，
也不得因為某個訊號TRAIN期表現好就悄悄不提這個落差。

## 3. 鎖定的 TRAIN 門檻（訂死存證，2026-09-15，看到任何結果前寫下）

沿用總司令 2026-09-04 原話逐字鎖定，**看到任何回測數字之前寫入本檔案並
commit**，之後不得因為結果不理想而回頭調鬆：

1. **MDD 門檻**：overlay 的 TRAIN 期最大回撤，較基底（TAIEX 買進持有）
   縮小 **≥35%**（例：基底 MDD -30% → overlay MDD 須 ≤-19.5%）。
2. **上檔捕捉率門檻**：overlay 在基底上漲日的報酬總和 ÷ 基底上漲日報酬
   總和 **≥75%**（overlay 犧牲的上檔不能超過 25%）。
3. **危機視窗門檻**：見上方第 2 節——原始定義是 6 個視窗中 ≥5 個回撤縮小，
   本協定因資料覆蓋度限制改為誠實回報 4 個 TRAIN 可測視窗的實際結果，
   不換算比例。
4. **alpha 顯著性不是門檻**，只回報（原指示第 2 點最後一句）。
5. **快殺規則**：原文「TRAIN 期 6 個危機視窗若 <4 個縮小回撤，直接判死進
   墓園，不進深度驗證」。本協定的操作型定義（因資料覆蓋度限制）：**4 個
   TRAIN 可測視窗中，若縮小回撤的視窗數 <3（原始比例 4/6≈66.7% 換算到
   4 個視窗分母最接近的整數門檻），列為「疑似快殺」，但正式判死需總司令
   確認換算方式可接受**——本協定選擇「如實回報數字＋標記疑似判定」而不
   是「自行認定換算合理就直接判死」，因為這涉及修改原始寫死數字的解讀，
   跟`CRITERIA_V2_LOCK.md`「規則寫死之後不能再動，但規則本身要不要換算
   是另一個問題」是同一種謹慎。

## 4. 候選訊號清單（每個 ≤2 參數，抗過擬合，附經濟理由，原指示逐字保留）

1. **市場廣度**（`HYPOTHESIS_QUEUE.md` #28，% 站上 200MA、A/D line、新高
   新低差）：**未實作**——這個專案抓的是個股清單，沒有現成「當日全市場
   漲跌家數」聚合欄位，臨時湊一個屬於這輪工作單位以外的地基建置量（跟
   `regime_overlay.py` docstring 記錄的「待補」原因一致），本協定不新增
   這塊地基建置。
2. **指數 200MA 趨勢濾網**（本協定「第一個訊號」，見下方第 5 節）：TAIEX
   收盤 ＞ 200 日均線＝多頭（滿曝險），反之空頭（降曝險）。經濟理由：
   200 日均線是最廣泛引用的長期趨勢分界（Weinstein 階段理論、本專案
   `strategies/weinstein_stage2.py` 已用同一定義），趨勢轉空通常領先於
   基本面惡化被市場正式定價，危機前的長期均線通常已經走平或向下，這是
   「先動」的經濟理由。參數：MA 窗口（200）、空頭曝險水位（本協定測
   0.50）——共 2 個。
3. **已實現波動度 regime**（20 日 vol 分位）：延用`regime_conditions.py`
   PIT-safe 定義（`.rolling(20)` vs `.expanding()` 中位數）。經濟理由：
   波動度往往在危機爆發前幾週就開始墊高（選擇權市場/避險需求領先反映
   不確定性上升），可以比純價格趨勢更早警示。參數：rolling 窗口
   （20）、high/low 分界（expanding median）——共 2 個。本協定這輪不
   單獨測這條，留給下一輪（見第 10 節待辦）。
4. **融資餘額成長率**當風險訊號重測（`HYPOTHESIS_QUEUE.md` #26 曾測到
   88.5 百分位，這裡改用下檔指標重新評估而非原本的選股角度）：經濟理由
   融資餘額快速膨脹代表散戶槓桿堆疊，歷史上常是市場過熱/脆弱期的同步或
   領先訊號，餘額急速去化則反映強制平倉賣壓已經發生。本協定這輪不測，
   留給下一輪。
5. **由高點回撤 X% 的斷路器**：經濟理由是直接的機械式停損，不需要任何
   領先指標，缺點是必然落後（回撤已經發生才觸發）。依`CLAUDE.md`七之三
   第9關「absorbing state 檢查」，任何用策略自身績效當輸入的機制都要
   逐筆印出觸發/解除事件確認解除條件數學上真的可能被滿足——本協定這輪
   不測，留給下一輪，且下一輪測試前要先設計解除條件的檢查。

## 5. 第一個訊號的具體規格（本輪正式測試對象）

**候選訊號2「指數200MA趨勢濾網」**，二元曝險：
- 多頭（收盤>200日均線）：曝險 1.00
- 空頭（收盤≤200日均線）：曝險 0.50

曝險用`exposure.shift(1)`對齊（收盤後才知道當天的regime，最早只能影響
下一個交易日，不能影響當天報酬），沿用`regime_overlay.py`已驗證過的
時序對齊寫法，不重新發明。

## 6. 專屬控制組（比橫截面更嚴，原指示逐字保留）

(a) **隨機開關對照**：用相同「在市時間比例」隨機切換 ≥300 次（本協定
    用排列(permutation)法：把 TRAIN 期每天的曝險值隨機打亂時間順序，
    保證每個曝險水位的總天數跟真實訊號完全相同，只打亂"哪一天"套用哪個
    曝險），overlay 的 MDD 改善百分位必須 >90。
(b) **訊號延遲1週再測**：exposure 額外多移後5個交易日，若延遲後保護效果
    消失，代表有前視偏誤，判死。
(c) **全額計入來回成本與whipsaw次數**：每次曝險水位變動視為交易，套用
    `validation/costs.py`既有費率×變動幅度比例。
(d) **參數高原**：MA窗口與空頭曝險水位各自±30%網格，結果須「一整片都
    好」不能只有單點好。
(e) **OOS期危機視窗必須同樣縮小回撤**：這條留給VAL/HOLDOUT階段，TRAIN
    階段不看。

## 7. 統計偽影家族提醒（`CLAUDE.md`七之三既有規則，本協定不重新發明）

本訊號屬於「②曝險力道連續縮放」家族的二元簡化版——`CLAUDE.md`已明講
「新機制設計優先用連續曝險縮放而非binary二元閘門」，本協定第一版刻意
先用binary版本（跟`regime_overlay.py`第一版一致），**這是已知的家族②
風險，不因為是刻意的設計選擇而放寬控制組要求**，(a)隨機開關對照就是
這條的對應控制組。

## 8. 第一個訊號TRAIN期正式結果（2026-09-15，`regime_overlay_trend_filter_gate.py`）

**結論：FAIL——死在第3節鎖定的MDD門檻（≥35%），成本是主因，不是危機視窗
關卡。** 完整逐項數字、控制組(a)(b)(d)結果、25格參數高原掃描見
`STRATEGY_GRAVEYARD.md`「TAIEX 200MA趨勢濾網 regime overlay」條目與
`TRIALS_LEDGER.md`#243（`trial_registry.register_trial()`登記）。摘要：

| 門檻 | 結果 | 判定 |
|---|---|---|
| MDD縮小≥35%（淨,扣成本） | 1.8%（毛未扣成本28.8%） | **FAIL** |
| 上檔捕捉率≥75% | 78.9% | PASS |
| 危機視窗（TRAIN可測4個） | 4/4改善 | 不影響判死（第1條已FAIL） |
| 控制組(a)隨機開關百分位>90 | 100.0 | PASS |
| 控制組(b)延遲1週MDD | -37.84%（比基底更差,翻轉） | 疑似前視偏誤 |
| 控制組(d)參數高原25格達35%門檻 | 0/25 | FAIL |

**關鍵教訓（給下一個候選訊號用）**：毛報酬看起來像通過門檻邊緣
（28.8% vs 35%門檻），但這正是`CLAUDE.md`七之三「效果量級越誇張，越
應該懷疑」的反例提醒——**任何overlay候選在還沒套用`validation/costs.py`
真實切換成本之前的數字都不是結果**，本協定第一版沒有在主線就內建成本
（`regime_overlay.py`舊版sanity測試也沒有扣成本，只是那次的性質是「非
PASS/FAIL判定」，不受這條影響），這輪把成本內建進`apply_overlay()`主
路徑（`overlay_return`欄位＝扣成本後的淨值，`overlay_return_gross`只
留作對照）之後才發現保護幾乎全被8.4次/年的切換頻率吃掉。下一個候選
（已實現波動度regime／融資餘額／回撤斷路器）開發時**從一開始就要把
`validation/costs.py`接進主結果路徑**，不要先出毛報酬再事後補。

## 9. FUT軌配合結果（`PENDING_QUEUE.md`「二」，2026-09-15，
`regime_overlay_trend_filter_gate_fut.py`）

同一套鎖定門檻、同一個候選訊號（200MA趨勢濾網，binary曝險1.00/0.50），
換標的成TX（台指期）連續合約買進持有，不重新訂門檻。選TX而非MTX/TE的
理由：`FUT_LOG.md`第335輪已查證三者重疊窗口日報酬相關係數0.997/0.955，
幾乎是同一個大盤beta的三種包裝，同家族只算一次。成本模型改用期貨慣例
（`deep_dive_fut_basis_carry.py`同款`ROUND_TRIP_COST_BPS_1X=5.0`，期交稅
主導，沿用股票的0.3%證交稅會嚴重高估期貨成本）。

**資料覆蓋度比股票軌更好**：TX連續合約起點2000-01-04（早於股票軌TAIEX
起點2010-01-04），TRAIN期（<=2020-12-31）能涵蓋2008/2011/2015/2018/2020
**5個**危機視窗（只有2022在VAL期不計），比股票軌的4個更接近原始「6視窗」
設計。

**結論：FAIL——但明顯比股票軌更接近門檻，值得記錄細節而非草草判死。**

| 門檻 | 結果 | 判定 |
|---|---|---|
| MDD縮小≥35%（淨,扣成本） | 28.1%（毛28.5%，股票軌僅1.8%） | **FAIL** |
| 上檔捕捉率≥75% | 78.3% | PASS |
| 危機視窗（TRAIN可測5個） | 5/5改善 | 不影響判死（第1條已FAIL） |
| 控制組(a)隨機開關百分位>90 | 100.0 | PASS |
| 控制組(b)延遲1週MDD | -42.30%（仍縮小23.8%,方向一致無翻轉） | 無前視偏誤疑慮 |
| 控制組(d)參數高原25格達35%門檻 | 4/25（集中在bear_exposure≤0.425角落） | 不構成「一整片都好」 |

**跟股票軌的關鍵差異**：①MDD縮小28.1% vs 1.8%，期貨成本（年化僅0.167%,
期交稅遠低於股票證交稅）幾乎不吃掉保護效果，股票軌的保護幾乎全被成本
吃掉；②控制組(b)延遲後方向一致無翻轉，股票軌翻轉為疑似前視偏誤——
這代表FUT軌的殘留保護效果不像股票軌那樣可疑；③控制組(d)參數高原4/25
集中在`bear_exposure≤0.425`的角落，這更可能是「降低空頭曝險水位機械性
縮小MDD」的效果（曝險越接近0，MDD當然越小），不是參數穩健的證據——
**不構成通過控制組(d)的「一整片都好」要求**，鎖定的基準參數點
（MA=200,bear_exposure=0.50）本身沒有跨過35%門檻，依「門檻TRAIN先訂死」
原則不能因為看到這次結果就直接改用0.35重新判過關。

**下一步（需總司令核准才能開新一輪，不是本次結果的參數優化）**：
用`bear_exposure=0.35`（或更低）當新的鎖定參數點，重新走一次完整協定
（包含控制組(a)(b)(d)），才能誠實回答「降低曝險水位本身是不是一個
更好的候選」，而不是在看到這次結果後直接宣稱0.35那個格子過關。

## 10. 下一步（本輪工作範圍之外，登記待辦不動工）

- **候選2（本輪已測，股票軌+FUT軌皆FAIL，見第8/9節）不建議直接重搜
  參數**——股票軌25格參數高原0/25、控制組(b)方向翻轉；FUT軌雖然更
  接近門檻，但能過關的格子集中在`bear_exposure≤0.425`角落（機械性
  效果，非穩健證據）。換一組MA窗口/曝險水位不會改變股票軌的結論，
  值得測的是**不同機制**（加緩衝帶抑制切換頻率、改連續曝險縮放），
  不是同一個binary機制換參數——**除非**總司令核准開新一輪用
  `bear_exposure=0.35`當FUT軌新的鎖定參數點重測（見第9節結尾）。
- 市場廣度訊號地基建置（候選1）。
- 已實現波動度regime單獨測試（候選3，目前只在`regime_overlay.py`跟
  趨勢濾網組合測，沒單獨測過)——**吸取候選2教訓，開發時第一版就把
  成本模型接進主結果路徑，不要先出毛報酬**。
- 融資餘額成長率下檔訊號重測（候選4）。
- 回撤斷路器 + absorbing state檢查（候選5）。
- 總司令裁示：第3節「5.快殺規則」的視窗數換算方式是否可接受（股票軌
  4/6分母、FUT軌5/6分母，兩軌這次都是MDD門檻直接FAIL,危機視窗改善數
  反而是好看的方向,這個換算爭議這次沒有實際影響判定,但下一個候選若
  MDD門檻邊緣擦過,就會需要這個答案)。

## 11. 候選5「回撤斷路器＋absorbing state檢查」鎖定規格（2026-09-19，看到任何
結果前寫下並commit，之後不得回頭調整；對應`PENDING_QUEUE.md`
`regime.候選5`，馬拉松自走輪次）

**帽子：研究（規格）＋驗證（單次TRAIN判定）。** 門檻沿用第3節，一個字都
不改；危機視窗仍是「4個TRAIN可測、非6個」（第2節但書）。

**機制（2個自由參數，第11關參數上限閘門≤5，OK）：**
- 基底＝TAIEX買進持有（同第0/5節）；只用TRAIN期。
- 狀態機二態：NORMAL（曝險1.00）／TRIPPED（曝險0.50，沿用第5節
  bear_exposure，不新增參數）。
- 輸入＝**策略自己的淨值（已扣成本）**相對自身歷史峰值的回撤dd
  （這正是第9關要求檢查absorbing state的那一類機制）。
- 觸發：收盤後dd ≤ −X，X＝**15%**（整數起點，非搜出來；TRAIN期基底MDD
  量級約−30%，15%約是一半，事前理由是「回撤到基底最大回撤的一半才認賠
  降曝險」）。
- 解除：收盤後dd ≥ −X×R，R＝**0.5**（回到觸發線的一半，即−7.5%；製造
  遲滯帶防止在觸發線附近來回切換）。
- 時序：當日收盤後決定的狀態，最早影響下一交易日（同`shift(1)`慣例）。
  峰值不因觸發而重置（重置＝人為製造解除，屬於偽影，不做）。
- 切換成本：第一版就內建`COST_PER_UNIT_EXPOSURE_CHANGE`於主結果路徑
  （吸取候選2教訓）。成本前置關卡（1a-0）：預期切換頻率遠低於
  8.4次/年（狀態機每次觸發至少要−15%回撤），成本門檻不成為死因，可進
  gate 1。

**Absorbing state檢查（第9關，逐筆印出，不只信聚合統計）：**
1. 逐筆列出每一次「觸發日／解除日／持續交易日數／觸發時dd／解除時dd／
   期間基底報酬」事件表。
2. 數學可達性：解除需自身淨值由谷底回升 (1−X×R)/(1−X)−1＝8.8%，TRIPPED
   曝險0.50下基底需回升約17.6%——有限、非不可能，但**遲滯帶非零**。
3. 判準（鎖定）：任何一次觸發後「從未解除直到樣本結束」，或任一次
   TRIPPED持續 >504個交易日（約2年），或全期TRIPPED時間占比 >50%，
   一律判「準吸收態」＝機制FAIL（記錄，不回頭調R湊過關）。

**控制組（同第6節）：** (a)排列法n=300，曝險值排列後成本照扣，百分位>90；
(b)延遲5交易日（狀態機因回饋路徑依賴，延遲版須重跑整條狀態機，不是
事後平移）；(c)報切換次數與年化成本；(d)參數高原：X∈{10.5,12.75,15,
17.25,19.5}% × R∈{0.35,0.425,0.5,0.575,0.65}，25格，須「一整片都好」，
同時每格也跑absorbing判準。判定順序：MDD縮小≥35%（淨）未過→FAIL，直接
記墓園，不進深度驗證、不調參數。

## 12. 候選5結果＋候選3「已實現波動度regime單獨測試」鎖定規格（2026-09-19，
看結果前寫下並commit）

**候選5結果（`regime_overlay_drawdown_breaker_gate.py`，已登記`TRIALS_LEDGER.md`
#246，墓園條目見`STRATEGY_GRAVEYARD.md`）：FAIL。** MDD縮小13.5%（<35%）、
上檔捕捉62.7%（<75%）、準吸收態（TRIPPED占比71.1%、最長719交易日）、
參數高原0/25。詳見墓園條目。

**候選3規格（`PENDING_QUEUE.md` `regime.候選3`；帽子：研究＋驗證；門檻沿用
第3節，4個TRAIN可測視窗但書照附）：**
- 基底＝TAIEX買進持有，TRAIN期。二態曝險：高波動→0.50、低波動→1.00（沿用
  第5節bear_exposure，不新增參數）。
- 訊號：`vol20 = 日報酬.rolling(20).std()`（`regime_conditions.py`第72行
  PIT-safe既有定義），高波動＝`vol20 > vol20.expanding(min_periods=60).median()`。
  只有2個參數（rolling窗口20、分界＝expanding中位數），皆為既有定義，
  不搜參數。
- 時序：收盤後決定、`shift(1)`次日生效；**成本第一版即接入主路徑**
  （`COST_PER_UNIT_EXPOSURE_CHANGE`，吸取#243教訓）。
- 成本前置關卡（1a-0）：預期切換頻率——vol20穿越expanding中位數會頻繁
  來回（比200MA更雜訊），**預期>10次/年**；#243在8.4次/年已被成本吃到
  MDD縮小僅1.8%。這關的預期判斷是「大概率成本前置關卡未過」，但仍實跑一次
  以取得實測切換次數證據（不是靠預期直接判死；若實測切換頻率>8.4次/年
  且淨MDD縮小<毛的一半，死因標「成本前置關卡未過」）。
- 控制組(a)(b)(c)同第6節（(b)延遲5日）；(d)參數高原：rolling窗口
  ∈{14,17,20,23,26}（±30%）×分界分位數∈{0.35,0.425,0.5,0.575,0.65}
  （expanding分位數，取代中位數；0.5＝鎖定基準點）。
- 判定順序：MDD縮小≥35%（淨）未過→FAIL，直接記墓園，不調參數。

## 13. 候選3結果＋候選1「市場廣度regime」鎖定規格（2026-09-19，看結果前寫下
並commit）

**候選3結果（`regime_overlay_realized_vol_gate.py`，`TRIALS_LEDGER.md`#247）：
FAIL——成本前置關卡未過。** 毛MDD縮小36.9%剛過門檻，切換135次（年12.9次）
成本年化4.43%把淨MDD縮小吃到0.0%；上檔捕捉72.9%亦未過；參數高原0/25。

**候選1規格（`PENDING_QUEUE.md` `regime.候選1`；已先讀`#28`死因：`#28`是
「廣度背離旗標」binary 0.3/1.0，300檔樣本，percentile 54/51貼在50；本條
是「廣度水位」當降曝險regime，用途不同，且宇宙改用本機全部4位數代號個股
快取而非300檔樣本——**但這個宇宙是「今天仍在市」的快取，存活者偏誤
（偽影⑦）未修正，結論須附此但書**）：
- 訊號：`breadth_t` ＝ 當日有效股票（已有≥200日歷史）中「收盤>自身200日
  均線」的比例（純過去資料，PIT-safe）；用未還原收盤價（除息缺口會輕微低估
  廣度，已知偏差，不修）。
- 規則：`breadth_t < 0.50`→曝險0.50，否則1.00（固定門檻0.50，「不到一半個股
  站上長期均線」的自然分界，非搜出來；2個參數：MA窗口200、門檻0.50）。
- 時序：`shift(1)`次日生效；成本第一版即接入主路徑（`apply_overlay`）。
- 基底＝TAIEX買進持有，TRAIN期；門檻、控制組(a)(b)(c)同第6節；(d)高原：
  MA窗口{140,170,200,230,260}×門檻{0.35,0.425,0.5,0.575,0.65}。
- 成本前置關卡：預期切換頻率介於200MA濾網與波動度之間（廣度是平滑序列但
  在門檻附近會來回），實測切換>8.4次/年且淨MDD縮小<毛的一半→死因標「成本前置
  關卡未過」。判定順序同前：MDD縮小≥35%（淨）未過→FAIL，不調參數。
- 若跟`#28`同死因（訊號貼在50%無加值）→直接記FAIL，不勉強做完整協定。

## 14. 候選1結果＋候選4「融資餘額成長率下檔regime」鎖定規格（2026-09-19，
看結果前寫下並commit）

**候選1結果（`regime_overlay_breadth_gate.py`，`TRIALS_LEDGER.md`#248）：FAIL
——成本前置關卡未過＋上檔捕捉不足。** 淨MDD縮小−12.6%（毛26.4%）、切換
9.5次/年、半倉時間56.5%；高原6/25全在門檻0.575/0.65的機械性恆半倉角落。
與`#28`死因不同（廣度序列有資訊，但被成本吃掉）。存活者偏誤但書照附。

**候選4規格（`PENDING_QUEUE.md` `regime.候選4`；已讀`#26`/`#56`：輸入序列與`#26`
逐字相同（全市場總融資餘額，TWSE MI_MARGN，`margin_debt_market_client.py`），
`#26`死於train/val正負號相反；本條改成「降曝險regime＋成本＋控制組」用途，
方向事前綁定如下，不看結果選方向）：**
- 資料：本機快取週頻（每週五）全市場融資餘額，2012-05-04起；TAIEX日頻。
  **資料覆蓋度但書**：起點2012-05、加上成長窗口與expanding暖機，實測可判
  期約2013下半起，TRAIN可測危機視窗僅2015/2018Q4/2020Q1共**3個**（2011落在
  資料起點之前），比股票軌其他候選的4個更少，須附此但書。
- 訊號：`growth_t = 餘額_t / 餘額_{t-12週} − 1`；「過熱」＝`growth_t >`
  過去`expanding(min_periods=52週)`第80百分位。**方向事前綁定＝過熱→降曝險**
  （經濟理由：散戶槓桿堆疊＝脆弱期；不測去化方向，不看結果換方向）。
- 規則：過熱→曝險0.50，否則1.00；訊號於週五收盤後已知，前向填補到之後的
  日頻，`shift(1)`次日生效；成本第一版即內建（`apply_overlay`）。2個參數
  （12週、第80百分位）。
- 基底＝TAIEX買進持有TRAIN期；門檻、控制組(a)(b)(c)同第6節；(d)高原：
  成長窗口{8,10,12,14,16}週×百分位{0.60,0.70,0.80,0.90,0.95}。
- 若毛效果本身已無（如與`#26`同死因：單一總體序列樣本自由度天生不足）→
  直接記FAIL。判定順序同前：MDD縮小≥35%（淨）未過→FAIL，不調參數。

## 15. FUT軌`bear_exposure`事前網格鎖定（2026-09-19總司令裁示【裁示】
三，看到任何網格結果前寫下並commit）

**背景**：`PROPOSAL_2026-09-19_fut_regime_overlay_bear035.md`提案「以
0.35為新鎖定點重測」，但提案自己誠實揭露「0.35是看過`#244`（原始
bear_exposure=0.50）結果後才挑的」——總司令裁示：**單挑事後選定的
0.35重測，跑出來的顯著性沒有意義，不准直接跑A（核准0.35單點重測）**。
改成事前網格，把0.35連同鄰近值一起測，三格全部登記進
`trial_registry.register_trial()`（進而計入`selection_bias_ledger.py`
的N），**必須報告三格的完整結果，不准只報最好看的那一格**。

**鎖定規格**（`MA_WINDOW=200`固定不變，沿用`#244`原始鎖定，只有
`bear_exposure`是本次要測的變因，其餘規格——基底/成本模型/門檻/
控制組(a)(b)(c)(d)——完全沿用第9節`regime_overlay_trend_filter_gate_
fut.py`既有函式，不重新設計）：

- **網格**：`bear_exposure ∈ {0.25, 0.35, 0.45}`（三格，事前決定，不是
  看結果後再挑）。
- **判準（跟第3節鎖定門檻一致，逐格套用）**：MDD縮小≥35%（淨）、上檔
  捕捉率≥75%、危機視窗改善數（TRAIN期可測5個）、控制組(a)隨機開關
  百分位>90、控制組(b)延遲1週後方向是否翻轉、控制組(c)成本。
- **高原/懸崖判準（本次新增，套用既有「參數高原」精神但反過來用）**：
  若三格裡只有0.35（或任何單一格）通過MDD縮小≥35%門檻、其餘兩格都不
  通過，判定為**參數懸崖（parameter cliff）**，整體結論**FAIL**，不得
  以「至少有一格PASS」為由判PASS——跟`CLAUDE.md`七之三節既有的「參數
  高原」通過標準（一整片都好，不是單點）同一個精神，這裡反過來當否決
  規則用：單點好、鄰居不好＝訊號脆弱／過擬合到那個特定水位，不是穩健
  發現。**三格都PASS或三格都FAIL，才是乾淨的結論（分別是「效果穩健、
  只是原始0.50選太保守」或「這個訊號在期貨軌換曝險水位也救不回來」）**；
  只有中間格PASS、兩端都FAIL（或任何非單調的鋸齒狀結果）比照懸崖處理，
  同樣判FAIL並在報告裡明確描述形狀，不假裝是高原。
- **事前聲明**：本次網格結果無論如何都要寫進`STRATEGY_GRAVEYARD.md`
  （PASS或FAIL都要記，不是只記FAIL），且每一格各自登記一筆
  `trial_registry.register_trial()`（3筆），供`selection_bias_ledger.py`
  正確計入多重比較N——不因為這是「同一個提案的三個網格點」就只登記
  一筆，那會低估搜尋成本，跟`CLAUDE.md`七之三節「V的來源與樣本數」
  的既有紀律衝突。

## 15. 替代A「連續型曝險調節」鎖定規格（2026-09-19，看到任何報酬／MDD數字
之前寫下並commit，之後不得回頭調整；對應`PENDING_QUEUE.md` `regime.替代A`，
馬拉松自走輪次）

**帽子：研究（只寫規格＋成本前置估算）。** 本輪**不跑任何報酬／MDD回測**；
單次TRAIN判定留給下一個驗證帽輪次（做與判分離）。門檻沿用第3節，一個字都
不改；危機視窗仍是「4個TRAIN可測、非6個」。

**與二元版(`#243`)的關係（事前綁定，不看結果選訊號）：** 訊號來源與`#243`
**完全相同**——TAIEX收盤 vs 200日均線，只把「站上/跌破二選一」換成「離均線
多遠」的連續函數。這是「同一訊號、換機制形式」的對照，不是新訊號。
選200MA而非候選5回撤訊號的理由：候選5的輸入是策略自身淨值（第9關類），
連續化會另外引入路徑依賴；200MA訊號只依賴外生價格，對照最乾淨。

**機制：**
- `dist_t = close_t / MA200_t − 1`；`sd_t = dist的expanding標準差`
  （`min_periods=252`，只用當下與過去，PIT-safe）；`risk_z_t = −dist_t / sd_t`
  （越在均線下方越大）。
- 曝險 `E_t = clip(1 − k × risk_z_t, 0.5, 1.0)`；`k = 0.5`（事前綁定：均線
  下方1個標準差達地板0.5）。地板0.5＝二元版`bear_exposure`，不新增參數。
- 時序：`E`收盤後才知道，`shift(1)`次日生效；成本第一版就內建，主結果路徑
  用`regime_overlay_trend_filter_gate.COST_PER_UNIT_EXPOSURE_CHANGE`
  （0.685%/單位曝險變動，與二元版同一慣例，並排比較才公平）。
  **已知保守偏誤（誠實揭露）**：該慣例每單位曝險變動收「來回」成本，實際單向
  變動只付一邊；此偏誤對二元版與連續版一視同仁，僅供並排比較，
  另附「單向成本」敏感度**只回報、不作判定依據**。
- 自由參數：k（1個）；MA視窗200沿用`#243`、地板0.5沿用`#243`、
  `min_periods=252`為結構常數，皆非新fit參數。≤5個，第11關OK。
- 基底＝TAIEX買進持有TRAIN期（2010-01-04～2020-12-31）。

**成本前置關卡（1a-0，本輪已算，`regime_alt_a_cost_precheck.py`，只用訊號序列，
不涉及報酬）：** 主規格年化Σ|Δ曝險|＝2.16、年化成本拖累1.48%；對照二元版
4.10／2.81%，連續版成本約為二元版的53%。網格9格年化成本拖累1.38%～2.37%。
平均曝險0.914、曝險≤0.5的天數僅11.1%（二元版空頭時間約為其數倍）。
**判讀（事前寫下）**：成本不是先天死因，但**保護力度先天小於二元版**——
二元版毛MDD縮小28.8%（<35%門檻）已是在「空頭全程半倉」下取得，連續版半倉
時間更短，毛保護預期不會更強。這是成本前置關卡「介於50%~100%」以外的
另一種風險（保護力度不足），**不足以本輪直接判死**（沒看報酬不下結論），
但SPEC事前預期為「低機率通過」，寫在這裡供日後對照，不得因結果而改寫。

**判定（鎖定，順序同前）：**
1. 淨MDD縮小≥35% 且 上檔捕捉≥75%（第3節）→ PASS，進深度驗證。上檔捕捉
   >85%另標「強通過」（佇列原文條件，僅作標籤）。`[自行裁量]`：佇列原文
   只寫「>85%且MDD改善→深挖」，75%～85%區間未定義，此處取第3節既有鎖定
   門檻為準，不另訂更鬆或更緊的新門檻。
2. 任一未過 → FAIL，**連續形式與二元形式一起結案**，機制類別「曝險水位
   調節函數」不再嘗試第三種函數形式（除非總司令另行核准換函數形式本身）。
   `[自行裁量]`：佇列原文只明寫「上檔捕捉<75%→兩種形式結案」；「MDD未過但
   上檔≥75%」（如`#243`）未寫分支，因第3節PASS需兩條件同時成立，唯一非FAIL
   出口是PASS，故一律走結案。

**必報表（並排，同一訊號二元vs連續）：** MDD（毛/淨）、上檔/下檔捕捉、CAGR、
Sharpe/Sortino/Calmar、年化Σ|Δ曝險|與成本拖累、危機視窗4個逐一、平均曝險。

**控制組（同第6節，連續版特別處理）：**
(a) 排列法n=300：把TRAIN期每日曝險值時間順序打亂（總曝險分布相同），成本
    照扣，overlay淨MDD縮小百分位須>90；
(b) 延遲5交易日重跑（曝險額外`shift(5)`），保護若消失＝疑似前視；
(c) 報切換次數（連續版以「曝險變動日數」與Σ|Δ|表示）與年化成本；
(d) 參數高原：MA視窗{140,200,260}(±30%)×k{0.35,0.5,0.65}(±30%)，9格，
    「一整片都好」＝**≥7/9格同時達MDD≥35%（淨）且上檔≥75%**，另每格報
    年化成本。
(e) **家族⑥提醒**：連續縮放屬統計偽影家族②（曝險力道連續縮放），控制組
    通過標準沿用「排列法百分位>90」與(d)高原，不因「這是刻意設計」放寬。

**登記：** 驗證輪次跑完後用`trial_registry.register_trial()`登記9個網格點
（每個「機制×參數點」一筆，未登記判定無效），並更新
`SELECTION_BIAS_LEDGER.md`的N與Bonferroni門檻；本輪為規格輪，**尚未登記
任何判定**。holdout全程不碰。存活者偏誤但書：TAIEX指數本身無個股存活者
問題，此條不適用（僅報酬指數/價格指數口徑須在結果中註明：用價格指數，
未含股利，基底與overlay同口徑）。
