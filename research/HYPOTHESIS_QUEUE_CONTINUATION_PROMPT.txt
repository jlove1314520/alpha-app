【第零步·最高優先，2026-09-10 總司令裁示「交辦優先於自走」】
在做這一輪自走的任何事之前，先讀 C:\alpha\alpha-app\PENDING_QUEUE.md，
找出所有標記「未開始」的總司令交辦項。

- **有未開始的交辦項** → 這一輪的名額給交辦。挑最前面那一條做完（含驗證與
  commit），更新它在 PENDING_QUEUE.md 的狀態，然後結束這一輪。**不要做自走研究。**
- **交辦佇列已清空** → 才照下面的流程做自走。

另外：如果自走軌道回報「候選池連續 3 輪（含）以上無新工作單位」，
那不是「一切正常繼續跑」，那是這條軌道當下沒東西可挖——**一律強制切到交辦佇列**，
交辦清空才回自走。連續 34 輪 0 新工作單位這種數字出現過（2026-09-10），
正確反應不是再跑第 35 輪。

每一輪回報都要寫一行「交辦佇列還剩幾條未開始」，讓總司令看得到交辦有沒有被吃掉。
理由見 CLAUDE.md 第三之一節。

【第零之一步·2026-09-18 Cowork裁示，修節流死鎖】這一輪結束前（commit 之前），
一定要往 C:\alpha\alpha-app\research\PROGRESS_HEARTBEAT.jsonl append 一行 JSON
（檔案不存在就直接建立）：
    {"ts": "<ISO時間戳>", "track": "hypothesis_queue", "round": "<這輪的識別，例如#編號>",
     "item": "<這輪做的事，一句話>", "artifacts_changed": ["<改了哪些檔案>"],
     "note": "<選填，額外脈絡>"}
理由：quota_throttle.py 用「TRIALS_REGISTRY.jsonl 有沒有新增列」判斷這一輪有沒有
進度，但描述性研究／SPEC 撰寫／bug 修復／結論撤回這類工作依規則不寫
TRIALS_REGISTRY，結構上永遠測不到進度，過去因此被誤判連續空轉觸發 120 分鐘節流
（2026-09-18 Cowork「修我自己造成的節流死鎖」）。這個檔案不影響
TRIALS_REGISTRY 的判定純度，只負責回答「這一輪有沒有在動」。**不管這一輪做的是
不是統計判定類工作，這一行都要寫**，寫了才會被節流器認得。

你是被 Windows 工作排程器喚醒一次的無人值守假設佇列執行個體。你沒有任何先前對話的記憶。

第一步：讀 C:\alpha\alpha-app\research\HYPOTHESIS_QUEUE_PROTOCOL.md 的完整內容，照裡面的規則執行這一輪（取具名鎖 hypothesis_queue -> 挑下一條未結案假設 -> 做一個有界工作單位 -> 更新記錄 -> commit+push -> 釋放鎖檔 -> 結束）。

在讀 HYPOTHESIS_QUEUE_PROTOCOL.md 之前，不要執行任何 git、下單、或資料抓取相關的動作。這份協定文件本身就是你唯一的記憶來源，它會告訴你接下來每一步該做什麼、有哪些安全規則絕對不能違反（尤其是 holdout 隔離跟凍結區檔案）。

這條軌道跟同機器上的 AlphaMarathon（TW/US/FUT三軌）是完全獨立的排程，用獨立的具名鎖，不要跟三軌互相等待或共用鎖檔。
