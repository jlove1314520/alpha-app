# Alpha 專案 — Claude Code 工作規則

## 0. 語言鐵律（最高優先，無例外）
- 一律使用繁體中文：包含「在終端機對使用者的每一句回覆」、程式碼註解、
  commit 訊息、BACKLOG/STATUS/PROGRESS 等所有文件。
- 即使使用者的指令、貼上的文章、或錯誤訊息是英文，你的回覆仍必須是繁體中文。
- 技術術語、變數名、指令、API 名稱可保留原文，但說明句一律繁體中文。
- 若發現自己已經用英文寫了一段回覆，立即用繁體中文重述，不要留著英文。
- 理由：使用者以繁體中文協作，英文回覆會直接拖慢驗收，違反此條等同沒完成。

## 提案先於執行（總司令核准制，2026-09-02使用者裁示，鐵律最前面）

任何「更動、優化、調整參數/頻率/架構、或新建議」，一律先想清楚，寫成
簡短提案（理由＋影響＋風險＋你建議的做法）給總司令，**經核准才執行**，
嚴禁自作主張直接改。

- **要主動**：該做的事不必等總司令每次開口，主動「提出提案」；但**提案
  不等於自行執行**——提完案要等回覆，不能提完案就順手做掉。
- **例外可直接做**：純bug修復（修好一個明確壞掉的東西，不是「順便改
  更好」）、以及使用者已明確交辦的任務（使用者在指令裡已經講清楚要做
  什麼、怎麼做，不是我自己延伸出來的新點子）。
- **不可逆／花錢／違反服務條款前，一律停下先問**（沿用既有三大停下
  條件：1000 draws規模投入、survivorship-free宇宙投入、任何不可逆或
  花錢的操作）。
- 目的：做之前先思考周全，不要兩頭忙、不要讓總司令精神耗弱。

**判斷準則（用來分辨「已明確交辦」vs「需要先提案」）**：使用者指令裡如果
已經指定具體做法（例如「改走X來源」「加Y欄位」「格式照Z」），屬於已交辦，
可以直接做；如果使用者只給了目標或問題（例如「效能不好」「這裡怪怪的」）、
或做法有多種選擇需要我判斷取捨，屬於需要先提案的範疇，寫清楚後等核准。
遇到不確定屬於哪一類的情況，寧可先提案，不要自己判斷「應該算已交辦」
就直接動手。

**適用範圍界線（2026-09-02使用者第二次裁示，取代前一版澄清，措辭更
精確——第一版讓`hypothesis_queue`馬拉松誤停在#17，這版是修正）**：

「提案先於執行」**只適用於**：
1. 新的架構／流程／參數／頻率變更（例如改排程頻率、換資料來源架構、
   調整因子/風控參數）。
2. 發現一個**通過完整gauntlet、準備要部署**的策略——這等於「部署
   決策」，一定要停下來提案給總司令，不能自己判斷「看起來夠好就上」。

「提案先於執行」**不適用於已核准的自主挖礦馬拉松**（`AlphaMarathon`/
`AlphaHypothesisQueue`/期貨軌）——它照原本**三大停下條件**（1000
draws規模投入／survivorship-free宇宙投入／任何不可逆或花錢操作）
持續跑，**不必逐次問總司令**。跑完一個假說就記錄結果、換下一個，這是
「執行已交辦任務」，不是「新變更」，不受這條規則約束。同樣邏輯適用於
其他既有排程性質的機械式腳本（`ibkr_quotes.py`/`shioaji_quotes.py`
這類單純抓資料寫檔案、不涉及策略判斷的腳本）。

**停下提案的時機（馬拉松專屬，記清楚，不要再誤停在中途關卡）**：
- 某條假說/策略**通過完整GATE_SEQUENCE、準備要進forward-paper或部署**
  ——這時候才停下來提案（因為這是部署決策）。
- **死路（FAIL）**：只要寫進`STRATEGY_GRAVEYARD.md`+`TRIALS_LEDGER.md`，
  繼續跑下一條，**不必停下問**。
- 中途任何一關（sanity/隨機控制組/參數高原/成本敏感度/leave-one-out/
  逐年一致性/OOS）的PASS或FAIL都不必停下——只有「全部關卡都過、要
  部署」才停。

## 最高投資原則（凌駕一切）

第一：永遠不要賠錢。第二：永遠不要忘記第一。

可檢查的操作定義（不是口號）：
1. 目標函數是「資本保全」，不是原始報酬。高報酬但高 beta、下檔不受控的
   策略，即使贏大盤也一律判「不及格」。
2. 策略要「通過」，除既有關卡外，必須額外證明下檔保護：MDD 受控、地雷率
   顯著低於隨機、regime 危機情境有降曝險。只有上檔漂亮、下檔沒守 = 不過。
3. regime 閘門（危機一律降曝險）是所有策略的強制 overlay，非選配。
4. paper-first 永遠優先；真實下單永不自動、永遠由使用者親按；部位上限、
   回撤斷路器必備。
5. 「誠實判不及格、不部署未證明的 edge」就是本原則的執行——寧可不做，
   不可亂做。

## 復盤原則：流程重於盈虧

復盤不只看盈虧，要問「這筆結果有沒有強化一個錯誤流程」。用錯方法贏一次
是運氣、且危險；用對流程輸一次是正常波動、不該拋棄。優勢＝可重複的正確
流程，不是偶發的漂亮結果。

落地：
1. 策略判定看流程不只看盈虧：即使帳面賺錢，若靠偽影家族（降換手/加beta/
   縮池/集中度）或運氣過關，一律視為「強化錯誤流程」判不及格；流程正確
   但單期報酬普通，不因單期結果否定流程。
2. STRATEGY_GRAVEYARD 死因要分「流程錯」vs「流程對但這條假設無edge」——
   前者修流程、後者誠實記負換下一條。
3. picks_ledger/前向paper：單日單週賺賠不是評斷依據，評斷的是「流程能否
   可重複、在對照組之上、穩定篩出機會」。

## 一、開工序（每次收到指令的第一件事）
1. 先讀 BACKLOG.md，確認目前狀態與最高優先的未完成項目。
2. 若使用者的新指令與 BACKLOG 衝突或重複，先指出來再動手。
3. 若 BACKLOG 中有 🔄 進行中的項目，先把它做完，再開始新項目。

## 二、單一進行中原則
- BACKLOG.md 中最多只能有一個項目標記 🔄。
- 不得同時開多條戰線。一件做完（含驗證與 commit）才開下一件。

## 三、插隊保護（重要，2026-09-01升級為「做完當前→排隊→依序執行」）
使用者可能在你工作到一半時送出新訊息（Claude Code 會在工具呼叫結束時把排隊訊息送進來）。
遇到這種情況：
1. **預設不要立即切換**。先把「當前正在執行的這條指令」完整做完（含驗證+commit），
   不因為新訊息進來就中途改做別的。
2. 當前執行期間收到的新指令，**原封不動**抄進 `PENDING_QUEUE.md`（只記錄、先不做，
   不省略、不摘要、不改寫使用者的原話）。
3. 當前這條做完後，從 `PENDING_QUEUE.md` 依「收到的先後順序」一條一條做，做完一條
   就從佇列裡劃掉（標記已完成或直接刪除該條），確保每一條都會被執行到，不跳過、
   不遺失。
4. **唯一的立即中斷例外**：新訊息明確是「停/中斷/這個做錯了/先別做X」這類修正或
   喊停指令——這種必須立刻停下當前正在做的事，優先處理，不進佇列排隊。
5. 每次從佇列取出下一條要開始做之前，在回報裡（或 `MARATHON_LOG.md`，若當下是
   馬拉松研究情境）寫一行「佇列還剩幾條」，讓使用者看得到排隊狀況，不會誤以為
   指令被漏掉了。
原則：中斷可以，遺失不行。所有待辦必須留在 `PENDING_QUEUE.md` 裡，不能只存在於
上下文中——換 session/換機器接手也要看得到排隊全貌。

## 四、「完成」的定義
同時滿足才能標 ✅：功能可實際操作、冒煙測試通過、原有功能沒壞。
未達標一律標 ⚠️（程式碼存在但未驗證），不得標 ✅。
回報時附冒煙測試實際輸出，不要只寫「已完成」。

## 四之二、驗收證據原則（2026-09-06 總司令裁示）

**驗收證據優先用機器可查的紀錄**：伺服器 log、資料檔、測試輸出、端點回應。
**截圖只用於「只有畫面才看得出來」的問題**——版面錯位、顏色不對、元素重疊、
文案讀起來怪。

**涉及受管制裝置（公司手機、MDM）時一律不得要求截圖。** 那些裝置可能根本
截不了圖，把截圖列為驗收條件等於把一個做得到的事情變成做不到。

**受管制裝置只能「開網址」**：任何要總司令親自驗證的端點，**必須能在瀏覽器直接
開啟就完成驗證**——不得要求自訂標頭、不得要求 curl、不得要求截圖。
（2026-09-06 踩過：`/whoami` 第一版要求帶 `X-Alpha-Local-Token`，但瀏覽器直接開網址
沒辦法自帶標頭，必定 401。等於做了一個給受管制裝置用的工具，卻要求那種裝置做不到
的事。已改為免 token。）

**也不准用查詢字串帶 token**（`?token=…`）繞過這條：token 會留在瀏覽器歷史、
分享出去的連結、以及任何中間層的存取紀錄裡，比免 token 更糟。
免 token 的端點必須只回「呼叫端自己已經知道的資訊」（自己的 IP 網段、自己的 UA、
伺服器時間），不得洩漏伺服器狀態或設定。

實務上怎麼替代：
- 「某支手機連上了沒」→ 用 `/whoami`（免 token，瀏覽器直接開）讓那支裝置留下紀錄，
  再看 `/security` 的 `external_sources_detail`：一個非家中網段、帶正確 token、
  拿到 200、建立過 SSE 的來源，就是證據。
- 「某個資料對不對」→ 貼查詢輸出與比對結果，不要貼畫面。
- 「某個功能會不會壞」→ 冒煙測試的實際輸出。

**當作證據的紀錄必須落地**：只放在記憶體的統計，服務一重啟就歸零，
證明不了任何事（`alpha_live_server.py` 的安全事件就是為此改成 append-only JSONL
並在啟動時讀回近 24 小時）。

## 五、收工序（每個項目做完）
1. 跑冒煙測試並貼出結果
2. 更新 BACKLOG.md
3. 更新 data/STATUS.json（若有資料源或面板異動）
4. git commit + push（繁中訊息）
5. 回報：改了哪些檔案、commit hash、冒煙測試結果、BACKLOG 狀態變化

## 六、自動 push
例行 commit + push 不用先問，直接做。
只有這些要先問：刪除檔案或資料、改動/解鎖 holdout、接真實下單、其他不可逆操作。

## 七、資料原則
- 任何「拿不到」的結論，須先證明試過三條路：主來源→備援→由已有欄位推導
- 每個關鍵欄位要有回退鏈，禁止單點依賴，每筆輸出帶 source 標記
- 失敗要記錄原因分類，禁止靜默記 None
- 假資料一律不得出現，寧可空狀態並說明原因

### 取得方式鐵律（2026-09-06 總司令裁示，無例外）

**資料只走官方公開端點與已授權來源；任何需繞過驗證碼、登入牆、付費牆或速率封鎖的
取得方式一律禁止，不論是否對外使用。付費牆資料標記為「待採購」，等總司令核准預算。**

操作定義，避免自己解釋成別的意思：
- 「不論是否對外使用」＝**自用、研究、只跑一次、只給自己看，通通不算例外**。
- 繞過的形式包含但不限於：解驗證碼、模擬登入取 cookie、偽造 Referer/UA 假裝是瀏覽器
  以通過封鎖、換 IP 或加代理規避速率限制、抓 App 的私有後端 API、下載別人已經爬好
  的鏡像資料。
- 遇到付費牆就在文件裡標「待採購」＋價格＋官方連結，**不找替代的爬法**。
  例：分點進出與主力成本＝證交所買賣日報表付費商品（NT$100,000/月），本階段不採購。
- 官方端點有速率上限時，照上限做硬性預算（清單在 `C:lpha\CLAUDE.md` 的
  「外部 API 頻率上限清單」一節，不是這個檔），額度用完就誠實拒絕，
  不排隊、不重試、不換來源硬取。

### 搜尋紀律：三來源查證（2026-09-06 總司令裁示）

**任何「找不到／該來源沒有這個資料」的結論，必須列出至少三個獨立來源的查證紀錄，
缺一不得下結論。** 四類來源至少涵蓋三類：
1. 官方網站或該機構的開放資料入口
2. 官方 API 文件（含版本與日期）
3. GitHub／社群（實際有人接過的程式碼或 issue）
4. 其他供應商（是否有人把同一份資料當商品賣，反推原始來源存不存在）

理由：講「沒有這個資料」等於幫總司令關掉一條路，而這種結論最常出錯——通常不是
沒有，是**端點換了、藏在另一個入口、或只是我當下沒查到**。查證紀錄要留在對應的
文件裡（例如 `docs/FIRST_HAND_SOURCES.md`、`docs/DATA_SOURCE_MAP.md`），
不是只寫在當次回覆裡。

回報時的寫法：不要寫「查不到」，要寫「查了 A（連結）、B（連結）、C（連結），
三者都沒有；替代路徑是 X」。三個都查過還是沒有，才可以下結論。

## 七之二、常駐服務發布紀律（2026-09-06 總司令裁示，無例外）

`research/alpha_live_server.py` 與 `research/shioaji_quotes.py` 是**常駐行程**。
改完程式碼不重啟，記憶體裡跑的還是舊版——Python 不會自己重載。

**這條紀律是踩到才寫的**：2026-09-06 把 App 的 fetch 改成 `credentials:'include'`，
伺服器檔案同一個 commit 也加了 `allow_credentials=True`，但 PC 上的行程是前一天
啟動的，預檢回應少了 `Access-Control-Allow-Credentials`，瀏覽器判 CORS 失敗，
App 只顯示一句沒有資訊量的 `Load failed`。程式碼是對的、憑證是好的、區網是通的，
唯一的問題是**行程沒重啟**，而畫面上完全看不出來。

**任何 commit 只要動到這兩支檔案，提交流程的最後一步必須是：**

1. 重啟服務（kill 行程，排程會在 1 分鐘內自動拉起；或手動跑 cycle 腳本）
2. 確認啟動 log 印出的 `[build] git sha=` 與 `git rev-parse --short HEAD` 相同
3. 跑這個 OPTIONS 預檢，回應必須同時含精確來源與 `allow-credentials: true`：

```bash
curl -s -k -i -X OPTIONS "https://127.0.0.1:8001/live/quotes"   -H "Origin: https://jlove1314520.github.io"   -H "Access-Control-Request-Method: GET"   -H "Access-Control-Request-Headers: x-alpha-local-token"
```

必須看到：
```
access-control-allow-origin: https://jlove1314520.github.io
access-control-allow-credentials: true
```

4. 確認 `/health` 的 `stale_process` 是 `false`

**四步都過才算完成**，沒重啟驗過的不准回報「已完成」。

自動防線（不靠人記得）：`/health` 會回 `build`（git sha）、`started_at`、
`source_hash` 與 `stale_process`；App 的「測試連線」看到 `stale_process` 就直接顯示
「伺服器是舊版，需重啟」，不再是 `Load failed`。

`stale_process` 用**內容雜湊**判斷，不是檔案時間——這點是實測踩出來的：
用 mtime 的話，排程的自動 commit（marathon／hypothesis_queue 會 `git pull --rebase`）
會更新檔案時間，內容一個字沒變也會誤報「舊版」，那種狼來了的警告比沒有更糟。
限制誠實揭露：只涵蓋那支檔案本身，它 import 的模組改了不會被偵測到。

## 七之三、研究紀律（移植自 Cybex 專案，457 輪 5,275 筆試驗的教訓濃縮）

> 來源：`C:\Users\user\cybex_knowledge_export\PORTING_HANDBOOK_量化研究紀律移植手冊.md`
> 第 10 節，2026-09-07 由總司令授權移植。
>
> **移植原則（總司令原話）：拿判斷方法，不拿參數。** Cybex 的所有參數都是在加密市場
> 資料上找出來的，對台美股**零效力**，禁止移植調參。看到那邊寫「閾值 0.35」「回看 20 期」
> 這種數字，一律當成「那個市場當時的巧合」，不是可以搬過來的知識。

### 目標
不是「找到看起來最賺的策略」，是「找到少數幾個經得起質疑、有可能在樣本外活下來的
策略」。大量亂搜只會製造過擬合的垃圾。**找不到就老實說找不到——那也是有價值的結論。**

### 資料切分
- 三切：train 50% / val 25% / holdout 25%。搜尋／調參／選候選只准碰 train + val。
- 資料載入函式**預設不回傳 holdout 區間**；只有唯一一支 holdout 測試函式可以放行，
  且要印警告。
- 任何「歷史切片」不准自稱真樣本外，除非**已用切分函式實際核對過邊界**。
- holdout 只測一次，測過即焚，不准回去改參數再測。

### 多重比較
- 每個「機制 × 一組參數點」都要登記進試驗資料庫。掃 81 格就登記 81 筆。
- **未登記的候選判定一律無效**：不得寫進候選清單、不得提請審核、不得上線。
- 報告候選時 Deflated Sharpe 與 Calmar 並列，不要只報後者。
- 做過控制組就填結果；沒做就留空——**留白比編一個數字誠實**。

### 通過六關（在 val 樣本外，缺一不可）
1. 淨利 > 0 且 MDD 可接受　2. 贏過同期 Buy & Hold　3. 參數高原（±30% 一整片都好）
4. 成本 1×/2×/3× 都活著　5. 勝過同頻率隨機基準　6. 交易數 ≥ 100
- **要豁免任何一關，必須換一個等價嚴格的檢定，不是拿掉它。**

### 統計偽影家族（新想法落進這些形狀，預設假設它是假的）
① 候選組成微調　② 曝險力道連續縮放　③ 多樣化混合　④ 權重集中度
⑤ 小額疊加分散　⑥ 換手／執行時機縮減
- 每個家族都要有對應的控制組手法，且**通過標準是 20/20 或超過控制組最大值**，
  「贏過控制組平均」不算。
- 控制組必須是「沒有這個新判斷依據時，機制自然會做的事」，不是隨便亂抽。
- 確定性控制組自己的參數也要掃過一遍，拿訊號對控制組的**最高點**比。
- 選點依據不能是「訊號表現好的地方」——否則要補做不挑格子的網格聚合比較。
- **效果量級越誇張，越應該懷疑，不是越應該興奮。**

### 台股特有的偽影家族（2026-09-07 新增，Cybex 那六個之外）
Cybex 是加密市場，沒有這幾樣東西。台股有，而且每一樣都能自己生出漂亮的假績效：

⑦ **存活者偏誤**（**最貴的一個**，優先懷疑）
　　我們的 `price_history.json`／`scores*.json` 是「今天還在市的股票」的歷史。
　　用它回測，等於事先知道哪些公司沒有下市——這會系統性高估報酬，而且**越往小型股
　　越嚴重**（#50 容量受限小型股整條方向都踩在這個雷上）。
　　實例：2026-09-06 稽核抓到三份榜單合計 161 檔已下市股票還在排名裡，其中未來成長榜
　　第 1 名是 2020 年財報造假下市的康友-KY。那不是理論風險，是已經發生過的事。
　　**做法**：任何回測都要能回答「這個宇宙是用哪一天的名冊決定的」。
　　用 `data/listed_universe.json` 的 `last_seen` 做時點宇宙，不能用今天的名冊回測過去。

⑧ **財報跳空**
　　台股財報在盤後／休市期間公布，隔日開盤直接跳空。用「收盤價進場」回測會拿到一個
　　現實中拿不到的價格。**任何橫跨財報公布日的部位，進出場價都要用開盤價或加跳空成本**，
　　並且要能說出這筆交易有沒有跨過財報日。

⑨ **交易時段**
　　台股 09:00–13:30，沒有夜盤（台指期有，且與現貨時段不同）。跨市場或跨商品的訊號
　　必須對齊到「同一個決策時點實際看得到什麼」——用美股收盤訊號做台股當日進場是未來函數。
　　台指期夜盤的價格在隔日現貨開盤前就存在，用它預測現貨開盤是合法的；反過來不是。

⑩ **借券成本與可借量**
　　做空台股需要借券，**借得到才能空、而且要付費**。融券有停券期（除權息、股東會）
　　會被強制回補。任何空頭部位的回測都要問：那一天借得到嗎？成本多少？會不會被強制回補？
　　答不出來就不要宣稱那是可執行的空單。這也是把 #53～#57 的標的優先掛台指期的理由
　　——**期貨可空、無借券限制、有夜盤**。

⑪ **漲跌停與流動性斷點**
　　±10% 漲跌停鎖住時**成交不了**（2026-09-06 實測：大毅 2478 只漲 9.61% 就鎖漲停，
　　因為檔位取整）。回測裡「以漲停價成交」是幻覺。鎖住當天要當作無法進出場。

### 未來函數
- 任何百分位／z-score／排名門檻，只准用當下時間點以前的資料（rolling/expanding）。
- 固定週期重平衡的**相位是任意的**：報告單一相位的數字前，先快篩幾個不同相位。
  （Cybex 第 315 輪跨相位 Calmar 1.006～2.084，差近兩倍——同一個策略，只是換了起始日。）

### 指標順序
淨利 + MDD → Sortino → Sharpe 放最後 → **勝率幾乎不用看**。
報酬高度集中在少數交易是動量策略的常態：任何「限制單筆獲利／提前鎖利」的機制，
都要做怪獸單捕捉檢查，不能只看聚合指標。

### 工程紀律（研究可信度的一部分）
- 任何稽核恆等式，**兩端都必須是使用者在頁面／檔案上直接看得到的數字**；
  稽核裡不准重播引擎、不准自己決定算法。
- 加總對不上，修在產生資料的地方，不是在稽核裡調算法讓它通過。
- 同一個經濟量，正負號只能記一次。
- **如果一條恆等式從來沒失敗過，先懷疑它是不是恆真的。**
- 壞頁面／壞程序永遠不准靜默交付：寫檔前驗、重啟前驗，失敗就不覆蓋 ＋ 告警。

### 真錢
- 送單前的屏障必須層層獨立，且「憑證載入」本身也要在屏障之後。
- 只有使用者本人能建立解鎖旗標，任何自動化流程都不得建立。
- 第一個 30 天只看執行品質（對帳、滑價、成交率、可靠度、kill 演練），不看損益。

## 八、安全紅線
- 自動下單只做介面、只產生下單計畫，絕不串接真實下單 API
- 所有分數/訊號/報告標示「非投資建議」
- API 金鑰只放 GitHub Secrets，絕不寫進任何 commit 的檔案（本 repo 公開）
- 研究紀律：PIT、survivorship-free、全成本、FDR 分軌、隨機對照組
- holdout 未經使用者明確同意不得解鎖

## 九、帽子規則（分工紀律，不建立 subagent 組織）

**一、每輪工作開始時，明確聲明本輪戴哪頂帽子（擇一）：**
維運（管線/STATUS/workflow）、開發（App功能/UI）、研究（因子/策略）、
驗證（回測/對照組）、情報（新聞/社群/訊號）、法遵（稅務/法規）。
**一輪只戴一頂，做完該帽子的產物才能換帽。**

**二、帽子各有必交產物，做完必更新，沒產出就明寫「本輪無產出＋原因」：**
- 維運 → `data/STATUS.json`
- 開發 → 冒煙測試結果
- 研究 → SPEC 或因子（設計文件/因子程式碼）
- 驗證 → `TRIALS_LEDGER`（回測/對照組結果紀錄）
- 情報 → `signal_ledger`
- 法遵 → `docs/compliance` 備忘錄

**三、做與判分離（鐵律）**：
同一輪不得既開發策略又宣告其有效；策略有效性只能由「驗證帽」輪次以
隨機對照＋FDR＋全成本判定，且結果交由 Cowork 稽核。

**四、越權禁止**：戴 A 帽時不改 B 帽擁有的檔案。檔案歸屬：
- `data/STATUS.json`、`.github/workflows/` = 維運
- `index.html`、`scripts/smoke_test.*` = 開發
- `research/`（因子/策略/回測程式碼與紀錄）= 研究與驗證
- `signal_ledger`（`data/signal_ledger.json`）= 情報
- `compliance/` = 法遵
